---
description: Pull spoke edits back into bd (last-writer-wins)
argument-hint: "[--since <iso8601>]"
---

Claude Code registers this file as `/cairn:sync-pull`. Follow the sibling `cairn-sync-pull.md` in this directory with the same `$ARGUMENTS`.
