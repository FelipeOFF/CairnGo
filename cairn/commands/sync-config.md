---
description: Configure two-way bd ↔ spoke sync (writes .cairn/sync.json)
---

Claude Code registers this file as `/cairn:sync-config`. Follow the sibling `cairn-sync-config.md` in this directory with the same `$ARGUMENTS`.
