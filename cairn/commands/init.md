---
description: Wire git + beads, write tracker templates and plugin-root
argument-hint: "[target-dir]"
---

Claude Code registers this file as `/cairn:init`. Follow the sibling `cairn-init.md` in this directory with the same `$ARGUMENTS`.
