---
description: Render READY / DOING / BLOCKED from bd
argument-hint: "[--brief] [--json] [--plain]"
---

Claude Code registers this file as `/cairn:status`. Follow the sibling `cairn-status.md` in this directory with the same `$ARGUMENTS`.
