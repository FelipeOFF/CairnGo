---
name: sync-config
description: "Configure Cairn's optional beads sync with Jira, GitHub, GitLab, Asana or Azure Boards. Use when enabling or editing a spoke or invoking cairn sync-config."
---

# cairn sync-config

On Codex, first read the [Codex adapter](../../references/codex.md) for paths
and tool mapping. Then follow [cairn-sync-config](../../commands/cairn-sync-config.md).
The selected command is `sync-config`; text after the skill invocation supplies
its arguments, not another command. Follow the configuration workflow with the user; store token environment variable names, never secrets.

