---
phase: 13-state-corroboration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cairn/scripts/cairn-status.py
  - tests/cairn-corroboration.bats
autonomous: true
requirements: [CORR-01, CORR-02, CORR-03, CORR-04]
beads: [CairnGo-oms, CairnGo-jc5, CairnGo-puf, CairnGo-3ce]

must_haves:
  truths:
    - "A phase whose -SUMMARY.md exists but whose phase-N bd issues are still open reports corroboration=='conflict' with a 'blocks'-severity item naming both the disk claim and the bd claim (CORR-01, CORR-02)"
    - "A phase whose only disagreement is roadmap-complete-but-nothing-on-disk also reports a 'blocks' conflict, independent of bd (CORR-01)"
    - "A phase whose only disagreement is STATE.md's active_phase pointer while disk is already verified reports an 'informs'-severity conflict, and phase_next_command still returns the normal disk-driven command, not /cairn:doctor (D-05)"
    - "When the bd query cannot be completed, every phase's evidence.bd reads 'unknown' and no phase's corroboration reads 'ok' — proven by a bats test that makes a real bd invocation fail, not by reading the code (CORR-03, D-07: the bd axis loses its vote, the fact is stated, nothing is invented)"
    - "disk_state keeps its exact 4 values, type and meaning in --json; phase_next_command never raises KeyError under any corroboration value (CORR-04)"
    - "Two readable sources disagreeing is already a conflict — no majority rule, no tiebreak (D-06); bd_state()/corroborate() are pure functions with no AskUserQuestion or any prompting capability anywhere in their call graph — they report, the prose in cairn/commands/*.md is what will later offer options (D-02)"
  artifacts:
    - path: cairn/scripts/cairn-status.py
      provides: "bd_state(), corroborate(), phase_model() additive evidence/corroboration/conflicts keys, phase_next_command() conflict guard, main() bd-probe fail-open degrade"
    - path: tests/cairn-corroboration.bats
      provides: "bats coverage for the corroboration engine, including the forced bd-failure test"
  key_links:
    - from: "cairn-status.py:phase_model()"
      to: "cairn-status.py:corroborate()"
      via: "per-row call inside the existing row-building loop, using disk_state/roadmap complete/bd_state/state_md active_phase already in scope"
      pattern: "corroborate\\("
    - from: "cairn-status.py:main()"
      to: "cairn-status.py:phase_model()"
      via: "a new bd_ok flag threaded from a cheap bd probe into phase_model(planning_dir, issues, bd_ok=bd_ok)"
      pattern: "bd_ok"
---

<objective>
Build the corroboration engine at the root of milestone v1.4: `phase_model()` in
`cairn/scripts/cairn-status.py` gains additive per-phase evidence from four
independent sources (disk, bd, the ROADMAP checkbox, STATE.md's `active_phase`),
a three-state verdict (`ok` / `conflict` / `unknown`), and an itemized conflicts
list carrying a `blocks` or `informs` severity — never a fifth `disk_state`
value, never a majority vote, and never a silent "ok" when a source could not
be read.

Purpose: this is the literal fix for the milestone's root cause — a phase's
state currently comes from checking whether four filenames exist, never
opening them, never asking bd. Every other plan in this phase (rendering,
doctor, the ship gate) and every later phase in the milestone (14-17) reads
from what this plan produces.
Output: `bd_state()`, `corroborate()`, `phase_model()`'s additive keys,
`phase_next_command()`'s conflict guard, and `main()`'s bd-unreachable
degrade path — verified end-to-end via `cairn-status.sh --json`, not read
from the code.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-state-corroboration/13-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: bd_state() + corroborate() wired into phase_model(), end to end</name>
  <files>cairn/scripts/cairn-status.py, tests/cairn-corroboration.bats</files>
  <read_first>
    cairn/scripts/cairn-status.py (read `phase_model()` lines ~581-623,
    `phase_disk_state()`, `phase_next_command()` lines ~626-646,
    `issue_phase_ns()`, `in_done_phase()`, `state_frontmatter()`,
    `roadmap_phase_rows()` — every function this task's new code calls or
    sits beside), tests/cairn-phase-model.bats (the existing test shape and
    `phase_field` helper to copy), .planning/research/ARCHITECTURE.md §1
    ("Exact functions/files that must change").
  </read_first>
  <behavior>
    - `bd_state(issues, n, roadmap_done_set)`: among `issues` whose
      `issue_phase_ns()` includes `n`, keep only those where the issue's
      phase-label set is exactly `{n}` OR is a subset of
      `roadmap_done_set | {n}` (an issue also carrying an undone OTHER
      phase's label is genuinely live work for THAT phase, not evidence
      against phase n — mirrors the existing `in_done_phase` ALL-not-ANY
      discipline so this never fabricates a false conflict from a
      legitimate cross-phase issue). Returns `"none"` when nothing
      qualifies, `"closed"` when every qualifying issue's status is
      `"closed"`, `"in_progress"` when any qualifying issue is
      `"in_progress"`, else `"open"`.
    - `corroborate(n, disk_state, roadmap_complete, bd_val, bd_ok,
      state_md_active_phase)` returns `(verdict, evidence, conflicts)`.
      `evidence = {"disk": disk_state, "bd": bd_val if bd_ok else
      "unknown", "roadmap": "complete" if roadmap_complete else
      "incomplete", "state_md": "active" if state_md_active_phase == n
      else None}`. Three independent rules, each appending a dict
      `{"severity", "sources", "detail"}` to `conflicts` on a hit — exactly
      two severities, `"blocks"` and `"informs"`, per D-09, each rule
      below carrying its own one-line justification for which severity it
      gets, the same discipline CORR-07's allowlist justifications use:
        - R1 (severity `"blocks"`, sources `["disk","bd"]` — justification:
          disk and bd are both direct, artifact-backed reads of real work
          state; a disagreement between them is exactly ROADMAP SC2's
          scenario and D-05's "disk vs bd invalidates"): only when
          `bd_ok` and `bd_val != "none"` — `disk_state in
          ("executed","verified")` disagreeing with `bd_val == "closed"`
          (either direction: disk says done and bd says not-closed, OR
          disk says not-done and bd says closed). This is also D-06's rule
          in code: two readable sources disagreeing is already `conflict`,
          no majority, no tiebreak.
        - R2 (severity `"blocks"`, sources `["roadmap","disk"]` —
          justification: a checked box with nothing built is at least as
          dangerous as R1, and independent of whether bd has any opinion
          at all):
          `roadmap_complete` is True and `disk_state not in
          ("executed","verified")` — the checkbox claims done but nothing
          is built, independent of bd. (The REVERSE — disk verified,
          checkbox unticked — is the existing, accepted lag documented in
          `phase_model()`'s own comment and must NOT fire.)
        - R3 (severity `"informs"`, sources `["state_md","disk"]` —
          justification: STATE.md's active_phase is a workflow POINTER,
          not a work-completion signal; per D-05 it must never invalidate
          `/cairn:work N` on its own, so it can never outrank R1/R2):
          `state_md_active_phase == n` and `disk_state in
          ("executed","verified")` — STATE.md still points here while
          disk already shows it finished (D-05's exact example).
      Verdict: `"conflict"` if `conflicts` is non-empty; else `"unknown"`
      if not `bd_ok`; else `"ok"`. `evidence.bd == "unknown"` (an
      unreadable source) must never by itself produce `"ok"` (D-07: the bd
      axis simply casts no vote — it never invents a verdict) — R2/R3 can
      still fire and produce `"conflict"` even when bd is unreadable,
      since they do not need bd. Neither `bd_state()` nor `corroborate()`
      ever prompts, blocks on input, or calls anything resembling
      `AskUserQuestion` — per D-02, that capability does not exist inside
      a Python process; these functions only report, structured and
      deterministic.
    - `phase_model(planning_dir, issues=None, bd_ok=True)`: gains the
      `bd_ok` kwarg (default `True` — unchanged behavior for every
      existing caller/signature use). Before the per-phase loop, compute
      `roadmap_done_set = {n for n, row in rows.items() if
      row.get("complete")}`. Inside the loop, after `row["disk_state"] =
      phase_disk_state(pdir)`, compute `bd_val = bd_state(issues or [], n,
      roadmap_done_set)` and call `corroborate(...)`, attaching
      `row["evidence"]`, `row["corroboration"]`, `row["conflicts"]`. Read
      STATE.md's `active_phase` once (via `state_frontmatter()` +
      `normalize_phase()`, matching how `main()` already does it) and pass
      it through — do not re-read STATE.md per phase.
    - `phase_next_command(p)`: add a guard BEFORE the existing bare dict
      subscript (which is untouched — this is the whole point of D-04's
      backward-compat requirement): if `p.get("corroboration") ==
      "unknown"` OR (`p.get("corroboration") == "conflict"` and any of
      `p["conflicts"]` has `severity == "blocks"`), return
      `"/cairn:doctor"`. An `"informs"`-only conflict falls through
      unchanged to the existing dict lookup (D-05: a stale STATE.md
      pointer alone does not invalidate `/cairn:work N`).
  </behavior>
  <action>
    Implement `bd_state()` and `corroborate()` as new top-level functions
    in cairn-status.py, placed near `phase_disk_state()`. Wire both into
    `phase_model()` exactly as specified above — additive keys only,
    `disk_state` itself is never read differently and never assigned a
    fifth value anywhere. Add the `phase_next_command()` guard clause.
    Update `main()`'s call site to pass `bd_ok` (Task 2 wires the actual
    probe; for this task pass `bd_ok=True` as a literal placeholder at the
    `phase_model(planning_dir, ready + doing + blocked + closed)` call
    site — Task 2 replaces the literal with the real probe result). Do not
    touch rendering (`phase_panel_lines`, `html_phases`, `phase_state_text`)
    in this task — Plan 13-02 owns that.
  </action>
  <verify>
    <automated>cd /path/to/repo && bats tests/cairn-corroboration.bats tests/cairn-phase-model.bats tests/cairn-status.bats</automated>
  </verify>
  <done>bd_state() and corroborate() exist and are wired into phase_model()'s per-phase row (evidence/corroboration/conflicts), phase_next_command()'s dict subscript is untouched behind a new guard clause, and the disk-vs-bd and roadmap-vs-disk "blocks" scenarios both surface as --json conflicts naming both sides.</done>
  <acceptance_criteria>
    - A fixture with phase N's `-SUMMARY.md` present and a bd issue labeled
      `phase-N` still `open` (or `in_progress`): `cairn-status.sh --json`
      shows `phases[].corroboration == "conflict"` for phase N, with a
      `conflicts[]` entry `severity == "blocks"`, `sources == ["disk","bd"]`,
      and `detail` naming BOTH the disk state and the bd state (grep the
      detail string for both the word "executed" or "verified" AND the bd
      issue's status word — never one side chosen silently).
    - A fixture with phase N's ROADMAP checkbox checked but no phase
      directory on disk at all: `corroboration == "conflict"` with a
      `sources == ["roadmap","disk"]` "blocks" item, even when there are
      zero bd issues for phase N (`evidence.bd == "none"`).
    - A fixture with STATE.md `active_phase: N` and phase N's disk state
      `"verified"`, bd agreeing (or no bd issues): `corroboration ==
      "conflict"` with exactly one `severity == "informs"` item, AND
      `phases[].next_command` for phase N is unchanged from what it would
      be without STATE.md's claim (still `None`/the disk-driven value —
      never `"/cairn:doctor"`).
    - `disk_state` values for every phase in a mixed fixture (none/planned/
      executed/verified) are byte-identical to `tests/cairn-phase-model.bats`'s
      existing assertions — that whole file still passes unmodified.
    - `bd_state(issues, n, roadmap_done_set)` on an issue labeled
      `phase-3,phase-4` where 3 is roadmap-complete and 4 is not, called
      with `n=3`: returns `"none"` (excluded), never counted against phase
      3 — a direct unit-style bats assertion via `python3 -c` importing
      nothing (call the function through a small `python3 -c` snippet that
      execs the module, OR assert this indirectly through `--json` on a
      fixture built for it; either is acceptable as long as it is a real,
      automated check).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bd-unreachable fail-open — every phase reports unknown, none reports ok</name>
  <files>cairn/scripts/cairn-status.py, tests/cairn-corroboration.bats</files>
  <read_first>
    cairn/scripts/cairn-status.py (`main()` lines ~2170-2260, `run_bd()`,
    `fetch_lanes()`), tests/cairn-status.bats (the `"bd missing from PATH
    exits 5"` test at line ~385 — copy its stub-building technique, but
    this test needs `bd` present-and-failing, not absent), .planning/research/PITFALLS.md
    (Pitfall 3 — "the single highest-priority test to write").
  </read_first>
  <behavior>
    - Test 1: with `.beads/` present and a stubbed `bd` binary that exits
      nonzero on every invocation (present on PATH, so `shutil.which("bd")`
      succeeds and the script does not die at the top-of-main guard), a
      fixture with several pending phases: `cairn-status.sh --json` still
      produces output (does not crash, does not hang), every phase's
      `evidence.bd == "unknown"`, and every phase's `corroboration` is
      either `"conflict"` (if R2/R3 independently fired) or `"unknown"` —
      never `"ok"`.
    - Test 2: same stubbed failure, exit code is `EXIT_NO_BD` (5) — the
      existing "bd unavailable" contract is preserved even though the
      board now renders content instead of dying silent.
  </behavior>
  <action>
    In `main()`, replace the unconditional `ready, doing, blocked, closed =
    fetch_lanes(root)` call (inside the `if (root / ".beads").is_dir():`
    branch) with a cheap probe first: `bd -C <root> list --limit 1 --json`
    via `subprocess.run`. On a nonzero return code, set `bd_ok = False`,
    `ready = doing = blocked = closed = []`, `n_closed = 0`, and set `note`
    to a message naming the failure and pointing at `/cairn:doctor` (do not
    overwrite an existing note-setting branch — this replaces the current
    `.beads/`-present branch's happy path only). On success, proceed with
    the existing `fetch_lanes(root)` call exactly as today and set
    `bd_ok = True`. The `.beads/`-absent branch (no directory at all) keeps
    `bd_ok = True` — that is "no bd usage here", not "bd failed", and must
    still resolve every phase's bd axis to `"none"`, not `"unknown"`.
    After building `data`, still exit `EXIT_NO_BD` when `bd_ok` is False —
    the script prints its JSON (or renders its board) FIRST, then exits 5
    (do not `sys.exit` before the print/render call). Pass the real
    `bd_ok` into `phase_model(planning_dir, ready + doing + blocked +
    closed, bd_ok=bd_ok)`, replacing Task 1's literal placeholder.
  </action>
  <verify>
    <automated>cd /path/to/repo && bats tests/cairn-corroboration.bats</automated>
  </verify>
  <done>A real bd-query failure (bd present on PATH, .beads/ present, the invocation itself fails) still produces --json output, exits 5, and shows evidence.bd == "unknown" with corroboration never "ok" for every phase — proven by a forced-failure test, and the .beads/-absent path still resolves to "none", not "unknown".</done>
  <acceptance_criteria>
    - The forced-failure bats test actually invokes a `bd` binary that
      exits nonzero (a real subprocess failure, constructed the same way
      as `tests/cairn-status.bats`'s existing PATH-stub test) — not a
      mocked Python call and not an assertion read from source.
    - `$status -eq 5` AND `$output` parses as valid JSON with every
      `phases[].evidence.bd == "unknown"` in that same run.
    - The `.beads/`-absent scenario (existing behavior, unrelated to this
      task) is unchanged: still exit 0, still a GSD-only board note, still
      `evidence.bd == "none"` (not `"unknown"`) for every phase — add one
      assertion proving this distinction so a future edit cannot collapse
      "no bd" and "bd unreachable" into the same value by accident.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bd subprocess output → corroboration engine | `bd`'s JSON output (issue status, labels) is trusted local-tool data today; this plan makes it a VOTING input whose absence must not silently read as agreement |
| STATE.md / ROADMAP.md → corroboration engine | Hand-edited markdown, parsed leniently; a malformed or adversarial checkbox/frontmatter must degrade, never crash `phase_next_command()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Repudiation | `corroborate()` verdict | high | mitigate | `unknown` is a distinct, never-collapsed-into-`ok` value (CORR-03, Task 2's forced-failure test) — an unreadable source can never be misrepresented as agreement |
| T-13-02 | Tampering | `phase_next_command()` dict subscript | high | mitigate | `disk_state`'s value space is never widened (D-04); the new guard is a clause BEFORE the untouched literal dict, so no code path can introduce a fifth key and `KeyError` |
| T-13-03 | Denial of Service | bd subprocess probe in `main()` | low | accept | the probe is a single bounded `list --limit 1` call with the same subprocess pattern already used everywhere in this file; no new timeout is introduced beyond the existing convention, matching current risk |
| T-13-04 | Information Disclosure | conflict `detail` strings surfaced in `--json`/board | low | accept | detail strings are built from already-public phase state (disk_state, bd status, roadmap flags) already exposed by the existing `--json` contract; nothing new is disclosed |
</threat_model>

<verification>
- `bats tests/cairn-corroboration.bats tests/cairn-phase-model.bats tests/cairn-status.bats` all pass.
- `python3 -c "import ast; ast.parse(open('cairn/scripts/cairn-status.py').read())"` (syntax sanity) passes.
- Manual: `bash cairn/scripts/cairn-status.sh --json | python3 -m json.tool >/dev/null` on this repo's own `.planning/` succeeds without error.
</verification>

<success_criteria>
`phase_model()` computes a three-way corroboration verdict from four
independent sources without ever widening `disk_state`, without ever
treating an unreadable bd as agreement, and `phase_next_command()` cannot
raise `KeyError` under any corroboration value — all proven by bats, not by
code review.
</success_criteria>

<output>
Create `.planning/phases/13-state-corroboration/13-01-SUMMARY.md` when done
</output>
