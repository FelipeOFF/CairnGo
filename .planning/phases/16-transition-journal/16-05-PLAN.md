---
phase: 16-transition-journal
plan: 05
type: execute
wave: 3
depends_on: ["16-01", "16-04"]
files_modified:
  - cairn/scripts/cairn-doctor.py
  - tests/cairn-doctor.bats
  - .gitignore
autonomous: true
requirements: [JOUR-02]
beads: [CairnGo-mf3]

must_haves:
  truths:
    - "check_phase_corroboration()'s report names, for every conflict, when EACH of the conflict's cited sources (e.g. disk and bd) last moved — the exact seeded timestamp, not a placeholder or the word 'unknown' — pulled from cairn-journal.py last-moved (JOUR-02's own wording, 'cita quando cada lado se moveu')"
    - "a phase with a conflict but no journal history yet (never observed) reports 'never observed' per source rather than a blank, a crash, or a fabricated timestamp"
    - "a broken/unreachable cairn-journal.py degrades check 11 to the SAME warn-not-crash posture every other subprocess-shell-out failure in this function already has — it never turns a real blocking conflict into a pass, and never fails doctor's own run over a purely cosmetic enrichment"
  artifacts:
    - path: cairn/scripts/cairn-doctor.py
      provides: "check_phase_corroboration()'s per-conflict-item text now cites each cited source's last-moved timestamp"
    - path: tests/cairn-doctor.bats
      provides: "a seeded-conflict fixture proving the exact timestamp appears in the report"
    - path: .gitignore
      provides: ".cairn/journal.jsonl* entry (the live file and any orphaned compaction temp siblings)"
  key_links:
    - from: "cairn-journal.py last-moved --phase N --json"
      to: "check_phase_corroboration()'s per-item detail string, one call per phase with at least one conflict item (never one call per conflict)"
      via: "the same shell-out-and-parse-defensively pattern check_lease_stale() and check_phase_corroboration()'s own existing cairn-status.py --json call already use in this file"
      pattern: "last-moved"
---

<objective>
Close the loop on JOUR-02: enrich `cairn-doctor.py`'s existing check 11
(`check_phase_corroboration`, id `"phase-corroboration"`) so every
conflict item names when each of its cited sources last moved, pulled
from `cairn-journal.py last-moved` — and add the `.gitignore` entry the
journal has needed since Plan 16-01 first wrote to `.cairn/journal.jsonl`.

Purpose: this is D-04's "dentro do relatório de conflito" — the ONLY
place JOUR-02's history surfaces by design (not the status board, not a
new command; a "last transition" line on the board was explicitly
rejected in 16-CONTEXT.md as noise when nothing disagrees). A conflict is
exactly the moment this information stops being noise and starts being
the first thing a human needs to resolve it.

Output: `check_phase_corroboration()`'s report text names last-moved
timestamps per conflict source; `.gitignore` carries the journal entry.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-transition-journal/16-CONTEXT.md
@cairn/scripts/cairn-doctor.py
@cairn/scripts/cairn-journal.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: check_phase_corroboration() cites each conflict source's last-moved timestamp</name>
  <files>cairn/scripts/cairn-doctor.py</files>
  <read_first>
    cairn/scripts/cairn-doctor.py's `check_phase_corroboration()` (lines
    ~935-1000) — the exact function this task extends, including its
    existing degrade-to-warn discipline (returncode not in (0,5) or a
    JSON-decode failure -> WARN, never crash the whole doctor run) that
    this task's new subprocess call must match precisely.
    `check_lease_stale()` (lines ~1293-1358) — the closer sibling
    pattern: it already shells to `cairn-lease.py status --all --json`
    and degrades the SAME way; copy its try/except shape for the new
    `cairn-journal.py last-moved` call rather than inventing a third
    variant of the same defensive pattern. `corroboration_recommendation`
    /`CORROBORATION_RECOMMENDATION` (used just above
    `check_phase_corroboration`) for how per-source text is already
    assembled, so the new last-moved text composes naturally into the
    existing `f"{n}: {c.get('detail', '')} ({sev}) — {rec}"` line rather
    than fighting its format.
  </read_first>
  <behavior>
    - For a phase with one or more conflict items, `cairn-journal.py
      last-moved --phase N --json` is called EXACTLY ONCE for that phase
      (cached across however many conflict items that phase has — a
      phase with two conflicts, e.g. `["disk","bd"]` and
      `["roadmap","disk"]`, still triggers only one subprocess call, not
      two), never once per conflict item.
    - Each conflict item's text gains a clause per cited source, e.g.
      "disk last moved 2026-07-31T10:00:00+00:00, bd last moved never
      observed" — using the exact `ts` string `last-moved` reports for an
      observed source, and the literal phrase "never observed" for a
      source whose `last-moved` value is null.
    - A `cairn-journal.py last-moved` subprocess failure (nonzero exit,
      unparsable JSON, missing entirely) for one phase degrades that
      phase's items to their EXISTING (pre-this-plan) text with no
      last-moved clause appended — it does not change `status`
      ("fail"/"warn"/"ok"), does not add a spurious WARN-level item, and
      does not affect any OTHER phase's items.
  </behavior>
  <action>
    Add a small `journal_last_moved(root, phase)` helper mirroring
    `check_lease_stale()`'s subprocess shape exactly: `subprocess.run(
    [sys.executable, str(SCRIPTS_DIR / "cairn-journal.py"), "last-moved",
    "--phase", str(phase), "--json", "--project-dir", str(root)],
    capture_output=True, text=True)`, returning `None` on any
    `OSError`/`SubprocessError`, non-zero exit, or `json.JSONDecodeError`
    — never raising. Inside `check_phase_corroboration()`'s existing
    per-phase loop (where it currently builds `items` from
    `p.get("conflicts")`), call `journal_last_moved(root, n)` ONCE, cache
    it per phase (a local dict keyed by `n`, populated lazily the first
    time that phase needs it), and when the result is not `None`, append
    a clause per `sev`/source pair to the existing detail line: for each
    source in `c.get("sources") or []`, look up
    `last_moved.get(source)` — if present, format its `"ts"`; if the
    axis key itself is missing/null, use "never observed". Leave every
    other part of the function (the `unknown`-verdict branch, `status`
    computation, the WARN/fail thresholding) completely untouched.
  </action>
  <verify>
    <automated>cd /path/to/repo && bats tests/cairn-doctor.bats -f "last-moved"</automated>
  </verify>
  <done>a conflict item's text names each cited source's last-moved timestamp (or "never observed"); the subprocess call happens once per phase, not once per conflict; a broken cairn-journal.py degrades cleanly with no change to status/severity.</done>
  <acceptance_criteria>
    - A phase seeded (via direct `cairn-journal.sh observe`/`lease` calls
      in the fixture, not through a full `/cairn:status` run) with a
      known `disk` last-moved timestamp and NO `bd` observation ever
      recorded, then made to produce a real `["disk","bd"]` conflict:
      `cairn-doctor.sh --json`'s corresponding item text contains the
      EXACT seeded timestamp string for `disk` and the literal phrase
      "never observed" for `bd`.
    - `CAIRN_JOURNAL=/nonexistent/path cairn-doctor.sh --json` on the
      same fixture: `status`/`detail` for check `"phase-corroboration"`
      are IDENTICAL to a run with a working journal (same fail/warn/ok,
      same conflict count) — only the last-moved clause is missing from
      the item text.
    - A phase whose `journal_last_moved()` call is invoked more than once
      per doctor run is a failing test — assert via a call-counting
      `CAIRN_JOURNAL` stub (mirroring the one-call-per-render assertion
      pattern from Plan 16-04), not by inspecting the code.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: .gitignore entry and the doctor-report regression fixture</name>
  <files>.gitignore, tests/cairn-doctor.bats</files>
  <read_first>
    .gitignore's existing `.cairn/*` entries (id-map.json, state.json,
    conflicts.json, hook.log) — match their exact one-line-per-pattern
    style and placement (near the other `.cairn/` entries, not a new
    section).
  </read_first>
  <action>
    Add `.cairn/journal.jsonl*` to `.gitignore` (the glob covers both the
    live file and any orphaned `journal.jsonl.tmp-*`/lock-file siblings
    Plan 16-02's compaction can leave behind), placed alongside the
    existing `.cairn/id-map.json`/`.cairn/state.json`/
    `.cairn/conflicts.json`/`.cairn/hook.log` lines. Write the doctor
    regression test: seed a real conflict (same fixture shape as Task 1),
    run `git add -A` in the fixture repo, assert `.cairn/journal.jsonl`
    does NOT appear in `git status --porcelain`/`git diff --cached
    --name-only` — proving the ignore entry actually takes effect, not
    just that it was added to the file.
  </action>
  <verify>
    <automated>cd /path/to/repo && bats tests/cairn-doctor.bats -f "gitignore"</automated>
  </verify>
  <done>`.cairn/journal.jsonl` and its compaction temp siblings are never staged by `git add -A`, proven by a test that actually stages and checks, not by reading the .gitignore file.</done>
  <acceptance_criteria>
    - The new `.gitignore` line is a glob (`.cairn/journal.jsonl*`), not
      a single bare filename — it must also cover
      `journal.jsonl.tmp-XXXXXX` and `journal.jsonl.compact.lock`.
    - `git status --porcelain` after `git add -A` in a fixture with a
      live journal AND a leftover compaction tmp file (create both
      directly in the test) shows neither file staged or untracked-and-
      visible.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `check_phase_corroboration()`'s enrichment | Purely additive text on top of an already-computed `severity`/`status` verdict — must never be able to change what doctor blocks on, only what it SAYS about a block that was already going to happen |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-09 | Tampering | a `cairn-journal.py last-moved` failure silently downgrading a "fail" to a "warn" | high | mitigate | the last-moved call only appends TEXT to an already-decided item; `status`/`any_blocks` are computed exactly as before this plan, from `corroborate()`'s own `severity` field, never from whether the journal enrichment succeeded |
| T-16-10 | Information Disclosure | the journal's `.gitignore` entry itself | n/a | accept | this task ADDS the ignore entry (closing the gap Plan 16-01 left open by design — it needed the file to exist first to prove the append/read primitives), no new disclosure surface introduced |
| T-16-SC | Tampering | npm/pip/cargo installs | n/a | accept | no new package-manager installs in this plan |
</threat_model>

<verification>
- `bats tests/cairn-doctor.bats` passes in full, including every pre-
  existing test plus this plan's last-moved and gitignore tests.
- `git status --porcelain` on this repo after this plan's changes shows
  no `.cairn/journal.jsonl` regardless of whether any prior test run left
  one behind locally.
</verification>

<success_criteria>
The doctor's conflict report names, for every conflict, when each cited
source last moved — pulled from the journal, degrading cleanly when the
journal is absent or broken, and the journal itself is never accidentally
committed.
</success_criteria>

<output>
Create `.planning/phases/16-transition-journal/16-05-SUMMARY.md` when done
</output>
