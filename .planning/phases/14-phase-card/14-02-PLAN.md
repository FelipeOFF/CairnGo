---
phase: 14-phase-card
plan: "02"
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - cairn/scripts/cairn-status.py
  - cairn/templates/status-board.html
  - tests/cairn-phase-card.bats
  - tests/cairn-phase-model.bats
  - tests/cairn-corroboration.bats
autonomous: true
requirements: [CARD-01, CARD-02, CARD-03, CARD-04]
beads: [CairnGo-r4t, CairnGo-hew, CairnGo-1u6, CairnGo-gnk]

must_haves:
  truths:
    - "The terminal board and the HTML page render the phase table from the identical phases[] data: a value present for a phase in --json (purpose, research, plans, issues, verify, waits, next command) appears as the same literal substring in both the terminal render and the HTML page, proven by a test that extracts from --json and greps both surfaces — never by comparing each surface's own hardcoded string"
    - "NEXT COMMANDS no longer renders as its own terminal section; the command still names each phase's next legal /cairn:* action and the reason for that ordering still appears, now beside that phase's purpose"
    - "A phase's purpose text is never truncated in the terminal's end-of-panel list — unlike the fixed-width table columns above it, which do truncate"
    - "A phase with nothing built yet (disk_state none) shows a dash in its data columns rather than a fabricated 0/0"
    - "A conflicting phase's marker and reason still occupy exactly one line (the state column), inherited unchanged from Phase 13's D-03 — this plan only narrows that column's width, it does not reopen the one-line rule"
  artifacts:
    - cairn/scripts/cairn-status.py
    - cairn/templates/status-board.html
    - tests/cairn-phase-card.bats
  key_links:
    - "phase_purpose_text()/phase_research_text()/phase_issues_text()/phase_verify_text() are the single functions both phase_panel_lines() (terminal) and html_phases() (HTML) call — neither surface re-derives its own wording"
    - "next_commands()'s reason field, unchanged, now read by phase_panel_lines()'s purpose-list loop instead of a dedicated NEXT COMMANDS header block"
---

<objective>
Rebuild the terminal pending-phase panel into the table-plus-purpose-list
layout the user chose after comparing four rendered variants (D-01), fold
`NEXT COMMANDS` into a `next` column plus a relocated reason (D-02), mirror
every new field in the HTML page, and prove terminal ≡ HTML with a test that
extracts from `--json` and greps both renders.

Purpose: this is the plan a human actually sees — Plan 14-01 only produced
JSON. CARD-01/02/04 are observable ONLY once this renders; CARD-03 is
provable only once both surfaces exist to compare.
Output: `cairn-status.py`'s `phase_panel_lines()` and `html_phases()`
rewritten to the new shape; `status-board.html` CSS for the new purpose
paragraph; a cross-surface parity test; two pre-existing tests repaired
where they asserted the now-removed `NEXT COMMANDS` section or the
now-removed "waits on N" phrase inside the state column.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-phase-card/14-CONTEXT.md
@.planning/phases/13-state-corroboration/13-02-SUMMARY.md
@cairn/scripts/cairn-status.py
@cairn/templates/status-board.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Terminal — table + purpose list, NEXT COMMANDS folded in (D-01, D-02, D-04)</name>
  <files>cairn/scripts/cairn-status.py, tests/cairn-phase-model.bats, tests/cairn-corroboration.bats</files>
  <read_first>
    cairn-status.py: `phase_panel_lines()` in full (this task rewrites its
    body), `phase_state_text()`, `phase_progress_text()`,
    `conflict_summary_text()`, `conflict_marker()` (all inherited unchanged
    from Phase 13 — reused, not reopened), `join_numbers()`, `truncate()`,
    `display_width()`, `render_spans()`. `tests/cairn-phase-model.bats`
    lines ~250-320 (the pending-list and next-command tests this task's
    changes will break) and `tests/cairn-corroboration.bats` lines ~459-530
    (the conflict-marker rendering tests this task must keep green).
  </read_first>
  <action>
    Add four shared text functions next to `phase_progress_text()` (same
    docstring style — one sentence on what, one on why both surfaces call
    it): `phase_purpose_text(p)` returns `p.get("purpose") or p.get("title")
    or "(no purpose recorded)"` — the data-layer `purpose` from Plan 14-01,
    falling back the same way `title` already falls back to
    `"(untitled)"` elsewhere. `phase_research_text(p)` returns `"yes"` when
    `p.get("research_done")` else `"—"`. `phase_issues_text(p)` returns
    `f"{p['issues_done']}/{p['issues_total']}"` when `p.get("issues_total")`
    is truthy (non-zero), else `"—"` — a phase with zero bd issues mapped to
    it has nothing to report, distinct from a phase with real-but-all-open
    issues (which DOES show `"0/N"`, real information, never a dash).
    `phase_verify_text(p)` returns `p["verify_status"]` when truthy; else
    `"pending"` when `p.get("disk_state") == "executed"` (a SUMMARY exists,
    no VERIFICATION.md yet); else `"—"`.

    Rewrite `phase_panel_lines()`'s pending-phase loop. Remove the two
    existing state-text augmentations entirely — the `if prog: state =
    f"{state} {sep} {prog}"` plans-progress append and the `if
    p["blocked_by"]: state = f"{state} {sep} waits on ..."` append — both
    become their own columns below, so the state column returns to showing
    ONLY `phase_state_text(p)` (or, for a conflict/unknown verdict, the
    unchanged `conflict_marker()`/`conflict_summary_text()` pair from Phase
    13, D-03 — inherited, not reopened).

    Build the table with these columns, in order: `#`, `phase` (title),
    `state`, `rsch`, `plans`, `issues`, `verify`, `waits`, `next`. Compute
    fixed content widths for the six short columns as a starting point —
    `rsch` 5, `plans` 6, `issues` 7, `verify` 12, `waits` 7, `next` 15 —
    with a 2-space gutter between every column (matching the existing
    gutter style used elsewhere in this function). Split whatever width
    remains after `#`, the six fixed columns and their gutters between
    `phase` (title) and `state`, roughly 3:2 in title's favor, each with a
    floor (`phase` >= 18, `state` >= 12) so neither collapses at narrow
    widths. Treat these exact numbers as a starting point, not a
    requirement — adjust them as needed so every pre-existing `--width 100`
    / `--width 200` / `--width 240` test in `tests/cairn-corroboration.bats`
    still passes, in particular the two tests asserting a ~70-77 character
    conflict detail string renders UNTRUNCATED at `--width 200` and
    `--width 240` (search for `disk reports phase 1 executed` and
    `STATE.md still points at phase 2`): the `state` column at those widths
    must be wide enough to hold them whole. Print a header sub-row
    immediately under `PENDING PHASES N` naming the columns (dim text,
    `SGR_DIM`), built with the SAME width variables the data rows use, so
    header and data always line up.

    Per pending phase, in `pending_phases()`'s existing order, build the row
    from: `#` = the phase number; `phase` = title, truncated; `state` =
    conflict marker+summary (truncated to the state width) for a
    conflict/unknown verdict, else `phase_state_text(p)` alone (no
    append-in, per above); `rsch` = `phase_research_text(p)`; `plans` =
    `phase_progress_text(p) or "—"` (that function returns `""` when
    `plans_total` is empty — render the dash instead of a blank cell);
    `issues` = `phase_issues_text(p)`; `verify` = `phase_verify_text(p)`
    (truncated to the verify width with `style.ell` if a status string runs
    long); `waits` = `join_numbers(p["blocked_by"]) or "—"`; `next` =
    `truncate(p["next_command"] or "—", next_width, style.ell)`. The
    `n_blocks`/`n_informs` tally and the "`N blocks · M informs —
    /cairn:doctor for the itemized report`" summary line stay exactly as
    they are today, computed inline while building the rows.

    Delete the entire `if cmds: ... NEXT COMMANDS ...` block (D-02: this
    header/section is gone from the terminal). In its place, add a new
    section: a blank line, then a bold `PURPOSE` header (matching the
    `SGR_BOLD` style `PENDING PHASES` already uses). Before the loop, build
    `reason_by_phase = {c["phase"]: c["reason"] for c in cmds if
    c["phase"] is not None}` from the SAME `cmds = data.get("next_commands")
    or []` list already read at the top of this function (unchanged
    computation — `next_commands()` itself is not touched by this plan).
    Then, for each pending phase in the same order as the table: `text =
    phase_purpose_text(p)`; if `reason_by_phase.get(p["number"])` exists,
    `text = f"{text} — {reason_by_phase[p['number']]}"`. Wrap `text` via
    `textwrap.wrap(style.asciify(text), max(30, width - num_w - 4))` (same
    pattern the existing parallelism-note wrap already uses) — this is the
    ONE place in the panel where text is wrapped rather than truncated, per
    D-01's explicit "purpose never truncates" requirement. Render the first
    wrapped line prefixed with the right-justified phase number and two
    spaces (matching the table's own `#` column style); indent continuation
    lines by `num_w + 2` spaces. The existing parallelism-note block (`par =
    data.get("parallelism")` through its `textwrap.wrap` loop) is unchanged
    and stays immediately after this new PURPOSE section, in the same
    relative position it held after the old NEXT COMMANDS section.

    Fix the two pre-existing tests this rewrite breaks. In
    `tests/cairn-phase-model.bats`: delete the line `assert_output_contains
    "NEXT COMMANDS"` from the "the next command carries the reason it sits
    where it does" test (its other three assertions — `/cairn:plan 3`,
    `phase 4 waits on it`, `waits on phase 3` — are unchanged text still
    produced by `next_commands()`, now read from the PURPOSE section
    instead, and should still pass as-is). Update the "a pending phase says
    what it waits on" test's `assert_output_contains "waits on 3"` — that
    exact phrase was the removed state-column append; replace it with an
    assertion whose intent survives the redesign (the blocked phase's row
    visibly names phase 3 — either check the `waits` column value directly,
    or assert the PURPOSE section's relocated `"waits on phase 3"` reason
    text, whichever is more reliable to grep for at the chosen widths). In
    `tests/cairn-corroboration.bats`: run the full file after this
    rewrite and confirm the marker-count and full-detail-string tests
    (search comments above for "Plan 13-02" to find them) still pass;
    adjust only the width constants above if truncation clips something a
    test depends on — do not weaken any assertion's meaning to make it pass.
  </action>
  <verify>
    <automated>bats tests/cairn-phase-model.bats tests/cairn-corroboration.bats</automated>
  </verify>
  <acceptance_criteria>
    Running `cairn-status.sh --width 100` against a fixture with one blocked
    pending phase shows that phase's row and its purpose in the new PURPOSE
    section, and the removed section's own bold header line is absent from
    the rendered output (behavioral check, not a source grep — the fixed
    string being deleted also names the thing being verified, so proving it
    by output is the honest test, not proving it by its own absence from the
    source). `bats tests/cairn-phase-model.bats tests/cairn-corroboration.bats`
    both pass in full.
  </acceptance_criteria>
  <done>The terminal renders the D-01/D-02 table+PURPOSE layout with no
  separate NEXT COMMANDS section, and every pre-existing bats test for
  `cairn-status.py` still passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HTML mirror + cross-surface parity test (D-01, D-02, D-04, CARD-03)</name>
  <files>cairn/scripts/cairn-status.py, cairn/templates/status-board.html, tests/cairn-phase-card.bats</files>
  <read_first>
    cairn-status.py: `html_phases()` in full. `status-board.html` around
    `.phase-title`/`.phase-meta`/`.phase-req` (~lines 495-538) for the
    existing CSS shape to extend, not replace.
    `tests/cairn-phase-model.bats`'s "the terminal panel and the HTML page
    carry the same commands and reasons" test and
    `tests/cairn-corroboration.bats`'s "a blocks conflict's detail string is
    verbatim-identical across --json, the terminal panel and the HTML page"
    test — both are the exact extract-from-json-grep-both-surfaces pattern
    this task's new test must follow.
  </read_first>
  <action>
    In `html_phases()`'s pending-phase loop, add a purpose paragraph inside
    each `<li class="phase...">`'s `phase-body` block, placed right after
    the existing `phase-title` span and before the existing `{reqs}`/
    `phase-meta` spans: `f'&lt;span class="phase-purpose"&gt;
    {esc(phase_purpose_text(p))}&lt;/span&gt;'` (reuse the exact `esc()` gate
    every other bd/GSD string in this function already passes through).
    Extend the existing `meta` list — built today as `meta =
    [esc(phase_state_text(p))]` then conditionally `prog`/`blocked_by`
    appends — with three more entries, inserted after the existing `prog`
    append and before the existing `blocked_by` append: `f'&lt;span
    class="n"&gt;{esc(phase_research_text(p))}&lt;/span&gt;'`, `f'&lt;span
    class="n"&gt;{esc(phase_issues_text(p))}&lt;/span&gt;'`, `f'&lt;span
    class="n"&gt;{esc(phase_verify_text(p))}&lt;/span&gt;'` — reusing the
    same `phase_*_text()` functions Task 1 defined, never re-deriving the
    wording independently (this is what makes cross-surface parity provable
    rather than coincidental). The existing `cmd-list`/"next commands"
    `panel-col` (built by the rest of `html_phases()`, unchanged) stays as
    it is — HTML has room for its own two-column layout and does not need to
    imitate the terminal's section removal, per CONTEXT.md's explicit
    allowance, as long as the same command+reason fields are present
    (they already are, untouched by this plan).

    In `status-board.html`, add a `.phase-purpose` rule immediately after
    the existing `.phase-title` block: font-size `var(--t-xs)`, color
    `var(--bone-dim)`, `line-height: 1.45`, `margin: 3px 0 0` — a purpose
    sentence runs longer than a title, so it needs its own line-height, not
    `.phase-title`'s.

    Add the cross-surface parity test to `tests/cairn-phase-card.bats`
    (created by Plan 14-01): a fixture with at least one phase carrying an
    `NN-RESEARCH.md` file, a bd fixture giving it a non-trivial
    issues_done/issues_total split, and an `NN-VERIFICATION.md` with a
    `status:` value. Run `--json`, extract `purpose`, the rendered
    `phase_research_text`/`phase_issues_text`/`phase_verify_text` values for
    that phase (call the same functions via the `PY_LOAD` module-import
    pattern already established in `tests/cairn-corroboration.bats`, or
    recompute the expected literal strings directly from the extracted JSON
    fields — either is acceptable as long as the expected value is DERIVED
    from `--json`, never hand-typed against what the renderer is expected to
    produce). Run `--width 200` and `--html board.html`. Assert every one of
    those derived strings appears as a literal substring in BOTH the
    terminal output and `board.html` — the same verbatim-identical pattern
    already proven for conflict detail strings in
    `tests/cairn-corroboration.bats`, never comparing each surface against
    its own separately-hardcoded expectation.
  </action>
  <verify>
    <automated>bats tests/cairn-phase-card.bats tests/cairn-phase-model.bats</automated>
  </verify>
  <acceptance_criteria>
    The new cross-surface test in `tests/cairn-phase-card.bats` passes, and
    fails when run against a version of `html_phases()` that omits the
    research/issues/verify meta spans (sanity-check this by temporarily
    commenting them out locally and confirming the test goes red, per this
    plan's own anti-false-green discipline — do not leave that change in the
    committed code). `grep -qF 'class="phase-purpose"' cairn/scripts/
    cairn-status.py` and the same string in a generated `board.html` fixture
    both succeed.
  </acceptance_criteria>
  <done>Terminal and HTML render `purpose`/research/issues/verify from the
  identical `phases[]` data, proven by a test that extracts from `--json`
  and greps both surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `phases[].purpose`/research/issues/verify text -> terminal render | Already-gated text (Plan 14-01's `clean()`) rendered via `render_spans()`/`truncate()`, same path every other board field uses |
| `phases[].purpose`/research/issues/verify text -> HTML render | Already-gated text rendered via `esc()`, same path every other HTML field uses |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04 | Tampering | `phase_purpose_text()` rendered in the terminal | low | mitigate | Reuses the existing `truncate()`/`style.asciify()`/`render_spans()` pipeline every other board field already passes through — no new unescaped print path; the text was already `clean()`-gated at the data layer (Plan 14-01, T-14-01). |
| T-14-05 | Tampering | `phase_purpose_text()` rendered in HTML | low | mitigate | Reuses `esc()` exactly like `phase_state_text()`/title text already do — a Card/Goal paragraph containing `<script>` or quote characters renders as inert text, never breaks out of its `<span>`. |
| T-14-06 | Information Disclosure | New `verify`/`issues`/`rsch` columns surface data that previously only existed in `--json` | low | accept | This is the phase's entire purpose (CARD-01/02): the data already existed in `--json` and on disk; this plan makes it visible on a board already scoped to repo-local operators, not a new trust boundary. |
</threat_model>

<verification>
- `bats tests/cairn-phase-card.bats tests/cairn-phase-model.bats tests/cairn-corroboration.bats tests/cairn-status.bats` — full green.
- Manual: `cairn-status.sh --width 100` and `cairn-status.sh --html /tmp/board.html` against `make_gsd_fixture` both run without crashing and show the new columns/purpose text.
</verification>

<success_criteria>
The terminal board and the HTML page both name, per pending phase: its
purpose, whether research happened, plans done/total, issues closed/total,
the verification verdict, what it waits on, and the next command with its
reason — read from one `phases[]` model, proven identical by a test that
extracts from `--json` and greps both renders. `NEXT COMMANDS` no longer
exists as a terminal section. Every pre-existing test in this repo's `bats`
suite for `cairn-status.py` still passes.
</success_criteria>

<output>
Create `.planning/phases/14-phase-card/14-02-SUMMARY.md` when done.
</output>
