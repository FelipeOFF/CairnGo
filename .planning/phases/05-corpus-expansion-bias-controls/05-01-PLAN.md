---
phase: 05-corpus-expansion-bias-controls
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - benchmarks/tasks/bugfix-inventory/task.json
  - benchmarks/tasks/bugfix-inventory/prompt.md
  - benchmarks/tasks/bugfix-inventory/verify.sh
  - benchmarks/tasks/bugfix-inventory/fixture/inventory.py
  - benchmarks/tasks/bugfix-inventory/fixture/orders.py
  - benchmarks/tasks/bugfix-inventory/fixture/tests/test_orders.py
  - benchmarks/tasks/feature-todo/task.json
  - benchmarks/tasks/feature-todo/prompt.md
  - benchmarks/tasks/feature-todo/verify.sh
  - benchmarks/tasks/feature-todo/fixture/todo.py
  - benchmarks/tasks/feature-todo/fixture/tests/test_todo.py
  - benchmarks/tasks/refactor-report/task.json
  - benchmarks/tasks/refactor-report/prompt.md
  - benchmarks/tasks/refactor-report/verify.sh
  - benchmarks/tasks/refactor-report/fixture/report.py
  - benchmarks/tasks/refactor-report/fixture/tests/test_report.py
  - benchmarks/tasks/microedit-greet/task.json
  - benchmarks/tasks/microedit-greet/prompt.md
  - benchmarks/tasks/microedit-greet/verify.sh
  - benchmarks/tasks/microedit-greet/fixture/greet.py
  - benchmarks/tasks/microedit-greet/fixture/tests/test_greet.py
  - benchmarks/tasks/longhorizon-notify/task.json
  - benchmarks/tasks/longhorizon-notify/prompt.md
  - benchmarks/tasks/longhorizon-notify/verify.sh
  - benchmarks/tasks/longhorizon-notify/fixture/events.py
  - benchmarks/tasks/longhorizon-notify/fixture/notifications.py
  - benchmarks/tasks/longhorizon-notify/fixture/app.py
  - benchmarks/tasks/longhorizon-notify/fixture/tests/test_pipeline.py
  - tests/bench-corpus.bats
autonomous: true
requirements: [CORP-01]
beads: [CairnGo-me5]

must_haves:
  truths:
    - "Five new task fixtures exist (bugfix-inventory, feature-todo, refactor-report, microedit-greet, longhorizon-notify), each following the smoke-convert fixture contract exactly: task.json + prompt.md + fixture/ + verify.sh outside the fixture, stdlib unittest only"
    - "Every new task's verify.sh exits non-zero against its shipped (unsolved) fixture and exits 0 against a hand-solved copy — proven at $0 via bats, zero agent/API involvement, both directions for all 5 tasks"
    - "microedit-greet is labeled category: honest-non-win in its task.json — a genuinely trivial one-line docstring fix where any planning/bookkeeping overhead is pure cost, not value"
    - "refactor-report's verify.sh rejects a functionally-correct-but-unrefactored fixture via a structural anti-cheat check (duplicated accumulation line count), not just behavior tests — passing unittest alone is not enough"
    - "longhorizon-notify requires implementing NotificationLog + record_notification and wiring them into app.py's setup(), reading events.py's EventBus, before its 5 tests pass — a genuine 3-file implement+wire+test task"
    - "No new task's prompt.md mentions any arm name (cairn, gsd, ralph) or arm-favoring language ('plan first', 'just edit') — every prompt states only the task"
  artifacts:
    - path: benchmarks/tasks/bugfix-inventory/task.json
      provides: "task metadata with category: bugfix"
      contains: "\"category\": \"bugfix\""
    - path: benchmarks/tasks/feature-todo/task.json
      provides: "task metadata with category: feature"
      contains: "\"category\": \"feature\""
    - path: benchmarks/tasks/refactor-report/task.json
      provides: "task metadata with category: refactor"
      contains: "\"category\": \"refactor\""
    - path: benchmarks/tasks/microedit-greet/task.json
      provides: "task metadata with category: honest-non-win (the required unfavorable category)"
      contains: "\"category\": \"honest-non-win\""
    - path: benchmarks/tasks/longhorizon-notify/task.json
      provides: "task metadata with category: long-horizon"
      contains: "\"category\": \"long-horizon\""
    - path: benchmarks/tasks/refactor-report/verify.sh
      provides: "behavior tests + anti-cheat structural duplication check"
      contains: "total += r"
    - path: tests/bench-corpus.bats
      provides: "two-direction (unsolved-fails / hand-solved-passes) $0 proof for all 5 new tasks"
      contains: "verify.sh passes against a hand-solved fixture"
  key_links:
    - from: "benchmarks/tasks/*/task.json prompt_file"
      to: "benchmarks/tasks/*/prompt.md"
      via: "bench-run.py reads task[\"prompt_file\"] (default prompt.md) as the literal claude -p prompt text"
      pattern: "prompt_file"
    - from: "tests/bench-corpus.bats @test blocks"
      to: "benchmarks/tasks/*/verify.sh"
      via: "bash <task>/verify.sh <workdir> against a copied unsolved fixture, then a copied hand-solved fixture"
      pattern: "verify.sh"
    - from: "benchmarks/tasks/refactor-report/verify.sh anti-cheat grep"
      to: "benchmarks/tasks/refactor-report/fixture/report.py duplicated line"
      via: "grep -cF 'total += r[\"amount\"]' report.py, must be <=1 after extraction (was 3)"
      pattern: "total \\+= r"
---

<objective>
Add the 5 new task fixtures the corpus needs per CORP-01 (bugfix, feature, refactor, honest-non-win micro-edit, long-horizon), each provable solved/unsolved at $0 exactly like smoke-convert, so the corpus reaches 6 pre-declared task categories before any comparative baseline results exist (Pitfall 3: cherry-picking).

Purpose: CORP-01's diversity + bias-control requirement — a benchmark corpus authored entirely by the tool's own author is only credible if it includes a category the tool is expected to lose or tie (05-CONTEXT.md's locked "honest non-win" decision), plus enough distinct task *types* (not repeated variations of one shape) to start separating a real effect from run-to-run noise (PITFALLS.md Pitfall 2/3).
Output: 5 new task dirs under `benchmarks/tasks/` (each: task.json, prompt.md, verify.sh, fixture/), and `tests/bench-corpus.bats` proving both directions ($0) for all 5.
</objective>

<execution_context>
@/Users/felipeoliveira/.claude/plugins/cache/cairngo/gsd/4.3.1/workflows/execute-plan.md
@/Users/felipeoliveira/.claude/plugins/cache/cairngo/gsd/4.3.1/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-corpus-expansion-bias-controls/05-CONTEXT.md
@benchmarks/tasks/smoke-convert/task.json
@benchmarks/tasks/smoke-convert/prompt.md
@benchmarks/tasks/smoke-convert/verify.sh
@benchmarks/tasks/smoke-convert/fixture/convert.py
@benchmarks/tasks/smoke-convert/fixture/tests/test_convert.py
@tests/bench-verify.bats
@tests/helpers.bash
</context>

<interfaces>
<!-- Exact target content for every new file, and the exact bats proof content.
Use directly — no exploration, no paraphrasing prompt.md wording, no altering
test assertions (the tests ARE the spec each fixture must satisfy). -->

### The fixture contract (from smoke-convert, unchanged)

Every task dir: `task.json` (id, category, timeout_s, max_turns, prompt_file, model — model/max_turns mirror the existing convention but are NOT what actually governs a live run: `bench-run.py` sources `--model`/`--max-turns` exclusively from the baseline manifest's `claude_flags`, identical across all 4 arms by design (FAIR-02) — task.json's `max_turns` is documentation of intended size class, not a live behavior switch), `prompt.md` (the literal `claude -p` prompt text, neutral — states the task, never "plan first"/"just edit", never an arm name), `verify.sh` (executable, `<workdir>` as `$1`, exit 0 = solved), `fixture/` (the starting state `bench-run.py` copies into a fresh workdir; `fixture/tests/` holds the oracle tests).

---

### Task 1 files — `bugfix-inventory` (category: bugfix) and `feature-todo` (category: feature)

**`benchmarks/tasks/bugfix-inventory/task.json`:**
```json
{
  "id": "bugfix-inventory",
  "category": "bugfix",
  "timeout_s": 180,
  "max_turns": 10,
  "prompt_file": "prompt.md",
  "model": "claude-haiku-4-5-20251001"
}
```

**`benchmarks/tasks/bugfix-inventory/prompt.md`:**
```
Fix the bug in the order fulfillment flow so all tests in `tests/test_orders.py` pass. `fulfill_order()` in `orders.py` should reserve stock via `Inventory.reserve()` in `inventory.py`; if any line item in the order cannot be fulfilled, the whole order must be rejected and inventory must be left completely unchanged.
```

**`benchmarks/tasks/bugfix-inventory/verify.sh`:**
```bash
#!/usr/bin/env bash
# verify.sh <workdir> -- exit 0 = task solved, non-zero = not solved.
# Never staged inside <workdir>; invoked with its path as an argument.
set -euo pipefail
WORKDIR="$1"
cd "$WORKDIR"
exec python3 -m unittest tests.test_orders -v
```

**`benchmarks/tasks/bugfix-inventory/fixture/inventory.py`** (given, correct — not the bug):
```python
"""Inventory stock tracking."""


class Inventory:
    def __init__(self, stock):
        self.stock = dict(stock)

    def available(self, sku):
        return self.stock.get(sku, 0)

    def reserve(self, sku, qty):
        """Deduct qty from stock. Raises ValueError if insufficient stock."""
        current = self.available(sku)
        if qty > current:
            raise ValueError(f"insufficient stock for {sku}")
        self.stock[sku] = current - qty
```

**`benchmarks/tasks/bugfix-inventory/fixture/orders.py`** (the bug — never actually reserves stock, so `tests.test_orders` fails):
```python
"""Order fulfillment logic."""

from inventory import Inventory


def fulfill_order(inventory: Inventory, order):
    """order: list of (sku, qty) tuples. Returns True if fully fulfilled.

    Raises ValueError if any line item cannot be reserved. BUG: never
    actually reserves stock, so inventory is never decremented.
    """
    for sku, qty in order:
        if inventory.available(sku) < qty:
            raise ValueError(f"insufficient stock for {sku}")
    return True
```

**`benchmarks/tasks/bugfix-inventory/fixture/tests/test_orders.py`:**
```python
import unittest

from inventory import Inventory
from orders import fulfill_order


class TestOrders(unittest.TestCase):
    def setUp(self):
        self.inv = Inventory({"widget": 10, "gadget": 3})

    def test_fulfill_reduces_stock(self):
        fulfill_order(self.inv, [("widget", 4)])
        self.assertEqual(self.inv.available("widget"), 6)

    def test_fulfill_multiple_lines(self):
        fulfill_order(self.inv, [("widget", 2), ("gadget", 1)])
        self.assertEqual(self.inv.available("widget"), 8)
        self.assertEqual(self.inv.available("gadget"), 2)

    def test_insufficient_stock_raises(self):
        with self.assertRaises(ValueError):
            fulfill_order(self.inv, [("gadget", 5)])

    def test_insufficient_stock_leaves_inventory_unchanged(self):
        with self.assertRaises(ValueError):
            fulfill_order(self.inv, [("widget", 1), ("gadget", 5)])
        self.assertEqual(self.inv.available("widget"), 10)
        self.assertEqual(self.inv.available("gadget"), 3)


if __name__ == "__main__":
    unittest.main()
```

**Hand-solved `orders.py`** (validate ALL lines before reserving ANY — required so a failing order leaves inventory untouched, per the 4th test):
```python
"""Order fulfillment logic."""

from inventory import Inventory


def fulfill_order(inventory: Inventory, order):
    """order: list of (sku, qty) tuples. Returns True if fully fulfilled.

    Validates every line item before reserving anything, so a failing order
    leaves inventory completely unchanged.
    """
    for sku, qty in order:
        if inventory.available(sku) < qty:
            raise ValueError(f"insufficient stock for {sku}")
    for sku, qty in order:
        inventory.reserve(sku, qty)
    return True
```

---

**`benchmarks/tasks/feature-todo/task.json`:**
```json
{
  "id": "feature-todo",
  "category": "feature",
  "timeout_s": 180,
  "max_turns": 10,
  "prompt_file": "prompt.md",
  "model": "claude-haiku-4-5-20251001"
}
```

**`benchmarks/tasks/feature-todo/prompt.md`:**
```
Add `pending()` and `summary()` methods to `TodoList` in `todo.py` so the tests in `tests/test_todo.py` pass. `pending()` returns the list of items not yet marked done, sorted by `priority` descending. `summary()` returns a dict with `total`, `pending`, and `done` counts.
```

**`benchmarks/tasks/feature-todo/fixture/todo.py`** (methods missing — the spec to implement):
```python
"""A minimal todo list."""


class TodoItem:
    def __init__(self, text, priority=0):
        self.text = text
        self.priority = priority
        self.done = False


class TodoList:
    def __init__(self):
        self.items = []

    def add(self, text, priority=0):
        item = TodoItem(text, priority=priority)
        self.items.append(item)
        return item

    def complete(self, text):
        for item in self.items:
            if item.text == text:
                item.done = True
                return True
        return False

    # TODO: implement pending() and summary() per prompt.md
```

**`benchmarks/tasks/feature-todo/fixture/tests/test_todo.py`:**
```python
import unittest

from todo import TodoList


class TestTodoList(unittest.TestCase):
    def setUp(self):
        self.todos = TodoList()
        self.todos.add("write report", priority=2)
        self.todos.add("water plants", priority=0)
        self.todos.add("fix bug", priority=5)
        self.todos.complete("water plants")

    def test_pending_excludes_done_items(self):
        pending_texts = [item.text for item in self.todos.pending()]
        self.assertNotIn("water plants", pending_texts)
        self.assertIn("write report", pending_texts)
        self.assertIn("fix bug", pending_texts)

    def test_pending_sorted_by_priority_descending(self):
        pending_texts = [item.text for item in self.todos.pending()]
        self.assertEqual(pending_texts, ["fix bug", "write report"])

    def test_summary_counts(self):
        self.assertEqual(self.todos.summary(),
                          {"total": 3, "pending": 2, "done": 1})

    def test_summary_updates_after_more_completions(self):
        self.todos.complete("fix bug")
        self.assertEqual(self.todos.summary(),
                          {"total": 3, "pending": 1, "done": 2})


if __name__ == "__main__":
    unittest.main()
```

**`benchmarks/tasks/feature-todo/verify.sh`:**
```bash
#!/usr/bin/env bash
# verify.sh <workdir> -- exit 0 = task solved, non-zero = not solved.
set -euo pipefail
WORKDIR="$1"
cd "$WORKDIR"
exec python3 -m unittest tests.test_todo -v
```

**Hand-solved `TodoList` additions** (append to the class):
```python
    def pending(self):
        return sorted((item for item in self.items if not item.done),
                      key=lambda item: item.priority, reverse=True)

    def summary(self):
        done = sum(1 for item in self.items if item.done)
        return {"total": len(self.items), "pending": len(self.items) - done,
                "done": done}
```

---

### `tests/bench-corpus.bats` — header + Task 1's 4 tests (Tasks 2/3 append more `@test` blocks to this same file)

```bash
#!/usr/bin/env bats
# bench-corpus.bats — two-direction verify.sh proof (unsolved fails, hand-
# solved passes) for the 5 new corpus tasks added in Phase 5 (bugfix-inventory,
# feature-todo, refactor-report, microedit-greet, longhorizon-notify). Mirrors
# tests/bench-verify.bats's pattern for smoke-convert. Zero agent or API
# involvement — every "solved" fixture below is hand-written.
#
# Assertion style note (same as bench-verify.bats): a failing `[[ ]]` or
# `! cmd` mid-test does NOT fail a bats test on this bash, so checks use
# explicit `[ "$status" -eq/-ne N ]`.

load 'helpers'

BENCH_TASKS_DIR="$CAIRN_REPO_ROOT/benchmarks/tasks"

# ---------------------------------------------------------------------------
# bugfix-inventory
# ---------------------------------------------------------------------------

@test "bugfix-inventory: verify.sh fails against the unsolved fixture" {
  cp -r "$BENCH_TASKS_DIR/bugfix-inventory/fixture" "$BATS_TEST_TMPDIR/unsolved"
  run bash "$BENCH_TASKS_DIR/bugfix-inventory/verify.sh" "$BATS_TEST_TMPDIR/unsolved"
  [ "$status" -ne 0 ]
}

@test "bugfix-inventory: verify.sh passes against a hand-solved fixture" {
  cp -r "$BENCH_TASKS_DIR/bugfix-inventory/fixture" "$BATS_TEST_TMPDIR/solved"
  cat > "$BATS_TEST_TMPDIR/solved/orders.py" <<'EOF'
"""Order fulfillment logic."""

from inventory import Inventory


def fulfill_order(inventory: Inventory, order):
    """order: list of (sku, qty) tuples. Returns True if fully fulfilled.

    Validates every line item before reserving anything, so a failing order
    leaves inventory completely unchanged.
    """
    for sku, qty in order:
        if inventory.available(sku) < qty:
            raise ValueError(f"insufficient stock for {sku}")
    for sku, qty in order:
        inventory.reserve(sku, qty)
    return True
EOF
  run bash "$BENCH_TASKS_DIR/bugfix-inventory/verify.sh" "$BATS_TEST_TMPDIR/solved"
  [ "$status" -eq 0 ]
}

# ---------------------------------------------------------------------------
# feature-todo
# ---------------------------------------------------------------------------

@test "feature-todo: verify.sh fails against the unsolved fixture" {
  cp -r "$BENCH_TASKS_DIR/feature-todo/fixture" "$BATS_TEST_TMPDIR/unsolved"
  run bash "$BENCH_TASKS_DIR/feature-todo/verify.sh" "$BATS_TEST_TMPDIR/unsolved"
  [ "$status" -ne 0 ]
}

@test "feature-todo: verify.sh passes against a hand-solved fixture" {
  cp -r "$BENCH_TASKS_DIR/feature-todo/fixture" "$BATS_TEST_TMPDIR/solved"
  cat > "$BATS_TEST_TMPDIR/solved/todo.py" <<'EOF'
"""A minimal todo list."""


class TodoItem:
    def __init__(self, text, priority=0):
        self.text = text
        self.priority = priority
        self.done = False


class TodoList:
    def __init__(self):
        self.items = []

    def add(self, text, priority=0):
        item = TodoItem(text, priority=priority)
        self.items.append(item)
        return item

    def complete(self, text):
        for item in self.items:
            if item.text == text:
                item.done = True
                return True
        return False

    def pending(self):
        return sorted((item for item in self.items if not item.done),
                      key=lambda item: item.priority, reverse=True)

    def summary(self):
        done = sum(1 for item in self.items if item.done)
        return {"total": len(self.items), "pending": len(self.items) - done,
                "done": done}
EOF
  run bash "$BENCH_TASKS_DIR/feature-todo/verify.sh" "$BATS_TEST_TMPDIR/solved"
  [ "$status" -eq 0 ]
}
```

---

### Task 2 files — `refactor-report` (category: refactor) and `microedit-greet` (category: honest-non-win)

**`benchmarks/tasks/refactor-report/task.json`:**
```json
{
  "id": "refactor-report",
  "category": "refactor",
  "timeout_s": 180,
  "max_turns": 10,
  "prompt_file": "prompt.md",
  "model": "claude-haiku-4-5-20251001"
}
```

**`benchmarks/tasks/refactor-report/prompt.md`:**
```
Refactor `report.py` to remove the duplicated summation loop across `total_sales`, `total_refunds`, and `total_tax` by extracting it into a shared helper. Every function's return value must stay exactly the same for the same input — all existing tests in `tests/test_report.py` must keep passing.
```

**`benchmarks/tasks/refactor-report/fixture/report.py`** (unsolved — duplicated, but functionally correct: unittest already passes on this file; only the anti-cheat check fails):
```python
"""Sales report totals — each function repeats the same filter-and-sum loop."""


def total_sales(records):
    total = 0
    for r in records:
        if r["kind"] == "sale":
            total += r["amount"]
    return total


def total_refunds(records):
    total = 0
    for r in records:
        if r["kind"] == "refund":
            total += r["amount"]
    return total


def total_tax(records):
    total = 0
    for r in records:
        if r["kind"] == "tax":
            total += r["amount"]
    return total
```

**`benchmarks/tasks/refactor-report/fixture/tests/test_report.py`:**
```python
import unittest

from report import total_sales, total_refunds, total_tax


class TestReport(unittest.TestCase):
    def setUp(self):
        self.records = [
            {"kind": "sale", "amount": 100},
            {"kind": "sale", "amount": 50},
            {"kind": "refund", "amount": 20},
            {"kind": "tax", "amount": 8},
            {"kind": "tax", "amount": 2},
        ]

    def test_total_sales(self):
        self.assertEqual(total_sales(self.records), 150)

    def test_total_refunds(self):
        self.assertEqual(total_refunds(self.records), 20)

    def test_total_tax(self):
        self.assertEqual(total_tax(self.records), 10)

    def test_empty_records(self):
        self.assertEqual(total_sales([]), 0)
        self.assertEqual(total_refunds([]), 0)
        self.assertEqual(total_tax([]), 0)


if __name__ == "__main__":
    unittest.main()
```

**`benchmarks/tasks/refactor-report/verify.sh`** — **the exact anti-cheat structural check, byte-for-byte**. This is deliberately two-layered: `python3 -m unittest` runs first (still required to pass); the anti-cheat grep runs second and is what actually distinguishes "refactored" from "unrefactored but functionally correct" — the raw unsolved fixture passes unittest but MUST still fail verify.sh overall via this second check:
```bash
#!/usr/bin/env bash
# verify.sh <workdir> -- exit 0 = task solved, non-zero = not solved.
# Runs the behavior tests AND an anti-cheat structural check: the duplicated
# `total += r["amount"]` accumulation line must be deduplicated to at most
# one occurrence (it appears 3 times in the unrefactored fixture — one per
# function). Passing the behavior tests alone is never enough: the raw
# fixture already passes them (the duplication is functionally correct), so
# this second check is what actually proves extraction happened.
set -euo pipefail
WORKDIR="$1"
cd "$WORKDIR"
python3 -m unittest tests.test_report -v

DUP_COUNT=$(grep -v '^\s*#' report.py | grep -cF 'total += r["amount"]' || true)
if [ "$DUP_COUNT" -gt 1 ]; then
  echo "anti-cheat: 'total += r[\"amount\"]' still appears $DUP_COUNT times in report.py (expected <=1 after extracting a shared helper)" >&2
  exit 1
fi
echo "anti-cheat: duplication check passed ($DUP_COUNT occurrence(s))"
```

**Hand-refactored `report.py`** (extraction — `total += r["amount"]` now appears exactly once):
```python
"""Sales report totals."""


def _sum_by_kind(records, kind):
    total = 0
    for r in records:
        if r["kind"] == kind:
            total += r["amount"]
    return total


def total_sales(records):
    return _sum_by_kind(records, "sale")


def total_refunds(records):
    return _sum_by_kind(records, "refund")


def total_tax(records):
    return _sum_by_kind(records, "tax")
```

---

**`benchmarks/tasks/microedit-greet/task.json`:**
```json
{
  "id": "microedit-greet",
  "category": "honest-non-win",
  "timeout_s": 90,
  "max_turns": 6,
  "prompt_file": "prompt.md",
  "model": "claude-haiku-4-5-20251001"
}
```

**`benchmarks/tasks/microedit-greet/prompt.md`:**
```
Fix the typo in the docstring of `greet()` in `greet.py`: it currently reads "Retun a friendly greeting for NAME." and should read "Return a friendly greeting for NAME.". Do not change the function's behavior.
```

**`benchmarks/tasks/microedit-greet/fixture/greet.py`** (unsolved — the typo):
```python
"""Greeting utilities."""


def greet(name):
    """Retun a friendly greeting for NAME."""
    return f"Hello, {name}!"
```

**`benchmarks/tasks/microedit-greet/fixture/tests/test_greet.py`:**
```python
import unittest

from greet import greet


class TestGreet(unittest.TestCase):
    def test_docstring_fixed(self):
        self.assertEqual(greet.__doc__, "Return a friendly greeting for NAME.")

    def test_behavior_unchanged(self):
        self.assertEqual(greet("Ada"), "Hello, Ada!")


if __name__ == "__main__":
    unittest.main()
```

**`benchmarks/tasks/microedit-greet/verify.sh`:**
```bash
#!/usr/bin/env bash
# verify.sh <workdir> -- exit 0 = task solved, non-zero = not solved.
set -euo pipefail
WORKDIR="$1"
cd "$WORKDIR"
exec python3 -m unittest tests.test_greet -v
```

**Hand-solved `greet.py`:**
```python
"""Greeting utilities."""


def greet(name):
    """Return a friendly greeting for NAME."""
    return f"Hello, {name}!"
```

---

### Task 2 appends to `tests/bench-corpus.bats` — 4 more `@test` blocks (refactor-report x2, microedit-greet x2)

```bash
# ---------------------------------------------------------------------------
# refactor-report
# ---------------------------------------------------------------------------

@test "refactor-report: verify.sh fails against the unsolved (unrefactored) fixture" {
  cp -r "$BENCH_TASKS_DIR/refactor-report/fixture" "$BATS_TEST_TMPDIR/unsolved"
  run bash "$BENCH_TASKS_DIR/refactor-report/verify.sh" "$BATS_TEST_TMPDIR/unsolved"
  # Behavior tests pass on the raw fixture (the duplication is functionally
  # correct); verify.sh must still fail via the anti-cheat structural check.
  [ "$status" -ne 0 ]
}

@test "refactor-report: verify.sh passes against a hand-refactored fixture" {
  cp -r "$BENCH_TASKS_DIR/refactor-report/fixture" "$BATS_TEST_TMPDIR/solved"
  cat > "$BATS_TEST_TMPDIR/solved/report.py" <<'EOF'
"""Sales report totals."""


def _sum_by_kind(records, kind):
    total = 0
    for r in records:
        if r["kind"] == kind:
            total += r["amount"]
    return total


def total_sales(records):
    return _sum_by_kind(records, "sale")


def total_refunds(records):
    return _sum_by_kind(records, "refund")


def total_tax(records):
    return _sum_by_kind(records, "tax")
EOF
  run bash "$BENCH_TASKS_DIR/refactor-report/verify.sh" "$BATS_TEST_TMPDIR/solved"
  [ "$status" -eq 0 ]
}

# ---------------------------------------------------------------------------
# microedit-greet (honest-non-win)
# ---------------------------------------------------------------------------

@test "microedit-greet: verify.sh fails against the unsolved fixture" {
  cp -r "$BENCH_TASKS_DIR/microedit-greet/fixture" "$BATS_TEST_TMPDIR/unsolved"
  run bash "$BENCH_TASKS_DIR/microedit-greet/verify.sh" "$BATS_TEST_TMPDIR/unsolved"
  [ "$status" -ne 0 ]
}

@test "microedit-greet: verify.sh passes against a hand-solved fixture" {
  cp -r "$BENCH_TASKS_DIR/microedit-greet/fixture" "$BATS_TEST_TMPDIR/solved"
  cat > "$BATS_TEST_TMPDIR/solved/greet.py" <<'EOF'
"""Greeting utilities."""


def greet(name):
    """Return a friendly greeting for NAME."""
    return f"Hello, {name}!"
EOF
  run bash "$BENCH_TASKS_DIR/microedit-greet/verify.sh" "$BATS_TEST_TMPDIR/solved"
  [ "$status" -eq 0 ]
}
```

---

### Task 3 files — `longhorizon-notify` (category: long-horizon)

**`benchmarks/tasks/longhorizon-notify/task.json`:**
```json
{
  "id": "longhorizon-notify",
  "category": "long-horizon",
  "timeout_s": 300,
  "max_turns": 14,
  "prompt_file": "prompt.md",
  "model": "claude-haiku-4-5-20251001"
}
```

**`benchmarks/tasks/longhorizon-notify/prompt.md`:**
```
Implement the order-notification pipeline across `notifications.py` and `app.py`:

1. In `notifications.py`, implement `NotificationLog` with:
   - `self.entries` starting as an empty list
   - `record_notification(log)` — a function that returns a handler callable;
     calling that handler with a payload dict appends the payload to `log.entries`
   - `NotificationLog.summary()` returning a dict `{"count": <len(entries)>}`

2. In `app.py`, implement `setup(bus, log)` so that a handler built from
   `record_notification(log)` is subscribed to both the `"order_placed"` and
   `"order_shipped"` event types on `bus` (an `EventBus` from `events.py`).
   Other event types must not be logged.

See `tests/test_pipeline.py` for the exact expected behavior.
```

**`benchmarks/tasks/longhorizon-notify/fixture/events.py`** (given, complete — read-only reference, not the thing to change):
```python
"""A minimal synchronous event bus."""


class EventBus:
    def __init__(self):
        self._handlers = {}

    def subscribe(self, event_type, handler):
        self._handlers.setdefault(event_type, []).append(handler)

    def publish(self, event_type, payload):
        for handler in self._handlers.get(event_type, []):
            handler(payload)
```

**`benchmarks/tasks/longhorizon-notify/fixture/notifications.py`** (unsolved — stub):
```python
"""Notification logging — NOT YET IMPLEMENTED.

Implement NotificationLog and record_notification per prompt.md.
"""


class NotificationLog:
    pass


def record_notification(log):
    raise NotImplementedError
```

**`benchmarks/tasks/longhorizon-notify/fixture/app.py`** (unsolved — stub):
```python
"""Application wiring: connects the event bus to the notification log."""

from notifications import record_notification


def setup(bus, log):
    """TODO: subscribe record_notification(log) to the relevant event types."""
    pass
```

**`benchmarks/tasks/longhorizon-notify/fixture/tests/test_pipeline.py`:**
```python
import unittest

from app import setup
from events import EventBus
from notifications import NotificationLog


class TestPipeline(unittest.TestCase):
    def setUp(self):
        self.bus = EventBus()
        self.log = NotificationLog()
        setup(self.bus, self.log)

    def test_relevant_events_are_logged(self):
        self.bus.publish("order_placed", {"order_id": 1})
        self.bus.publish("order_shipped", {"order_id": 1})
        self.assertEqual(self.log.entries,
                          [{"order_id": 1}, {"order_id": 1}])

    def test_irrelevant_events_are_not_logged(self):
        self.bus.publish("order_cancelled", {"order_id": 2})
        self.assertEqual(self.log.entries, [])

    def test_order_preserved_across_multiple_events(self):
        self.bus.publish("order_placed", {"order_id": 1})
        self.bus.publish("order_placed", {"order_id": 2})
        self.bus.publish("order_shipped", {"order_id": 1})
        self.assertEqual(self.log.entries,
                          [{"order_id": 1}, {"order_id": 2}, {"order_id": 1}])

    def test_summary_counts_entries(self):
        self.bus.publish("order_placed", {"order_id": 1})
        self.bus.publish("order_shipped", {"order_id": 1})
        self.assertEqual(self.log.summary(), {"count": 2})

    def test_fresh_log_starts_empty(self):
        self.assertEqual(self.log.entries, [])
        self.assertEqual(self.log.summary(), {"count": 0})


if __name__ == "__main__":
    unittest.main()
```

**`benchmarks/tasks/longhorizon-notify/verify.sh`:**
```bash
#!/usr/bin/env bash
# verify.sh <workdir> -- exit 0 = task solved, non-zero = not solved.
set -euo pipefail
WORKDIR="$1"
cd "$WORKDIR"
exec python3 -m unittest tests.test_pipeline -v
```

**Hand-solved `notifications.py`:**
```python
"""Notification logging."""


class NotificationLog:
    def __init__(self):
        self.entries = []

    def summary(self):
        return {"count": len(self.entries)}


def record_notification(log):
    def handler(payload):
        log.entries.append(payload)
    return handler
```

**Hand-solved `app.py`:**
```python
"""Application wiring: connects the event bus to the notification log."""

from notifications import record_notification


def setup(bus, log):
    handler = record_notification(log)
    bus.subscribe("order_placed", handler)
    bus.subscribe("order_shipped", handler)
```

### Task 3 appends to `tests/bench-corpus.bats` — final 2 `@test` blocks (longhorizon-notify)

```bash
# ---------------------------------------------------------------------------
# longhorizon-notify
# ---------------------------------------------------------------------------

@test "longhorizon-notify: verify.sh fails against the unsolved fixture" {
  cp -r "$BENCH_TASKS_DIR/longhorizon-notify/fixture" "$BATS_TEST_TMPDIR/unsolved"
  run bash "$BENCH_TASKS_DIR/longhorizon-notify/verify.sh" "$BATS_TEST_TMPDIR/unsolved"
  [ "$status" -ne 0 ]
}

@test "longhorizon-notify: verify.sh passes against a hand-solved fixture" {
  cp -r "$BENCH_TASKS_DIR/longhorizon-notify/fixture" "$BATS_TEST_TMPDIR/solved"
  cat > "$BATS_TEST_TMPDIR/solved/notifications.py" <<'EOF'
"""Notification logging."""


class NotificationLog:
    def __init__(self):
        self.entries = []

    def summary(self):
        return {"count": len(self.entries)}


def record_notification(log):
    def handler(payload):
        log.entries.append(payload)
    return handler
EOF
  cat > "$BATS_TEST_TMPDIR/solved/app.py" <<'EOF'
"""Application wiring: connects the event bus to the notification log."""

from notifications import record_notification


def setup(bus, log):
    handler = record_notification(log)
    bus.subscribe("order_placed", handler)
    bus.subscribe("order_shipped", handler)
EOF
  run bash "$BENCH_TASKS_DIR/longhorizon-notify/verify.sh" "$BATS_TEST_TMPDIR/solved"
  [ "$status" -eq 0 ]
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: bugfix-inventory + feature-todo fixtures, plus their bats proofs</name>
  <files>benchmarks/tasks/bugfix-inventory/task.json, benchmarks/tasks/bugfix-inventory/prompt.md, benchmarks/tasks/bugfix-inventory/verify.sh, benchmarks/tasks/bugfix-inventory/fixture/inventory.py, benchmarks/tasks/bugfix-inventory/fixture/orders.py, benchmarks/tasks/bugfix-inventory/fixture/tests/test_orders.py, benchmarks/tasks/feature-todo/task.json, benchmarks/tasks/feature-todo/prompt.md, benchmarks/tasks/feature-todo/verify.sh, benchmarks/tasks/feature-todo/fixture/todo.py, benchmarks/tasks/feature-todo/fixture/tests/test_todo.py, tests/bench-corpus.bats</files>
  <read_first>
    benchmarks/tasks/smoke-convert/ (already loaded in context — the exact fixture contract this task replicates: task.json shape, prompt.md as literal claude -p text, verify.sh's `cd "$WORKDIR" && exec python3 -m unittest tests.test_X -v` pattern, fixture/ + fixture/tests/ layout)
    tests/bench-verify.bats (already loaded in context — the two-direction proof pattern: `cp -r fixture unsolved`, run verify.sh, assert non-zero; `cp -r fixture solved`, overwrite the buggy file via heredoc, run verify.sh, assert zero)
    this plan's Interfaces block above — exact content for every file this task writes
  </read_first>
  <action>
    Create `benchmarks/tasks/bugfix-inventory/` and `benchmarks/tasks/feature-todo/` exactly as given in the Interfaces block's "Task 1 files" section — task.json, prompt.md, verify.sh (chmod +x), fixture/*.py, fixture/tests/test_*.py, byte-for-byte. Do not paraphrase prompt.md wording (both are neutral: they state the task, never "plan first"/"just edit", never an arm name — CORP-01's bias-control decision) and do not alter any test assertion (the tests are the spec each fixture must satisfy).

    Then create `tests/bench-corpus.bats` with the header comment and the 4 `@test` blocks given in the Interfaces block's "`tests/bench-corpus.bats` — header + Task 1's 4 tests" section, using the hand-solved `orders.py`/`todo.py` contents given there verbatim as the "solved" fixture overwrites. This file will be appended to by Task 2 and Task 3 below — do not add a second `load 'helpers'` line or duplicate the `BENCH_TASKS_DIR` variable when those tasks append.
  </action>
  <verify>
    <automated>bats tests/bench-corpus.bats</automated>
  </verify>
  <acceptance_criteria>
    - `bats tests/bench-corpus.bats` exits 0, exactly 4 tests, all passing
    - `python3 -m unittest discover -s benchmarks/tasks/bugfix-inventory/fixture -t benchmarks/tasks/bugfix-inventory/fixture` run against the raw (unmodified) fixture reports a failure (the bug is genuinely present, not accidentally already-fixed)
    - `grep -qF '"category": "bugfix"' benchmarks/tasks/bugfix-inventory/task.json` and `grep -qF '"category": "feature"' benchmarks/tasks/feature-todo/task.json` both exit 0
    - `grep -qiE 'cairn|gsd|ralph' benchmarks/tasks/bugfix-inventory/prompt.md benchmarks/tasks/feature-todo/prompt.md` exits non-zero (no match — no arm names)
    - both verify.sh files are executable (`test -x`)
  </acceptance_criteria>
  <done>bugfix-inventory and feature-todo task dirs exist with the full fixture contract; tests/bench-corpus.bats proves both directions for each at $0.</done>
</task>

<task type="auto">
  <name>Task 2: refactor-report (with anti-cheat) + microedit-greet fixtures, plus their bats proofs</name>
  <files>benchmarks/tasks/refactor-report/task.json, benchmarks/tasks/refactor-report/prompt.md, benchmarks/tasks/refactor-report/verify.sh, benchmarks/tasks/refactor-report/fixture/report.py, benchmarks/tasks/refactor-report/fixture/tests/test_report.py, benchmarks/tasks/microedit-greet/task.json, benchmarks/tasks/microedit-greet/prompt.md, benchmarks/tasks/microedit-greet/verify.sh, benchmarks/tasks/microedit-greet/fixture/greet.py, benchmarks/tasks/microedit-greet/fixture/tests/test_greet.py, tests/bench-corpus.bats</files>
  <read_first>
    this plan's Interfaces block above — "Task 2 files" section has the exact target content for every file, including the byte-for-byte anti-cheat verify.sh
    tests/bench-corpus.bats (Task 1's output — append to this file, do not recreate it)
  </read_first>
  <action>
    Create `benchmarks/tasks/refactor-report/` and `benchmarks/tasks/microedit-greet/` exactly as given in the Interfaces block. `refactor-report/verify.sh` is the one task in this corpus with a two-layer oracle: it MUST run `python3 -m unittest tests.test_report -v` first, then the anti-cheat check — `grep -v '^\s*#' report.py | grep -cF 'total += r["amount"]'` must be `<=1` (it is exactly `3` in the shipped unsolved fixture; the raw fixture's unittest run genuinely PASSES since the duplicated code is functionally correct, so the anti-cheat grep is what makes verify.sh fail on the unsolved fixture — write the script exactly as shown, including the `|| true` on the grep pipeline so `set -e`/`pipefail` don't abort on a legitimate zero-match count). `microedit-greet/task.json` MUST carry `"category": "honest-non-win"` — this is the phase's one required unfavorable category (CORP-01); do not use this label on any other task.

    Append the 4 new `@test` blocks given in the Interfaces block's "Task 2 appends to `tests/bench-corpus.bats`" section to the END of the existing `tests/bench-corpus.bats` (do not touch Task 1's 4 tests). Use the hand-refactored `report.py` and hand-solved `greet.py` contents given there verbatim.
  </action>
  <verify>
    <automated>bats tests/bench-corpus.bats</automated>
  </verify>
  <acceptance_criteria>
    - `bats tests/bench-corpus.bats` exits 0, exactly 8 tests, all passing
    - against the raw unsolved `refactor-report/fixture/report.py`: `python3 -m unittest` (run manually from that dir) exits 0 (duplication is functionally correct) but `bash benchmarks/tasks/refactor-report/verify.sh <copy-of-fixture>` exits non-zero (anti-cheat catches it) — both halves of this claim are exercised by the bats tests above
    - `grep -qF '"category": "refactor"' benchmarks/tasks/refactor-report/task.json` and `grep -qF '"category": "honest-non-win"' benchmarks/tasks/microedit-greet/task.json` both exit 0
    - `grep -qiE 'cairn|gsd|ralph' benchmarks/tasks/refactor-report/prompt.md benchmarks/tasks/microedit-greet/prompt.md` exits non-zero
  </acceptance_criteria>
  <done>refactor-report (with anti-cheat verify.sh) and microedit-greet (honest-non-win) task dirs exist; tests/bench-corpus.bats now proves both directions for 4 of the 5 new tasks at $0.</done>
</task>

<task type="auto">
  <name>Task 3: longhorizon-notify fixture (3-file implement+wire+test), its bats proof, and full-corpus sanity run</name>
  <files>benchmarks/tasks/longhorizon-notify/task.json, benchmarks/tasks/longhorizon-notify/prompt.md, benchmarks/tasks/longhorizon-notify/verify.sh, benchmarks/tasks/longhorizon-notify/fixture/events.py, benchmarks/tasks/longhorizon-notify/fixture/notifications.py, benchmarks/tasks/longhorizon-notify/fixture/app.py, benchmarks/tasks/longhorizon-notify/fixture/tests/test_pipeline.py, tests/bench-corpus.bats</files>
  <read_first>
    this plan's Interfaces block above — "Task 3 files" section has the exact target content, including the complete (already-correct) events.py that ships as-is
    tests/bench-corpus.bats (Tasks 1+2's output — append the final 2 tests, do not recreate)
  </read_first>
  <action>
    Create `benchmarks/tasks/longhorizon-notify/` exactly as given in the Interfaces block: `events.py` ships already-implemented (the agent reads it but does not need to change it — it's the pre-existing dependency the new code wires into); `notifications.py` and `app.py` ship as stubs (`NotificationLog` a bare `pass`, `record_notification` raising `NotImplementedError`, `setup()` a no-op) that must be implemented and wired together per prompt.md before `tests/test_pipeline.py`'s 5 tests pass — this is the genuine multi-file implement+wire+test shape CORP-01 calls for.

    Append the final 2 `@test` blocks given in the Interfaces block's "Task 3 appends" section to the end of `tests/bench-corpus.bats` (now 10 tests total). Then run the full existing + new bats suite together as a sanity check that nothing in the shared `tests/bench-corpus.bats` file collides across the 3 tasks that wrote it, and that the pre-existing smoke-convert proof is unaffected.
  </action>
  <verify>
    <automated>bats tests/bench-verify.bats tests/bench-corpus.bats</automated>
  </verify>
  <acceptance_criteria>
    - `bats tests/bench-verify.bats tests/bench-corpus.bats` exits 0, 3 + 10 = 13 tests total, all passing
    - `grep -qF '"category": "long-horizon"' benchmarks/tasks/longhorizon-notify/task.json` exits 0
    - `grep -qiE 'cairn|gsd|ralph' benchmarks/tasks/longhorizon-notify/prompt.md` exits non-zero
    - the corpus now has exactly 6 task dirs under `benchmarks/tasks/`: `ls -d benchmarks/tasks/*/ | wc -l` equals 6
    - `jq -r '.category' benchmarks/tasks/*/task.json 2>/dev/null | sort -u` — every one of the 5 new tasks reports a category (smoke-convert's own category is added in the companion plan, wave 2)
  </acceptance_criteria>
  <done>longhorizon-notify (3-file implement+wire+test) task dir exists; tests/bench-corpus.bats proves both directions for all 5 new tasks (10 tests); the full corpus is 6 task dirs, verified together with the pre-existing smoke-convert proof at $0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus author -> fixture starting state | The 5 new fixtures' initial (unsolved) code is author-written, deterministic, stdlib-only — no external input at authoring time |
| agent-under-test -> fixture files -> `verify.sh` execution | During a live benchmark run (not this plan — a later, live-key-gated run), an agent (any of the 4 arms) rewrites fixture files inside a disposable workdir; `verify.sh` then executes that agent-modified code with the harness's full inherited environment (pre-existing, unchanged design per `bench-run.py`'s own docstring) |
| `refactor-report`'s anti-cheat grep -> `report.py`'s literal source text | A structural heuristic (byte-literal substring count), not a semantic/AST check — it certifies whether extraction occurred, not full code quality |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-05-01 | Tampering | 5 new fixture Python files | mitigate | stdlib `unittest` only, zero new dependencies, deterministic and fully author-controlled at commit time (CORP-01's "pre-declared, frozen before any results exist" decision) |
| T-05-02 | Elevation of Privilege / Tampering | Agent-under-test-rewritten fixture code executed by `verify.sh` with the harness's full inherited environment | accept | Identical, already-accepted trust boundary established in Phase 1 (`bench-run.py`'s workdir is a disposable `mktemp` dir, `rmtree`'d after every run; the oracle is deliberately not further isolated so it can find `python3`/`unittest` on `PATH`) — unchanged by this plan, which only adds more fixtures inside the same existing boundary |
| T-05-03 | Tampering (of the anti-cheat signal) | `refactor-report`'s duplication-count anti-cheat is a literal-substring grep, not an AST/semantic check | mitigate | Documented limitation (this plan's threat model + the verify.sh comment itself): a sufficiently adversarial rewrite could dodge the exact string while leaving duplication logically intact; accepted as a reasonable heuristic proxy for "did extraction actually happen," proportionate to a $0 stdlib-only fixture, not a claim of exhaustive structural analysis |
| T-05-SC | Tampering | Package/plugin source legitimacy | accept | No npm/pip/cargo installs in this plan (stdlib-only fixtures) — the Package Legitimacy Gate does not apply, matching the precedent already established for phases with no new package installs |
</threat_model>

<verification>
Before declaring this plan complete:
- [ ] `bats tests/bench-verify.bats tests/bench-corpus.bats` — 13 tests, all pass, $0
- [ ] All 5 new task dirs exist under `benchmarks/tasks/` with the full fixture contract (task.json, prompt.md, verify.sh, fixture/)
- [ ] Every new task.json carries a `category` field; exactly one (`microedit-greet`) is `"honest-non-win"`
- [ ] `refactor-report/verify.sh` genuinely distinguishes "tests pass" from "actually refactored" (proven by the bats pair: raw fixture fails verify.sh despite unittest passing on its own)
- [ ] No prompt.md in the corpus mentions cairn/gsd/ralph or arm-favoring language
</verification>

<success_criteria>
- CORP-01 (partial — the corpus-diversity half) satisfied: 6 pre-declared task categories exist, each provable at $0 in both directions, one explicitly labeled unfavorable to cairn
- ROADMAP Phase 5 SC1 (multiple representative task categories) and SC2 (an explicitly unfavorable category) are structurally satisfied by this plan; SC2's "runs through the same four-arm pipeline as every other task" claim is proven in the companion plan (05-02), which needs these fixtures to exist first
- Zero regression to smoke-convert or any existing bats suite
- Zero Claude API spend (fixture authoring and bats proofs are entirely local/static)
</success_criteria>

<output>
Create `.planning/phases/05-corpus-expansion-bias-controls/05-01-SUMMARY.md` when done
</output>


<!-- checker fix: EVERY task's verify.sh (all 5 new fixtures, Tasks 1-3) must be chmod +x with a `test -x` acceptance check — the fixture contract requires executables consistently, not only in Task 1. -->
