---
phase: 15-phase-lease
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - cairn/scripts/cairn-doctor.py
  - tests/cairn-doctor.bats
autonomous: true
requirements: [LEASE-05, LEASE-04]
beads: [CairnGo-dwl, CairnGo-13q]

must_haves:
  truths:
    - "/cairn:doctor reports a stale phase lease (heartbeat older than 4h) by phase, holder and since-when, with the same WARN-only discipline as check 8 claims-stale — it never fails the doctor run (LEASE-05)"
    - "The healthy fixture (no lease ever acquired) reports the new check 'ok', and every existing assertion of doctor's total check count is updated in lockstep"
    - "The lease bookkeeping issue is never flagged by check 6 (orphans) for carrying no phase-* label"
  artifacts:
    - path: cairn/scripts/cairn-doctor.py
      provides: "check_lease_stale() (check 13), NO_PHASE_EXEMPT extended with 'lease', the checks list and docstring updated"
    - path: tests/cairn-doctor.bats
      provides: "coverage for the new check plus the updated total-check-count assertions"
  key_links:
    - from: "check_lease_stale()"
      to: "cairn-lease.py status --all --json (Plan 15-01)"
      via: "subprocess.run, the same shell-out-to-a-sibling-script pattern check_maps_fresh() already uses for cairn-map.py --check and check_phase_corroboration() uses for cairn-status.py --json — no TTL math is re-derived here"
      pattern: "status --all"
---

<objective>
Give `/cairn:doctor` a thirteenth check: a stale phase lease reported with
the same discipline check 8 (`claims-stale`) already applies to a stale
issue claim — WARN, itemized by phase/holder/since-when, never a doctor
FAIL, because a stale lease is exactly the "reclaimable, not a bug" case
D-03/LEASE-04 describe.

Purpose: LEASE-05 is explicit that this must mirror claims-stale's
discipline "one level up" — the doctor's job here is visibility, not
enforcement; the actual unblock is either the next `acquire` reclaiming it
automatically, or a human running `cairn-lease.sh release <N>` directly.

Output: `check_lease_stale()` wired into `cairn-doctor.py`'s check list,
`NO_PHASE_EXEMPT` extended so the lease bookkeeping issue is never flagged
as an orphan, and the module docstring/tests updated to match.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-phase-lease/15-CONTEXT.md
@cairn/scripts/cairn-lease.py
@cairn/scripts/cairn-doctor.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: check 13 — lease-stale, itemized and WARN-only</name>
  <files>cairn/scripts/cairn-doctor.py, tests/cairn-doctor.bats</files>
  <read_first>
    cairn/scripts/cairn-doctor.py (the module docstring's numbered check
    list — check 12 external-ref is the last one today, this becomes
    check 13; `check_claims_stale()` as the exact WARN-only shape to
    mirror; `check_maps_fresh()`/`check_phase_corroboration()` as the
    shell-out-to-another-script templates; `NO_PHASE_EXEMPT`'s current
    three entries; `main()`'s `checks = [...]` list and the exit-code
    tally at the bottom), tests/cairn-doctor.bats (the healthy-fixture
    test's `.checks | length` assertion — currently 13, must become 14).
  </read_first>
  <behavior>
    `check_lease_stale(root)`: shells to `[sys.executable, str(SCRIPTS_DIR
    / "cairn-lease.py"), "status", "--all", "--json", "--project-dir",
    str(root)]`, capture_output=True, text=True. Non-zero exit or
    unparsable JSON → status "warn", detail explaining the check could not
    be computed, empty items — never crash the doctor run over this one
    check (same degrade shape as check_phase_corroboration()). Parse the
    JSON array; for every entry where held is true AND stale is true,
    append one item naming the phase, the holder, actor, acquired_at,
    heartbeat_at, and the reclaim path ("reclaimable — the next
    /cairn:work N takes it automatically, or run cairn-lease.sh release N
    to clear it now"). status: "warn" if any items, else "ok". Never
    "fail" — a stale lease is not itself a doctor failure, matching
    claims-stale's own never-fails posture exactly.
  </behavior>
  <action>
    Add `check_lease_stale()` following the existing check function shape
    ({"id": "lease-stale", "status", "detail", "items"}). Add `"lease"` to
    `NO_PHASE_EXEMPT` (currently {"migrated-todo", "backlog", "quick"}) —
    the lease bookkeeping issue carries no phase-* label by design (Plan
    15-01), so without this it would be flagged by check 6 (orphans) as a
    non-closed issue with no phase-* label. Insert
    `check_lease_stale(root)` into `main()`'s `checks = [...]` list as
    check 13 (after `check_external_ref(...)`), and update the module
    docstring's numbered check list (0-13, fourteen checks total) and the
    "Checks" intro line ("thirteen checks in total" → "fourteen checks in
    total", also update check 0's own docstring line which currently
    states the total). Update every existing test in tests/cairn-doctor.bats
    that asserts `.checks | length` (the healthy-fixture test and any
    other exact-count assertion) from 13 to 14.
  </action>
  <verify>
    <automated>cd /path/to/repo && bats tests/cairn-doctor.bats</automated>
  </verify>
  <done>check 13 (lease-stale) itemizes every phase with a stale lease, WARNs without ever failing the doctor run, and the lease bookkeeping issue is exempted from check 6 (orphans).</done>
  <acceptance_criteria>
    - A fixture with a lease acquired then its heartbeat_at hand-set (via
      `bd update --metadata`, not a real 4-hour sleep) to more than 4
      hours in the past: `cairn-doctor.sh --json` shows the `lease-stale`
      check `status == "warn"`, one item naming the phase and the holder
      by path, and the doctor process still exits 0 (warn never fails the
      run) — not 7.
    - The SAME fixture with a FRESH heartbeat (just acquired, no
      manipulation): `lease-stale` reports `status == "ok"`, empty items.
    - The pre-existing healthy fixture (make_doctor_fixture, no lease ever
      touched): `.checks | length` is 14, `lease-stale` reads "ok", and
      the fixture's overall "every check ✓" assertion still passes.
    - A fixture where a lease has been created and immediately released
      (vacant, held=false): check 6 (orphans) reports NO item for the
      lease issue — it is exempted by its `lease` label, not merely absent
      from the roadmap-phase set.
    - `cairn-doctor.sh --json` on a repo where `cairn-lease.py` itself
      would fail (e.g. bd genuinely unavailable — reuse the existing
      "bd missing from PATH" test seam already used elsewhere in this
      file) does not crash the whole doctor run; `lease-stale` degrades to
      "warn" with an explanatory detail rather than propagating a
      traceback.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `cairn-lease.py status --all --json` subprocess output → doctor check 13 | Same trust level as every other doctor check already shelling to another cairn script; parsed defensively (JSON decode failure degrades to WARN, never a crash) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-05 | Repudiation | `cairn-lease.py status --all` failing or erroring silently passing this check as "ok" | medium | mitigate | subprocess failure or unparsable JSON degrades explicitly to "warn" with a stated reason, mirroring `check_phase_corroboration()`'s own degrade path — never collapses into "ok" |
</threat_model>

<verification>
- `bats tests/cairn-doctor.bats` passes in full, including every existing
  test whose `.checks | length` assertion was updated to 14.
- `bash cairn/scripts/cairn-doctor.sh --json --project-dir .` on this repo
  runs without a traceback.
</verification>

<success_criteria>
`/cairn:doctor` names every phase carrying a stale lease, with holder and
since-when, WARNs but never fails on it, and the lease bookkeeping issue
never pollutes any other doctor check.
</success_criteria>

<output>
Create `.planning/phases/15-phase-lease/15-03-SUMMARY.md` when done
</output>
