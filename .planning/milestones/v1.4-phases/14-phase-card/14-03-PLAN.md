---
phase: 14-phase-card
plan: "03"
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - cairn/scripts/cairn-doctor.py
  - tests/cairn-doctor.bats
autonomous: true
requirements: [CARD-02]
beads: [CairnGo-hew]

must_haves:
  truths:
    - "A phase that reads disk_state: verified (has an NN-VERIFICATION.md) while a PLAN.md still lacks its own SUMMARY.md is named by /cairn:doctor's new check, by filename, for EVERY such phase — not gated to ROADMAP-complete phases the way check 5 (phase-complete-open) already is"
    - "A phase that is merely mid-flight — some plans done, others not yet executed, disk_state: executed or less, no VERIFICATION.md yet — produces ZERO items from this check: an ordinary wave gap is not the anomaly D-04 asks doctor to name"
    - "A phase whose NN-VERIFICATION.md exists but carries no readable status: field in its frontmatter is named by /cairn:doctor's new check"
    - "The new check's status is 'ok' or 'warn', never 'fail' — it flags record hygiene, not a corroboration blocker, and never changes doctor's overall exit code"
    - "A phase with every artifact present and readable produces zero items and status ok"
  artifacts:
    - cairn/scripts/cairn-doctor.py
    - tests/cairn-doctor.bats
  key_links:
    - "check_phase_artifacts() cross-references main()'s already-computed disk_reasons (disk_incomplete_reasons()) against phases[].disk_state from the SAME cairn-status.py --json subprocess call check_phase_corroboration() already established — one subprocess call gates BOTH halves of this check, no duplicate frontmatter parser in this file"
---

<objective>
`/cairn:doctor` gains a check that names which artifact is missing when a
phase's board row would otherwise show only a dash — the doctor half of
D-04's narrowing of the phase card's missing-artifact story (the board shows
a plain dash for a phase with nothing yet; an UNEXPECTED absence, like a
plan lacking its summary or a verification file with no readable verdict, is
what doctor names).

Purpose: ROADMAP's success criterion 3 for this phase ("a phase missing an
artifact says which one, instead of the absence reading as absence of
information") is deliberately NOT the board's job past a bare dash — it is
doctor's, the same division of labor Phase 13 already established for
per-source conflict detail. This plan is that half.
Output: `check_phase_artifacts()` wired into `cairn-doctor.py`'s check list;
bats coverage proving it names both failure shapes and never fails the run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-phase-card/14-CONTEXT.md
@cairn/scripts/cairn-doctor.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: check_phase_artifacts — missing SUMMARY and unreadable verify_status (D-04)</name>
  <files>cairn/scripts/cairn-doctor.py</files>
  <read_first>
    cairn-doctor.py: `disk_incomplete_reasons()` (the exact helper this
    check reuses unconditionally — currently only consumed by check 5,
    gated to roadmap-complete phases), `check_phase_corroboration()` in
    full (the subprocess-to-`cairn-status.py --json` pattern this check
    copies), `main()`'s `checks = [...]` list and its preceding setup
    (`disk_reasons = disk_incomplete_reasons(planning_dir)` is already
    computed there — reuse it, do not recompute). `phase_disk_state()` in
    `cairn-status.py` (what `disk_state == "verified"` actually means: an
    `NN-VERIFICATION.md` file exists for that phase — nothing about the
    ROADMAP checkbox). NOTE: an earlier draft of this task gated the
    missing-SUMMARY item on nothing at all (any phase with a plans/summary
    gap, regardless of state) — a plan-checker caught that this fires on
    completely ordinary mid-flight work (a phase between waves always has
    some plans without summaries yet) and is noise, not signal. The design
    below gates it on `disk_state == "verified"` instead: a phase someone
    ran `/cairn:verify` on despite a plan never having been summarized is a
    genuine anomaly; a phase still being worked is not.
  </read_first>
  <behavior>
    - A phase that reads `disk_state: "verified"` (has an
      `NN-VERIFICATION.md`) while one of its PLAN.md files still lacks its
      own SUMMARY.md -> the new check reports a `warn` item naming that
      specific PLAN.md by filename — check 5 (`phase-complete-open`) reports
      nothing for this phase unless it is ALSO ROADMAP-complete, proving
      this check's scope is genuinely broader on the disk-state axis, not a
      duplicate.
    - A phase with two PLAN.md files, only one with a SUMMARY.md, and NO
      `NN-VERIFICATION.md` yet (ordinary mid-flight — e.g. this very phase's
      own directory between waves 1 and 2) -> ZERO items for that phase.
      This is the specific false-positive a plan-checker caught and this
      task exists to avoid reintroducing.
    - A phase whose `NN-VERIFICATION.md` exists but whose frontmatter has no
      `status:` line at all -> a `warn` item naming that phase and file.
    - A phase with a clean plans/summary pairing and either no
      VERIFICATION.md or one with a readable `status:` -> no item for it.
    - Every scenario above -> the check's own `status` is `warn` (items
      present) or `ok` (none), never `fail`.
  </behavior>
  <action>
    Add `check_phase_artifacts(root, planning_dir, disk_reasons)` near
    `check_phase_corroboration()`, following its docstring shape (what it
    checks, its check id, why its severity policy is what it is, and —
    explicitly — why the missing-SUMMARY half is gated on `disk_state ==
    "verified"` rather than firing on every plans/summary gap: an ungated
    version was tried first and rejected for warning on ordinary mid-flight
    phases).

    Shell out to `cairn-status.py --json` exactly the way
    `check_phase_corroboration()` already does (same `subprocess.run([...,
    "--json", "--planning-dir", str(planning_dir)], ...)` call). On a
    `returncode not in (0, 5)` or a JSON-decode failure, this check CANNOT
    determine disk_state for its gate, so both halves are unavailable:
    return a single `warn` item noting `phase-artifacts` could not run
    (mirroring `check_phase_corroboration()`'s own degrade) rather than
    falling back to the ungated `disk_reasons` dump — falling back would
    silently reintroduce the exact noise this task's redesign removes.

    On success, build `state_by_n = {p["number"]: p.get("disk_state") for p
    in data.get("phases") or []}` and `verify_by_n = {p["number"]:
    p.get("verify_status") for p in data.get("phases") or []}`. Build
    `items` in two passes, both `warn`-only:

    First, from the ALREADY-COMPUTED `disk_reasons` dict (do not call
    `disk_incomplete_reasons()` again): for every `(n, reason)` in
    `sorted(disk_reasons.items())`, append `f"phase {n}: {reason}"` ONLY
    when `state_by_n.get(n) == "verified"` — this is the narrowed gate: a
    phase in `disk_reasons` that has NOT reached `verified` yet (still
    `none`/`planned`/`executed`) is ordinary in-progress work and produces
    no item.

    Second, for each `n, ds` in `sorted(state_by_n.items())`: if `ds ==
    "verified"` and not `verify_by_n.get(n)`, append `f"phase {n}: has a
    VERIFICATION.md but no readable 'status:' field in its frontmatter"`.

    Return `{"id": "phase-artifacts", "status": "warn" if items else "ok",
    "detail": (f"{len(items)} phase(s) with an unexpected missing/unreadable
    artifact" if items else "every phase's artifacts are complete and
    readable"), "items": items}`. Document in the docstring, explicitly,
    that this check's status is NEVER `"fail"` — a deliberate choice
    distinct from `phase-corroboration`'s `blocks`/`fail` behavior, because
    a missing SUMMARY or an unreadable verdict is a record-hygiene gap, not
    contradictory evidence about what actually happened (D-01's "cairn never
    stops the flow" applied to hygiene, not correctness, findings).

    Wire it into `main()`'s `checks = [...]` list, passed `(root,
    planning_dir, disk_reasons)` — `disk_reasons` is already a local
    variable in `main()` by the time the list is built, so this is a
    one-line addition, not new plumbing.
  </action>
  <verify>
    <automated>bats tests/cairn-doctor.bats -f phase-artifacts</automated>
  </verify>
  <acceptance_criteria>
    `python3 -c "import ast; ast.parse(open('cairn/scripts/cairn-doctor.py').read())"`
    passes. `cairn-doctor.sh --json` against a healthy fixture includes a
    `phase-artifacts` entry in `.checks[]` with `status: "ok"` and an empty
    `items` array.
  </acceptance_criteria>
  <done>`check_phase_artifacts()` exists, is wired into `main()`'s checks
  list, gates the missing-SUMMARY item on `disk_state == "verified"`, and
  never returns `status: "fail"`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bats coverage — warn cases, mid-flight false-positive regression, never-fail, degrade path</name>
  <files>tests/cairn-doctor.bats</files>
  <read_first>
    `tests/cairn-doctor.bats`'s existing `phase-corroboration:` test block
    (search for "phase-corroboration (check 11, CORR-06) — 13-03") as the
    direct precedent for this check's own test block's shape — fixture
    setup, `assert_json_eq`, and the exit-code assertions.
  </read_first>
  <action>
    Add a new test block ("phase-artifacts (check N, CARD-02/D-04) — 14-03")
    to `tests/cairn-doctor.bats` with:

    A clean-fixture test asserting `phase-artifacts` reads `status: "ok"`
    with zero items on the standard healthy fixture already used elsewhere
    in this file.

    A missing-SUMMARY test that creates a phase with two PLAN.md files, only
    one with a SUMMARY.md, AND an `NN-VERIFICATION.md` for that phase (so
    `disk_state` reads `"verified"` and the narrowed gate fires) — ROADMAP
    checkbox left unticked — and asserts a `warn` item naming that PLAN.md's
    filename, AND asserts the OVERALL exit code stays 0 (never 7 — this
    check must not turn a warn into a run failure).

    A SIBLING mid-flight test, the direct regression proof for the
    narrowing this task's Task 1 added: the SAME two-PLAN-one-SUMMARY setup
    but WITHOUT any `NN-VERIFICATION.md` for that phase — asserts
    `phase-artifacts` produces ZERO items for that phase and `status: "ok"`
    (or `"ok"` overall if no other phase has an item). This is the case a
    plan-checker caught as a false positive in an earlier draft — a phase
    between waves, ordinary in-progress work — and must stay green.

    An unreadable-verify-status test that creates `NN-VERIFICATION.md` with
    no `status:` line (or a frontmatter block missing entirely) and asserts
    a `warn` item naming the phase, again with exit code 0.

    One test that runs `cairn-doctor.sh` when EVERY OTHER check would pass
    but `phase-artifacts` alone has warn items, asserting the run's overall
    exit code is still 0 (`n_fail == 0` even with warnings present, matching
    this repo's established `phase-corroboration: state_md-vs-disk informs
    conflict warns, never fails the run` precedent).
  </action>
  <verify>
    <automated>bats tests/cairn-doctor.bats -f phase-artifacts</automated>
  </verify>
  <acceptance_criteria>
    All new tests pass, including the mid-flight regression test proving a
    two-PLAN/one-SUMMARY phase with no VERIFICATION.md yet produces zero
    items. The full suite (`bats tests/cairn-doctor.bats`) still passes in
    full, unaffected outside the new test block.
  </acceptance_criteria>
  <done>`phase-artifacts` is covered for the ok, verified-with-missing-
  SUMMARY, mid-flight-no-false-positive, unreadable-verify-status and
  overall-exit-code-stays-0 cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `cairn-doctor.py` -> subprocess -> `cairn-status.py --json` | Local, already-established trusted subprocess boundary (identical to `check_phase_corroboration()`'s existing call) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-07 | Tampering | `check_phase_artifacts()`'s subprocess call to `cairn-status.py` | low | accept | List-form `subprocess.run` (no `shell=True`, no string interpolation into a shell), the exact pattern `check_phase_corroboration()` already uses safely today — no new attack surface introduced. |
| T-14-08 | Information Disclosure | `disk_reasons` filenames/phase numbers surfaced for every `disk_state: "verified"` phase (not just roadmap-complete ones) | low | accept | This is the check's entire purpose (D-04, narrowed to the `verified` gate to avoid mid-flight noise — see Task 1); the filenames and phase numbers already exist in the repo's own `.planning/` tree, visible to anyone with repo access — doctor is a local, repo-scoped tool. |
</threat_model>

<verification>
- `bats tests/cairn-doctor.bats` — full suite green, including the new `phase-artifacts` block.
- `cairn-doctor.sh --json | python3 -c "import json,sys; d=json.load(sys.stdin); assert any(c['id']=='phase-artifacts' for c in d['checks'])"` against any fixture.
</verification>

<success_criteria>
`/cairn:doctor` names, by filename and phase number, any plan lacking its
summary in a phase that has already reached `disk_state: verified` (broader
than check 5's roadmap-complete-only gate, but not so broad it fires on
ordinary mid-flight work) and any verification file with no readable
verdict — without ever failing the overall doctor run on its own account.
</success_criteria>

<output>
Create `.planning/phases/14-phase-card/14-03-SUMMARY.md` when done.
</output>
