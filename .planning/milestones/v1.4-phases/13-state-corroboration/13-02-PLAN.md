---
phase: 13-state-corroboration
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - cairn/scripts/cairn-status.py
  - cairn/templates/status-board.html
  - tests/cairn-corroboration.bats
autonomous: true
requirements: [CORR-02, CORR-07]
beads: [CairnGo-jc5, CairnGo-u7s]

must_haves:
  truths:
    - "A conflicting phase renders one line on the terminal PENDING PHASES panel, marker plus a truncated reason, per D-03's template A — never a multi-line dump on the board itself"
    - "The terminal board and the HTML page render the identical marker word and the identical detail text for the same fixture, proven by a test that renders both and string-compares, not by eyeballing (D-04)"
    - "A 'blocks' conflict and an 'informs'-only conflict render with visibly different glyphs/tone on both surfaces"
    - "The documented corpus of known-harmless combinations (no bd issues tracked for a phase, a cross-phase issue with one leg still open, a regenerated NN-BEADS-MAP.md, a touched-but-unmodified SUMMARY.md) produces zero conflicts, each entry carrying a one-line justification in the test file (CORR-07)"
  artifacts:
    - path: cairn/scripts/cairn-status.py
      provides: "conflict_marker()/conflict_summary_text() shared helpers, phase_panel_lines() and html_phases() conflict rendering, Style glyph additions"
    - path: cairn/templates/status-board.html
      provides: ".phase.is-conflict / .is-informs CSS using --oxide/--amber, matching the existing tonal (not badge) styling convention"
    - path: tests/cairn-corroboration.bats
      provides: "cross-surface parity test and the CORR-07 harmless-diff corpus"
  key_links:
    - from: "cairn-status.py:phase_panel_lines()"
      to: "cairn-status.py:conflict_summary_text()"
      via: "shared helper call, so terminal and HTML text can never independently drift"
      pattern: "conflict_summary_text\\("
    - from: "cairn-status.py:html_phases()"
      to: "cairn-status.py:conflict_summary_text()"
      via: "the SAME shared helper call as phase_panel_lines()"
      pattern: "conflict_summary_text\\("
---

<objective>
Render Plan 13-01's corroboration verdict identically on the terminal board
and the HTML page, using D-03's chosen template (one phase, one line, marker
plus truncated reason — the itemized detail lives only in `/cairn:doctor` and
`--json`), and prove — with a written, tested allowlist — that this specific
four-source comparison never fires on a set of known-harmless state
combinations.

Purpose: CORR-02 requires that disagreement never resolves to a silently
chosen winner; rendering the same named claims on both surfaces (D-04) is
what makes that visible to a human. CORR-07 exists because an untested
detector trains people to ignore it the first time it cries wolf.
Output: terminal + HTML conflict rendering, and a committed, justified
harmless-diff corpus proven at $0 to produce zero false conflicts.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-state-corroboration/13-CONTEXT.md
@.planning/research/PITFALLS.md
.planning/phases/13-state-corroboration/13-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: shared conflict-text helpers + terminal panel rendering (D-03)</name>
  <files>cairn/scripts/cairn-status.py, tests/cairn-corroboration.bats</files>
  <read_first>
    cairn/scripts/cairn-status.py (`phase_panel_lines()` lines ~1190-1254,
    `phase_state_text()`, the `Style` class lines ~1014-1063 — `g_stale`,
    `SGR_RED`, `SGR_YELLOW`), .planning/phases/13-state-corroboration/13-CONTEXT.md
    (D-03's exact mockup block — read as STRUCTURE, translate the illustrative
    Portuguese words to English; this repo's UI text is English throughout,
    "not planned"/"planned"/"executed"/"verified"/"waits on" — do not paste
    Portuguese strings into the codebase).
  </read_first>
  <behavior>
    - `conflict_summary_text(p)`: given a phase row with `corroboration ==
      "conflict"`, pick the first `"blocks"`-severity item in `conflicts`
      if any exist, else the first item (informs). Return
      `f"{word} — {detail}"` where `word` is `"conflict"` when a blocks
      item was picked, `"diverges"` when only informs items exist, and
      `detail` is that item's `detail` string verbatim (already built in
      Plan 13-01, already names both sources' claims — do not re-derive
      it here).
    - `conflict_marker(p, style)`: returns `(glyph, sgr)` for a conflicting
      phase — `style.g_conflict`/`SGR_RED` when any item is `"blocks"`,
      else `style.g_informs`/`SGR_YELLOW`. For `corroboration ==
      "unknown"`, returns `(style.g_stale, SGR_DIM)` with the caller text
      `"corroboration unknown"`.
    - `Style.__init__` gains `g_conflict` (`"✗"` utf / `"x"` ascii) and
      `g_informs` (`"⚠"` utf / `"!"` ascii), alongside the existing
      `g_stale`/`g_next`/`g_dep`/`g_who` pattern.
  </behavior>
  <action>
    Add `conflict_summary_text()` and `conflict_marker()` as new top-level
    functions near `phase_state_text()`. Add the two new `Style` glyph
    attributes in both the ascii and utf branches. In `phase_panel_lines()`,
    for each pending phase `p`: when `p["corroboration"] == "conflict"`,
    REPLACE the `state` string entirely (do not append it after) with the
    marker glyph plus `conflict_summary_text(p)`, truncated to the same
    `budget` the title already uses, rendered as its own colored span
    (glyph+text using `conflict_marker`'s sgr) INSTEAD of the current
    single `SGR_DIM` span — this is a one-phase-one-line replacement, per
    D-03, not an addition alongside "not planned"/"planned"/etc. When
    `corroboration == "unknown"`, replace `state` similarly with
    `"corroboration unknown"` in `SGR_DIM`. When `corroboration == "ok"`,
    render exactly as today (no behavior change). After the per-phase
    loop, when at least one pending phase is conflicting, append a summary
    line to the panel: count phases with a `"blocks"` item (`n_blocks`)
    and phases whose ONLY conflicts are `"informs"` (`n_informs`), and
    print (English, mirroring D-03's structure) something equivalent to
    `"✗ {n_blocks} blocks · ⚠ {n_informs} informs — /cairn:doctor for the
    itemized report"`, omitting either half when its count is zero.
  </action>
  <verify>
    <automated>cd /path/to/repo && bats tests/cairn-corroboration.bats</automated>
  </verify>
  <done>conflict_summary_text() and conflict_marker() exist; a conflicting phase renders one PENDING PHASES line replacing the normal state text with the marker + reason, blocks and informs phases carry visibly different glyphs/colors, and a per-panel summary line counts each severity separately.</done>
  <acceptance_criteria>
    - `cairn-status.sh --width 100` on a fixture with one "blocks"-conflict
      phase and one "informs"-only phase: the PENDING PHASES panel shows
      ONE line per phase (never two), each carrying its marker word
      (`grep -qF "conflict —"` / `grep -qF "diverges —"`) and the
      before-truncation text still names both sides for a wide-enough
      terminal width.
    - The panel's summary line correctly counts blocks vs informs phases
      separately (assert both numbers via a fixture with 1 of each).
    - `--ascii` renders `x`/`!` in place of `✗`/`⚠` (grep-based check, not
      a full render diff).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HTML rendering (identical fields) + cross-surface parity test (D-04)</name>
  <files>cairn/scripts/cairn-status.py, cairn/templates/status-board.html, tests/cairn-corroboration.bats</files>
  <read_first>
    cairn/scripts/cairn-status.py (`html_phases()` lines ~1914-1983),
    cairn/templates/status-board.html (the `.phase`, `.phase-meta`,
    `.phase.is-waiting` CSS block ~lines 447-521 and the `--oxide`/`--amber`
    variable definitions ~lines 27-30 — reuse these, do not invent new
    colors), tests/cairn-phase-model.bats (the existing
    `"the terminal panel and the HTML page carry the same commands and
    reasons"` test — copy this exact write-json/write-html/write-terminal/
    compare shape for the new assertion).
  </read_first>
  <action>
    In `html_phases()`, for each pending phase whose `corroboration` is
    `"conflict"` or `"unknown"`: replace `meta[0]` (currently
    `esc(phase_state_text(p))`) with `esc(conflict_summary_text(p))` for
    `"conflict"`, or `esc("corroboration unknown")` for `"unknown"` — the
    SAME helper Task 1 wrote, called from here too, so the text cannot
    independently drift between surfaces. Add a CSS class to the `<li>`
    (`phase-conflict` when any item is `"blocks"`, `phase-informs` when
    only informs items exist, `phase-unknown` for the unknown verdict).
    In `status-board.html`'s stylesheet, add rules using the EXISTING
    `--oxide` (blocks) and `--amber` (informs) variables, matching the
    tonal style already used by `.phase.is-waiting .phase-title` — a
    color shift on the existing elements, not a new badge/pill/icon-tile.
  </action>
  <verify>
    <automated>cd /path/to/repo && bats tests/cairn-corroboration.bats</automated>
  </verify>
  <done>html_phases() renders the same conflict_summary_text()/conflict_marker() output as the terminal panel, with a distinguishing CSS class using the existing --oxide/--amber tokens, and a bats test proves textual identity across --json, terminal and HTML for a blocks case, an informs case, and an unknown case.</done>
  <acceptance_criteria>
    - New bats test: build a fixture with a "blocks" conflict on phase N,
      run `--json`, `--width 100`, and `--html board.html` against the
      SAME fixture, then assert `conflict_summary_text`'s exact detail
      string (read from the `--json` output's `conflicts[].detail`)
      appears verbatim in BOTH the terminal `$output` and `board.html` —
      a real string-equality check across three renders, not visual
      inspection.
    - Repeat for an "informs"-only conflict and for the "unknown" verdict
      — three fixtures, three cross-surface assertions total.
    - `board.html` for a conflicting phase carries the `phase-conflict` (or
      `phase-informs`/`phase-unknown`) class, verifiable via `grep`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: harmless-diff corpus, written and proven at zero cost (CORR-07)</name>
  <files>tests/cairn-corroboration.bats</files>
  <read_first>
    .planning/research/PITFALLS.md (Pitfall 1 — "the fixture corpus of
    known-harmless diffs... proven to produce zero false conflicts, the same
    discipline bench-corpus.bats already applies to this repo's cost
    claims"), tests/helpers.bash (`make_bd_fixture`, `make_gsd_fixture`).
  </read_first>
  <behavior>
    Four corpus entries, each its own `@test`, each opening with a one-line
    comment stating WHY it is harmless:
    1. A phase with zero bd issues under `phase-N` at all (`evidence.bd ==
       "none"`) whatever disk/roadmap say (short of an actual R2 trigger)
       — justification: absence of bd data must never be read as
       disagreement.
    2. An issue labeled `phase-3,phase-4` where phase 3 is roadmap-complete
       and disk-verified, phase 4 is not — justification: a cross-phase
       issue open only because a LATER phase isn't done is real work for
       that later phase, not evidence phase 3's own work is unfinished
       (the exact `bd_state()` exclusion Plan 13-01 built).
    3. Regenerating a phase's `NN-BEADS-MAP.md` (touch or rewrite it,
       change its mtime and content) between two `--json` runs —
       justification: `disk_state`/`corroborate()` only ever look at
       `-PLAN.md`/`-SUMMARY.md`/`-VERIFICATION.md` suffixes; the map file
       is invisible to this comparison by construction.
    4. Touching (not modifying) an existing `-SUMMARY.md` — justification:
       `phase_disk_state()` is existence-only, never mtime- or
       content-sensitive.
  </behavior>
  <action>
    Add a new top-of-file comment block in `tests/cairn-corroboration.bats`
    titled "Harmless-diff corpus (CORR-07)" listing the four entries and
    their justifications, then implement each as a `@test` asserting
    `corroboration == "ok"` (or, for entry 2, specifically that phase 3's
    verdict is `"ok"` while phase 4's may legitimately be anything) via
    `--json`.
  </action>
  <verify>
    <automated>cd /path/to/repo && bats tests/cairn-corroboration.bats</automated>
  </verify>
  <done>The four harmless-diff corpus entries are committed as individually justified bats tests, all pass with corroboration == "ok", and the full cairn-corroboration.bats suite (this plan's three tasks plus Plan 13-01's) passes together.</done>
  <acceptance_criteria>
    - All four corpus tests pass, each producing `corroboration == "ok"`
      for its target phase — grep the file for the four `@test` names and
      confirm each carries an inline justification comment directly above
      it (`grep -B1 '@test' tests/cairn-corroboration.bats` shows a
      comment line before each).
    - The full `tests/cairn-corroboration.bats` suite (Plan 13-01's tests
      plus this plan's) passes together in one `bats` invocation.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conflict detail text → terminal/HTML output | Built from already-public phase state; HTML output goes through the existing `esc()` gate (clean + HTML-escape), so no new injection surface is introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05 | Tampering | HTML conflict class/text rendering | low | mitigate | reuse the existing `esc()` HTML-escaping gate for all new text; no new unescaped interpolation point is added to `html_phases()` |
| T-13-06 | Spoofing | cross-surface divergence (terminal says one thing, HTML says another) | medium | mitigate | both renderers call the SAME `conflict_summary_text()`/`conflict_marker()` helpers — the D-04 test proves textual identity, not just visual similarity |
</threat_model>

<verification>
- `bats tests/cairn-corroboration.bats` passes in full (all three tasks' tests together).
- `bats tests/cairn-phase-model.bats tests/cairn-status.bats` still pass unmodified (no regression to existing rendering).
</verification>

<success_criteria>
A conflicting or unknown-verdict phase renders one line, on both the terminal
board and the HTML page, using text and glyphs traced back to a single shared
helper — and a written, justified corpus proves this specific comparison
produces zero false positives on four realistic harmless scenarios.
</success_criteria>

<output>
Create `.planning/phases/13-state-corroboration/13-02-SUMMARY.md` when done
</output>
