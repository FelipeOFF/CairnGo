---
phase: 16-transition-journal
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - cairn/scripts/cairn-journal.py
  - cairn/scripts/cairn-journal.sh
  - tests/cairn-journal.bats
autonomous: true
requirements: [JOUR-05]
beads: [CairnGo-hgq]

must_haves:
  truths:
    - "compacting the journal changes zero phases' last-moved answer — proven by a before/after diff test that calls last-moved for every touched phase before and after compact and asserts the two are identical, never by code inspection alone (JOUR-05)"
    - "compaction never rewrites journal.jsonl in place — it always writes a sibling temp file first and swaps via os.rename, so a crash between the two steps leaves the ORIGINAL journal fully intact and only an orphaned temp file behind"
    - "two invocations racing a compaction do not corrupt the journal — a non-blocking flock around compact()'s critical section means a contended compaction is skipped for that invocation (the caller's own append still proceeds normally) rather than raced"
    - "a record appended by a SECOND process while a compaction is already mid-read-fold is never silently discarded by that compaction's rename — proven by a test that injects a real concurrent append into a real compaction's read-to-rename window and asserts the appended record survives in the post-compaction file (Pitfall 14's own prescribed test: 'fire two near-simultaneous writes… assert both writes are preserved, none silently lost')"
  artifacts:
    - path: cairn/scripts/cairn-journal.py
      provides: "compact subcommand plus the automatic size-triggered call from observe/lease"
    - path: tests/cairn-journal.bats
      provides: "the replay-equivalence fixture — N synthetic records across multiple phases, compacted, last-moved diffed before/after"
  key_links:
    - from: "compact()'s tempfile.mkstemp + os.rename swap"
      to: "the exact primitive .planning/research/STACK.md measured os.rename SILENTLY OVERWRITING for lease acquisition, and phase 15 rejected it there for that reason"
      via: "here the silent overwrite is the wanted property (D-03) — the same measured finding, opposite conclusion depending on what the code needs; documented inline in cairn-journal.py so neither call site gets 'fixed' by copying the other's primitive"
      pattern: "os.rename"
    - from: "size_at_read, derived from the exact raw bytes compact() itself reads and folds (never a separate stat() call, which would reopen the same race one level up)"
      to: "a fresh os.stat() taken immediately before os.rename(), inside the same flock hold"
      via: "the append path (observe/lease) deliberately takes no lock — this size comparison is the ONLY guard between a compaction's point-in-time read and a concurrently-landed append, and its abort-on-mismatch behavior is what keeps that lock-free append design safe"
      pattern: "size re-validation"
---

<objective>
Add automatic, size-triggered compaction to cairn-journal.py (D-03): fold
every phase's full event history into one `snapshot` record per phase,
write those snapshot records to a NEW sibling file, then swap it into place
with `os.rename` — never a rewrite of the live journal in place.

Purpose: the journal is scoped to a project's entire lifetime with no
natural discard point (unlike cairn-migrate.py's per-run resumable
journal, which gets thrown away after one migration). Left unbounded,
every `history`/`last-moved` read gets slower every month. Compaction has
to be designed in from the start (JOUR-05), not bolted on once growth
becomes a visible problem, and it has to prove — mechanically, not by
narration — that the compacted file answers "when did each side last
move" identically to the full, uncompacted replay.

Why rename and not an in-place rewrite: the journal exists specifically
to survive a crash. Rewriting in place puts a rewrite on the exact path a
crash does the most damage — a crash mid-rewrite doesn't leave a torn
LINE (which Plan 16-01's Task 3 already knows how to quarantine), it
leaves the WHOLE FILE in an indeterminate state. With the sibling-file-
then-rename approach, a crash mid-compaction leaves the original journal
completely untouched and only an orphaned temp file behind.

A second hazard, distinct from a crash: `observe`/`lease` append with no
lock at all (correctly — that is what makes `O_APPEND` cheap and safe for
concurrent writers). That means a compaction's `_read_records()` is a
point-in-time snapshot that a concurrent append can outrun — the append's
`os.write()` lands on the live inode while compaction is still folding,
and if compaction's `os.rename()` then swaps in a sibling built from the
STALE read, the appended record physically existed for a moment and is
then gone, with no error on either side and a resulting file that is
perfectly valid JSONL. This is Pitfall 14 in the research, and this
plan's Task 1 closes it with an explicit re-check immediately before the
rename, not just the flock (which only guards two compactions against
each other, never an append against a compaction).

Output: `compact` subcommand — including the pre-rename staleness re-
check — wired as an automatic pre-write check inside `observe`/`lease`,
plus the replay-equivalence bats test (JOUR-05) and the concurrent-append
survival test (the direct fix for the hazard above).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-transition-journal/16-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@cairn/scripts/cairn-journal.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: compact subcommand — sibling write, non-blocking-flock-guarded rename swap, automatic trigger</name>
  <files>cairn/scripts/cairn-journal.py, tests/cairn-journal.bats</files>
  <read_first>
    cairn/scripts/cairn-journal.py as written in Plan 16-01 — the
    `_read_records()`/`_last_known()`/`_append_record()` functions this
    task builds directly on top of, and the record schema (state_changed/
    verdict_changed/lease_changed) it folds into a new `snapshot` kind.
    .planning/research/STACK.md's "Placement (verified, load-bearing)" and
    "Acquire primitive (verified)" sections — the exact, measured finding
    that `os.rename(tmp, dest)` silently overwrites an existing
    destination with NO exception, which is why phase 15 rejected it for
    lease acquisition (it cannot detect "someone already holds this") and
    exactly why it is correct HERE (a compacted file is meant to
    unconditionally replace the live one, once it is fully and correctly
    written). .planning/research/PITFALLS.md's Pitfall 8 (unbounded
    growth, the WAL-checkpoint framing) and Pitfall 14 (races on shared
    mutable files, recommending reusing gbsync.py's flock fix) — read
    Pitfall 14 specifically to understand WHY this task adds a
    non-blocking flock around compaction's own critical section even
    though D-03 already settled the REWRITE mechanism (rename, not
    flock-guarded in-place edit) — the flock here guards against two
    concurrent COMPACTIONS racing each other, a narrower and different
    concern than the one D-03 resolved. Read Pitfall 14's own prescribed
    test verbatim: "fire two near-simultaneous writes… in a bats test and
    assert both writes are preserved, none silently lost" — the flock
    ALONE does not satisfy this (it only stops two compactions from
    racing each other; the append path takes no lock, by design, so a
    compaction can still read a stale snapshot while an append lands on
    the live file underneath it) — this task's action includes the
    additional pre-rename re-validation that actually closes it, and its
    acceptance criteria include the concurrent-write test this citation
    describes.
  </read_first>
  <behavior>
    - `compact [--project-dir D] [--json]` on a journal that does not
      exist yet: no-op, exit 0, creates nothing (never fabricates an
      empty journal file).
    - `compact` on a real journal with events across multiple phases:
      folds each phase's full history (via `_last_known()`, already
      built) into exactly one `snapshot` record per phase carrying
      `state` (the four evidence axes, each `{"value":..., "ts":...}` or
      null), `verdict` (`{"value":..., "ts":...}` or null), `lease`
      (`{"value":..., "holder":..., "ts":...}` or null), and
      `compacted_through_ts` (the `ts` of the latest real event folded
      in). Writes these snapshot records — and ONLY these, nothing else
      — to a brand-new file via `tempfile.mkstemp(dir=journal_path.parent,
      prefix="journal.jsonl.tmp-")`, closes it, then `os.rename(tmp_path,
      journal_path)`.
    - The size threshold that triggers automatic compaction is a named
      module constant (`JOURNAL_COMPACT_THRESHOLD_BYTES`, pick something
      in the low hundreds of KB — small enough this task can test it
      without padding a multi-megabyte fixture, large enough that routine
      day-to-day phase work does not compact every few appends;
      document the reasoning in the docstring, not just the number).
      `observe` and `lease` both check the journal's current byte size
      BEFORE opening for append, in the SAME process invocation, and call
      `compact()` first if over threshold — never as a background
      process (each `/cairn:*` invocation is already a single short-lived
      process per this codebase's existing model; a background compactor
      would just be a second process racing the first for no benefit).
    - A second `compact` call (or a concurrent `observe`/`lease` that
      would trigger auto-compaction) while one compaction is already
      mid-flight: the second caller's compaction attempt is SKIPPED
      (non-blocking `fcntl.flock(LOCK_EX | LOCK_NB)` on a dedicated lock
      file, e.g. `journal.jsonl.compact.lock`, sitting next to the
      journal — held only for the read-fold-write-rename critical
      section) — the second caller's own append still proceeds normally
      immediately afterward, uncompacted-but-correct; it is never blocked
      waiting for the other compaction to finish.
    - A record appended by a SEPARATE process (via `observe`/`lease`,
      which take no lock, correctly) after compaction has already read
      the file but before compaction's `os.rename()` swaps in the
      compacted sibling: the compaction detects this — via a size check
      taken right after its own read compared against a fresh `os.stat()`
      taken immediately before the rename, both while still holding the
      compaction flock — and ABORTS (does not rename, discards its own
      sibling file, exits 0 exactly like the lock-contention case above)
      rather than completing a rename that would silently discard the
      concurrently-appended record. The live journal — append intact —
      is left exactly as the appending process left it; the NEXT
      `observe`/`lease` invocation will simply re-trigger auto-compaction
      against the now-fuller file.
  </behavior>
  <action>
    Implement `compact(root)`. Acquire the non-blocking compaction flock
    FIRST (`fcntl.flock(LOCK_EX | LOCK_NB)` on `journal.jsonl.compact.lock`
    next to the journal); on `BlockingIOError` (another compaction already
    holds it), return immediately without writing anything — the caller's
    own append then proceeds against the still-live (uncompacted) journal
    file, which is always correct, just not yet compacted. With the lock
    held: obtain `size_at_read` from the EXACT bytes that get parsed and
    folded, never from a separate `stat()` call taken after the fact — a
    second independent syscall reopens the identical TOCTOU gap one level
    up (an append landing between the read and that later stat would
    inflate `size_at_read` past what was actually folded, and the later
    pre-rename check would then wrongly see no change). Concretely: if
    `_read_records()` (from Plan 16-01) does its own internal
    `path.read_bytes()` and does not already expose the raw byte count,
    refactor it into a thin wrapper around a new `_parse_records(raw)`
    helper — `_read_records(path)` becomes `raw = path.read_bytes();
    return _parse_records(raw)` — and have `compact()` call the raw read
    itself (`raw = journal_path.read_bytes(); size_at_read = len(raw);
    records, warnings = _parse_records(raw)`) so `size_at_read` is
    derived from the SAME bytes that produced the fold, by construction,
    with no second read or stat in between. Update every existing caller
    of `_read_records()` (`history`, `last-moved`, `observe`'s own
    `_last_known()` lookup) to keep working against the now-thin wrapper
    — their behavior is unchanged, only the internals are split. For each
    phase with at least one record, build the `snapshot` record from
    `_last_known()`. IF the `CAIRN_JOURNAL_COMPACT_TEST_DELAY` env var is
    set (a float, seconds), `time.sleep()` for that long RIGHT HERE —
    after the read/fold, before writing the sibling file — this is a
    test-only seam (mirrors this codebase's existing `CAIRN_GBSYNC`/
    `CAIRN_MAP`/`CAIRN_GATE` env-seam convention) that gives a test a
    deterministic window in which to run a real, separate `observe`/
    `lease` process against the same journal. Write the snapshot records
    to the tempfile+rename recipe's sibling file as before (do NOT rename
    yet). Immediately before the `os.rename()` call, re-stat:
    `size_now = journal_path.stat().st_size`. If `size_now != size_at_read`
    — something was appended to the live file since this compaction's own
    read — ABORT: delete the just-written sibling temp file (`os.remove`,
    best-effort, an orphan here is harmless and matches the existing
    crash-orphan tolerance) and return WITHOUT calling `os.rename()`,
    exactly like the lock-contention branch above (exit 0, no error — an
    aborted compaction is not a failure, it is deferred to the next
    invocation). Only when `size_now == size_at_read` does the function
    proceed to `os.rename(tmp_path, journal_path)`. Directly above that
    `os.rename()` call, write a code comment restating the D-03 symmetry
    verbatim: STACK.md measured `os.rename` silently overwriting an
    existing destination, phase 15 rejected that exact behavior for lease
    acquisition (it needs to DETECT collision), and this call relies on
    the identical behavior for the opposite reason (it needs to
    unconditionally REPLACE) — so a future reader must not "fix" one call
    site by copying the other's primitive. Directly above the `size_now
    != size_at_read` check, write a second comment explaining WHY this
    exists even though the flock is already held: the flock only
    serializes compactions against each OTHER, never against an append —
    `observe`/`lease` take no lock by design, so this size re-check is
    the only thing standing between a concurrently-landed record and a
    rename that would silently discard it (cite Pitfall 14). Add the size
    check + auto-trigger call at the top of `observe`'s and `lease`'s
    handlers, before they open the journal for their own append. Add the
    explicit `compact` subcommand (argparse) for manual/on-demand
    invocation and for deterministic testing without needing to grow a
    fixture to the real threshold. Update the module docstring's Exit
    codes / Behavior sections and add a "Compaction" section documenting
    the threshold, the snapshot schema, the flock-guarded critical
    section, the pre-rename size re-validation and its abort-not-merge
    resolution, the `CAIRN_JOURNAL_COMPACT_TEST_DELAY` seam, and the D-03
    rename-symmetry note.
  </action>
  <verify>
    <automated>cd /path/to/repo && bats tests/cairn-journal.bats -f "compact"</automated>
  </verify>
  <reversibility rating="reversible">D-03's rename-swap compaction format (snapshot schema, threshold, flock guard) can be changed later without data loss — last-moved/history always fold-compute fresh from whatever real + snapshot records currently exist in the file, so a future format change is a new compact() implementation, not a migration.</reversibility>
  <done>compaction never rewrites journal.jsonl in place; a `compact` call on a nonexistent journal is a no-op; the auto-trigger fires on observe/lease past the size threshold; a contended compaction is skipped, not raced; a record appended by a real concurrent process during a real compaction's read-to-rename window survives, proven by injecting one, not by inspecting the lock.</done>
  <acceptance_criteria>
    - `compact --project-dir <fixture with no journal>` exits 0 and does
      NOT create `.cairn/journal.jsonl`.
    - After compaction, `journal.jsonl` contains exactly one `snapshot`
      record per phase that had any history — assert this by counting
      lines/parsing every line's `"event"` field, not just by trusting
      the command's own stdout summary.
    - Simulate a crash between the sibling write and the rename (write
      the tmp file, do NOT rename it, kill nothing needed — just assert
      directly): the ORIGINAL `journal.jsonl` is byte-for-byte unchanged
      from before the (incomplete) compaction attempt.
    - With the compaction lock file already held (acquire it directly
      in the test via a second process or `flock` shell builtin before
      calling `compact`), a `compact` call returns promptly (does not
      hang) and leaves the journal exactly as it was; an `observe` call
      made under the same held lock still appends its own record
      successfully (uncompacted).
    - THE LOAD-BEARING TEST (Pitfall 14's own prescribed shape): seed the
      journal with history for a couple of phases via real `observe`/
      `lease` calls. Start a REAL compaction in the background with
      `CAIRN_JOURNAL_COMPACT_TEST_DELAY` set (e.g. `1`):
      `CAIRN_JOURNAL_COMPACT_TEST_DELAY=1 bash cairn-journal.sh compact
      --project-dir "$PWD" &`, capture its PID. Sleep briefly (e.g.
      `0.2`s — long enough to be past the delay's start, short enough to
      still be inside the 1s window) then, from the TEST's own process
      (a genuinely separate process from the backgrounded compact), pipe
      a real `observe` payload for a NEW phase not yet in the journal
      into `cairn-journal.sh observe --project-dir "$PWD"`. `wait` on the
      backgrounded compact's PID; assert it exited 0. Assert
      `cairn-journal.sh history --phase <the new phase> --json` shows the
      concurrently-appended record present in the journal afterward —
      this is the exact assertion that would have FAILED before this
      task's pre-rename re-validation existed (the append would have
      physically landed, then been silently discarded by the stale-read
      rename). Do not assert on WHETHER the compaction itself completed
      or aborted for the phases seeded before the delay — either is
      correct; the one non-negotiable assertion is that the concurrently
      appended record is never lost.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: replay-equivalence proof — the load-bearing JOUR-05 test</name>
  <files>tests/cairn-journal.bats</files>
  <read_first>
    cairn/scripts/cairn-journal.py's `compact`/`last-moved`/`history` as
    written in this plan's Task 1 and Plan 16-01 — this task adds no new
    production code, only the test that proves the property JOUR-05
    actually requires.
  </read_first>
  <action>
    Write the bats test that is JOUR-05's real acceptance criterion.
    Append roughly 30 synthetic records spanning at least 3 different
    phases and all three real event kinds (state_changed via `observe`
    batches with several axis changes per phase, verdict_changed via
    `observe`'s verdict field flipping, lease_changed via direct `lease`
    calls) — via the real CLI, not by hand-writing JSONL. Capture
    `last-moved --phase N --json` for EVERY touched phase as the "before"
    snapshot, plus `history --json` (full, unfiltered) as a secondary
    check. Run `compact`. Capture the SAME `last-moved` calls again as
    "after." Assert every phase's before/after `last-moved` output is
    IDENTICAL (this is the load-bearing property — never assert raw
    record COUNT matches; compaction is explicitly allowed, and expected,
    to collapse many events into one snapshot per phase). Separately
    assert `history --json` after compaction contains exactly one record
    per touched phase and every one of those records has `"event":
    "snapshot"` (proving the file was actually rewritten to the smaller
    form, not merely that the answers happen to still be correct against
    an untouched file).
  </action>
  <verify>
    <automated>cd /path/to/repo && bats tests/cairn-journal.bats -f "replay equivalence"</automated>
  </verify>
  <done>last-moved output for every phase is provably identical before and after compaction, and history after compaction is a bounded set of snapshot records, not the original tail.</done>
  <acceptance_criteria>
    - The before/after `last-moved --json` comparison is a real diff
      (e.g. `diff <(echo "$before") <(echo "$after")` or a `jq` structural
      equality check) for every phase touched by the synthetic events —
      not a spot-check of one field on one phase.
    - `history --json` after compaction has strictly fewer records than
      before it (compaction actually shrank the file) whenever more than
      one real event was recorded per phase in the synthetic set.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the compaction critical section (read all -> fold -> write sibling -> rename) | Runs inside the same short-lived `cairn-journal.py` process as a normal `observe`/`lease` call; two concerns cross it: TWO compactions racing each other, and a lock-free `observe`/`lease` append landing mid-compaction |
| the append path (observe/lease) vs. a concurrent compaction | `observe`/`lease` take no lock, by design (that is what makes O_APPEND cheap for concurrent writers) — compaction is the only side of this boundary that has to defend itself, since the append side cannot and should not be slowed down to do so |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-04 | Tampering | two processes racing `compact()`'s read-fold-write-rename section (two compactions against each other) | medium | mitigate | non-blocking `fcntl.flock` around the critical section — a contended compaction is skipped for that invocation rather than both writers racing to `os.rename` over each other; the caller's own append still succeeds against the (uncompacted but correct) live file |
| T-16-05 | Denial of Service | a crash exactly between the sibling write and the `os.rename` swap | low | accept | the ORIGINAL journal is untouched by construction (rename is the only step that mutates the live path) — the only cost is one orphaned `journal.jsonl.tmp-*` file, which the next compaction attempt simply overwrites/ignores; no data is lost and no read path is broken |
| T-16-11 | Tampering | a record appended by `observe`/`lease` (lock-free, by design) landing on the live journal between compaction's `_read_records()` and its `os.rename()` — flagged by the plan checker: the flock alone does not prevent this, since the append path deliberately takes no lock at all | high | mitigate | a size re-check taken immediately before `os.rename()`, compared against the size captured right after the read that produced the compacted snapshot, both still inside the flock hold — a mismatch aborts the rename entirely (deletes the sibling, leaves the live file exactly as the appending process left it, exits 0) rather than completing a swap that would silently discard the concurrent write; proven by Task 1's own acceptance criteria via a real backgrounded compaction plus a real concurrent second-process append through the `CAIRN_JOURNAL_COMPACT_TEST_DELAY` seam, per Pitfall 14's own prescribed test shape |
| T-16-SC | Tampering | npm/pip/cargo installs | n/a | accept | no new package-manager installs in this plan — stdlib only (`tempfile`, `fcntl`), per house style |
</threat_model>

<verification>
- `bats tests/cairn-journal.bats` passes in full, including the compaction
  and replay-equivalence tests, alongside every test from Plan 16-01.
</verification>

<success_criteria>
Compaction is provably lossless for every query the journal exists to
answer ("when did each side last move"), never rewrites the live file in
place, survives a crash mid-compaction with the original journal
completely intact, and never discards a record a concurrent process
appended while the compaction was in flight — proven by actually racing
one against a real compaction, not by inspecting the lock.
</success_criteria>

<output>
Create `.planning/phases/16-transition-journal/16-02-SUMMARY.md` when done
</output>
