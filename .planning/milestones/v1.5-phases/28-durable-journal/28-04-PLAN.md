---
phase: 28-durable-journal
plan: "04"
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - tests/cairn-status.bats
  - tests/cairn-doctor.bats
  - tests/cairn-reconcile.bats
autonomous: true
requirements: [DJOUR-03]
beads: [CairnGo-a78]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 1
  confidence: high

must_haves:
  truths:
    - "Apagar o journal não muda veredito nenhum. O `journal_last_moved()` do doctor degrada a cláusula final de UM item para nada — nunca a severidade, nunca o exit code; o `journal_history()` do reconcile devolve `[]` e o bundle segue"
    - "O teste que já provava isso apagava `.cairn/journal.jsonl`. Sob partições isso deixou de apagar o journal, e o teste passaria por não ter apagado nada — que é a forma mais silenciosa de teste morto"
    - "A superfície nova é maior que a antiga: apagar precisa cobrir o diretório de partições E o arquivo herdado"
    - "Um plano que faça o board ou o doctor DEPENDER de partição legível quebrou o requisito"
  artifacts:
    - path: tests/cairn-status.bats
      provides: "a prova do JOUR-03 sob o layout novo — o render byte a byte idêntico depois de apagar tudo"
    - path: tests/cairn-doctor.bats
      provides: "a prova de que severidade e exit code não se movem sem journal"
    - path: tests/cairn-reconcile.bats
      provides: "a prova de que o bundle sai com history vazio e sem erro"
  key_links:
    - "tests/cairn-status.bats:1698 -> o `rm -f .cairn/journal.jsonl` que virou um no-op silencioso"
    - "cairn-doctor.py:journal_last_moved -> devolve None e a cláusula some; nada mais se move"
    - "cairn-reconcile.py:journal_history -> devolve [] e o bundle segue"
---

<objective>
Apagar o journal continua não mudando veredito nenhum — agora com a superfície nova
inteira apagada, e não com um `rm` que deixou de apagar qualquer coisa.

Propósito: o `DJOUR-03` já estava testado, e o desenho novo o transformou num teste
morto sem que nada ficasse vermelho. O `rm -f .cairn/journal.jsonl` do
`cairn-status.bats` remove um arquivo que não existe mais no caminho de escrita, e o
diff estrutural que vem depois compara um render consigo mesmo. É a forma mais
silenciosa de teste morto: verde, e não mede nada.

Saída: o apagar cobre a superfície inteira (o diretório de partições e o arquivo
herdado), e a prova se estende às duas outras superfícies de leitura — severidade e
exit code do doctor, e o bundle do reconcile.

**Não é escopo deste plano:**
- Qualquer mudança em `cairn-journal.py`, `cairn-doctor.py`, `cairn-status.py` ou
  `cairn-reconcile.py`. Se algum precisar mudar para o requisito valer, o requisito
  já estava quebrado e isso é achado, não tarefa.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
- `28-CONTEXT.md` D-10: o `DJOUR-03` sobrevive intacto, e isso já está testado.
- `tests/cairn-status.bats` — o teste do JOUR-03, com o `rm` que envelheceu.
</context>

<tasks>

<task type="auto">
  <name>Apagar a superfície inteira, nas três superfícies de leitura</name>
  <action>
1. **`cairn-status.bats`, o teste do JOUR-03**: o `[ ! -f .cairn/journal.jsonl ]` do
   começo e o `[ -f .cairn/journal.jsonl ]` do meio passam a falar do segmento da
   partição, perguntado ao próprio script (`provenance --json`), nunca recomputado
   pelo teste. O `rm -f .cairn/journal.jsonl` vira o apagar da superfície inteira:
   `rm -rf .cairn/journal` **e** `rm -f .cairn/journal.jsonl*`. E entra uma asserção
   nova, que é a que impede este teste de morrer de novo: **depois do apagar, o
   `history --json` devolve zero registros e zero partições**. Sem ela, um `rm` que
   erre o alvo torna a prova vazia outra vez, em silêncio.

2. **`cairn-doctor.bats`**: um teste novo, com a superfície inteira apagada entre
   duas execuções do doctor sobre a MESMA árvore, afirmando por **valor exato** que
   o status do `phase-corroboration`, a severidade de cada item e o exit code são os
   mesmos. A única diferença permitida é o desaparecimento da cláusula de
   `last moved`, e o teste afirma isso pelos dois lados: presente antes, ausente
   depois, com o resto idêntico.

3. **`cairn-reconcile.bats`**: um teste novo — com a superfície apagada, o bundle
   sai com `journal.history == []` e `journal.last_moved == null`, exit 0, e as
   outras seções do bundle preservadas.

**As quebras a aplicar de verdade**, com a asserção que cada uma derruba registrada
no SUMMARY:
- `journal_last_moved()` do doctor deixando a exceção subir em vez de devolver
  `None` -> o teste 2.
- `journal_history()` do reconcile deixando a exceção subir -> o teste 3.
- O `rm` do teste 1 apagando só o arquivo herdado (o estado em que este plano o
  encontrou) -> a asserção nova de zero partições.
  </action>
  <verify>
    <automated>bash cairn/scripts/cairn-test.sh --jobs 4 tests/cairn-reconcile.bats</automated>
  </verify>
  <done>Os três testes passam, o apagar cobre a superfície inteira, e as três quebras derrubam as asserções nomeadas.</done>
</task>

</tasks>

<verification>
- `bash cairn/scripts/cairn-test.sh --jobs 4 tests/cairn-journal.bats tests/cairn-reconcile.bats`
  verde.
- `bats -f 'JOUR-03|journal' tests/cairn-status.bats` verde.
- `bats -f 'last-moved|gitignore|journal' tests/cairn-doctor.bats` verde.
- `git diff --quiet HEAD -- .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md`
  sai limpo.
</verification>

<success_criteria>
1. Apagar o diretório de partições e o arquivo herdado não muda um byte do render do
   board.
2. Não muda o status, a severidade de nenhum item, nem o exit code do doctor — só
   remove a cláusula de `last moved`.
3. Não faz o reconcile falhar: `history` vazio, `last_moved` nulo, bundle inteiro.
4. O teste falha se o apagar errar o alvo, o que é exatamente como ele morreu.
</success_criteria>
