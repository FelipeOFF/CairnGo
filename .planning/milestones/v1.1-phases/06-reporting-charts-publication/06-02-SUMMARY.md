---
phase: 06-reporting-charts-publication
plan: "02"
subsystem: benchmarking
tags: [python, bats, markdown, generated-markers, publication]

# Dependency graph
requires:
  - phase: 05-corpus-baselines-aggregation
    provides: "bench-aggregate.py's aggregated.json cell schema, the pre-declared 6-task corpus, the 4 pinned baselines, and benchmarks/README.md's anchor sections (Baselines, Task corpus, Aggregation, Cost model, Variance pilot)"
provides:
  - "BENCHMARKS.md at repo root: methodology-first public evidence doc (Methodology, Raw data, Results with generated markers + pending notice, Reproduction, Changelog)"
  - "README.md Benchmarks section linking BENCHMARKS.md with a generated-marker teaser in pending state"
  - "benchmarks/scripts/bench-publish.py: marker-scoped regeneration of BENCHMARKS.md (and README teaser) from aggregated.json, mirroring cairn-map.py's exact splice mechanics"
  - "tests/bench-publish.bats + tests/fixtures/publish-aggregate.json: full contract proof at $0"
affects: [06-03 bench-all one-command reproduction, future live collection runs]

# Tech tracking
tech-stack:
  added: []
  patterns: ["generated-marker splice reuse: split_markers/replace-only-inner/append-never-destroy/write-only-when-changed ported from cairn-map.py into a shared regenerate() helper parametrized by marker pair"]

key-files:
  created:
    - BENCHMARKS.md
    - benchmarks/scripts/bench-publish.py
    - tests/bench-publish.bats
    - tests/fixtures/publish-aggregate.json
  modified:
    - README.md

key-decisions:
  - "TEASER_PENDING constant in bench-publish.py is byte-identical to README.md's committed teaser line, so publishing an empty aggregate is a true no-op on the README"
  - "bench-publish.py never creates a missing target file (EXIT_USAGE), unlike cairn-map.py: Task 1 owns the files' creation"
  - "Changelog entries dated only (no phase numbers) to keep GSD bookkeeping out of the product doc"

patterns-established:
  - "Publication surface pattern: hand-written prose + one generated-marker block per file, regenerated only from committed artifacts, staleness detectable via --check exit 3"

requirements-completed: [REPT-01, REPT-03]

# Metrics
duration: 32min
completed: 2026-07-26
---

# Phase 6 Plan 02: BENCHMARKS.md + bench-publish.py Summary

**Methodology-first BENCHMARKS.md with an honestly-pending generated Results block, README teaser markers, and bench-publish.py regenerating both from aggregated.json with cairn-map.py's proven splice mechanics — all proven by 7 bats tests against temp copies, $0 spent**

## Performance

- **Duration:** 32 min
- **Started:** 2026-07-26T08:51:48Z
- **Completed:** 2026-07-26T09:23:44Z
- **Tasks:** 3
- **Files modified:** 5

## Accomplishments

- REPT-01: `BENCHMARKS.md` published with all 5 sections; every methodology claim links a specific `benchmarks/README.md` anchor; the ~$40 ceiling appears identically in Methodology and Reproduction; the 2 committed rows are labeled verbatim "schema-validation runs, not comparison results"; zero fabricated numbers anywhere.
- REPT-03: `bench-publish.py` regenerates ONLY the content between the `cairn:generated:benchmarks` markers (and the README `benchmarks-teaser` markers under `--readme`), preserving every outside byte exactly, idempotent on double-run, append-never-destroy on markerless targets, `--check` exit 3 with unified diff on staleness.
- `README.md` carries a new Benchmarks section between Contributing and License & credits, with the teaser marker pair in pending state and a link to BENCHMARKS.md.

## Task Commits

1. **Task 1: BENCHMARKS.md + README teaser markers** - `b3c260e` (docs)
2. **Task 2 (TDD RED): failing bats + fixture** - `0a9d8a8` (test)
3. **Task 2 (TDD GREEN): bench-publish.py** - `2dd9443` (feat)
4. **Task 3: append-never-destroy / README teaser / --check proofs** - `47b4a9e` (test)

## Files Created/Modified

- `BENCHMARKS.md` - public evidence doc: Methodology, Raw data, Results (markers + pending notice inside), Reproduction (`bench-all.sh --yes`, `--dry-run` default, ~$40 ceiling), dated Changelog
- `README.md` - new Benchmarks section with `cairn:generated:benchmarks-teaser` marker pair (pending state) linking BENCHMARKS.md
- `benchmarks/scripts/bench-publish.py` - aggregated.json → markdown table → marker-scoped regeneration; EXIT 0/2/3; stdlib only
- `tests/bench-publish.bats` - 7 tests: usage errors, exact table values + byte-identical outside bytes, empty-cells pending line, double-run byte-identical idempotence, markerless-append byte-prefix proof, README teaser one-line + idempotence, --check staleness without writing
- `tests/fixtures/publish-aggregate.json` - synthetic 3-cell aggregate (one all-fail cell with null cost_median rendering `n/a`)

## Decisions Made

- Header line inside the generated block is `Generated by bench-publish (<label>) — do not edit between markers`: fully determined by `--in`+`--label`, no wall-clock dependence, mirroring cairn-map's HEADER_LINE convention.
- Cost cell formats only real numerics (`bool` excluded); anything else renders literal `n/a` — never a fabricated `$0.0000`.
- Changelog entries carry dates only (no phase numbers) so the product doc stays free of GSD bookkeeping; benchmarks/README.md remains the mechanics reference.

## Deviations from Plan

**1. [Sequencing] Task 3's fixture + core tests landed during Task 2's TDD RED phase**
- **Found during:** Task 2 (tdd="true" requires a failing test before implementation; the bats suite is Task 3's file)
- **Adjustment:** `tests/fixtures/publish-aggregate.json` and the 4 core contract tests were committed as Task 2's RED commit (`0a9d8a8`, observed 4/4 failing pre-implementation); Task 3 then added the remaining 3 tests (`47b4a9e`). Same files, same total coverage as planned — only the commit boundary moved to honor the RED gate.
- **Impact:** None on scope or content; TDD gate sequence (test → feat → test) is intact in git history.

**Total deviations:** 1 sequencing adjustment, 0 rule-based auto-fixes. No scope creep.

## TDD Gate Compliance

- RED: `0a9d8a8` test commit, suite observed failing 4/4 before implementation existed.
- GREEN: `2dd9443` feat commit, suite observed passing 4/4 immediately after.
- Full suite after Task 3: 7/7 passing.

## Verification Evidence (observed, not assumed)

- `grep -c` marker pair in BENCHMARKS.md: `2`; `schema-validation` present; README teaser marker + BENCHMARKS.md link present; `~$40` on lines 55 (Methodology) and 104 (Reproduction), identical figure.
- `python3 -m py_compile benchmarks/scripts/bench-publish.py`: exit 0. `--help`: exit 0, names `--in --benchmarks --label --readme --check`.
- `bats tests/bench-publish.bats`: `1..7`, 7 ok, 0 not ok.
- `bats tests/` full regression: `1..193`, 193 ok, 0 not ok, exit 0 (real exit code captured, not the pipe's).
- `git status --porcelain BENCHMARKS.md README.md` after the full bats run: empty — the real repo files were never mutated by any test.

## Known Stubs

- `BENCHMARKS.md` Results block and `README.md` teaser block intentionally carry pending-state text: this IS the plan's deliverable (honest pending state until a real `aggregated.json` exists). Resolved automatically by `bench-publish.py` the moment live collection (blocked on `ANTHROPIC_API_KEY`) produces data; the one-command path lands in plan 06-03.

## Issues Encountered

- First full-suite run was piped through `tail -8`, losing bats' real exit code; re-ran capturing `BATS_EXIT=0` and grepping `not ok` (0) for genuine evidence instead of assuming success.

## User Setup Required

None - no external service configuration required. Live collection remains blocked on `ANTHROPIC_API_KEY` (unchanged, pre-existing).

## Next Phase Readiness

- 06-03 (`bench-all.sh`) can reference the Reproduction section as-is; no further edit to BENCHMARKS.md is needed when it lands.
- When real data exists: `bench-aggregate.py → aggregated.json → bench-publish.py --in ... --readme README.md` fills both marker blocks; `--check` is CI-able for staleness.

## Self-Check: PASSED

All 5 plan files + SUMMARY exist on disk; all 4 task commits (b3c260e, 0a9d8a8, 2dd9443, 47b4a9e) present in git log.

---
*Phase: 06-reporting-charts-publication*
*Completed: 2026-07-26*
