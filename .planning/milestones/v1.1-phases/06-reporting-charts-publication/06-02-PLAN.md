---
phase: 06-reporting-charts-publication
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - BENCHMARKS.md
  - README.md
  - benchmarks/scripts/bench-publish.py
  - tests/bench-publish.bats
  - tests/fixtures/publish-aggregate.json
autonomous: true
requirements: [REPT-01, REPT-03]
beads: [CairnGo-u5p, CairnGo-r37]

must_haves:
  truths:
    - "BENCHMARKS.md exists at the repo root with a full methodology section (linking benchmarks/README.md's existing anchors), a raw-data section linking the 2 committed Phase 1 rows labeled as schema-validation runs (never as comparison results), a Results section carrying the exact generated-marker pair with an explicit pending-collection notice inside it, a Reproduction section documenting the one-command path and cost ceiling, and a dated Changelog"
    - "Root README.md links to BENCHMARKS.md and carries a one-line teaser wrapped in its own generated-marker pair, currently showing the pending state"
    - "bench-publish.py regenerates ONLY the content between <!-- cairn:generated:benchmarks:start/end --> in BENCHMARKS.md (and, when --readme is given, between <!-- cairn:generated:benchmarks-teaser:start/end --> in the target README) from aggregated.json — every byte outside the markers is preserved exactly"
    - "Running bench-publish.py twice with identical --in/--label/target-file content produces a byte-identical target file the second time (idempotent regeneration)"
    - "A target file that has the expected heading but no marker pair yet gets the block appended, never destroying existing content — same as cairn-map.py's own append-never-destroy contract"
  artifacts:
    - path: BENCHMARKS.md
      provides: "methodology-first public evidence doc: Methodology, Raw data, Results (markers + pending notice), Reproduction, Changelog"
      contains: "<!-- cairn:generated:benchmarks:start -->"
    - path: README.md
      provides: "link to BENCHMARKS.md plus a generated-marker teaser block"
      contains: "<!-- cairn:generated:benchmarks-teaser:start -->"
    - path: benchmarks/scripts/bench-publish.py
      provides: "aggregated.json -> markdown results table -> marker-scoped regeneration of BENCHMARKS.md (and optionally a README teaser), mirroring cairn-map.py's exact marker mechanics"
      contains: "def main"
    - path: tests/bench-publish.bats
      provides: "marker-scoped regeneration correctness, idempotence, append-never-destroy, README teaser, and --check staleness proofs — all against temp copies, never the real repo files"
      contains: "byte-identical"
  key_links:
    - from: "aggregated.json cells"
      to: "BENCHMARKS.md content between <!-- cairn:generated:benchmarks:start/end -->"
      via: "bench-publish.py's markdown table renderer, one row per sorted cell key"
      pattern: "cairn:generated:benchmarks:start"
    - from: "aggregated.json cells"
      to: "README.md content between <!-- cairn:generated:benchmarks-teaser:start/end -->"
      via: "bench-publish.py --readme, a single-line teaser"
      pattern: "cairn:generated:benchmarks-teaser:start"
    - from: "BENCHMARKS.md Reproduction section"
      to: "benchmarks/scripts/bench-all.sh (built in plan 06-03)"
      via: "the documented one-command REPT-04 invocation and cost ceiling, referenced by name so plan 06-03 needs no further edits to this file"
      pattern: "bench-all.sh"
---

<objective>
Publish `BENCHMARKS.md` (REPT-01) methodology-first, with the Results section explicitly pending first collection, and build `bench-publish.py` (REPT-03) — the generated-marker regeneration script that will later fill that section (and a root-README teaser) from `aggregated.json`, mirroring `cairn/scripts/cairn-map.py`'s exact marker mechanics.

Purpose: no full benchmark collection exists yet (blocked on `ANTHROPIC_API_KEY`, consistent with every prior phase per `STATE.md`). REPT-01/REPT-03 require the COMPLETE publication surface to exist and be provably correct now, with results explicitly marked pending — "the pending-results state is itself credibility" (`06-CONTEXT.md`): a repo that ships the full harness + methodology BEFORE any number exists is the inverse of the buildomator anti-pattern this milestone's research (`PITFALLS.md` #10) warns against.
Output: `BENCHMARKS.md`, an updated `README.md`, `benchmarks/scripts/bench-publish.py`, `tests/bench-publish.bats`, `tests/fixtures/publish-aggregate.json`.
</objective>

<execution_context>
@~/.claude/plugins/cache/cairngo/gsd/4.3.1/workflows/execute-plan.md
@~/.claude/plugins/cache/cairngo/gsd/4.3.1/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-reporting-charts-publication/06-CONTEXT.md
@benchmarks/README.md
@cairn/scripts/cairn-map.py
@benchmarks/scripts/bench-aggregate.py
@README.md
</context>

<interfaces>
<!-- cairn-map.py's exact marker-mechanics functions, from cairn/scripts/cairn-map.py. bench-publish.py must reproduce this same shape/contract (README->BENCHMARKS.md's own generated block, aggregated.json->bd), not invent a different mechanism. -->

```python
START_MARKER = "<!-- cairn:generated:start -->"    # bench-publish.py uses "<!-- cairn:generated:benchmarks:start -->" instead
END_MARKER = "<!-- cairn:generated:end -->"         # bench-publish.py uses "<!-- cairn:generated:benchmarks:end -->" instead

def split_markers(text):
    """(prefix incl. start marker, inner, suffix from end marker) or None
    when either marker is missing/misordered."""
    s = text.find(START_MARKER)
    e = text.find(END_MARKER)
    if s < 0 or e < 0 or e < s:
        return None
    return (text[:s + len(START_MARKER)],
            text[s + len(START_MARKER):e].strip("\n"),
            text[e:])

def md_cell(text):
    return str(text).replace("|", "\\|").replace("\n", " ").strip()
```

Write path logic (from `cairn-map.py main()`): compute `inner` (the freshly rendered content); read existing file text (or `None` if absent); `parts = split_markers(old_text)` when the file exists; `changed = old_inner is None or old_inner != inner`; if the file doesn't exist, create it with a fresh header + the marker block; if it exists but `parts is None` (no markers found), APPEND the block (`sep = "" if old_text.endswith("\n") else "\n"`; never destroy existing content); otherwise replace only `parts[1]` with the new `inner`, keeping `parts[0]`/`parts[2]` byte-identical. Write the file ONLY when `changed` is true (this is what makes a second identical run byte-identical and IO-idempotent).

Exit codes used by `cairn-map.py`, reuse the same numbering convention for `bench-publish.py`: `EXIT_OK = 0`, `EXIT_USAGE = 2`, `EXIT_STALE = 3` (returned only by `--check` when content differs).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: BENCHMARKS.md (methodology-first) + README.md link and teaser markers</name>
  <files>BENCHMARKS.md, README.md</files>
  <read_first>
    benchmarks/README.md (already loaded in context — the exact section headings/anchors this doc links into: Baselines, Task corpus, Randomized execution order, Aggregation, Cost model, Variance pilot)
    README.md (already loaded in context — current structure: Why / Quickstart / Already using GSD or beads / Plugins / Documentation / Requirements / Contributing / License & credits)
  </read_first>
  <action>
    Create `BENCHMARKS.md` at the repo root (sibling to `README.md`) with exactly these five `## ` sections, in this order:

    `## Methodology` — one paragraph each on: the harness (`bench-run.py` invoking `claude -p --output-format json` per HARN-02), environment isolation (fresh worktree + scoped `HOME`, `--bare`, per FAIR-01 — link `benchmarks/README.md#baselines`), the four pinned baselines with identical `claude_flags`/model, differing only in `provisioning.plugin_dirs` (link `benchmarks/README.md#baselines`), success-gating (`verify_passed is True and not is_error`, per METR-02 — link `benchmarks/README.md#aggregation-bench-aggregatepy`), the pre-declared 6-category task corpus including the deliberate honest-non-win category (per CORP-01 — link `benchmarks/README.md#task-corpus`), and the cost model (link `benchmarks/README.md#cost-model`, restating the ~$40 full-120-run-matrix ceiling number verbatim from that section's own worked example — never a re-derived or different number). Every claim here links to the specific `benchmarks/README.md` section that backs it, rather than re-explaining the mechanics.

    `## Raw data` — links `benchmarks/results/smoke-convert.jsonl` (the two committed Phase 1 rows), labeled explicitly and verbatim as "schema-validation runs, not comparison results" — state plainly why: they predate the `--baseline` flag becoming required, represent a single task with no repetition and no other arms, and (per `tests/bench-aggregate.bats`'s own documented behavior) are in fact REJECTED by `bench-aggregate.py` today for missing `baseline_id`. State that all future raw JSONL and any `aggregated.json` land under `benchmarks/results/` and are committed alongside the results they back.

    `## Results` — containing exactly the marker pair `<!-- cairn:generated:benchmarks:start -->` and `<!-- cairn:generated:benchmarks:end -->` (each on its own line), with a pending notice INSIDE the markers stating plainly: no full collection has run yet; this section regenerates automatically via `bench-publish.py` once a real `aggregated.json` exists; reproduce via the command in Reproduction below. State this explicitly as a credibility signal, not an apology — echo `06-CONTEXT.md`'s framing that a repo shipping the full harness and methodology BEFORE any cherry-picked number is the inverse of a bare marketing claim.

    `## Reproduction` — documents REPT-04's one command: `benchmarks/scripts/bench-all.sh --yes` (built in a later plan of this same phase — reference it by name/path only, no further edit to this section will be needed once it exists), with `--dry-run` as the always-safe, $0, print-only mode (the default whenever no explicit `--yes` is given). State the ~$40 cost ceiling for the full matrix (same number as Methodology's Cost model paragraph — must match exactly, never drift). State plainly that `--dry-run` never invokes any downstream script or spends anything.

    `## Changelog` — a dated entry for the corpus's pre-declaration (2026-07-26, Phase 5, per the corpus's own commit-before-results discipline) and a dated entry for this document's own creation (Phase 6). Future entries land here whenever the corpus, methodology, or reproduction command changes — never a silent edit after results exist.

    Update `README.md`: insert a new `## Benchmarks` section between the existing `## Contributing` and `## License & credits` sections. Content: one sentence stating cairn ships a reproducible benchmark suite comparing itself against vanilla/GSD-only/a competitor, then the marker pair `<!-- cairn:generated:benchmarks-teaser:start -->` / `<!-- cairn:generated:benchmarks-teaser:end -->` (each on its own line) holding exactly one line of pending-state teaser text (e.g. stating results are pending first collection), then a closing sentence linking `[BENCHMARKS.md](BENCHMARKS.md)` for full methodology/raw data/reproduction.
  </action>
  <verify>
    <automated>grep -c '<!-- cairn:generated:benchmarks:start -->\|<!-- cairn:generated:benchmarks:end -->' BENCHMARKS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<!-- cairn:generated:benchmarks:start -->\|<!-- cairn:generated:benchmarks:end -->' BENCHMARKS.md` equals 2 (both markers present, once each)
    - `grep -q 'schema-validation' BENCHMARKS.md` exits 0
    - `grep -q '<!-- cairn:generated:benchmarks-teaser:start -->' README.md` and `grep -q 'BENCHMARKS.md' README.md` both exit 0
    - `grep -c '\$40' BENCHMARKS.md` is >= 1 and the same figure appears identically in both the Methodology and Reproduction sections
  </acceptance_criteria>
  <done>BENCHMARKS.md exists with all 5 sections and exact marker strings; README.md links BENCHMARKS.md and carries the teaser marker pair — both committed with the honest pending state, zero fabricated numbers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bench-publish.py — marker-scoped regeneration from aggregated.json</name>
  <files>benchmarks/scripts/bench-publish.py</files>
  <read_first>
    cairn/scripts/cairn-map.py (already loaded in context — the exact marker-splice mechanics this script must mirror)
    this plan's Interfaces block above
    BENCHMARKS.md and README.md (written in Task 1 of this plan — the exact marker strings and target files this script regenerates)
  </read_first>
  <behavior>
    - `bench-publish.py --in <aggregated.json> --benchmarks <path, default "BENCHMARKS.md"> --label <caption>` reads `aggregated.json`, renders a markdown table (`| Task | Baseline | Category | Pass | Cost (median) |`, one row per cell sorted by the SAME `"task_id::baseline_id"` key order the aggregated.json itself uses), and writes it between `<!-- cairn:generated:benchmarks:start -->` / `<!-- cairn:generated:benchmarks:end -->` in `--benchmarks`, preserving every byte outside the markers exactly.
    - A cell's Pass column renders `f"{n_passed}/{n_total} ({round(pass_rate*100)}%)"`; Cost column renders `f"${cost_median:.4f}"` when non-null, else the literal string `n/a` (never a fabricated `$0.0000`).
    - `"cells": {}` (empty aggregate) renders the literal line `_No cells in aggregated.json — pending first collection._` instead of an empty/headerless table — a legitimate empty rerun must never look identical to "nothing happened."
    - Every table cell sourced from `task_id`/`baseline_id`/`category` is escaped via a `md_cell()`-equivalent helper (escape `|` -> `\|`, strip newlines) before being placed in the table, mirroring `cairn-map.py`'s own `md_cell()`.
    - The generated block also carries one header line naming this script and the `--label` caption (mirrors `cairn-map.py`'s `HEADER_LINE` convention) — this text is fully determined by `--in`+`--label`, never by wall-clock time, so identical inputs always regenerate identical output.
    - `--readme <path>` (optional): same marker-splice mechanics against `<!-- cairn:generated:benchmarks-teaser:start -->` / `...-teaser:end -->` in the given file, but the injected content is exactly ONE line: a short teaser (cell count + `--label` + a pointer to `BENCHMARKS.md`) when cells is non-empty, or the same pending-state line Task 1 already wrote when cells is empty.
    - A target file (`--benchmarks` or `--readme`) that exists but has neither marker: the block is APPENDED, and every pre-existing byte is preserved — never destroyed. A target file that doesn't exist at all is a usage error (`EXIT_USAGE`) — unlike `cairn-map.py`, `bench-publish.py` never invents `BENCHMARKS.md`/`README.md` from nothing; Task 1 of this plan already created them.
    - Running twice with byte-identical `--in`/`--label`/target-file starting content writes the SAME bytes the second time — the write only happens when content actually changed (mirrors `cairn-map.py`'s `changed` check), so a second run is a true no-op.
    - `--check`: writes nothing; exits 0 when the regenerated inner content already matches what's on disk, exits 3 with a unified diff on stdout when it doesn't (mirrors `cairn-map.py`'s `--check`/`EXIT_STALE` contract exactly).
    - Malformed/missing `--in`, a `--benchmarks`/`--readme` path that does not exist, or `--in` JSON missing a `"cells"` key: `EXIT_USAGE`=2 before any write.
  </behavior>
  <action>
    Create `benchmarks/scripts/bench-publish.py` following this repo's conventions (`.planning/codebase/CONVENTIONS.md`): docstring-first (usage + behavior + exit codes), `argparse`, `EXIT_OK=0`/`EXIT_USAGE=2`/`EXIT_STALE=3` constants, `die(msg, code)` printing `[bench-publish] error: ...`. Stdlib only. No `.sh` wrapper — same rationale as `bench-chart.py` in the sibling plan (mirrors `bench-matrix.py`/`stage-plugins.py`'s un-wrapped precedent).

    Structure the marker-splice logic as a small reusable helper (e.g. `regenerate(path, start_marker, end_marker, inner, allow_missing_file=False)` returning `(changed, new_text_or_None)`) so the exact same append-never-destroy / replace-only-inner / no-write-when-unchanged logic runs for both `--benchmarks` (required) and `--readme` (optional) without duplicating the marker-splice code — port `split_markers()` from `cairn/scripts/cairn-map.py` nearly verbatim (same signature/return shape), swapping in this script's own marker string constants (`<!-- cairn:generated:benchmarks:start/end -->`, and `<!-- cairn:generated:benchmarks-teaser:start/end -->` for the `--readme` target).

    `main()`: parse args -> load+validate `--in` -> render the results table (and the one-line teaser, when `--readme` given) -> call the shared regenerate helper once per target -> print one `[bench-publish] wrote/unchanged <path>` line per target actually processed -> `sys.exit(EXIT_OK)` (or `EXIT_STALE` under `--check` when any target differs).
  </action>
  <verify>
    <automated>python3 -m py_compile benchmarks/scripts/bench-publish.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile benchmarks/scripts/bench-publish.py` exits 0
    - `python3 benchmarks/scripts/bench-publish.py --help` exits 0 and mentions `--in`, `--benchmarks`, `--label`, `--readme`, `--check`
    - `grep -c 'def split_markers\|def regenerate' benchmarks/scripts/bench-publish.py` is >= 1
  </acceptance_criteria>
  <done>bench-publish.py exists, compiles clean, and implements the marker-splice/idempotence/append-never-destroy/--check contract — verified functionally by Task 3's bats suite.</done>
</task>

<task type="auto">
  <name>Task 3: tests/bench-publish.bats — regeneration correctness, idempotence, and never-touch-the-real-repo-files proof</name>
  <files>tests/bench-publish.bats, tests/fixtures/publish-aggregate.json</files>
  <read_first>
    tests/bench-aggregate.bats and tests/bench-bias-controls.bats (already loaded in context — the `load 'helpers'` + `extract_section`-style patterns and assertion-style gotcha this file must reuse)
    benchmarks/scripts/bench-publish.py (written in Task 2 of this plan)
  </read_first>
  <action>
    Create `tests/fixtures/publish-aggregate.json`: a synthetic aggregated.json with 2-3 cells, at least one with a real `cost_median`/`pass_rate` and one with `n_passed: 0`/`cost_median: null`.

    Create `tests/bench-publish.bats` (header comment, `load 'helpers'`, `BENCH_SCRIPTS_DIR="$CAIRN_REPO_ROOT/benchmarks/scripts"`, `FIXTURES_DIR="$CAIRN_REPO_ROOT/tests/fixtures"`). CRITICAL: every test copies the REAL committed `BENCHMARKS.md`/`README.md` into `$BATS_TEST_TMPDIR` first (`cp "$CAIRN_REPO_ROOT/BENCHMARKS.md" "$BATS_TEST_TMPDIR/BENCHMARKS.md"`) and runs `bench-publish.py` against the TEMP COPY's path — the bats suite must never write to the real repo-root `BENCHMARKS.md`/`README.md`. `@test` blocks:
    - CLI usage: missing `--in`, a nonexistent `--in`, and malformed JSON in `--in` each die `EXIT_USAGE`=2 naming the flag; a `--benchmarks` path that doesn't exist dies `EXIT_USAGE`=2.
    - Table correctness: run against the temp `BENCHMARKS.md` copy + `publish-aggregate.json`; assert the content between the markers contains the exact `cost_median`/`pass_rate`/`n_passed`/`n_total` values from the fixture (formatted as Task 2 specifies), AND assert every byte of the temp file OUTSIDE the marker pair (the Methodology/Raw data/Reproduction/Changelog prose written in Task 1) is byte-identical before and after (a `diff` of the pre-run and post-run file with the marker block itself excluded/normalized, or an explicit substring check that the surrounding prose is untouched).
    - Empty-cells honesty: an `aggregated.json` with `"cells": {}` produces the exact `_No cells in aggregated.json — pending first collection._` line inside the markers, never a bare/empty table.
    - Idempotence: run bench-publish.py twice in a row against the same temp `BENCHMARKS.md` copy with identical `--in`/`--label`; `diff` the file's state after run 1 against its state after run 2 — empty diff, exit 0 both times.
    - Append-never-destroy: build a small synthetic markdown file (NOT the real BENCHMARKS.md) with arbitrary prose and no marker pair at all; run bench-publish.py against it; assert the original prose survives byte-for-byte and the marker block is appended at the end.
    - README teaser: copy the real `README.md` to a temp path, run with `--readme <temp-path>`; assert the teaser markers now contain exactly one line reflecting the fixture's cell count/label, and the rest of the temp README is untouched; run a second time and assert idempotence (byte-identical) the same way as the BENCHMARKS.md case.
    - `--check`: run `--check` against a temp `BENCHMARKS.md` copy that already matches the fixture's expected output — exit 0, no write; then mutate the fixture (change one cost value) and re-run `--check` against the SAME already-regenerated temp file — exit 3, with a non-empty diff on stdout, and the temp file itself unchanged (still exit-3-worthy on a follow-up check).
  </action>
  <verify>
    <automated>bats tests/bench-publish.bats</automated>
  </verify>
  <acceptance_criteria>
    - `bats tests/bench-publish.bats` exits 0, all tests passing
    - `bats tests/` (full suite) exits 0 — zero regression
    - `git status --porcelain BENCHMARKS.md README.md` is empty after the bats run (proves the real repo files were never mutated by any test)
  </acceptance_criteria>
  <done>tests/bench-publish.bats fully proves marker-scoped regeneration correctness, idempotence, append-never-destroy, the README teaser path, and --check staleness detection — all against temp copies, zero mutation of the real repo files, $0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `aggregated.json` cell data (task/baseline/category strings, cost/pass numbers) -> `bench-publish.py` -> markdown table injected into `BENCHMARKS.md`/`README.md`, later rendered by GitHub | Same provenance boundary as the chart plan's SVG rendering — repo-controlled today, but the regeneration step must not assume well-formed input |
| `bench-publish.py` marker-splice write path -> `BENCHMARKS.md`/`README.md` on disk | A bug in the splice logic could silently destroy hand-written methodology prose outside the markers — the single highest-value thing this plan protects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-06-05 | Tampering | `task_id`/`baseline_id`/`category` values interpolated into markdown table cells | mitigate | `md_cell()`-equivalent escaping (`\|` escaped, newlines stripped) applied to every interpolated cell, mirroring `cairn-map.py`'s own `md_cell()` — prevents a hostile/malformed id from breaking the table's row structure |
| T-06-06 | Tampering (data loss) | Marker-splice logic accidentally overwriting hand-written content outside the markers | mitigate | Exact `split_markers()`/replace-only-inner mechanics ported from `cairn-map.py`; proven by a dedicated bats test asserting byte-identical content outside the markers before/after every regeneration |
| T-06-07 | Repudiation | A stale `BENCHMARKS.md`/`README.md` drifting from the actual `aggregated.json` without a visible signal | accept | `--check` mode gives an explicit, scriptable staleness signal (exit 3 + diff), matching `cairn-map.py`'s own precedent; git history is the underlying audit trail |
| T-06-08 | Tampering | bats tests accidentally mutating the REAL committed `BENCHMARKS.md`/`README.md` instead of temp copies | mitigate | Every test in `tests/bench-publish.bats` operates on an explicit `cp`'d temp copy under `$BATS_TEST_TMPDIR`; the plan's own acceptance criteria include a `git status --porcelain` check on the real files after the bats run |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan — stdlib Python only; the Package Legitimacy Gate does not apply |
</threat_model>

<verification>
Before declaring this plan complete:
- [ ] `bats tests/bench-publish.bats` passes, $0, zero regression to `bats tests/` overall
- [ ] `python3 -m py_compile benchmarks/scripts/bench-publish.py` exits 0
- [ ] `BENCHMARKS.md` and `README.md` carry the exact required marker pairs with an honest pending notice inside
- [ ] Marker-scoped regeneration proven idempotent and append-never-destroy, entirely against temp copies
- [ ] `git status --porcelain BENCHMARKS.md README.md` is clean after the full bats run
</verification>

<success_criteria>
- REPT-01 fully satisfied: `BENCHMARKS.md` is methodology-first, links raw data honestly labeled, and states results as explicitly pending.
- REPT-03 fully satisfied: the generated-marker regeneration machinery exists, is proven correct/idempotent, and never destroys hand-written content.
- Zero regression to any existing script, test, or documentation file.
- Zero unplanned Claude API spend.
</success_criteria>

<output>
Create `.planning/phases/06-reporting-charts-publication/06-02-SUMMARY.md` when done
</output>
