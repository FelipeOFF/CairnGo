---
phase: 06-reporting-charts-publication
plan: "03"
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - benchmarks/scripts/bench-all.sh
  - tests/bench-all.bats
autonomous: true
requirements: [REPT-04]
beads: [CairnGo-dew]

must_haves:
  truths:
    - "bench-all.sh with no flags (or explicit --dry-run) prints the full execution plan (resolved tasks, baselines, reps, total run count, dollar cost ceiling) and exits 0 WITHOUT invoking stage-plugins.py, bench-matrix.py, bench-aggregate.py, bench-chart.py, or bench-publish.py at all — proven by a tripwire seam, not by inspection alone"
    - "bench-all.sh --yes without ANTHROPIC_API_KEY set dies a usage error before invoking anything — spend is never attempted without a key"
    - "bench-all.sh --yes runs stage-plugins.py -> bench-matrix.py -> bench-aggregate.py -> bench-chart.py -> bench-publish.py in that exact order, aborting immediately on the first non-zero exit"
    - "The full pipeline, exercised end-to-end against the CAIRN_BENCH_CLAUDE_BIN stub with a scoped-down task/baseline/rep count, produces a matrix.jsonl, an aggregated.json, chart SVGs, and a filled-in (non-pending) Results section in a TEMP COPY of BENCHMARKS.md — entirely at $0"
    - "The documented cost ceiling printed by --dry-run matches the ~$40 figure already published in BENCHMARKS.md's Methodology and Reproduction sections — no drift between the two"
  artifacts:
    - path: benchmarks/scripts/bench-all.sh
      provides: "the single documented REPT-04 reproduction command: plan+ceiling always printed, --dry-run default (zero invocation), --yes gated on an explicit flag AND a present ANTHROPIC_API_KEY"
      contains: "stage-plugins.py"
    - path: tests/bench-all.bats
      provides: "tripwire proof of the --dry-run zero-invocation contract, usage-error proofs, and a full $0 stub pipeline run producing a filled temp BENCHMARKS.md"
      contains: "tripwire"
  key_links:
    - from: "bench-all.sh --yes pipeline"
      to: "benchmarks/scripts/bench-chart.py and benchmarks/scripts/bench-publish.py (built in plans 06-01/06-02 of this same phase)"
      via: "sequential subprocess invocation, aggregated.json passed from bench-aggregate.py's --out into both scripts' --in"
      pattern: "bench-chart.py"
    - from: "bench-all.sh's printed cost ceiling"
      to: "BENCHMARKS.md Methodology + Reproduction sections (written in plan 06-02)"
      via: "the same hardcoded ~$40 figure, cross-referenced to benchmarks/README.md's own Cost model worked example — never independently re-derived"
      pattern: "\\$40"
---

<objective>
Build `bench-all.sh` (REPT-04): the single documented command that reproduces the full benchmark run — `stage-plugins.py` → `bench-matrix.py` → `bench-aggregate.py` → `bench-chart.py` → `bench-publish.py` — with a loud, always-printed plan + dollar-cost ceiling, a `--dry-run` default that spends and invokes nothing, and an explicit `--yes` gate for the only path that can spend real money.

Purpose: REPT-04 requires reproduction to be one command, with its estimated cost stated up front. This is also the phase's (and the milestone's) final piece — after this plan, the ENTIRE benchmark pipeline (Phases 1-6) is a single, safely-gated, $0-by-default command away from producing real, publishable results, whenever `ANTHROPIC_API_KEY` becomes available and an operator explicitly authorizes the spend.
Output: `benchmarks/scripts/bench-all.sh`, `tests/bench-all.bats`.
</objective>

<execution_context>
@~/.claude/plugins/cache/cairngo/gsd/4.3.1/workflows/execute-plan.md
@~/.claude/plugins/cache/cairngo/gsd/4.3.1/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-reporting-charts-publication/06-CONTEXT.md
@benchmarks/README.md
@benchmarks/scripts/bench-matrix.py
@benchmarks/scripts/stage-plugins.py
@benchmarks/scripts/bench-aggregate.py
@benchmarks/scripts/bench-chart.py
@benchmarks/scripts/bench-publish.py
@BENCHMARKS.md
@tests/helpers.bash
</context>

<interfaces>
<!-- Exact CLI contracts of every script bench-all.sh orchestrates, either already established (bench-matrix.py, stage-plugins.py, bench-aggregate.py) or defined by this phase's earlier plans (bench-chart.py, bench-publish.py). Use directly — no exploration. -->

`stage-plugins.py`: `--baseline <manifest.json>` (repeatable) or `--all`; `EXIT_OK=0`, `EXIT_USAGE=2`. `--baseline benchmarks/baselines/vanilla.json` against a baseline with an empty `provisioning.plugin_dirs` list is a true no-op (zero entries to stage, zero network).

`bench-matrix.py`: `(--task <dir> | --tasks <spec>) --baselines <name,name,...> --out <jsonl> --seed <int> [--baselines-dir <dir>] [--reps <N>, default 5]`. `--tasks` accepts a comma-separated list OR a single glob pattern (e.g. `"benchmarks/tasks/*"`). `EXIT_OK=0`, `EXIT_USAGE=2`.

`bench-aggregate.py`: `--in <jsonl> [--in <jsonl> ...] --out <aggregated.json>`. `EXIT_OK=0`, `EXIT_USAGE=2`.

`bench-chart.py` (this phase's plan 06-01): `--in <aggregated.json> --out-dir <dir> --label <caption>`. `EXIT_OK=0`, `EXIT_USAGE=2`. `--out-dir` is auto-created if missing.

`bench-publish.py` (this phase's plan 06-02): `--in <aggregated.json> --benchmarks <path, default "BENCHMARKS.md"> --label <caption> [--readme <path>] [--check]`. `EXIT_OK=0`, `EXIT_USAGE=2`, `EXIT_STALE=3` (--check only). `--benchmarks`/`--readme` targets must already exist (usage error otherwise) — both already exist in the real repo as of plan 06-02.

`benchmarks/README.md`'s Cost model: full matrix = `6 tasks × 4 arms × 5 reps = 120 runs`, declared ceiling **~$40**. `bench-all.sh` hardcodes this same ~$40 figure (it has no live pricing API to query) — it must never drift from `benchmarks/README.md`'s and `BENCHMARKS.md`'s own published number.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: bench-all.sh — plan-first, dry-run-default, explicit-yes-to-spend orchestrator</name>
  <files>benchmarks/scripts/bench-all.sh</files>
  <read_first>
    benchmarks/scripts/bench-matrix.py, benchmarks/scripts/stage-plugins.py, benchmarks/scripts/bench-aggregate.py (already loaded in context — exact CLI contracts, mirrored in this plan's Interfaces block)
    benchmarks/scripts/bench-chart.py, benchmarks/scripts/bench-publish.py (already loaded in context — built by the sibling plans this plan depends on)
    benchmarks/README.md's Cost model section (already loaded in context — the ~$40 figure this script must restate verbatim)
  </read_first>
  <action>
    Create `benchmarks/scripts/bench-all.sh`: `#!/usr/bin/env bash`, `set -euo pipefail`, a header comment documenting usage and the exit-code contract (0 ok; 2 usage error; otherwise the propagated exit code of whichever pipeline step failed under `set -e` — this script is an orchestrator, not a new independent numeric-code contract). No `.py` companion — this IS the entry point, matching the "thin bash wrapper" pattern already used elsewhere in this repo, just orchestrating multiple sibling scripts instead of one.

    CLI flags (hand-rolled `case`/`while` loop over positional args, matching this repo's simple-script bash convention): `--dry-run` | `--yes` (mutually exclusive; a usage error if both given); `--seed <int>` (default: a fixed documented constant, `20260726`, matching the same seed already used in `benchmarks/README.md`'s Variance pilot recipe, for consistency — override for any other reproducible run); `--tasks <spec>` (default `"benchmarks/tasks/*"`); `--baselines <name,name,...>` (default `"vanilla,gsd-only,cairn,competitor-ralph-specum"`); `--reps <N>` (default `5`); `--out <dir>` (default `benchmarks/results`); `--charts-dir <dir>` (default `benchmarks/charts`); `--benchmarks <path>` (default `BENCHMARKS.md`); `--readme <path>` (default `README.md`). Resolve the scripts directory via an overridable env seam `CAIRN_BENCH_SCRIPTS_DIR` (default: this script's own `dirname`) — mirrors the existing `CAIRN_BENCH_CLAUDE_BIN` seam philosophy in this subtree, and is what lets bats point the orchestrator at tripwire stubs without touching the real scripts.

    Mode resolution: the default (neither `--dry-run` nor `--yes` given) is ALWAYS `--dry-run` — this is the safe default regardless of whether `ANTHROPIC_API_KEY` happens to be set in the environment (spend must always be an explicit, unambiguous `--yes`, never inferred from env-var presence). If `--yes` is given but `ANTHROPIC_API_KEY` is unset/empty, die a usage error BEFORE printing the plan or invoking anything ("--yes requires ANTHROPIC_API_KEY to be set").

    Always print the plan FIRST, in both modes, before anything else: the resolved `--tasks` spec, the resolved baseline name list, `--reps`, the computed `total_runs` (task-count × baseline-count × reps — task-count from a `ls`/glob count when `--tasks` contains `*`, else the comma-count), the hardcoded ~$40 ceiling (restate it verbatim — never recompute a different number), the resolved mode (`DRY-RUN` or `LIVE`), and whether `ANTHROPIC_API_KEY` is present (`yes`/`no` — NEVER print the key's value).

    Dry-run path: after printing the plan, print one line making the zero-invocation contract explicit (e.g. `[bench-all] dry-run: no downstream script invoked, $0 spent`) and `exit 0` — literally do not call `stage-plugins.py`, `bench-matrix.py`, `bench-aggregate.py`, `bench-chart.py`, or `bench-publish.py` anywhere on this path.

    `--yes` path: derive `LABEL` from the pinned model id read out of one of the resolved baseline manifests (e.g. `jq -r .model "<baselines-dir>/vanilla.json"`) concatenated with today's UTC date (`date -u +%Y-%m-%d`), formatted the same way `bench-chart.py`/`bench-publish.py`'s `--label` is documented to look elsewhere in this repo. Then, in order, aborting immediately on the first non-zero exit (`set -e` handles this automatically — no manual `if` needed per step):
      1. `stage-plugins.py`, called once PER baseline name actually resolved for this run (`--baseline "<baselines-dir>/<name>.json"` per name — never a blanket `--all`, so a scoped-down test run never touches manifests/plugins outside what it actually asked for).
      2. `bench-matrix.py --tasks "$TASKS" --baselines "$BASELINES" --out "$OUT/matrix.jsonl" --seed "$SEED" --reps "$REPS"`.
      3. `bench-aggregate.py --in "$OUT/matrix.jsonl" --out "$OUT/aggregated.json"`.
      4. `bench-chart.py --in "$OUT/aggregated.json" --out-dir "$CHARTS_DIR" --label "$LABEL"`.
      5. `bench-publish.py --in "$OUT/aggregated.json" --benchmarks "$BENCHMARKS_PATH" --label "$LABEL" --readme "$README_PATH"`.
    Print a final summary line naming `$OUT`, `$CHARTS_DIR`, and the same "total_cost_usd is Anthropic's own client-side estimate, never authoritative billing data" caveat already used throughout `benchmarks/README.md`.

    Explicitly document in the header comment (for future readers, and to close the T-06-08 threat below) that this script must NEVER be wired into `.github/workflows/ci.yml` or any other automated/scheduled trigger — it is a deliberately manual, operator-invoked command.
  </action>
  <verify>
    <automated>bash -n benchmarks/scripts/bench-all.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n benchmarks/scripts/bench-all.sh` exits 0 (syntax-clean)
    - `ANTHROPIC_API_KEY= bash benchmarks/scripts/bench-all.sh --dry-run` exits 0 and its stdout contains both `DRY-RUN` and the exact substring `$40`
    - `bash benchmarks/scripts/bench-all.sh --dry-run --yes` exits 2 (mutually-exclusive usage error)
    - `ANTHROPIC_API_KEY= bash benchmarks/scripts/bench-all.sh --yes` exits 2 (spend requires a real key) before printing any pipeline-step output
  </acceptance_criteria>
  <done>bench-all.sh exists, is syntax-clean, always prints the plan+ceiling first, defaults to a true zero-invocation dry-run, and gates --yes on both the explicit flag and a present ANTHROPIC_API_KEY — verified functionally by Task 2's bats suite.</done>
</task>

<task type="auto">
  <name>Task 2: tests/bench-all.bats — tripwire zero-invocation proof + full $0 stub pipeline end-to-end</name>
  <files>tests/bench-all.bats</files>
  <read_first>
    tests/helpers.bash (already loaded in context — make_env_asserting_claude_stub and the CAIRN_BENCH_CLAUDE_BIN seam this test reuses for the live-pipeline portion)
    benchmarks/scripts/bench-all.sh (written in Task 1 of this plan)
  </read_first>
  <action>
    Create `tests/bench-all.bats` (header comment; `load 'helpers'`; `BENCH_SCRIPTS_DIR="$CAIRN_REPO_ROOT/benchmarks/scripts"`). `@test` blocks:

    - Mutually-exclusive flags: `--dry-run --yes` together dies exit 2.
    - `--yes` without a key: `ANTHROPIC_API_KEY=` (unset/empty) + `--yes` dies exit 2, naming `ANTHROPIC_API_KEY` in the error output, before any other line resembling a pipeline-step invocation appears in stdout.
    - Tripwire zero-invocation proof (the deep-work acceptance criterion for this plan): set `CAIRN_BENCH_SCRIPTS_DIR` to a fresh `$BATS_TEST_TMPDIR/tripwire-scripts` directory populated with 5 executable stub files named `stage-plugins.py`, `bench-matrix.py`, `bench-aggregate.py`, `bench-chart.py`, `bench-publish.py`, each of which (if ever invoked) touches a sentinel file (e.g. `$BATS_TEST_TMPDIR/INVOKED-<name>`) and exits 1. Run `bench-all.sh --dry-run` (and separately, with no flags at all, `ANTHROPIC_API_KEY` unset) against this tripwire dir; assert exit 0 in both cases, assert stdout contains the plan (`total_runs`, the `$40` ceiling) and the `DRY-RUN` mode label, and assert NONE of the 5 sentinel files exist afterward — mechanical proof that zero downstream scripts were invoked, not just an inference from reading the code.
    - Full pipeline end-to-end at $0 (the deep-work acceptance criterion "full pipeline end-to-end com stub ($0) produz BENCHMARKS.md Results preenchido em cópia temp"): using `make_env_asserting_claude_stub` (or an equivalent canned-JSON stub) on `CAIRN_BENCH_CLAUDE_BIN`, a dummy non-empty `ANTHROPIC_API_KEY` value, and `bench-all.sh`'s real, un-tripwired `CAIRN_BENCH_SCRIPTS_DIR` (this repo's actual `benchmarks/scripts/`) — invoke `bench-all.sh --yes` scoped down via `--tasks benchmarks/tasks/smoke-convert --baselines vanilla --reps 1 --out "$BATS_TEST_TMPDIR/out" --charts-dir "$BATS_TEST_TMPDIR/charts" --benchmarks "$BATS_TEST_TMPDIR/BENCHMARKS.md" --readme "$BATS_TEST_TMPDIR/README.md"`, where the two target files are first `cp`'d from the real committed `BENCHMARKS.md`/`README.md` into `$BATS_TEST_TMPDIR` (so markers exist to regenerate into, and the REAL repo files are never touched). Assert: exit 0; `$BATS_TEST_TMPDIR/out/matrix.jsonl` has exactly 1 line; `$BATS_TEST_TMPDIR/out/aggregated.json` exists and its `.cells` has exactly 1 key; at least one `*.svg` file exists under `$BATS_TEST_TMPDIR/charts`; the temp `BENCHMARKS.md`'s content between the `<!-- cairn:generated:benchmarks:start/end -->` markers now contains the `smoke-convert`/`vanilla` row (no longer the Task-1-authored pending notice) while the surrounding Methodology/Raw data/Reproduction/Changelog prose is untouched.
    - No real spend anywhere: assert the real repo's `git status --porcelain BENCHMARKS.md README.md benchmarks/results/ benchmarks/charts/` is empty after every test in this file runs (the full-pipeline test above only ever wrote into `$BATS_TEST_TMPDIR`) — the single mechanical proof that this entire bats file, tripwire test included, spent and touched nothing outside its own sandbox.
  </action>
  <verify>
    <automated>bats tests/bench-all.bats</automated>
  </verify>
  <acceptance_criteria>
    - `bats tests/bench-all.bats` exits 0, all tests passing
    - `bats tests/` (full suite) exits 0 — zero regression
    - `git status --porcelain BENCHMARKS.md README.md benchmarks/results/ benchmarks/charts/` is empty after the bats run
  </acceptance_criteria>
  <done>tests/bench-all.bats proves --dry-run's zero-invocation contract via a mechanical tripwire (not just code inspection), and proves the full stage->matrix->aggregate->chart->publish pipeline end-to-end against a stub at $0, producing a filled-in Results section in a temp BENCHMARKS.md copy — with zero mutation of any real repo file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator's shell -> `bench-all.sh --yes` -> real Anthropic API spend (via the full downstream pipeline) | The single point in this entire benchmark suite where a command can spend real, uncontrolled money if misused |
| `bench-all.sh` stdout -> operator's terminal | The plan+ceiling print is the human's last chance to abort before any spend; it must never leak secret values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-06-09 | Tampering / Elevation of Privilege (accidental spend) | `--yes` flag | mitigate | `--yes` is always explicit and mutually exclusive with `--dry-run`; the default (no flags) is always `--dry-run` regardless of `ANTHROPIC_API_KEY` presence; `--yes` additionally requires a non-empty `ANTHROPIC_API_KEY` or dies before invoking anything; the plan + `$40` ceiling is printed BEFORE any spend in every mode; proven by a mechanical tripwire bats test, not just code review |
| T-06-10 | Tampering | Automated/CI trigger accidentally wiring `bench-all.sh --yes` into a scheduled or on-push job, causing uncontrolled spend on every run | mitigate | `bench-all.sh` is not registered in `.github/workflows/ci.yml` or any other trigger in this plan (explicitly out of scope — the real collection run is a deliberate, deferred, post-milestone operator action per `06-CONTEXT.md`'s Deferred Ideas); the script's own header comment documents this constraint for future readers |
| T-06-11 | Information Disclosure | `ANTHROPIC_API_KEY` leaking into `bench-all.sh`'s own stdout/plan-print output | mitigate | The plan-print step only ever prints presence/absence (`yes`/`no`) of `ANTHROPIC_API_KEY`, never its value — consistent with the existing "scan output for key material before committing" discipline already documented in `benchmarks/README.md` |
| T-06-12 | Tampering | The hardcoded `~$40` ceiling drifting silently out of sync with `benchmarks/README.md`'s / `BENCHMARKS.md`'s own published Cost model figure | accept | Single-source risk, mitigated by cross-referencing the exact same figure across all three locations at authoring time (this plan, plan 06-02's `BENCHMARKS.md`, and the pre-existing `benchmarks/README.md`); no automated cross-file consistency check exists yet — acceptable for this phase, revisit if the cost model ever changes |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan — pure bash orchestration of already-existing stdlib Python scripts; the Package Legitimacy Gate does not apply |
</threat_model>

<verification>
Before declaring this plan complete:
- [ ] `bats tests/bench-all.bats` passes, $0, zero regression to `bats tests/` overall
- [ ] `bash -n benchmarks/scripts/bench-all.sh` exits 0
- [ ] The tripwire test mechanically proves `--dry-run` (and the no-flags default) invokes zero downstream scripts
- [ ] The full-pipeline stub test proves `matrix.jsonl` -> `aggregated.json` -> a chart SVG -> a filled temp `BENCHMARKS.md` Results section, end-to-end, at $0
- [ ] `git status --porcelain` on the real `BENCHMARKS.md`/`README.md`/`benchmarks/results/`/`benchmarks/charts/` is clean after the full bats run
- [ ] The printed `~$40` ceiling matches `BENCHMARKS.md`'s and `benchmarks/README.md`'s own published figure exactly
</verification>

<success_criteria>
- REPT-04 fully satisfied: a single documented command (`bench-all.sh`) reproduces the full benchmark run, states its estimated dollar cost up front, and never spends without an explicit, key-gated `--yes`.
- The complete Phase 1-6 pipeline is now one command away from producing real, publishable results, whenever an operator explicitly authorizes it — with zero live calls made during this phase's own planning or execution.
- Zero regression to any existing script or test.
- Zero unplanned Claude API spend.
</success_criteria>

<output>
Create `.planning/phases/06-reporting-charts-publication/06-03-SUMMARY.md` when done
</output>
