---
phase: 04-competitor-baseline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - benchmarks/baselines/competitor-ralph-specum.json
  - benchmarks/scripts/bench-run.py
  - tests/bench-run.bats
  - benchmarks/README.md
autonomous: true
requirements: [COMP-01]
beads: [CairnGo-3jj]

must_haves:
  truths:
    - "The competitor baseline manifest (competitor-ralph-specum.json) pins an exact, immutable git tag (v4.0.0) and carries a defaults_source field citing the exact vendor docs its configuration was taken from"
    - "The competitor manifest's model and claude_flags are byte-identical to vanilla/gsd-only/cairn — only provisioning differs (FAIR-02)"
    - "bench-run.py resolves a nested --plugin-dir target (staged_path + plugin_dir_subpath) for the competitor's non-root plugin.json location, without changing behavior for any existing manifest that omits plugin_dir_subpath"
    - "A plugin_dir_subpath target that does not exist on disk dies loud (EXIT_USAGE) before any claude subprocess is launched — never silently measures a broken/dead-weight arm"
    - "The real tzachbon/smart-ralph@v4.0.0 repo is staged locally and its nested plugins/ralph-specum/.claude-plugin/plugin.json structurally exists at the exact path the manifest declares"
    - "The plugin load-check is provable at $0 via bats (stub-observed argv) and documented as a live `claude -p \"/help\"` command; when ANTHROPIC_API_KEY is absent, the live check is explicitly recorded as PENDING with its exact reproduction command, never silently skipped"
  artifacts:
    - path: benchmarks/baselines/competitor-ralph-specum.json
      provides: "pinned competitor manifest with plugin_dir_subpath + defaults_source, claude_flags/model byte-identical to cairn.json"
      contains: "plugin_dir_subpath"
    - path: benchmarks/scripts/bench-run.py
      provides: "backward-compatible nested --plugin-dir resolution (Path(staged_path) / plugin_dir_subpath)"
      contains: "plugin_dir_subpath"
    - path: tests/bench-run.bats
      provides: "$0 proof of subpath --plugin-dir resolution, missing-target fail-loud before spend, and manifest fairness/pin structural assertions"
      contains: "plugin_dir_subpath"
    - path: benchmarks/README.md
      provides: "documented 4th baseline, the plugin_dir_subpath schema extension, and the load-check (stub proof referenced + live command + explicit PENDING status)"
      contains: "competitor-ralph-specum"
  key_links:
    - from: "benchmarks/baselines/competitor-ralph-specum.json provisioning.plugin_dirs[0].plugin_dir_subpath"
      to: "benchmarks/scripts/bench-run.py load_baseline() + --plugin-dir argv construction"
      via: "Path(entry[\"staged_path\"]) / entry.get(\"plugin_dir_subpath\", \"\")"
      pattern: "plugin_dir_subpath"
    - from: "tests/bench-run.bats new @test blocks"
      to: "benchmarks/scripts/bench-run.py"
      via: "stub-observed argv assertion + EXIT_USAGE die-message assertion + jq -S diff against cairn.json"
      pattern: "plugin_dir_subpath"
    - from: "benchmarks/README.md live load-check command"
      to: "benchmarks/plugins/ralph-specum/v4.0.0/plugins/ralph-specum (staged by Task 1)"
      via: "documented claude -p \"/help\" --plugin-dir <path> command, grep -o 'ralph-specum:[a-z-]*'"
      pattern: "ralph-specum"
---

<objective>
Add the competitor arm this milestone's whole comparison depends on being fair: a pinned, version-locked `tzachbon/smart-ralph`/`ralph-specum` baseline manifest (`competitor-ralph-specum.json`), a small backward-compatible extension to `bench-run.py` so it can resolve a `--plugin-dir` target that lives in a nested subpath of the staged repo (unlike GSD/context-mode/cairn, which all sit at repo root), and the load-check that proves the arm actually loads (not "vanilla with dead weight") — provable at $0 via bats, documented for live execution, and explicitly PENDING (not silently skipped) while `ANTHROPIC_API_KEY` is absent.

Purpose: COMP-01 + ROADMAP Phase 4's own stated risk — "a misconfigured competitor arm published publicly is the single worst reputational outcome available" (04-CONTEXT.md). Everything here is additive to infrastructure Phase 1-3 already built and proved at $0 (04-RESEARCH.md's "Don't Hand-Roll" key insight): no new staging code path, no new test idiom, no new dependency.
Output: `competitor-ralph-specum.json` (pinned v4.0.0, `defaults_source`, `plugin_dir_subpath`); `bench-run.py` with nested-`--plugin-dir` resolution; `tests/bench-run.bats` extended with subpath/fail-loud/fairness coverage; `benchmarks/README.md` documenting the 4th arm and its load-check status.
</objective>

<execution_context>
@~/.claude/plugins/cache/cairngo/gsd/4.3.1/workflows/execute-plan.md
@~/.claude/plugins/cache/cairngo/gsd/4.3.1/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-competitor-baseline/04-CONTEXT.md
@.planning/phases/04-competitor-baseline/04-RESEARCH.md
@benchmarks/scripts/bench-run.py
@benchmarks/scripts/stage-plugins.py
@benchmarks/baselines/cairn.json
@benchmarks/baselines/gsd-only.json
@tests/bench-run.bats
@tests/helpers.bash
@benchmarks/README.md
</context>

<interfaces>
<!-- Exact current-state snippets, the exact target manifest, and the exact target code. Use directly — no exploration. -->

### Target manifest — `benchmarks/baselines/competitor-ralph-specum.json` (verbatim from 04-RESEARCH.md's Code Examples section, already verified schema-compatible with `load_baseline()`/`stage_entry()`)

```json
{
  "name": "competitor-ralph-specum",
  "description": "tzachbon/smart-ralph's ralph-specum plugin: spec-driven, task-by-task autonomous execution. Chosen over github/spec-kit (no Claude Code plugin manifest anywhere in repo -- a specify-cli project scaffolder, not --plugin-dir-provisionable), bmad-code-org/BMAD-METHOD (no root .claude-plugin/plugin.json -- only marketplace.json -- and every skill requires a separate `npx bmad-method install` scaffold incompatible with this harness's provisioning model), and obra/superpowers (261k stars vs. 431 -- vastly larger adoption, but its documented default workflow has a hard design-approval gate with zero non-interactive escape hatch, unlike ralph-specum's --quick mode). Pinned to v4.0.0 (verified live via `git ls-remote --tags` 2026-07-26 -- the newest real git tag; plugin.json at HEAD self-reports 4.10.1, untagged -- disclosed staleness, not a provisioning error). --plugin-dir target is the plugins/ralph-specum/ subpath inside the cloned repo, per the vendor's own documented local-dev instructions (README.md, v4.0.0, 'Installation' section).",
  "model": "claude-haiku-4-5-20251001",
  "defaults_source": "https://github.com/tzachbon/smart-ralph/blob/v4.0.0/README.md#quick-start (documents /ralph-specum:start ... and the --quick mode as the two first-party default entry points) and https://github.com/tzachbon/smart-ralph/blob/v4.0.0/plugins/ralph-specum/references/quick-mode.md (full non-interactive execution contract, including the explicit 'non-interactive -- do not block or prompt user' directive)",
  "claude_flags": {
    "bare": true,
    "max_turns": 8,
    "no_session_persistence": true,
    "permission_mode": "acceptEdits"
  },
  "provisioning": {
    "plugin_dirs": [
      {
        "plugin": "ralph-specum",
        "source": { "type": "git", "repo": "tzachbon/smart-ralph", "ref": "v4.0.0" },
        "staged_path": "benchmarks/plugins/ralph-specum/v4.0.0",
        "plugin_dir_subpath": "plugins/ralph-specum",
        "build": []
      }
    ]
  }
}
```
`claude_flags` and `model` are byte-identical to `vanilla.json`/`gsd-only.json`/`cairn.json` (FAIR-02) — only `provisioning` differs, exactly as the existing three manifests already establish.

### Current `bench-run.py` `load_baseline()` plugin-dir existence check (lines 106-110) — the ONLY validation call site to change

```python
    for entry in manifest["provisioning"]["plugin_dirs"]:
        if not Path(entry["staged_path"]).is_dir():
            die(f"plugin '{entry['plugin']}' staged_path not found: "
                f"{entry['staged_path']} (stage and build it before running)",
                EXIT_USAGE)
```

Target (Pattern 1, 04-RESEARCH.md lines 221-229 — join `plugin_dir_subpath` before the existence check; `Path("/a/b") / ""` is a documented pathlib no-op, so every manifest that omits `plugin_dir_subpath` — vanilla/gsd-only/cairn — resolves to the exact same `target` it does today):

```python
    for entry in manifest["provisioning"]["plugin_dirs"]:
        target = Path(entry["staged_path"]) / entry.get("plugin_dir_subpath", "")
        if not target.is_dir():
            die(f"plugin '{entry['plugin']}' staged_path (+ plugin_dir_subpath) "
                f"target not found: {target} (stage and build it before running)",
                EXIT_USAGE)
```
**Keep the literal substring `staged_path` inside the die message** — `tests/bench-run.bats`'s existing test `"unstaged plugin staged_path dies EXIT_USAGE before any row or claude launch"` (line 150) greps the harness's stderr output for exactly that word (`grep -qF "staged_path"`); dropping it from the message breaks that pre-existing, unrelated-to-this-plan test.

### Current `bench-run.py` `main()` `--plugin-dir` argv construction (lines 222-223) — the ONLY invocation call site to change

```python
        for entry in manifest["provisioning"]["plugin_dirs"]:
            cmd += ["--plugin-dir", str(entry["staged_path"])]
```

Target (same join, mirrored):

```python
        for entry in manifest["provisioning"]["plugin_dirs"]:
            target = Path(entry["staged_path"]) / entry.get("plugin_dir_subpath", "")
            cmd += ["--plugin-dir", str(target)]
```

### Documented live load-check command (Pattern 2, 04-RESEARCH.md lines 242-254) — copy verbatim into `benchmarks/README.md`

```bash
claude -p "/help" \
  --plugin-dir benchmarks/plugins/ralph-specum/v4.0.0/plugins/ralph-specum \
  --model claude-haiku-4-5-20251001 --bare --no-session-persistence \
  --permission-mode acceptEdits --output-format json \
  | grep -o 'ralph-specum:[a-z-]*' | sort -u
# Expected (non-exhaustive): ralph-specum:start, ralph-specum:new,
# ralph-specum:research, ralph-specum:requirements, ralph-specum:design,
# ralph-specum:tasks, ralph-specum:implement, ralph-specum:status,
# ralph-specum:help
```
This is a single short call (no task fixture, no file edits) — the same "cheap enough to always run for real when a key exists" precedent already established by `benchmarks/README.md`'s existing "Live isolation smoke check" section.

### `stage-plugins.py` — confirmed unchanged (04-RESEARCH.md Pattern 1's own conclusion)
`stage_entry()` stages the FULL repo at `staged_path` regardless of where inside it the eventual `--plugin-dir` target sits — it has no notion of `plugin_dir_subpath` and needs none. Do not edit `benchmarks/scripts/stage-plugins.py`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Pin, write, and stage the ralph-specum competitor manifest</name>
  <files>benchmarks/baselines/competitor-ralph-specum.json</files>
  <read_first>
    .planning/phases/04-competitor-baseline/04-RESEARCH.md (Code Examples section — the manifest this task writes verbatim; Package Legitimacy Audit table; Pitfall 3 on stale-vs-tagged versions)
    benchmarks/baselines/cairn.json (the byte-identical claude_flags/model this manifest must match)
    benchmarks/scripts/stage-plugins.py (the staging script this task invokes for real — read to confirm the exact CLI contract: `--baseline <manifest.json>`)
    this plan's Interfaces block above (exact target JSON)
  </read_first>
  <action>
    Before writing the manifest, reconfirm `v4.0.0` is still the newest real git tag on `tzachbon/smart-ralph` with a quick `git ls-remote --tags https://github.com/tzachbon/smart-ralph.git` (Pitfall 3/5 discipline: never trust a `plugin.json` version field as if it were a clonable ref — always resolve pins via the live tag list; expect this to match 04-RESEARCH.md's same-day finding). Write `benchmarks/baselines/competitor-ralph-specum.json` with exactly the content in this plan's Interfaces block (name, description, model, `defaults_source`, `claude_flags`, `provisioning.plugin_dirs[0]` with `plugin_dir_subpath: "plugins/ralph-specum"`, pinned `ref: "v4.0.0"`, empty `build: []`) — do not paraphrase or shorten the `description`/`defaults_source` fields; they are the audit trail a reader checks the arm's configuration against.

    Then materialize the REAL pinned clone for real: run `stage-plugins.py --baseline benchmarks/baselines/competitor-ralph-specum.json`. This is a public, unauthenticated `git clone --branch v4.0.0 --depth 1` of a public GitHub repo with zero build commands (no `package.json` in `ralph-specum` — 04-RESEARCH.md's Standard Stack confirms this) — it costs $0 in Claude API spend and matches the exact precedent already established when GSD/context-mode were staged for real in Phase 2 (`benchmarks/plugins/gsd/v4.3.1`, `benchmarks/plugins/context-mode/v1.0.169`, both already present on disk and gitignored via `.gitignore:13`). If the sandbox executing this task has no outbound network access, note this explicitly as a deviation and leave the staging step for the next environment that does — do not fabricate a staged directory.
  </action>
  <verify>
    <automated>python3 -c "import json; m=json.load(open('benchmarks/baselines/competitor-ralph-specum.json')); c=json.load(open('benchmarks/baselines/cairn.json')); assert m['model']==c['model'], 'model mismatch'; assert m['claude_flags']==c['claude_flags'], 'claude_flags mismatch'; assert m['provisioning']['plugin_dirs'][0]['plugin_dir_subpath']=='plugins/ralph-specum'; assert m['provisioning']['plugin_dirs'][0]['source']['ref']=='v4.0.0'; assert m['provisioning']['plugin_dirs'][0]['source']['repo']=='tzachbon/smart-ralph'; assert m.get('defaults_source'); print('manifest OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import json; json.load(open('benchmarks/baselines/competitor-ralph-specum.json'))"` exits 0 (valid JSON)
    - the verify command above prints `manifest OK`
    - IF staging succeeded (network available): `test -f benchmarks/plugins/ralph-specum/v4.0.0/plugins/ralph-specum/.claude-plugin/plugin.json` exits 0, and `cat benchmarks/plugins/ralph-specum/v4.0.0/.staged-ref` prints `v4.0.0`
    - IF staging could not run (no network in this sandbox): documented as a deviation, Task 2/3's tests below do not depend on the real staged tree (they use synthetic fixtures) so the plan is not blocked
  </acceptance_criteria>
  <done>competitor-ralph-specum.json committed with the exact pin/defaults_source/plugin_dir_subpath from the Interfaces block, model/claude_flags byte-identical to cairn.json; the real tzachbon/smart-ralph@v4.0.0 is staged locally (or the staging attempt and its outcome is documented as a deviation if network was unavailable).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backward-compatible nested --plugin-dir resolution in bench-run.py, plus bats coverage</name>
  <files>benchmarks/scripts/bench-run.py, tests/bench-run.bats</files>
  <read_first>
    benchmarks/scripts/bench-run.py (current file, already loaded in context — load_baseline() lines 90-111, main()'s cmd/plugin-dir construction lines 212-223)
    tests/bench-run.bats (current file, already loaded in context — make_env_asserting_claude_stub usage, the existing "unstaged plugin staged_path dies EXIT_USAGE" test at lines 150-170, and the existing "manifest plugin_dirs entries become --plugin-dir flags" test at lines 172-212, whose inline-fixture-manifest style this task's new tests must follow)
    tests/helpers.bash (make_env_asserting_claude_stub, lines 326-354 — reuse unmodified, no new helper needed)
    benchmarks/baselines/competitor-ralph-specum.json (Task 1's output — the real manifest one of this task's new tests validates structurally)
    this plan's Interfaces block above (exact before/after code for both call sites, and the die-message correctness note)
  </read_first>
  <behavior>
    - Given a `plugin_dirs` entry WITHOUT `plugin_dir_subpath` (every existing `vanilla`/`gsd-only`/`cairn` manifest), `--plugin-dir` still resolves to the bare `staged_path`, unchanged — proven by the full pre-existing `tests/bench-run.bats` suite continuing to pass with zero edits to those three manifests.
    - Given an entry WITH `plugin_dir_subpath` (e.g. `"plugins/fake-plugin"`) whose joined target directory exists on disk, the constructed claude argv contains `--plugin-dir` immediately followed by `"<staged_path>/plugins/fake-plugin"`, never the bare `staged_path`.
    - Given an entry WITH `plugin_dir_subpath` whose joined target does NOT exist on disk (even though `staged_path` itself does exist), `load_baseline()` dies `EXIT_USAGE` (2) before any claude subprocess is launched; the die message still contains the literal substring `staged_path` (the pre-existing test's grep contract) and also names the specific missing joined path; no JSONL row is written; the claude stub is never invoked.
    - The real, committed `competitor-ralph-specum.json` (Task 1's output) is structurally valid on its own: pinned `source.ref == "v4.0.0"`, `plugin_dir_subpath == "plugins/ralph-specum"`, a non-empty `defaults_source` string, and `claude_flags`/`model` byte-for-byte identical to `cairn.json`'s (`jq -S` diff, zero output) — provable without staging or invoking claude at all.
  </behavior>
  <action>
    In `benchmarks/scripts/bench-run.py`, replace the two call sites exactly as shown in this plan's Interfaces block: (1) `load_baseline()`'s plugin-dir existence-check loop (lines 106-110) — join `Path(entry["staged_path"])` with `entry.get("plugin_dir_subpath", "")` into a `target` variable before the `is_dir()` check, and update the `die()` message to `f"plugin '{entry['plugin']}' staged_path (+ plugin_dir_subpath) target not found: {target} (stage and build it before running)"` — the literal word `staged_path` MUST remain in the message text (not just in a variable's value) so the pre-existing bats test that greps for it keeps passing; (2) `main()`'s `--plugin-dir` flag construction (lines 222-223) — compute the same `target` join and append `["--plugin-dir", str(target)]` instead of the bare `entry["staged_path"]`. Update the module docstring's Behavior step 5 (lines 22-26) to mention that `--plugin-dir` targets `staged_path` joined with an optional `plugin_dir_subpath` when the manifest declares one.

    Add three new `@test` blocks to `tests/bench-run.bats`, following the file's existing inline-fixture-manifest + `make_env_asserting_claude_stub`/tripwire-stub conventions (no new bash helpers needed):
    1. A subpath-resolution test: create a nested fixture dir (`mkdir -p "$BATS_TEST_TMPDIR/fake-repo/plugins/fake-plugin"`), write an inline manifest whose single `plugin_dirs` entry sets `staged_path` to `$BATS_TEST_TMPDIR/fake-repo` and `plugin_dir_subpath` to `"plugins/fake-plugin"`, run via `make_env_asserting_claude_stub`, and assert (mirroring the existing "manifest plugin_dirs entries become --plugin-dir flags" test's own `index("--plugin-dir")`/`+1` jq idiom) that the argv element immediately after `--plugin-dir` equals `"$BATS_TEST_TMPDIR/fake-repo/plugins/fake-plugin"`, not the bare `fake-repo` path.
    2. A missing-subpath-target test: create `$BATS_TEST_TMPDIR/fake-repo` (exists) but deliberately do NOT create a `plugins/fake-plugin` subdirectory inside it; write the same shape of manifest as test 1 pointing `plugin_dir_subpath` at the non-existent nested dir; run with a tripwire stub (same pattern as the existing "unstaged plugin staged_path dies EXIT_USAGE" test — a stub that `touch`es a marker file if ever invoked); assert `[ "$status" -eq 2 ]`, `echo "$output" | grep -qF "staged_path"`, the joined target path also appears in the output, no `--out` JSONL file was created, and the tripwire marker file does not exist (the stub was never launched).
    3. A manifest-fairness/pin structural test (no staging, no claude invocation — pure `jq`): assert `benchmarks/baselines/competitor-ralph-specum.json` has `.provisioning.plugin_dirs[0].source.ref == "v4.0.0"` and `.provisioning.plugin_dirs[0].plugin_dir_subpath == "plugins/ralph-specum"` and a non-empty `.defaults_source`; then `run diff <(jq -S '{model, claude_flags}' "$BENCH_BASELINES_DIR/competitor-ralph-specum.json") <(jq -S '{model, claude_flags}' "$BENCH_BASELINES_DIR/cairn.json")` and assert `[ "$status" -eq 0 ]` and `[ -z "$output" ]` — proving FAIR-02 byte-identical `model`/`claude_flags` mechanically, not just by inspection.
  </action>
  <verify>
    <automated>bats tests/bench-run.bats && bats tests/bench-verify.bats tests/stage-plugins.bats tests/bench-matrix.bats tests/bench-aggregate.bats</automated>
  </verify>
  <acceptance_criteria>
    - `bats tests/bench-run.bats` exits 0, all pre-existing tests plus the 3 new ones pass
    - `bats tests/bench-verify.bats tests/stage-plugins.bats tests/bench-matrix.bats tests/bench-aggregate.bats` exits 0 (zero regression from touching `bench-run.py`)
    - `grep -v '^#' benchmarks/scripts/bench-run.py | grep -c 'plugin_dir_subpath'` >= 2 (the two call sites)
    - `grep -c 'staged_path' benchmarks/scripts/bench-run.py` — the die message in `load_baseline()` still contains the literal word `staged_path`
    - `grep -v '^#' tests/bench-run.bats | grep -c 'plugin_dir_subpath'` >= 3 (the three new tests)
  </acceptance_criteria>
  <done>bench-run.py resolves plugin_dir_subpath at both call sites with zero behavior change for manifests that omit it; a missing joined target dies loud before any spend; the real competitor manifest is proven structurally fair (pin + defaults_source + byte-identical claude_flags/model) by a dedicated $0 bats test; full existing suite remains green.</done>
</task>

<task type="auto">
  <name>Task 3: Document the competitor arm, schema extension, and load-check in benchmarks/README.md</name>
  <files>benchmarks/README.md</files>
  <read_first>
    benchmarks/README.md (current file, already loaded in context — the Baselines table, the Staging subsection, and the exact "Live isolation smoke check: PENDING" section this task's new section mirrors)
    .planning/phases/04-competitor-baseline/04-RESEARCH.md (Summary section for the one-line "what it measures" description; this plan's Interfaces block for the exact live load-check command)
  </read_first>
  <action>
    Add `competitor-ralph-specum.json` as a fourth row in the Baselines table: provisioning `tzachbon/smart-ralph@v4.0.0` (subpath `plugins/ralph-specum`), "what it measures" summarizing the `--quick` non-interactive mechanism (the `PreToolUse` hook denying `AskUserQuestion`) that made it the winning candidate over `github/spec-kit`/`bmad-code-org/BMAD-METHOD` (structurally disqualified — no `--plugin-dir`-loadable manifest) and `obra/superpowers` (larger adoption, but no non-interactive escape hatch). Update the file's opening paragraph and the "## Baselines" intro sentence from "three pinned baseline manifests" to "four pinned baseline manifests".

    Under the existing "### Staging (`stage-plugins.py`)" section, add a short paragraph documenting the `plugin_dir_subpath` schema extension: what it is for (a plugin whose `.claude-plugin/plugin.json` lives in a subdirectory of the cloned repo rather than at repo root — `ralph-specum` is the first manifest to use it), that it is optional and backward-compatible (`Path(staged_path) / ""` is a no-op, so every manifest that omits it is unaffected), and that `stage-plugins.py` itself needed zero changes (it always stages the full repo at `staged_path`).

    Add a new "## Competitor plugin load-check" section (placed after "## Randomized execution order" and before "## Aggregation", matching the file's existing chronological build-up): state that the wiring is proven at $0 by `tests/bench-run.bats`'s new `plugin_dir_subpath` tests (name them). Then check whether `ANTHROPIC_API_KEY` is present in the current shell. If absent (the expected case, matching 04-RESEARCH.md's same-day finding), write a "PENDING" subsection in the exact style of the file's existing "## Live isolation smoke check: PENDING" section: state the key was absent when this phase completed, embed the exact reproduction command from this plan's Interfaces block verbatim (the `claude -p "/help" --plugin-dir ...` pipeline with its expected non-exhaustive `ralph-specum:*` command list), and note this is a single short `/help` call with no task fixture or file edits — cheap enough to run for real the moment a key exists. If `ANTHROPIC_API_KEY` IS present when this task executes, run that exact command for real instead and replace the PENDING text with the actually observed `ralph-specum:*` command list (same "run it for real when it's cheap and a key exists" precedent the isolation smoke check itself sets) — and scan the captured output for API key material before committing it, per the existing isolation-smoke-check section's own documented caveat.
  </action>
  <verify>
    <automated>grep -qF "competitor-ralph-specum" benchmarks/README.md && grep -qF "plugin_dir_subpath" benchmarks/README.md && grep -qF "ralph-specum" benchmarks/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF "competitor-ralph-specum" benchmarks/README.md` exits 0
    - `grep -qF "plugin_dir_subpath" benchmarks/README.md` exits 0
    - `grep -qF "Competitor plugin load-check" benchmarks/README.md` exits 0
    - a `PENDING` or an actually-captured `ralph-specum:` command list appears under that section — never silently absent
  </acceptance_criteria>
  <done>README documents the 4th arm in the Baselines table, the plugin_dir_subpath schema extension, and an explicit load-check status (PENDING with the exact reproduction command when ANTHROPIC_API_KEY is absent, or a real captured result when present).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator machine -> `tzachbon/smart-ralph` (public git clone, pinned tag) | Third-party plugin source fetched over the network by `stage-plugins.py`, staged locally under gitignored `benchmarks/plugins/` |
| staged `ralph-specum` bash hooks -> claude subprocess | `quick-mode-guard.sh` / `stop-watcher.sh` / `load-spec-context.sh` execute with the operator's real `PATH`, inside the isolated-but-not-network-isolated `HOME` `bench-run.py` already constructs |
| `competitor-ralph-specum.json` (manifest input) -> `bench-run.py` argv construction | `plugin_dir_subpath` is a manifest-driven path-join input, validated and resolved before any subprocess launch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-04-01 | Tampering | `tzachbon/smart-ralph` git source pin | mitigate | Pinned to the exact immutable tag `v4.0.0` (never `main`/the untagged `4.10.1` HEAD), reconfirmed live via `git ls-remote --tags` in Task 1 before writing the manifest (Pitfall 3); re-verify the same command immediately before any future publish cycle per Pitfall 5 |
| T-04-02 | Tampering | `ralph-specum`'s bash hooks (`quick-mode-guard.sh`, `stop-watcher.sh`, `load-spec-context.sh`) run with the operator's `PATH` | accept | Identical threat class already accepted for GSD's/context-mode's own hook/MCP code in Phase 2 (precedent T-02-06/T-02-07); mitigated by pinning to a reviewed tag + the existing isolated-`HOME` design, not by sandboxing hook execution — no new mechanism introduced here |
| T-04-03 | Elevation of Privilege | `claude` subprocess runs untrusted competitor code under `--permission-mode acceptEdits` | accept | Same trust boundary already accepted for every other plugin arm since Phase 2; isolated disposable `HOME` + the existing `acceptEdits` gate (which already records `permission_denials` for arbitrary Bash attempts, per `benchmarks/README.md`'s "Observed behavior") is the accepted, unchanged mitigation |
| T-04-04 | Tampering (of comparison validity) | Manifest configuration silently diverging from the vendor's own documented defaults (tuned up or down) | mitigate | `defaults_source` field cites the exact README Quick Start + `quick-mode.md` sections the manifest was derived from (auditable by any reader); `claude_flags`/`model` kept byte-identical to every other arm, mechanically proven by a dedicated `jq -S` diff bats test (Task 2) rather than left to inspection |
| T-04-05 | Repudiation / Information Disclosure | A silently-broken arm ("vanilla with dead weight," per Pitfall 2 in 04-RESEARCH.md) gets measured and published as if it were a real competitor result | mitigate | Load-check (Pattern 2): stub-based bats proof (Task 2) that `--plugin-dir` resolves to the correct nested `plugin.json` path BEFORE any spend, plus a documented live `/help` command (Task 3) whose captured output belongs in the phase SUMMARY as the audit trail, per 04-CONTEXT.md's own "Specific Ideas" note |
| T-04-SC | Tampering | Package/plugin source legitimacy — this phase adds no npm/pip/cargo registry install | accept | Diligence already performed and documented in `04-RESEARCH.md`'s Package Legitimacy Audit (repo age, 431 stars, MIT license, no build/postinstall scripts, `gh api`/`git ls-remote` verified live) — Approved verdict. Not an npm/pip/cargo install (no `package.json` anywhere in `ralph-specum`), so the blocking-human-checkpoint gate for `[ASSUMED]`/`[SUS]` packages does not apply — matches the precedent already set by GSD's and context-mode's own git-sourced staging in Phase 2, neither of which required one either |
</threat_model>

<verification>
Before declaring this plan complete:
- [ ] `bats tests/bench-run.bats tests/bench-verify.bats tests/stage-plugins.bats tests/bench-matrix.bats tests/bench-aggregate.bats` all pass, $0
- [ ] `competitor-ralph-specum.json` parses, pins `v4.0.0`, carries `defaults_source`, and its `model`/`claude_flags` are byte-identical to `cairn.json`'s (mechanically proven, not just inspected)
- [ ] `bench-run.py` resolves `plugin_dir_subpath` correctly for the competitor manifest and is provably unchanged in behavior for `vanilla.json`/`gsd-only.json`/`cairn.json`
- [ ] A missing `plugin_dir_subpath` target dies `EXIT_USAGE` before any claude subprocess is ever launched
- [ ] `benchmarks/README.md` documents the 4th arm, the schema extension, and an explicit load-check status (PENDING with reproduction command, or a real captured result — never silently absent)
- [ ] The real `tzachbon/smart-ralph@v4.0.0` is staged locally when network access permits, with its nested `plugins/ralph-specum/.claude-plugin/plugin.json` structurally confirmed present
</verification>

<success_criteria>
- COMP-01 satisfied: a fourth arm is configured strictly from its own documented defaults, version-pinned, and provisionable through the identical isolated pipeline as vanilla/gsd-only/cairn
- The phase's own worst-outcome risk (a misconfigured competitor arm) is mitigated by a load-check that is both $0-provable and live-documented, never silently skipped
- Zero regression to any existing baseline manifest, script, or test
- Zero unplanned Claude API spend (the one live load-check stays PENDING, exactly like Phase 2/3's precedent, unless a key happens to be present)
</success_criteria>

<output>
Create `.planning/phases/04-competitor-baseline/04-01-SUMMARY.md` when done
</output>
