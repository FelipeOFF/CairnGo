---
phase: 01-verification-core-first-real-run
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - benchmarks/results/smoke-convert.jsonl
  - benchmarks/README.md
  - tests/bench-run.bats
  - .planning/phases/01-verification-core-first-real-run/01-RESEARCH.md
autonomous: true
requirements: [HARN-02]
beads: [CairnGo-9f5]

must_haves:
  truths:
    - "One real, non-stubbed claude -p --output-format json call executes against the smoke-convert task and bench-run.py captures its actual JSON response into a JSONL row"
    - "The committed row contains total_cost_usd, full usage (input/output/cache_creation/cache_read tokens), duration_ms, duration_api_ms, num_turns, and is_error from the real response, not from a stub"
    - "The row's wall_clock_ms was measured by bench-run.py's own external timer (python time.time()), independent of claude's self-reported duration_ms"
    - "The stub's canned JSON payloads in tests/bench-run.bats have been diffed against the real live response's field names/shape and corrected if drifted, so the zero-cost bats suite keeps proving something true about the real schema"
    - "benchmarks/README.md documents the observed cost, the observed process exit/is_error behavior, and what verify.sh proves, so a third party can reproduce the single live run and knows its cost before running it"
    - "RESEARCH.md's Open Question 1 (exit-code <-> is_error mapping) is marked (RESOLVED) with an inline RESOLVED: note recording what the live run actually observed"
  artifacts:
    - path: "benchmarks/results/smoke-convert.jsonl"
      provides: "The phase's first real, committed benchmark data point"
    - path: "benchmarks/README.md"
      provides: "Methodology: how to run the stub suite, how to trigger the one live run, its documented cost, the observed exit-code/is_error mapping"
  key_links:
    - from: "manual invocation with CAIRN_BENCH_CLAUDE_BIN unset"
      to: "benchmarks/scripts/bench-run.py resolve_claude_bin()"
      via: "shutil.which(\"claude\") fallback"
      pattern: "shutil\\.which"
    - from: "benchmarks/results/smoke-convert.jsonl (real schema)"
      to: "tests/bench-run.bats (stub schema)"
      via: "manual field-name/shape diff after the live run"
      pattern: "usage\\.(input_tokens|output_tokens|cache_creation_input_tokens|cache_read_input_tokens)"
    - from: "benchmarks/results/smoke-convert.jsonl (observed exit code vs is_error)"
      to: ".planning/phases/01-verification-core-first-real-run/01-RESEARCH.md (Open Question 1)"
      via: "manual edit marking (RESOLVED) + inline RESOLVED: note"
      pattern: "RESOLVED"
---

<objective>
Execute the ONE real, non-stubbed `claude -p --output-format json` call this phase permits, against the smoke-convert task built in Plan 01, using the harness built in Plan 02. Capture the live response into a committed JSONL row, reconcile the bats stub's canned schema against what the real call actually returned, document the run's actual cost and observed behavior in `benchmarks/README.md`, and close the loop on RESEARCH.md's open exit-code question.

Purpose: HARN-02's core claim -- that `bench-run.py` correctly captures the real `claude -p` JSON schema -- is only proven by a genuinely live call, not a stub. This is also the phase's proof artifact: CONTEXT.md's `<specifics>` states the row "gets committed as the first real data point." RESEARCH.md's Pitfall 2 warns that a stub built before any live call risks silent field-name drift from the real schema -- this plan closes that loop. RESEARCH.md's Open Question 1 (exit-code <-> is_error mapping, MEDIUM confidence, no official table) is also only answerable empirically by this same live run -- this plan closes that loop too, per RESEARCH.md's own recommendation ("the one live run in this phase is itself the empirical answer").
Output: `benchmarks/results/smoke-convert.jsonl` (one committed row from the real call), `benchmarks/README.md`, a corrected `tests/bench-run.bats` (only if drift is found), and `01-RESEARCH.md` with Open Question 1 marked resolved.
</objective>

<execution_context>
@~/.claude/plugins/cache/cairngo/gsd/4.3.1/workflows/execute-plan.md
@~/.claude/plugins/cache/cairngo/gsd/4.3.1/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verification-core-first-real-run/01-CONTEXT.md
@.planning/phases/01-verification-core-first-real-run/01-RESEARCH.md
@tests/README.md
@benchmarks/scripts/bench-run.py
@benchmarks/scripts/bench-run.sh
@benchmarks/tasks/smoke-convert/task.json
@tests/bench-run.bats
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute the single live claude -p run, capture its data, and reconcile the bats stub against it</name>
  <files>benchmarks/results/smoke-convert.jsonl, tests/bench-run.bats</files>
  <read_first>
    .planning/phases/01-verification-core-first-real-run/01-CONTEXT.md (`<specifics>`: "The single live run doubles as the phase's proof artifact")
    .planning/phases/01-verification-core-first-real-run/01-RESEARCH.md ("Pattern 3: The one live run -- triggering mechanism", lines ~254-266; "Flags for the Single Live Validation Run", lines ~335-356; "Common Pitfalls" #2 "Stub JSON drifting from the real schema", lines ~367-372; "Open Questions" #1, lines ~508-518)
    benchmarks/scripts/bench-run.py (Plan 02's output -- confirm its exact `--task`/`--out` CLI, its EXIT_OK/EXIT_USAGE contract, and that its row always includes `is_error` before invoking)
    tests/bench-run.bats (Plan 02's output -- the exact canned JSON payloads currently used by `make_claude_stub`, to diff against the real response)
  </read_first>
  <action>
    Confirm the real `claude` CLI resolves: `command -v claude`. If it does not resolve, STOP and report this to the user -- do not fabricate a run or substitute a different mechanism.
    Run bench-run.py exactly once, with `CAIRN_BENCH_CLAUDE_BIN` deliberately left UNSET (no env var override at all) so `resolve_claude_bin()` falls back to the real `claude` on `$PATH`: `python3 benchmarks/scripts/bench-run.py --task benchmarks/tasks/smoke-convert --out benchmarks/results/smoke-convert.jsonl`. This is the ONE real API call for the entire phase -- do not repeat it, do not add a second task/task-variant call, and do not re-run it "to be sure."
    Read the single resulting JSONL line and confirm every field HARN-02 requires is present and non-null: `total_cost_usd`, `usage.input_tokens`, `usage.output_tokens`, `usage.cache_creation_input_tokens`, `usage.cache_read_input_tokens`, `duration_ms`, `duration_api_ms`, `num_turns`, `is_error`, plus the harness-added `task_id`, `wall_clock_ms`, `verify_passed`. Also record the actual shell exit code of the `python3 benchmarks/scripts/bench-run.py ...` invocation itself alongside the row's `is_error` value -- this pairing is what Task 2 uses to resolve RESEARCH.md's Open Question 1 (note: `bench-run.py` always `sys.exit(EXIT_OK)`, so this is really about correlating the *inner* `claude` subprocess's own returncode, which you can also capture by re-inspecting the row's `subtype`/`is_error` fields together with whatever the live `claude` process reported; if the harness itself doesn't expose the raw claude returncode in the row, note the correlation qualitatively from the subtype/is_error fields alone -- do not add new instrumentation to bench-run.py to chase this, that would be scope creep beyond HARN-02).
    If the row is a genuine parse failure (`is_error: true` WITH a `parse_error` key, meaning the CLI produced no usable JSON on stdout at all -- distinct from a valid `error_max_turns`/`error_during_execution` result, which DOES carry `usage`/`total_cost_usd` and is an acceptable, real data point): STOP, report the exact captured stdout/stderr to the user, and do not silently commit an unusable row -- this would fail to validate the schema HARN-02 exists to prove. A valid result (success OR a documented error subtype with usage/cost populated) is the expected, acceptable outcome either way; task completion is NOT gated on the agent actually solving the fixture.
    Diff the real captured row's field names/nesting against the canned JSON payloads inside `tests/bench-run.bats`'s `make_claude_stub` call sites (per RESEARCH.md Pitfall 2): every key present in the real response (`type`, `subtype`, `duration_ms`, `duration_api_ms`, `is_error`, `num_turns`, `session_id`, `total_cost_usd`, `usage.{input_tokens,output_tokens,cache_creation_input_tokens,cache_read_input_tokens}`, and `result` when subtype is `success`) must exist with the identical name and nesting in the stub bodies. If any field is missing, misnamed, or differently nested in the stub, edit `tests/bench-run.bats` to correct it and re-run `bats tests/bench-run.bats` to confirm it still passes after the fix. If no drift is found, leave `tests/bench-run.bats` untouched and note "no drift found" for the SUMMARY.
    Note the actually-observed relationship between the real `claude` process's exit behavior and the row's `is_error` value (RESEARCH.md's Open Question 1) for use in Task 2's documentation and RESEARCH.md update.
  </action>
  <acceptance_criteria>
    - `test -f benchmarks/results/smoke-convert.jsonl`
    - `wc -l benchmarks/results/smoke-convert.jsonl` equals 1
    - `jq -e '.total_cost_usd != null' benchmarks/results/smoke-convert.jsonl` exits 0
    - `jq -e '.usage.input_tokens != null and .usage.output_tokens != null and .usage.cache_creation_input_tokens != null and .usage.cache_read_input_tokens != null' benchmarks/results/smoke-convert.jsonl` exits 0
    - `jq -e '.duration_ms != null and .duration_api_ms != null and .num_turns != null and .is_error != null' benchmarks/results/smoke-convert.jsonl` exits 0
    - `jq -e '.task_id == "smoke-convert" and .wall_clock_ms != null and (.verify_passed == true or .verify_passed == false)' benchmarks/results/smoke-convert.jsonl` exits 0
    - `jq -e 'has("parse_error")' benchmarks/results/smoke-convert.jsonl` exits 1 (no parse failure -- a genuine, schema-conformant response was captured)
    - `bats tests/bench-run.bats` still exits 0 after any stub reconciliation edit (or is untouched if no drift was found)
  </acceptance_criteria>
  <verify>
    <automated>jq -e '.total_cost_usd != null and .usage.input_tokens != null and .is_error != null' benchmarks/results/smoke-convert.jsonl && bats tests/bench-run.bats</automated>
  </verify>
  <done>benchmarks/results/smoke-convert.jsonl contains exactly one row, captured from a genuine live claude -p call, with every HARN-02-required field present and non-null; the bats stub schema has been confirmed (or corrected) to match the real live response's field shape; the observed exit-code/is_error relationship is recorded for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Document methodology, cost, and observed schema/exit-code findings in benchmarks/README.md; mark RESEARCH.md's Open Question 1 resolved</name>
  <files>benchmarks/README.md, .planning/phases/01-verification-core-first-real-run/01-RESEARCH.md</files>
  <read_first>
    tests/README.md (style analog -- terse, prescriptive doc, read in full for tone/structure)
    .planning/phases/01-verification-core-first-real-run/01-RESEARCH.md ("Recommended Project Structure", lines ~152-171, describing benchmarks/README.md's intended content; "Verified JSON Result Schema" cost-estimate caveat, lines ~325-329; "Open Questions" #1, lines ~508-518 -- the exact heading/wording to locate and mark resolved)
    benchmarks/results/smoke-convert.jsonl (Task 1's actual output -- pull the REAL total_cost_usd and other observed values from this file, do not estimate or round from memory)
  </read_first>
  <action>
    Write benchmarks/README.md with these sections, in this order: (1) what this directory is and Phase 1's explicit scope boundary -- verify.sh objectivity (HARN-01) plus one live schema-validating call (HARN-02/03); state explicitly that baselines, isolation (HOME override, worktrees), repetition, and aggregation are Phase 2/3 scope and are NOT built yet; (2) how to run the zero-cost stub suite: `bats tests/bench-verify.bats tests/bench-run.bats`; (3) how the `CAIRN_BENCH_CLAUDE_BIN` env-var seam works and why it silently falls back to the real `claude` binary when unset (this is precisely why the live run in this phase needed no special flag); (4) the exact command used for the one live run in this phase (`python3 benchmarks/scripts/bench-run.py --task benchmarks/tasks/smoke-convert --out benchmarks/results/smoke-convert.jsonl`) and its actually-observed cost, read directly out of `benchmarks/results/smoke-convert.jsonl`'s `total_cost_usd` field written in Task 1 -- quote the real number; (5) an explicit caveat, verbatim in spirit: `total_cost_usd` is Anthropic's own client-side estimate, not authoritative billing data (per the verified cost-tracking docs) -- repeat this caveat wherever cost is reported anywhere in this repo going forward; (6) the observed relationship between the real call's process exit behavior and the row's `is_error` field from this phase's one real run, written down for future phases to rely on with higher confidence than RESEARCH.md's prior MEDIUM-confidence assumption; (7) one line noting whether Task 1's stub-vs-real schema diff found any drift, and if so, what was corrected.
    Edit `.planning/phases/01-verification-core-first-real-run/01-RESEARCH.md`'s "Open Questions" section, item 1 (the exit-code <-> `is_error` mapping question): append `(RESOLVED)` to its heading/title line, and directly under its existing "Recommendation" line add a new line starting with exactly `RESOLVED:` followed by the concrete, actually-observed mapping from Task 1's live run (e.g. what the real `claude` process's exit behavior was and how it corresponded to the row's `is_error`/`subtype` fields) -- this converts RESEARCH.md's own MEDIUM-confidence assumption (A1 in the Assumptions Log) into a HIGH-confidence, empirically-recorded fact for future phases, closing the loop RESEARCH.md itself asked for.
  </action>
  <acceptance_criteria>
    - `test -f benchmarks/README.md`
    - `grep -qi 'total_cost_usd' benchmarks/README.md`
    - `grep -qi 'client-side estimate' benchmarks/README.md`
    - `grep -qi 'CAIRN_BENCH_CLAUDE_BIN' benchmarks/README.md`
    - `grep -qi 'bats tests/bench-verify.bats tests/bench-run.bats' benchmarks/README.md`
    - The real cost figure quoted in benchmarks/README.md matches `jq -r '.total_cost_usd' benchmarks/results/smoke-convert.jsonl` exactly (no rounding/estimation)
    - `grep -qF '(RESOLVED)' .planning/phases/01-verification-core-first-real-run/01-RESEARCH.md`
    - `grep -qF 'RESOLVED:' .planning/phases/01-verification-core-first-real-run/01-RESEARCH.md`
  </acceptance_criteria>
  <verify>
    <automated>test -f benchmarks/README.md && grep -qi 'total_cost_usd' benchmarks/README.md && grep -qi 'CAIRN_BENCH_CLAUDE_BIN' benchmarks/README.md && grep -qF '(RESOLVED)' .planning/phases/01-verification-core-first-real-run/01-RESEARCH.md && grep -qF 'RESOLVED:' .planning/phases/01-verification-core-first-real-run/01-RESEARCH.md</automated>
  </verify>
  <done>benchmarks/README.md exists, documents the real observed cost pulled from the committed JSONL row, includes the client-side-estimate caveat, and records the observed exit-code/is_error finding for future phases; RESEARCH.md's Open Question 1 is marked (RESOLVED) with an inline RESOLVED: note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| bench-run.py <-> real claude -p subprocess, agent-writable workdir | The one real live run lets an actual model edit files inside a disposable mktemp workdir |
| benchmarks/README.md <-> public repo | Documents a real dollar cost and CLI invocation publicly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-01-08 | Elevation of Privilege | The live `claude -p` call could attempt to act outside its intended workdir | mitigate | `--bare` (no ambient hooks/MCP/CLAUDE.md), no `--add-dir` passed, `--permission-mode acceptEdits` (auto-approves file edits/writes only, still gates arbitrary Bash/network) rather than `--dangerously-skip-permissions`/`bypassPermissions`; run from a disposable `mktemp` copy, never the real repo tree |
| T-01-09 | Denial of Service / Repudiation | Uncontrolled or repeated live calls silently reburning budget | mitigate | Task 1's action explicitly caps this phase to exactly one real call; `--max-turns 5` bounds runaway turns; `--no-session-persistence` leaves no resumable session behind |
| T-01-10 | Information Disclosure | The committed JSONL row or README leaking credentials | mitigate | bench-run.py's row schema (built in Plan 02) never includes raw env/credentials; README documents only cost and CLI flags, no secrets |
</threat_model>

<verification>
Before declaring this plan (and the phase) complete:
- [ ] `benchmarks/results/smoke-convert.jsonl` contains exactly one row from a genuine live call, all HARN-02 fields present
- [ ] `tests/bench-run.bats`'s stub schema has been confirmed (or corrected) to match the real live response's field shape, and still passes
- [ ] `benchmarks/README.md` documents the real observed cost and the client-side-estimate caveat
- [ ] RESEARCH.md's Open Question 1 is marked `(RESOLVED)` with an inline `RESOLVED:` note
- [ ] No second live `claude -p` call was made anywhere in this plan or the phase
</verification>

<success_criteria>
HARN-02 is fully proven end-to-end: bench-run.py executes one real, non-stubbed `claude -p --output-format json` call and correctly captures `total_cost_usd`, full `usage`, `duration_ms`, `duration_api_ms`, `num_turns`, and `is_error` into a committed JSONL row (ROADMAP Phase 1 Success Criterion 3). The phase's two riskiest primitives -- objective verification (Plan 01) and a real measurement call (this plan) -- are both proven before any statistics or presentation layer is built on top of them. RESEARCH.md's exit-code/is_error open question is closed with an empirically-observed answer.
</success_criteria>

<output>
Create `.planning/phases/01-verification-core-first-real-run/01-03-SUMMARY.md` when done
</output>
</output>
