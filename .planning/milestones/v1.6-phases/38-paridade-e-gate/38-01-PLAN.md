---
phase: 38-paridade-e-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/cairn-parity.bats
autonomous: true
requirements: [PAR-01]
beads:
  PAR-01: CairnGo-mu0p
must_haves:
  truths:
    - "todo handler que `cairn-gsd.py --list-implemented` enumera tem cobertura executável: um cenário no manifesto golden ou um teste bats que o invoca direto"
    - "a mesma comparação, aplicada a um universo fixture com um handler forjado sem cobertura, REPROVA — o guard morde"
    - "a comparação é uma função só, usada pelos dois testes; um guard cuja prova de mordida roda outro código não provou nada"
  artifacts:
    - tests/cairn-parity.bats
  key_links:
    - "handlers → `--list-implemented` (superfície de teste declarada no docstring do dispatcher, TRIV-04)"
    - "cobertura → scenarios.json `.scenarios[].verb` ∪ invocação direta em tests/*.bats"
---

<objective>
PAR-01: travar por asserção a cobertura por verbo que a medição M1 do CONTEXT já
encontrou de pé (87/87), de forma que ela não possa regredir em silêncio.

Purpose: um verbo novo sem cobertura tem que reprovar a suíte no dia em que nasce.
Output: tests/cairn-parity.bats com a função de comparação e os dois testes.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: a funcao de cobertura e os dois testes</name>
  <files>tests/cairn-parity.bats</files>
  <behavior>
  `uncovered_handlers <handlers-file> <scenarios-json> <bats-dir>` imprime, uma por
  linha, cada linha de `<handlers-file>` que não aparece nem como `.scenarios[].verb`
  do manifesto, nem como token invocado em nenhum `.bats` do diretório. Silêncio =
  cobertura completa.
  </behavior>
  <implementation>
  Dois testes sobre ela:
   1. "cobertura: todo verbo implementado tem cenario golden ou teste bats direto" —
      chama a função com os insumos REAIS (`$GSD --list-implemented`, o
      scenarios.json da casa, `tests/`) e exige saída vazia.
   2. "controle negativo: verbo implementado sem cenario nem bats VAZA pela mesma
      funcao" — chama a MESMA função com um handlers-file fixture que carrega uma
      linha forjada, e exige que a saída seja exatamente essa linha.
  Nome de `@test` sem acento (D-06).
  </implementation>
  <verify>bash cairn/scripts/cairn-test.sh --jobs 8 tests/cairn-parity.bats</verify>
  <done>Os dois testes passam, e o negativo falha quando a função é neutralizada.</done>
</task>

</tasks>

<verification>
- [ ] `cairn-test.sh tests/cairn-parity.bats` sai 0
- [ ] neutralizar a função (fazer `uncovered_handlers` imprimir nada) deixa o teste 2 VERMELHO
</verification>
