---
phase: 38-paridade-e-gate
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - tests/parity-scan.py
  - tests/cairn-parity.bats
  - tests/cairn-gsd.bats
  - cairn/scripts/cairn-gsd.py
  - cairn/scripts/cairn-gsd-state.py
  - cairn/gsd/contracts/contracts.json
  - cairn/gsd/contracts/worktree.json
  - cairn/gsd/contracts/misc.json
autonomous: true
requirements: [PAR-02]
beads:
  PAR-02: CairnGo-j6ap
must_haves:
  truths:
    - "toda chamada `gsd_run` do corpo vendorizado resolve num spelling que o dispatcher roteia — as 2 rotas mortas medidas em M2 deixam de existir"
    - "as 2 rotas mortas viram verbo servido, não chamada apagada: worktree.set-baseref grava worktree.baseRef=head no-clobber, requirements.revert-phase desmarca o checkbox que mark-complete marca"
    - "o scanner é uma ferramenta com seam de corpus: apontado a um corpus fixture com uma chamada forjada, ele acusa — senão o verde do corpus real não vale nada"
    - "as 2 entradas novas do universo declaram `scope: references`, e o teste de cobertura cruzada compara o inventário vivo MAIS o escopo declarado (D-03) — a cegueira do inventário vira dado escrito"
  artifacts:
    - tests/parity-scan.py
    - cairn/scripts/cairn-gsd-state.py
  key_links:
    - "scan → tabela spelling→verbo reconstruída de contracts.json + spellings[] das famílias (a MESMA fonte que build_routes usa)"
    - "worktree.set-baseref → semântica de cmdWorktreeSetBaseRef do clone pinado v1.10.0"
    - "requirements.revert-phase → inverso exato de handle_requirements_mark_complete"
---

<objective>
PAR-02, metade executável: nenhum `gsd_run` do runtime vendorizado morre em exit 2
silenciado por `|| true`.

Purpose: um workflow que chama verbo inexistente e engole o erro é a definição de
superfície que responde sem ter medido.
Output: o scanner, o guard, e os 2 verbos servidos.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — o scanner acusa as 2 rotas mortas</name>
  <files>tests/parity-scan.py, tests/cairn-parity.bats</files>
  <behavior>
  `tests/parity-scan.py --contracts <dir> --corpus <dir>` imprime `path:line<TAB>token`
  para cada `gsd_run` cujo prefixo mais longo de tokens não bate nenhum spelling.
  Normaliza token de fim de `$( )` e de prosa (`)`, crase, vírgula, ponto) antes de
  comparar — a varredura ingênua acusa `query verification status`, que resolve.
  </behavior>
  <implementation>
  Teste 1: contra `cairn/gsd/` real, exige saída vazia — VERMELHO agora, com as duas
  linhas de M2. Teste 2 (controle): contra `tests/fixtures/parity-corpus/`, um .md com
  uma chamada forjada, exige exatamente aquela linha.
  </implementation>
  <verify>bash cairn/scripts/cairn-test.sh --jobs 8 tests/cairn-parity.bats</verify>
  <done>Teste 1 vermelho nomeando os dois arquivos; teste 2 verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — os 2 verbos passam a ser servidos</name>
  <files>cairn/scripts/cairn-gsd-state.py, cairn/scripts/cairn-gsd.py, cairn/gsd/contracts/*.json, tests/cairn-gsd.bats</files>
  <behavior>
  `worktree.set-baseref`: grava `worktree.baseRef = "head"` em
  `<cwd>/.claude/settings.local.json`, no-clobber — já `head` devolve
  `skipped:"already-head"`, outro valor explícito devolve `skipped:"explicit-other"` e
  NÃO sobrescreve, JSON malformado é erro nomeado e não clobbera. Envelope
  `{changed, skipped, previous, baseRef, file}`.
  `requirements.revert-phase <IDS>`: inverso de mark-complete — `[x]` → `[ ]` só na
  linha do ID, tabela de traceability intocada.
  </behavior>
  <implementation>
  Handlers no irmão, spellings nas famílias (`worktree.json`, `misc.json`), índice em
  `contracts.json` com `scope: "references"`. O teste `cobertura cruzada` passa a somar
  o escopo declarado ao inventário vivo. Testes de comportamento em cairn-gsd.bats
  (cobrem PAR-01 para os verbos novos por bats direto).
  </implementation>
  <verify>bash cairn/scripts/cairn-test.sh --jobs 8 tests/cairn-parity.bats tests/cairn-gsd.bats</verify>
  <done>Scan vazio; universo == handlers; testes de comportamento verdes.</done>
</task>

</tasks>

<verification>
- [ ] `tests/cairn-parity.bats` e `tests/cairn-gsd.bats` saem 0
- [ ] `python3 tests/parity-scan.py --contracts cairn/gsd/contracts --corpus cairn/gsd` não imprime nada
</verification>
