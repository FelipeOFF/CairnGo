---
phase: 38-paridade-e-gate
plan: 03
type: execute
wave: 3
depends_on: [38-02]
files_modified:
  - cairn/gsd/parity.json
  - cairn/commands/new.md
  - cairn/commands/init.md
  - cairn/commands/ship.md
  - cairn/commands/milestone.md
  - cairn/commands/progress.md
  - cairn/commands/status.md
  - cairn/commands/config.md
  - cairn/commands/migrate.md
  - cairn/commands/help.md
  - tests/cairn-parity.bats
autonomous: true
requirements: [PAR-02, PAR-03, PAR-04]
beads:
  PAR-02: CairnGo-j6ap
  PAR-03: CairnGo-i2i3
  PAR-04: CairnGo-huyn
must_haves:
  truths:
    - "nenhum comando do cairn manda RODAR um verbo /gsd: que uma instalação limpa não resolve — os 10 passos medidos em M3 deixam de ser passo"
    - "toda menção /gsd: que sobrevive está registrada em cairn/gsd/parity.json com disposição e motivo, e o guard trava nos DOIS sentidos: menção sem entrada reprova, entrada sem menção reprova"
    - "um repositório de teste construído do zero, sem gsd-core instalado, sai 0 no cairn-doctor com zero ✗, e 0 no cairn-gate"
    - "o gate plantado com uma issue aberta numa fase completa sai 6 — um gate que nunca fica vermelho não é gate"
    - "os quatro comandos do ciclo apontam só caminhos que existem dentro do plugin, e nenhum deles nomeia /gsd:"
  artifacts:
    - cairn/gsd/parity.json
    - tests/cairn-parity.bats
  key_links:
    - "doctor limpo → seam CAIRN_INSTALLED_PLUGINS apontando um installed_plugins.json sem linhagem GSD externa"
    - "registro → cairn/gsd/parity.json {verb, disposition, reason, mentions[]}"
---

<objective>
Fechar PAR-02 (metade da prosa), PAR-03 e PAR-04: o repositório novo, o doctor limpo,
o gate verde — e a decisão D-01 aplicada aos 10 passos mortos.

Purpose: é o fecho do milestone; o que sobrar aqui vira dívida de v1.7.
Output: parity.json, os comandos que param de mentir, e a bateria do repositório novo.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — o registro de paridade dos comandos, nos dois sentidos</name>
  <files>tests/cairn-parity.bats, cairn/gsd/parity.json</files>
  <behavior>
  Guard: toda ocorrência `/gsd:<verbo>` sob `cairn/commands/` é (a) um verbo vendorizado
  em `cairn/gsd/commands/gsd/`, ou (b) uma entrada de `cairn/gsd/parity.json` cuja
  `mentions[]` cita aquele arquivo. Entrada do registro que não aparece em comando
  nenhum também reprova (allowlist obsoleta).
  Segundo guard: nenhuma ocorrência classificada como PASSO — linha que manda rodar —
  fora do passthrough declarado `gsd.md`.
  </behavior>
  <implementation>
  Escrever os dois testes ANTES de tocar comando ou registro: vermelhos pelos 10 passos
  e pelo `parity.json` ausente. Depois: reescrever os 10 passos e escrever o registro.
  </implementation>
  <verify>bash cairn/scripts/cairn-test.sh --jobs 8 tests/cairn-parity.bats</verify>
  <done>Os dois guards verdes com os comandos reescritos e o registro escrito.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: o repositorio novo — ciclo, doctor e gate</name>
  <files>tests/cairn-parity.bats</files>
  <behavior>
  Um repositório construído do zero (`make_tmp_repo` + `make_gsd_fixture` +
  `make_bd_fixture`), com `CAIRN_INSTALLED_PLUGINS` apontando um installed_plugins.json
  SEM linhagem GSD externa:
   1. os 4 comandos do ciclo apontam arquivos existentes e não nomeiam `/gsd:`;
   2. os verbos que o ciclo executa respondem ali (state.load, init.execute-phase,
      phase.list-plans, verify.plan-structure, commit);
   3. `cairn-doctor.sh` sai 0 e não imprime nenhum `✗`;
   4. `cairn-gate.sh` sai 0;
   5. controle: issue aberta plantada numa fase COMPLETA faz o gate sair 6.
  </behavior>
  <implementation>
  Fixture de plugins em `tests/fixtures/parity/installed_plugins.json`. O controle (5) é
  o oráculo do verde de (4).
  </implementation>
  <verify>bash cairn/scripts/cairn-test.sh --jobs 8 tests/cairn-parity.bats</verify>
  <done>Os cinco verdes, com (5) provando que o gate morde.</done>
</task>

</tasks>

<verification>
- [ ] `tests/cairn-parity.bats` sai 0
- [ ] `bash cairn/scripts/cairn-gate.sh` no repo real sai 0
- [ ] `grep -rn "/gsd:" cairn/commands/` só devolve prosa registrada e o passthrough
</verification>
