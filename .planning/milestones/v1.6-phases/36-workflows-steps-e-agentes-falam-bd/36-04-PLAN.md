---
phase: 36-workflows-steps-e-agentes-falam-bd
plan: 04
type: execute
wave: 4
depends_on: [36-03]
files_modified:
  - cairn/gsd/gsd-core/workflows/discuss-phase.md
  - cairn/gsd/gsd-core/workflows/discuss-phase/modes/advisor.md
  - cairn/gsd/gsd-core/workflows/discuss-phase/modes/chain.md
  - cairn/gsd/gsd-core/workflows/quick.md
  - cairn/gsd/gsd-core/workflows/quick/steps/research-phase.md
  - cairn/gsd/gsd-core/workflows/quick/steps/worktree-pre-dispatch-commit.md
  - tests/cairn-prompt-state.bats
  - cairn/gsd-adaptations.json
autonomous: true
requirements: [ADAPT-03]
beads:
  ADAPT-03: CairnGo-z782
must_haves:
  truths:
    - "a unidade de edição é a LINHA, nunca o bloco: discuss-phase.md:238-240 tem três leituras consecutivas no mesmo fence e só a terceira é fato — uma substituição por bloco converteria as três e transformaria leitura legítima de documento em regressão"
    - "quick.md:150 deixa de derivar o caminho do estado por dirname — a derivação existia porque o bundle de init não emite o campo (#2376 anotado no próprio arquivo em :148), e nenhum grep por nome de verbo acha esse sítio"
    - "a subclasse 'caminho de estado injetado em prompt de subagente' é tratada como CLASSE e não caso a caso: a leitura acontece FORA do workflow, então cobertura medida dentro do workflow declara verde enquanto o subagente segue lendo markdown"
    - "nenhum sítio adaptado passa a escrever em ROADMAP.md ou REQUIREMENTS.md por outro caminho — o bookkeep do cairn é o dono e converter prosa sobre roadmap em escrita direta é regressão"
    - "o commit de artefatos da sessão de discuss deixa de listar o arquivo de estado entre os arquivos commitados: com o bd dono do fato, commitar o markdown de estado é escrever estado em markdown por outra porta"
    - "leitura de documento em discuss-phase permanece: PROJECT.md, REQUIREMENTS.md, os CONTEXT.md anteriores, o índice de decisões, os manifestos de spike/sketch e os mapas de codebase não viram verbo"
  artifacts:
    - cairn/gsd/gsd-core/workflows/discuss-phase.md
    - cairn/gsd/gsd-core/workflows/quick.md
    - tests/cairn-prompt-state.bats
  key_links:
    - "discuss-phase.md:477 (prosa) ⟶ :480 (verbo state.record-session já convertido): o par antes/depois no mesmo bloco é o molde exato da conversão"
    - "quick.md:313 e :431 → prompt do subagente → o subagente carrega seu próprio preâmbulo (onda zero) e pede o fato ao binário: a leitura sai do markdown nas DUAS pontas"
    - "tabela de adaptados do oráculo ← discuss-phase.md, quick.md e seus fragments; isenções remanescentes com contagem conferida nos dois sentidos"
  prohibitions:
    - statement: "Nenhuma task substitui por bloco onde a semântica é por linha — o bloco-armadilha de discuss-phase.md:238-240 é o caso nominal"
    - statement: "Nenhuma task converte leitura de documento (PROJECT/REQUIREMENTS/CONTEXT/DECISIONS-INDEX/spikes/sketches/codebase) em chamada de verbo"
    - statement: "Nenhuma task toca plan-phase, verify-work, autonomous, execute-phase ou agentes — planos 05, 06 e 07"
    - statement: "Nenhuma task copia o gate porcelain-vazio; o gate de árvore desta fase é o oráculo dois-sentidos contra o cache entregue pelo 36-01"
---

<objective>
ADAPT-03, primeira metade: `discuss-phase` (8 chamadas, 10 menções a `.planning/`) e `quick`
(17 chamadas, 5 menções) passam pelo critério da fase. São os dois workflows onde as duas
armadilhas de medição do research aparecem inteiras — o bloco de três leituras consecutivas
com semânticas diferentes, e o caminho de estado que é derivado por `dirname` e injetado em
prompt de subagente, invisível a qualquer grep por nome de verbo.

Purpose: converter os sítios de fato dos dois workflows sem tocar em um único sítio de
documento, e fixar como CLASSE o tratamento do caminho injetado em prompt de subagente,
porque plan-phase, verify-work e os agentes repetem exatamente esse padrão nos planos
seguintes.
Output: discuss-phase e quick adaptados com seus fragments, a classe de injeção resolvida e
escrita no oráculo, tabela de adaptados crescida.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-CONTEXT.md
@.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-PATTERNS.md
@.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-03-SUMMARY.md
@cairn/gsd/contracts/estado.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: discuss-phase — o bloco-armadilha desmontado linha a linha</name>
  <files>cairn/gsd/gsd-core/workflows/discuss-phase.md, cairn/gsd/gsd-core/workflows/discuss-phase/modes/advisor.md, cairn/gsd/gsd-core/workflows/discuss-phase/modes/chain.md, cairn/gsd-adaptations.json</files>
  <read_first>
    - cairn/gsd/gsd-core/workflows/discuss-phase.md linhas 230-260 (o bloco de leituras, os manifestos de spike/sketch e o find de CONTEXT anteriores), 285-295 (mapas de codebase), 420-430 (o caminho do artefato criado), 470-515 (a prosa de atualização de estado, o verbo já convertido logo abaixo, o commit e os dois itens de checklist)
    - 36-PATTERNS.md §5a e §5d — a classificação linha a linha das cinco leituras do bloco; §5c — o par prosa/verbo no mesmo bloco, que é o molde de conversão
    - cairn/gsd/contracts/estado.json — os verbos de estado disponíveis (state.load, state.record-session, state.update, state.begin-phase e irmãos) e o que cada um devolve
    - 36-03-SUMMARY.md — as três regex de família como ficaram, para não converter o que o oráculo não pede
  </read_first>
  <behavior>
    - das três leituras consecutivas do bloco, as duas de documento permanecem intactas e só a de estado vira consulta ao binário
    - o find de CONTEXT.md anteriores, o índice de decisões, os manifestos de spike e sketch e o ls dos mapas de codebase permanecem — são documento
    - a prosa imperativa de atualização de estado some, ficando a chamada ao verbo que já existia logo abaixo dela
    - o commit da sessão deixa de listar o arquivo de estado entre os arquivos; o que a sessão produziu de documento continua sendo commitado
    - os dois itens de checklist do fim do arquivo passam a descrever o fato registrado no bd, nomeando o verbo, em vez do arquivo atualizado
    - os dois modes (advisor, chain) são medidos pelas três famílias; conversões só onde houver fato
    - o oráculo lista discuss-phase.md e os dois modes como adaptados; qualquer isenção vem com contagem e motivo
  </behavior>
  <action>
    Editar `discuss-phase.md` sítio a sítio, com a LINHA como unidade. No bloco de leituras,
    trocar apenas a linha do arquivo de estado por uma consulta ao verbo de carga de estado,
    mantendo as duas leituras de documento vizinhas byte a byte — é o caso nominal da
    armadilha registrada em PATTERNS §5d, e converter as três é a regressão mais fácil de
    cometer nesta fase.

    Na seção de encerramento: apagar a linha de prosa imperativa que manda atualizar o
    arquivo de estado, deixando a chamada ao verbo de registro de sessão que já está logo
    abaixo — o par antes/depois já convive no mesmo bloco e a conversão é remover a prosa,
    não escrever código novo.

    No commit da sessão: retirar o arquivo de estado da lista de arquivos. Com o bd dono do
    fato, o markdown de estado não é mais o que muda quando a sessão termina, e commitá-lo é
    escrita de estado em markdown por outra porta. O artefato de documento que a sessão
    produziu continua na lista. Não acrescentar ROADMAP.md nem REQUIREMENTS.md à lista sob
    nenhuma hipótese: o bookkeep do cairn é o dono desses dois.

    Nos dois itens de checklist, trocar a descrição do arquivo atualizado pela do fato
    registrado, nomeando o verbo — o checklist é lido por um modelo e é ele que decide se o
    passo foi cumprido; deixá-lo apontando o arquivo mantém o comportamento antigo mesmo com
    o comando trocado.

    Medir `modes/advisor.md` e `modes/chain.md` pelas três famílias antes de tocar. Converter
    só o que for fato; publicar o número de cada um, inclusive zero.

    Acrescentar os três caminhos à tabela de adaptados do oráculo e somar a onda 4 ao
    `waves[]` das entradas correspondentes em `cairn/gsd-adaptations.json`.
  </action>
  <verify>
    <automated>R="$PWD" && F=cairn/gsd/gsd-core/workflows/discuss-phase.md && test "$(grep -cE '(cat|grep|head|tail|wc|test -f|\[ -f)[^|]*\.planning/STATE\.md' "$F")" -eq 0 && test "$(grep -cE '(Update|Read|Write|Check) STATE\.md' "$F")" -eq 0 && grep -q 'PROJECT\.md' "$F" && grep -q 'REQUIREMENTS\.md' "$F" && grep -q 'DECISIONS-INDEX' "$F" && test "$(grep -c 'state.record-session' "$F")" -ge 1 && test "$(grep -E 'gsd_run query commit' "$F" | grep -c 'STATE\.md')" -eq 0 && bats tests/cairn-prompt-state.bats && bats tests/cairn-vendoring.bats</automated>
  </verify>
  <done>Só a leitura de estado do bloco virou verbo; as leituras de documento vizinhas estão intactas; a prosa imperativa saiu e o verbo que já existia ficou; o commit da sessão não carrega mais o arquivo de estado nem ganhou roadmap/requirements; os modes estão medidos e publicados.</done>
</task>

<task type="auto">
  <name>Task 2: quick — o caminho derivado por dirname e a tabela de tarefas rápidas</name>
  <files>cairn/gsd/gsd-core/workflows/quick.md, cairn/gsd/gsd-core/workflows/quick/steps/research-phase.md, cairn/gsd/gsd-core/workflows/quick/steps/worktree-pre-dispatch-commit.md, cairn/gsd-adaptations.json</files>
  <read_first>
    - cairn/gsd/gsd-core/workflows/quick.md linhas 145-155 (a derivação por dirname e a nota #2376 que explica por que ela existe), 305-320 e 425-435 (as duas injeções do caminho em prompt de subagente), 470-480 (a regra de não commitar artefatos de docs), 575-600 (a prosa do passo de atualização de estado e a leitura da tabela), 640-665 (o arquivo na lista de docs e o filtro por prefixo), 710-730 (os itens de checklist)
    - 36-PATTERNS.md §5b — as três subclasses do estado por variável, com quick.md:150 nomeado como caso à parte
    - cairn/gsd/contracts/*.json, verbo quick-tasks-append — a falha nomeada que substitui a leitura da tabela
    - 36-03-SUMMARY.md — como fast.md resolveu exatamente o mesmo predicado da tabela de tarefas rápidas
  </read_first>
  <behavior>
    - a derivação do caminho de estado por dirname some; nada no arquivo monta caminho de estado a partir de outro caminho
    - o passo de atualização de estado deixa de mandar ler a tabela para decidir: a chamada ao verbo é incondicional e a falha nomeada dele é o predicado, igual ao que fast.md já faz
    - as duas injeções do caminho em prompt de subagente somem: o prompt passa a instruir o subagente a pedir o fato ao binário, e o subagente já carrega o próprio preâmbulo desde a onda zero
    - a regra de não commitar artefatos de docs e o filtro por prefixo continuam funcionando: são política de commit, não leitura de estado
    - os itens de checklist descrevem o fato registrado, nomeando o verbo
    - os dois fragments são medidos pelas três famílias; a injeção do caminho no fragment de pesquisa recebe o mesmo tratamento das do arquivo raiz
  </behavior>
  <action>
    Remover a derivação do caminho de estado e a nota que a justificava, substituindo-a por
    uma linha curta que diga onde o fato passa a ser pedido — a nota citava o issue upstream
    porque o bundle não emitia o campo; com o bd dono do estado, o campo deixa de ser
    necessário e a dívida some em vez de ser herdada.

    No passo de atualização: apagar a prosa imperativa e a instrução de ler a tabela; deixar
    a chamada ao verbo de append e o tratamento que reproduz a razão da falha. É a mesma
    conversão do fast.md, e fazer diferente aqui criaria duas doutrinas para o mesmo
    predicado.

    Nas duas injeções em prompt de subagente e na do fragment de pesquisa: trocar a linha que
    entrega o caminho por uma que manda o subagente carregar o estado pelo binário. Registrar
    no SUMMARY que esta é a subclasse que a métrica dentro do workflow não enxerga — a
    leitura acontecia no subagente, e converter só o workflow declararia verde com o
    markdown ainda sendo lido do outro lado.

    Deixar intactos: a regra de não commitar artefatos de docs, o filtro por prefixo de
    `.planning/` no staging, e os caminhos do diretório de tarefas rápidas — são política e
    documento, não fato.

    Medir os dois fragments antes de tocar; converter só fato; publicar os números.
    Acrescentar os três caminhos à tabela de adaptados e somar a onda 4 em
    `cairn/gsd-adaptations.json` — atenção a `quick/steps/research-phase.md`, que NÃO carrega
    bloco de preâmbulo e portanto não foi registrado na onda zero: ele ganha entrada NOVA, na
    mesma task que o edita, pela regra do registro fixada no plano 01.
  </action>
  <verify>
    <automated>R="$PWD" && F=cairn/gsd/gsd-core/workflows/quick.md && test "$(grep -cE 'STATE_PATH|\{state_path\}|STATE_FILE|state_raw' "$F")" -eq 0 && test "$(grep -cE '(Update|Read|Write|Check) STATE\.md' "$F")" -eq 0 && test "$(grep -cE '(cat|grep|head|tail|wc|test -f|\[ -f)[^|]*\.planning/STATE\.md' "$F")" -eq 0 && grep -q 'quick-tasks-append' "$F" && test "$(grep -cE 'STATE_PATH|\{state_path\}' cairn/gsd/gsd-core/workflows/quick/steps/research-phase.md)" -eq 0 && bats tests/cairn-prompt-state.bats && bats tests/cairn-vendoring.bats</automated>
  </verify>
  <done>quick não deriva mais caminho de estado, não lê a tabela para decidir, e não entrega caminho de estado a subagente nenhum; a política de commit e os caminhos de documento seguem intactos; os dois fragments estão medidos e publicados.</done>
</task>

<task type="auto">
  <name>Task 3: a injeção de caminho em prompt de subagente vira regra do oráculo</name>
  <files>tests/cairn-prompt-state.bats</files>
  <read_first>
    - 36-PATTERNS.md §5b, subclasse do meio — os cinco sítios medidos que injetam o caminho num prompt (dois em quick, um no fragment de pesquisa, um em plan-phase, um em verify-work) e por que a métrica de dentro do workflow não os enxerga
    - 36-03-SUMMARY.md — a forma atual da família B e da tabela de isenções
    - tests/cairn-prompt-state.bats — o laço a estender
  </read_first>
  <behavior>
    - a família B ganha a forma de injeção explicitamente: um item de lista que entrega um caminho de estado como contexto para um subagente casa a família mesmo quando a variável está entre chaves
    - o caso de teste de falso-positivo cresce: entregar o caminho de um DOCUMENTO a um subagente continua legítimo e não casa
    - controle negativo: uma cópia forjada de um arquivo já adaptado, com a linha de injeção reinjetada, é mordida e nomeada
    - os dois sítios que ainda restam nessa forma — em plan-phase e verify-work, alvos do plano 36-05 — aparecem na tabela de isenções com contagem exata, motivo e o plano que as fecha
  </behavior>
  <action>
    Estender a família B do oráculo para cobrir a forma de injeção nas duas grafias medidas
    (com cifrão e chaves, e só entre chaves), e acrescentar ao caso de falso-positivo uma
    linha que entrega caminho de documento a subagente, provando que a extensão não empurra
    conversão de documento.

    Abrir na tabela de isenções as duas linhas dos sítios que sobram, com caminho, padrão,
    contagem medida hoje, motivo ("adaptado pelo plano 36-05") — e é justamente a conferência
    de contagem nos dois sentidos que garante que essas isenções morram quando o 36-05
    passar, em vez de virarem letra permanente.

    Acrescentar o controle negativo da injeção reinjetada, no molde da metade de liveness já
    usada pelas outras famílias.
  </action>
  <verify>
    <automated>R="$PWD" && bats tests/cairn-prompt-state.bats && bats tests/ && test "$(grep -rcE 'STATE_PATH|\{state_path\}' cairn/gsd/gsd-core/workflows/quick.md cairn/gsd/gsd-core/workflows/discuss-phase.md | awk -F: '{s+=$2} END {print s+0}')" -eq 0</automated>
  </verify>
  <done>A subclasse invisível está escrita como regra de máquina, com falso-positivo e controle negativo próprios; os dois sítios remanescentes estão declarados com contagem e com o plano que os fecha, e a conferência dois-sentidos garante que a isenção não sobreviva ao fecho.</done>
</task>

</tasks>

<verification>
- `bats tests/cairn-prompt-state.bats` e `bats tests/` verdes
- Oráculo de bytes dois-sentidos verde: caminhos divergentes do cache == registro de adaptações
- Zero residuo das três famílias em discuss-phase.md, quick.md e seus fragments, salvo isenções declaradas com contagem e plano de fecho
- Leituras de documento preservadas por inspeção do diff: nenhuma linha de PROJECT/REQUIREMENTS/CONTEXT/DECISIONS-INDEX/spikes/sketches/codebase/quick-dir foi tocada
</verification>

<success_criteria>
- ADAPT-03 metade a fechado: discuss-phase e quick sem leitura nem escrita de `.planning/` como fonte de estado
- A armadilha do bloco de três leituras foi desmontada por linha, com as duas de documento intactas
- A subclasse de injeção em prompt de subagente virou regra de máquina, não observação de SUMMARY
</success_criteria>

<output>
Criar `.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-04-SUMMARY.md`. Registrar:
a classificação linha a linha do bloco de leituras; o que aconteceu com a dívida do campo
não emitido pelo bundle de init; os números por arquivo e por fragment, inclusive zeros; e
a tabela de isenções aberta com o plano de fecho de cada uma.
</output>
