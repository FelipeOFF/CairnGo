---
phase: 36-workflows-steps-e-agentes-falam-bd
plan: 07
type: execute
wave: 7
depends_on: [36-06]
files_modified:
  - cairn/gsd/gsd-core/workflows/execute-phase.md
  - cairn/gsd/gsd-core/workflows/execute-phase/steps/codebase-drift-gate.md
  - cairn/gsd/gsd-core/workflows/execute-phase/steps/executor-isolation-dispatch.md
  - cairn/gsd/gsd-core/workflows/execute-phase/steps/gap-closure-artifacts.md
  - cairn/gsd/gsd-core/workflows/execute-phase/steps/partial-wave.md
  - cairn/gsd/gsd-core/workflows/execute-phase/steps/per-plan-worktree-gate.md
  - cairn/gsd/gsd-core/workflows/execute-phase/steps/post-merge-gate.md
  - cairn/gsd/gsd-core/workflows/execute-phase/steps/regression-gate-run.md
  - cairn/gsd/agents/gsd-executor.md
  - tests/cairn-prompt-state.bats
  - tests/fixtures/gsd-goldens/divergences.json
  - cairn/gsd-adaptations.json
autonomous: true
requirements: [ADAPT-04, ADAPT-02]
beads:
  ADAPT-04: CairnGo-73j4
  ADAPT-02: CairnGo-0yzd
must_haves:
  truths:
    - "execute-phase vem por último, como o roadmap manda, e por isso chega com o critério já fixado, o oráculo já mordendo e os cinco padrões de conversão já exercitados em seis arquivos"
    - "os SEIS sítios de node deste workflow deixam de depender de node: os quatro do arquivo raiz (manifesto de worktree) mais os DOIS do fragment de isolamento de executor (linhas 96 e 176), que o mapa da fase omitiu e nenhum plano anterior citava; com o do plan-phase já resolvido e os dois do verificador decididos por escrito, NODE-SOBREVIVE fica zerada no que é infra do GSD dentro do escopo"
    - "o bloco do escritor único da onda sobrevive à adaptação: com o bd dono do estado, a regra de 'o orquestrador é o único escritor' deixa de ser sobre última-escrita-vence em arquivo e passa a ser sobre quem registra o fato — e a instrução aos agentes de worktree muda junto, senão o agente adaptado escreve o que a orquestração já registrou"
    - "o caminho de estado injetado no prompt do agente executor some das três linhas de contexto: era o último sítio da subclasse invisível no corpus inteiro"
    - "a tabela de adaptados do oráculo FECHA: os 8 workflows raiz, os 15 fragments com preâmbulo e os 16 agentes, com um teste de completude que reprova quando um arquivo do escopo fica de fora"
    - "a lacuna de D-02 é MEDIDA e registrada com número, não descrita: os arquivos de references citados pelos workflows, as chamadas que eles fazem e os sítios de estado que sobram neles vão a divergences.json e ao SUMMARY como alvo da 37/38"
    - "o antes e o depois saem da ferramenta canônica: o antes pelo fecho sobre o cache, o depois pelo seam de corpus local que os testes da própria ferramenta já exercitam — e a razão do seam vai escrita, porque a ferramenta como está sempre resolve o corpus para o cache"
  artifacts:
    - cairn/gsd/gsd-core/workflows/execute-phase.md
    - cairn/gsd/agents/gsd-executor.md
    - tests/cairn-prompt-state.bats
    - tests/fixtures/gsd-goldens/divergences.json
  key_links:
    - "orquestrador → manifesto de worktree (escrita e três leituras) → caminhos das árvores da onda: um formato só, um motor só, sem node"
    - "bloco do escritor único → instrução aos agentes de worktree → agente executor: os três precisam contar a MESMA história sobre quem registra o fato, ou a onda registra duas vezes ou nenhuma"
    - "tabela de adaptados fechada + oráculo de bytes dois-sentidos + registro de adaptações: as três provas que sobrevivem à fase e defendem a árvore adaptada nas fases 37 e 38"
  prohibitions:
    - statement: "Nenhuma task faz o execute-phase escrever em ROADMAP.md ou REQUIREMENTS.md por caminho novo — o bookkeep do cairn é o dono; o que já era commit continua, o que era prosa de escrita vira fato registrado por verbo"
    - statement: "Nenhuma task converte os caminhos de artefato de fase (planos, sumários, verificações, todos, mapas) — são documento"
    - statement: "Nenhuma task re-pina o cache nem regrava a baseline de bytes para 'fazer a suíte passar': a baseline nova foi descartada no mapa por matar a capacidade de provar que nada além do adaptado mudou"
    - statement: "Nenhuma task edita cairn/gsd/MANIFEST.json — os totais dele descrevem o fecho do UPSTREAM, derivado do cache, e seguem verdadeiros"
---

<objective>
ADAPT-04, o arquivo mais caro do corpus, por último e com tudo já pronto: execute-phase tem
48 chamadas ao binário, 14 menções a `.planning/`, quatro sítios de node fora do preâmbulo,
o bloco do escritor único da onda e o prompt do agente executor. Junto vem o agente
executor, os sete fragments do workflow, e o fecho da fase: a tabela do oráculo cobrindo o
escopo inteiro, a medição antes/depois pela ferramenta canônica com o seam declarado, e a
lacuna de D-02 registrada com número medido em vez de descrita.

Purpose: fechar a fase com o caminho mais caro adaptado sob o critério já provado seis
vezes, e deixar três provas vivas — tabela de adaptados fechada, oráculo de bytes
dois-sentidos e registro de adaptações — que defendem a árvore nas fases 37 e 38.
Output: execute-phase e seus sete fragments adaptados, agente executor adaptado, node zerado
no que é infra, cobertura fechada por teste, números finais publicados.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-CONTEXT.md
@.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-PATTERNS.md
@.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-06-SUMMARY.md
@cairn/gsd/contracts/estado.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: execute-phase — os sítios de fato, o escritor único da onda e o prompt do executor</name>
  <files>cairn/gsd/gsd-core/workflows/execute-phase.md, cairn/gsd-adaptations.json</files>
  <read_first>
    - cairn/gsd/gsd-core/workflows/execute-phase.md linhas 38-45 (a instrução de abertura), 85-95 (os campos do bundle, incluindo o predicado de existência e o manifesto de seções corrigido pelo plano 02), 148-155 (o ramo do predicado), 178-195 (a derivação do plano corrente antes de confiar no estado, e a menção a progresso obsoleto), 318-330 e 450-458 (as duas prosas de atualização de estado), 700-760 (as instruções aos agentes de worktree e as três linhas de contexto injetadas no prompt do executor), 800-812 (o checklist do executor), 965-1005 (o bloco do escritor único da onda, o diff condicional e o commit), 1435-1460 (o encerramento e seu commit), 1485-1520 (o fecho de todos)
    - 36-PATTERNS.md §5 inteiro — os quatro padrões, com execute-phase aparecendo em três deles
    - 36-05-SUMMARY.md e 36-04-SUMMARY.md — as conversões já feitas para prosa imperativa, predicado de existência e injeção em prompt; este arquivo repete os três padrões e não inventa nenhum
    - cairn/gsd/contracts/estado.json — os verbos de início de fase, avanço de plano, atualização e progresso já usados por este mesmo workflow em outros pontos
  </read_first>
  <behavior>
    - a instrução de abertura passa a mandar carregar o contexto pelo binário, não ler o arquivo
    - o ramo do predicado de existência de estado segue a mesma forma fixada em autonomous: pergunta pelo portador, e a falha nomeia o comando que o cria
    - a derivação do plano corrente a partir dos commits continua sendo a fonte de verdade para redispatch, e a menção a progresso obsoleto passa a falar do fato registrado
    - as duas prosas de atualização de estado viram chamadas aos verbos que este mesmo arquivo já usa em outros pontos
    - as três linhas de contexto injetadas no prompt do executor perdem a do arquivo de estado; as de projeto e de config continuam, porque são documento e configuração
    - as instruções aos agentes de worktree e o bloco do escritor único contam a mesma história: quem registra o fato da onda é a orquestração, e o agente não registra em paralelo
    - o diff condicional e os commits do encerramento param de listar o arquivo de estado; roadmap e requisitos continuam exatamente como estão
    - o checklist do executor descreve o fato registrado, nomeando o verbo
  </behavior>
  <action>
    Converter sítio a sítio, na ordem do arquivo, usando os três padrões já exercitados —
    prosa imperativa vira verbo, predicado de existência vira pergunta pelo portador com
    falha nomeada, injeção de caminho em prompt some. Nenhum padrão novo é necessário e
    inventar um aqui contradiz seis arquivos já convertidos.

    No bloco do escritor único da onda: o motivo original era última-escrita-vence em
    arquivo compartilhado. Com o fato no bd, o motivo muda mas a regra continua valendo por
    outra razão — registro duplicado por agentes concorrentes. Reescrever o bloco para dizer
    isso, e alinhar as instruções dadas aos agentes de worktree: as três linhas que hoje
    proíbem o agente de tocar os arquivos compartilhados passam a proibir o registro paralelo
    do fato, com a mesma força. Se as três contarem histórias diferentes, a onda registra
    duas vezes ou nenhuma.

    Nos commits: retirar o arquivo de estado das listas de arquivos e do diff condicional que
    decide se há o que commitar. Roadmap e requisitos ficam onde estão — o bookkeep é o dono
    e esta task não muda quem escreve neles.

    Deixar intactos: os caminhos de artefato de fase, o manifesto de trabalhos assíncronos, o
    diretório de todos, os mapas de codebase e a leitura do arquivo de projeto. São
    documento.

    Publicar o número de sítios convertidos com o comando, e somar a onda 7 à entrada do
    arquivo no registro de adaptações.
  </action>
  <verify>
    <automated>R="$PWD" && F=cairn/gsd/gsd-core/workflows/execute-phase.md && test "$(grep -cE '(cat|grep|head|tail|wc|test -f|\[ -f)[^|]*\.planning/STATE\.md' "$F")" -eq 0 && test "$(grep -cE '(Update|Read|Write|Check) STATE\.md' "$F")" -eq 0 && test "$(grep -cE '\{state_path\}|STATE_PATH|STATE_FILE|state_raw' "$F")" -eq 0 && test "$(grep -E 'gsd_run query commit' "$F" | grep -c 'STATE\.md')" -eq 0 && grep -q 'state_exists' "$F" && grep -q 'PROJECT\.md' "$F" && bats tests/cairn-prompt-state.bats && bats tests/cairn-vendoring.bats</automated>
  </verify>
  <done>Os sítios de fato do execute-phase estão convertidos pelos padrões já provados; o escritor único da onda e as instruções aos agentes contam a mesma história sobre quem registra; os commits não carregam mais o arquivo de estado e não ganharam nada novo; documento intacto.</done>
</task>

<task type="auto">
  <name>Task 2: o manifesto de worktree sem node, e os sete fragments</name>
  <files>cairn/gsd/gsd-core/workflows/execute-phase.md, cairn/gsd/gsd-core/workflows/execute-phase/steps/codebase-drift-gate.md, cairn/gsd/gsd-core/workflows/execute-phase/steps/executor-isolation-dispatch.md, cairn/gsd/gsd-core/workflows/execute-phase/steps/gap-closure-artifacts.md, cairn/gsd/gsd-core/workflows/execute-phase/steps/partial-wave.md, cairn/gsd/gsd-core/workflows/execute-phase/steps/per-plan-worktree-gate.md, cairn/gsd/gsd-core/workflows/execute-phase/steps/post-merge-gate.md, cairn/gsd/gsd-core/workflows/execute-phase/steps/regression-gate-run.md, cairn/gsd-adaptations.json</files>
  <read_first>
    - cairn/gsd/gsd-core/workflows/execute-phase.md linhas 670-680 (a escrita inicial do manifesto de worktree), 893-930 (as duas leituras do campo de raiz do orquestrador e a listagem dos caminhos das árvores, com o bloqueio explícito quando o manifesto não pode ser lido)
    - cairn/gsd/gsd-core/workflows/execute-phase/steps/executor-isolation-dispatch.md linhas 96 e 176 — os DOIS sítios de node do fragment (parse da saída de isolamento e do resultado de execução), ausentes do mapa da fase e por isso ausentes de todos os planos anteriores
    - 36-PATTERNS.md, armadilha NODE-SOBREVIVE — os quatro sítios do arquivo raiz e por que um teste que só valida o preâmbulo declara verde um caminho que ainda quebra; a lista de lá é incompleta (7 contra 14 medidos), então medir antes de confiar
    - 36-05-SUMMARY.md — o motor escolhido para o helper de leitura de campo do plan-phase; este trabalho usa o MESMO, senão a fase entrega duas soluções para o mesmo problema
    - os sete fragments, um a um — medir antes de tocar
  </read_first>
  <behavior>
    - a escrita inicial do manifesto produz o mesmo JSON de hoje, com a mesma chave de raiz do orquestrador e a mesma lista vazia de árvores, e termina com quebra de linha
    - as duas leituras do campo de raiz devolvem exatamente o que devolviam, incluindo string vazia quando o campo está ausente ou o arquivo é ilegível
    - a listagem dos caminhos das árvores mantém o bloqueio explícito e a mensagem de recusa quando o manifesto está ausente, vazio ou inválido — é uma recusa deliberada e não pode virar lista vazia silenciosa
    - os dois sítios do fragment de isolamento de executor extraem o mesmo campo da mesma saída de hoje, com o mesmo comportamento quando o JSON é inválido — são um parse de saída do binário, não do manifesto, e por isso não herdam o tratamento do manifesto sem serem lidos
    - nenhum sítio do execute-phase nem de seus fragments depende de node depois desta task
    - os sete fragments são medidos pelas três famílias; conversões só onde houver fato, números publicados inclusive zeros
  </behavior>
  <action>
    Trocar o motor dos quatro sítios do manifesto de worktree, usando a mesma ferramenta de
    JSON escolhida no plano 05 para o helper de leitura de campo. Preservar, sítio a sítio:
    o formato exato do JSON escrito, o comportamento das leituras diante de campo ausente e
    de arquivo ilegível, e — o mais importante — a recusa explícita da listagem, que hoje
    distingue manifesto ausente, vazio e inválido, cada um com sua mensagem, e bloqueia a
    limpeza em vez de seguir com lista vazia. Uma conversão que transforme essa recusa em
    lista vazia troca um erro alto por perda silenciosa de árvores de trabalho.

    Trocar também o motor dos DOIS sítios de node do fragment de isolamento de executor
    (linhas 96 e 176): ambos parseiam uma saída JSON do binário para extrair um campo, com
    tratamento próprio para JSON inválido. O mapa da fase não os listou — a contagem
    verdadeira do corpus é 14 sítios, não 7 — e por isso nenhum plano anterior os citava,
    embora o gate desta task já os exigisse em zero. Preservar, em cada um, o campo extraído,
    o valor devolvido quando o JSON não parseia e o que o chamador faz com esse valor.

    Medir os sete fragments com o mesmo instrumento e converter só fato. Vários deles tratam
    de gates de worktree e de merge e provavelmente só têm caminho de documento; publicar o
    número de cada um, inclusive zero, é o que impede a próxima fase de reabrir trabalho já
    feito.

    Somar a onda 7 às entradas dos oito caminhos no registro de adaptações e acrescentá-los à
    tabela de adaptados do oráculo.
  </action>
  <verify>
    <automated>R="$PWD" && test "$(grep -c 'node -e' cairn/gsd/gsd-core/workflows/execute-phase.md)" -eq 0 && test "$(grep -rc 'node -e' cairn/gsd/gsd-core/workflows/execute-phase/steps/ | awk -F: '{s+=$2} END {print s+0}')" -eq 0 && test "$(grep -rl 'node -e' cairn/gsd/gsd-core/workflows/ | wc -l | tr -d ' ')" -eq 0 && grep -q 'WAVE_WORKTREE_MANIFEST' cairn/gsd/gsd-core/workflows/execute-phase.md && grep -qi 'refusing\|BLOCKED' cairn/gsd/gsd-core/workflows/execute-phase.md && bats tests/cairn-prompt-state.bats && bats tests/cairn-vendoring.bats</automated>
  </verify>
  <done>Nenhum workflow nem fragment depende mais de node — os quatro do arquivo raiz e os dois do fragment de isolamento, seis no total; o manifesto de worktree escreve e lê pelo mesmo motor do plan-phase; a recusa explícita da listagem sobreviveu à troca; os sete fragments estão medidos e publicados.</done>
</task>

<task type="auto">
  <name>Task 3: agente executor, cobertura fechada e os números finais da fase</name>
  <files>cairn/gsd/agents/gsd-executor.md, tests/cairn-prompt-state.bats, tests/fixtures/gsd-goldens/divergences.json, cairn/gsd-adaptations.json</files>
  <read_first>
    - cairn/gsd/agents/gsd-executor.md — as 17 chamadas ao binário, as três já convertidas para verbos de estado (progresso, métrica, sessão) e as menções a `.planning/`; e o que o agente faz quando roda em árvore de trabalho isolada
    - 36-01-SUMMARY.md — a receita do seam de medição de corpus local e os números do antes
    - 36-CONTEXT.md D-02 — a lacuna de references que precisa sair medida
    - 36-PATTERNS.md, armadilhas MANIFEST-PROSA e SHIM-HOMÔNIMO — as duas que precisam de uma frase no SUMMARY para não virarem susto na fase 37
    - tests/cairn-prompt-state.bats — a tabela de adaptados a fechar e o teste de completude a acrescentar
  </read_first>
  <behavior>
    - o agente executor não lê nem escreve estado em markdown; o que ele registra vai pelos verbos que ele já usa
    - a história que o agente conta sobre registrar fato quando roda isolado bate com a do bloco do escritor único do workflow
    - a tabela de adaptados cobre os 8 workflows raiz, os 15 fragments com preâmbulo e os 16 agentes, e existe um teste de completude que reprova quando um arquivo do escopo fica de fora da tabela
    - os números finais saem da ferramenta canônica: o antes pelo fecho sobre o cache, o depois pelo seam de corpus local, com a razão do seam escrita
    - a lacuna de D-02 aparece com número medido: quantos arquivos de references os workflows citam, quantas chamadas ao binário eles fazem e quantos sítios de estado sobram neles
  </behavior>
  <action>
    Medir e adaptar o agente executor pelo mesmo critério, alinhando o que ele diz sobre
    registro de fato em árvore isolada com o bloco do escritor único reescrito na Task 1.

    Fechar a tabela de adaptados do oráculo e acrescentar o teste de completude: o conjunto
    de arquivos do escopo declarado — os 8 workflows raiz, os fragments que carregam
    preâmbulo e os 16 agentes — comparado com a tabela por comm nos dois sentidos, reprovando
    com nome nos dois lados. É esse teste que impede que a fase 37, ao mexer no plugin, deixe
    um arquivo do escopo fora da vigilância sem ninguém notar.

    Medir a fase inteira e publicar. O antes é o fecho da ferramenta canônica sobre o cache.
    O depois é o mesmo fecho sobre a árvore adaptada, pelo seam de corpus local declarado no
    plano 01 — copiar a árvore para um repositório temporário com a mesma etiqueta, e apontar
    a ferramenta para ele com o commit esperado. A razão do seam vai escrita: a ferramenta,
    como está, sempre resolve o corpus para o cache e exige que o HEAD bata com o commit da
    etiqueta, então ela não mede a árvore editada sem esse desvio.

    Medir e registrar a lacuna de D-02 em `divergences.json` e no SUMMARY, com número: os
    arquivos de references citados pelos workflows, as chamadas que fazem ao binário, e os
    sítios de estado que sobram neles — incluindo nominalmente a leitura mecânica de estado
    que o research encontrou num deles. A entrada diz que é alvo das fases 37 e 38, e nomeia
    a exceção já aberta pelo plano 06.

    Na mesma entrada, somar as duas lacunas medidas que a fase decidiu não fechar e que não
    são de estado: os QUATRO arquivos que citam o runtime antigo sem carregar bloco de
    preâmbulo (`commands/gsd/plan-phase.md`, `skills/gsd-plan-phase/SKILL.md`,
    `references/execute-phase-between-wave-reset.md`, `references/universal-anti-patterns.md`,
    nomeados no SUMMARY do plano 01) e os CINCO sítios de node em references. Ambas com
    nome de arquivo e linha — a fase 37 mata o runtime antigo, e essas nove linhas são
    exatamente onde ela vai bater.

    Escrever no SUMMARY as duas frases que evitam susto na próxima fase: os totais do
    manifesto vendorizado descrevem o fecho do UPSTREAM, derivado do cache, e continuam
    verdadeiros depois da adaptação — a contagem da árvore adaptada mora no registro de
    adaptações; e o campo de pares comando-e-skill do manifesto é outro "shim", não o
    preâmbulo, com oito entradas contra os trinta e quatro blocos desta fase.
  </action>
  <verify>
    <automated>R="$PWD" && test "$(grep -cE '(cat|grep|head|tail|wc|test -f|\[ -f)[^|]*\.planning/STATE\.md' cairn/gsd/agents/gsd-executor.md)" -eq 0 && test "$(grep -cE '(Update|Read|Write|Check) STATE\.md' cairn/gsd/agents/gsd-executor.md)" -eq 0 && bats tests/cairn-prompt-state.bats && bats tests/cairn-vendoring.bats && bats tests/ && jq -e '[.divergences[] | select(.aspect|test("references"))] | length >= 1' tests/fixtures/gsd-goldens/divergences.json && jq -e '.adaptations | length >= 37' cairn/gsd-adaptations.json && T=$(mktemp -d) && cp -R cairn/gsd/. "$T/src" && git -C "$T/src" init -q && git -C "$T/src" add -A && git -C "$T/src" -c user.email=t@e -c user.name=t commit -qm m && git -C "$T/src" tag v1.10.0 && S=$(git -C "$T/src" rev-parse HEAD) && CLAUDE_PROJECT_DIR="$R" python3 cairn/scripts/cairn-inventory.py closure --source "$T/src" --expect-commit "$S" --cache-dir "$T/cache" --json | jq -e '.totals.files > 0'</automated>
  </verify>
  <done>O agente executor está adaptado e alinhado com o workflow sobre quem registra o fato; a cobertura do oráculo fecha por teste de completude nos dois sentidos; os números antes/depois saíram da ferramenta canônica com a razão do seam escrita; a lacuna de D-02 está registrada com número medido e nomeada como alvo da 37/38.</done>
</task>

</tasks>

<verification>
- `bats tests/` inteiro verde, com o cache do clone presente
- Oráculo de bytes dois-sentidos verde: o conjunto de caminhos divergentes do cache é exatamente o registro de adaptações, em ambos os sentidos
- Teste de completude do oráculo verde: nenhum arquivo do escopo declarado fora da tabela, nenhuma linha da tabela sem arquivo
- `grep -rl 'node -e' cairn/gsd/gsd-core/workflows/` vazio. Saldo final publicado, medido e não herdado: 14 sítios no corpus — 9 em escopo (7 de infra, todos convertidos, e os 2 do verificador que ficam por decisão escrita do plano 06) e 5 em references (2 em checkpoints, 3 no de sondagem sem especificação), fora por D-02 e registrados como lacuna
- Números antes/depois publicados com o comando de cada um; lacuna de references registrada com número
</verification>

<success_criteria>
- ADAPT-04 fechado: execute-phase adaptado por último, com os sítios convertidos e medidos
- ADAPT-02 fechado por inteiro: fast, debug e o caminho executado do execute-phase sem leitura nem escrita de `.planning/` como fonte de estado, com o preâmbulo apontando o binário desde a onda zero
- A fase entrega três provas vivas que sobrevivem a ela: tabela de adaptados fechada, oráculo de bytes dois-sentidos, registro de adaptações respeitado pelo vendor
- A lacuna que a fase deliberadamente não fecha está medida e nomeada, não descrita
</success_criteria>

<output>
Criar `.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-07-SUMMARY.md`. Registrar:
os números finais antes/depois com os dois comandos; a contagem de sítios convertidos por
arquivo, com os zeros; o saldo final de node no corpus medido de novo (14 no total, 9 em
escopo, 5 em references) e a decisão que sustenta cada remanescente; a lacuna de references
com número, mais os 4 arquivos que citam o runtime antigo fora do padrão de preâmbulo; e as
duas frases sobre os totais do manifesto e sobre o homônimo de shim, para que a fase 37 não
reabra nenhum dos dois.
</output>
