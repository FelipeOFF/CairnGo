---
phase: 36-workflows-steps-e-agentes-falam-bd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cairn/scripts/cairn-preamble.py
  - cairn/scripts/cairn-preamble.sh
  - cairn/gsd-adaptations.json
  - cairn/scripts/cairn-inventory.py
  - tests/cairn-preamble.bats
  - tests/cairn-vendoring.bats
  - tests/cairn-command-surfaces.bats
  - cairn/gsd/gsd-core/workflows/autonomous.md
  - cairn/gsd/gsd-core/workflows/debug.md
  - cairn/gsd/gsd-core/workflows/discuss-phase.md
  - cairn/gsd/gsd-core/workflows/execute-phase.md
  - cairn/gsd/gsd-core/workflows/fast.md
  - cairn/gsd/gsd-core/workflows/plan-phase.md
  - cairn/gsd/gsd-core/workflows/quick.md
  - cairn/gsd/gsd-core/workflows/verify-work.md
  - cairn/gsd/gsd-core/workflows/autonomous/steps/converge-fail-fast.md
  - cairn/gsd/gsd-core/workflows/execute-phase/steps/codebase-drift-gate.md
  - cairn/gsd/gsd-core/workflows/execute-phase/steps/executor-isolation-dispatch.md
  - cairn/gsd/gsd-core/workflows/execute-phase/steps/gap-closure-artifacts.md
  - cairn/gsd/gsd-core/workflows/execute-phase/steps/partial-wave.md
  - cairn/gsd/gsd-core/workflows/execute-phase/steps/per-plan-worktree-gate.md
  - cairn/gsd/gsd-core/workflows/execute-phase/steps/post-merge-gate.md
  - cairn/gsd/gsd-core/workflows/execute-phase/steps/regression-gate-run.md
  - cairn/gsd/gsd-core/workflows/plan-phase/steps/chunked-planning-mode.md
  - cairn/gsd/gsd-core/workflows/plan-phase/steps/prd-express-path.md
  - cairn/gsd/gsd-core/workflows/plan-phase/steps/stall-detection-helpers.md
  - cairn/gsd/gsd-core/workflows/quick/steps/worktree-pre-dispatch-commit.md
  - cairn/gsd/gsd-core/workflows/verify-work/steps/mvp-uat-framing.md
  - cairn/gsd/gsd-core/workflows/discuss-phase/modes/advisor.md
  - cairn/gsd/gsd-core/workflows/discuss-phase/modes/chain.md
  - cairn/gsd/agents/gsd-debug-session-manager.md
  - cairn/gsd/agents/gsd-debugger.md
  - cairn/gsd/agents/gsd-executor.md
  - cairn/gsd/agents/gsd-phase-researcher.md
  - cairn/gsd/agents/gsd-plan-checker.md
  - cairn/gsd/agents/gsd-planner.md
  - cairn/gsd/agents/gsd-ui-researcher.md
  - cairn/gsd/agents/gsd-verifier.md
  - cairn/gsd/commands/gsd/discuss-phase.md
  - cairn/gsd/skills/gsd-discuss-phase/SKILL.md
  - cairn/gsd/gsd-core/references/planner-load-graph-context.md
autonomous: true
requirements: [ADAPT-02]
beads:
  ADAPT-02: CairnGo-0yzd
must_haves:
  truths:
    - "D-01 materializado: os 34 blocos de preâmbulo resolvem `cairn/scripts/cairn-gsd.sh` a partir da raiz do repo; nenhum arquivo sob cairn/gsd/ ainda define o resolvedor do runtime node (medida: grep pelo marcador do bloco → 0 arquivos)"
    - "a contagem por MARCADOR de bloco (34) e a contagem por NOME do runtime antigo (40 arquivos) são medidas diferentes e ambas ficam registradas: dos 6 arquivos que citam o runtime sem carregar bloco, os 2 fragments em escopo (modes/text.md e plan-phase/steps/adr-ingest-express-path.md) são adaptados aqui porque instruem o modelo a chamar o que a fase 37 mata; os 4 de commands/skills/references vão a lacuna medida, com nome e linha"
    - "a categoria 'menção ao runtime morto' é ELIMINADA do escopo, não excetuada: 10 linhas fora de bloco de preâmbulo em 8 arquivos de workflows são convertidas — 6 instruções de comando viram chamada por `gsd_run` na grafia contratada, 1 afirmação falsa (verify-work.md:634) é REESCRITA porque o verbo audit-open responde exit 0 desde a fase 35, e 3 comentários de design passam a nomear o binário do repo: 6+1+3=10"
    - "o gate de revisabilidade deixa de ser numstat 1/1 por arquivo, que ficou mutuamente inconsistente com o gate de escopo, e passa a ser a propriedade que o numstat aproximava: toda linha mudada sob cairn/gsd/ cita o runtime antigo, a forma nova, a função de despacho ou o binário do repo — mais forte, porque proíbe edição não relacionada de qualquer tamanho"
    - "o registro de adaptações nasce e permanece VAZIO até a task que ADAPTA: caminho registrado é caminho que o oráculo dois-sentidos exige divergente, logo registrar antes de adaptar reprova a suíte por desenho"
    - "o registro consultado pelo vendor é resolvido a partir do --dest, nunca da raiz do projeto — dentro do bats a raiz cai no repo real e a rodada de fixture carregaria o registro de produção, matando os testes 5/6/7 que criam justamente dois caminhos registrados"
    - "a forma nova é IDÊNTICA nos 34 blocos — as duas variantes upstream (longa/curta, PATTERNS §1) colapsam numa linha só, com o motivo escrito: a cadeia de 19 diretórios de host existia para achar um runtime fora do repo, e o binário do repo não tem essa incerteza"
    - "a forma nova foi provada RODANDO em três posições: da raiz do repo, de um subdiretório do repo, e fora de qualquer repo cairn (falha nomeada exit 1 dizendo o caminho procurado e o comando que cria o fato) — não é substituição textual declarada verde por grep"
    - "VEND-BYTES resolvido na MESMA onda da primeira edição: assert_tree_bytes_match passa a exigir byte-igualdade FORA da allowlist e divergência DENTRO dela — um arquivo adaptado que voltou ao byte upstream é FALHA (perda de adaptação por vendor, merge ou revert)"
    - "VEND-REVERT resolvido: `cairn-inventory.py vendor` recusa nominalmente (exit 6) quando files[] intersecta a allowlist, nomeando cada caminho e a flag deliberada de sobrescrita; a fase deixa de ser desfeita em silêncio por uma re-vendorização"
    - "a allowlist é UM registro versionado (cairn/gsd-adaptations.json) lido por três consumidores — o reescritor, o oráculo de bytes e o vendor; nenhuma lista paralela"
    - "cairn-preamble.py recusa por nome qualquer caminho não registrado, cairn/gsd/contracts/** e cairn/gsd/MANIFEST.json (molde cairn-migrate.py:1861-1863)"
    - "regra do registro, válida para os planos 03 a 07: todo caminho editado sob cairn/gsd/ é registrado na MESMA task que o edita — caminho já registrado ganha a onda em `waves[]`, caminho novo ganha entrada nova. Registrar antes de editar deixa o oráculo vermelho; editar sem registrar, também"
    - "PORCELAIN invertido: o gate herdado das fases 33-35 (`git status --porcelain cairn/gsd/` vazio) NÃO é copiado; o gate equivalente é o conjunto de caminhos divergentes do cache == conjunto da allowlist, que vale antes e depois do commit"
  artifacts:
    - cairn/scripts/cairn-preamble.py
    - cairn/scripts/cairn-preamble.sh
    - cairn/gsd-adaptations.json
    - tests/cairn-preamble.bats
    - tests/cairn-vendoring.bats
  key_links:
    - "cairn/gsd-adaptations.json → cairn-preamble.py (recusa de caminho não registrado) → tests/cairn-vendoring.bats (oráculo dois-sentidos) → cairn-inventory.py cmd_vendor (recusa nominal): uma fonte, três consumidores"
    - "linha do preâmbulo → $CAIRN_GSD → cairn/scripts/cairn-gsd.sh → exec python3 cairn-gsd.py: os exits do cabeçalho de cairn-gsd.sh (0/1 contrato, 2 uso, 4 família) chegam ao workflow sem tradução"
    - "cairn-preamble.py --check → EXIT_STALE=3 + difflib.unified_diff (molde cairn-wrap.py:584-590) — o gate 'os 34 estão trocados' é o próprio --check, não um teste inventado"
  prohibitions:
    - statement: "Nenhuma task deste plano toca sítio de estado: a onda zero troca EXCLUSIVAMENTE a linha do preâmbulo. Converter um `cat` de estado aqui mistura duas medições e some com a prova de D-01"
    - statement: "Nenhuma task edita cairn/gsd/MANIFEST.json — totals.files/lines descrevem o FECHO DO UPSTREAM, derivado do cache (derived_from: closure --json), e continuam verdadeiros depois da adaptação; o número da árvore adaptada mora em gsd-adaptations.json"
    - statement: "Nenhuma task toca cairn/gsd/contracts/** — o vocabulário de verbo é da fase 35 e nada nesta onda muda verbo"
    - statement: "Nenhuma task copia o gate `git status --porcelain cairn/gsd/` vazio (armadilha PORCELAIN — reprova a fase 36 por desenho)"
    - statement: "Nenhuma task usa caminho absoluto desta máquina nos comandos de verificação: raiz sempre por $PWD/$R"
---

<objective>
A onda zero de D-01: a camada prompt passa a resolver o binário python do próprio repo.
Hoje nenhum dos 169 arquivos vendorizados menciona `cairn-gsd` — os 34 blocos de preâmbulo
procuram um runtime node em ~20 diretórios de host e terminam em `exit 1`. Antes desta
troca nenhuma outra edição da fase 36 tem efeito observável, e qualquer teste de ponta a
ponta mede o runtime errado.

Esta é também a PRIMEIRA escrita da casa sob `cairn/gsd/`, e ela desarma na mesma onda as
duas guardas construídas contra exatamente essa escrita: a byte-paridade contra o cache
(VEND-BYTES) e a re-vendorização silenciosa (VEND-REVERT). Escopo por PADRÃO, não por
diretório: os 34 blocos, inclusive os 3 fora de workflows/agents — D-02 governa sítio de
ESTADO, e deixar 3 preâmbulos apontando o runtime antigo entrega `exit 1` na fase 37, um
deles no caminho do gsd-planner.

Purpose: tornar o caminho que o usuário executa o mesmo caminho que os testes das fases
33-35 provam, e deixar a árvore adaptada defensável por máquina antes que 34 arquivos
divirjam.
Output: cairn-preamble.py (+.sh), cairn/gsd-adaptations.json, oráculo de bytes com
allowlist dois-sentidos, recusa nominal do vendor, 34 blocos trocados e provados rodando.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-CONTEXT.md
@.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-PATTERNS.md
@.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-RESEARCH.md
@cairn/scripts/cairn-gsd.sh
</context>

<tasks>

<task type="tracer">
  <name>Task 1: a forma nova do preâmbulo, provada rodando, e o reescritor que a aplica</name>
  <files>cairn/scripts/cairn-preamble.py, cairn/scripts/cairn-preamble.sh, cairn/gsd-adaptations.json, tests/cairn-preamble.bats, tests/cairn-command-surfaces.bats</files>
  <read_first>
    - 36-PATTERNS.md §1 inteiro — as duas variantes, o sha1 de cada uma, a tabela dos 34 arquivos com a linha de cada bloco, e o fato que decide a forma do reescritor: o bloco é UMA LINHA, sempre a primeira dentro de um fence bash
    - 36-PATTERNS.md §2 — os três moldes: cairn-wrap.py:102-114 (exits), :548-556 (docstring da escrita cirúrgica), :584-599 (--check com unified_diff + write-only-when-changed); cairn-migrate.py:1858-1882 (recusa nominal por nome de arquivo)
    - cairn/scripts/cairn-gsd.sh linhas 1-13 — o alvo da resolução e a tabela de exits que a forma nova herda sem tradução
    - cairn/scripts/cairn-inventory.py:625-637 (resolve_corpus) — o precedente de CLAUDE_PROJECT_DIR como raiz do projeto na casa
    - tests/cairn-command-surfaces.bats — o guard "every cairn script is reachable by command" e a forma da razão escrita (precedente 34-05, desvio 1)
  </read_first>
  <behavior>
    - `cairn-preamble.py --print-form` imprime a linha canônica e SÓ ela (uma linha, sem newline extra) — é a fonte única da forma; nenhum arquivo carrega uma segunda cópia autoritativa
    - `bash -c "$(cairn-preamble.sh --print-form); gsd_run query init.debug"` na RAIZ do repo → exit 0 e JSON com project_root
    - o mesmo, com cwd num subdiretório do repo (cairn/) → exit 0: a resolução sobe pelo toplevel do git, não pelo cwd
    - o mesmo, com cwd num diretório temporário que não é repo cairn → exit 1, stderr nomeando o caminho procurado e o comando que cria o fato; nunca exit 0 silencioso, nunca fallback markdown
    - `cairn-preamble.py check` sobre um arquivo já trocado → exit 0; sobre um arquivo ainda na forma antiga → exit 3 (EXIT_STALE) com unified_diff no stdout
    - `cairn-preamble.py apply` num caminho AUSENTE do registro → recusa nominal, exit 2, sem escrever (com o registro nascendo vazio, TODO caminho é recusado nesta task — é o estado correto: registrar sem adaptar deixaria o oráculo dois-sentidos da Task 2 vermelho por desenho)
    - `cairn-preamble.py apply` duas vezes seguidas sobre um arquivo de fixture, com registro forjado em diretório temporário apontando para ele → segunda rodada byte-idêntica e mtime intacto (write-only-when-changed)
  </behavior>
  <action>
    Criar `cairn/scripts/cairn-preamble.py` + o wrapper `.sh` no molde dos irmãos da casa
    (shebang, `set -euo pipefail`, `exec python3 "$HERE/cairn-preamble.py" "$@"`).

    O docstring-contrato abre declarando em voz alta o que nenhum script da casa fazia até
    agora, no molde do docstring de rede de cairn-inventory.py:13-27: ESTE SCRIPT ESCREVE
    SOB `cairn/gsd/` — E SÓ NAS LINHAS DE PREÂMBULO DOS CAMINHOS REGISTRADOS. Declarar
    também, no mesmo bloco, que "preâmbulo" aqui é o bloco de resolução de runtime de
    PATTERNS §1 e NÃO o `summary.shim_matches` de MANIFEST.json (o par comando↔skill) —
    a armadilha SHIM-HOMÔNIMO custa 16-versus-34 arquivos a quem confundir.

    Exits idênticos aos do molde: 0 ok, 2 uso/recusa nominal, 3 stale (mesmo significado
    que `cairn-map.py --check` e `cairn-wrap.py --check` já dão ao 3).

    A FORMA NOVA (constante única no script, sem duplicata em lugar nenhum): uma linha,
    posicionada exatamente onde a antiga estava (primeira linha dentro do fence bash), com
    esta cadeia de resolução e nada mais — (1) `$CAIRN_GSD` já exportado, se houver, como
    escape deliberado; (2) `${CLAUDE_PROJECT_DIR}/cairn/scripts/cairn-gsd.sh`; (3)
    `$(git rev-parse --show-toplevel)/cairn/scripts/cairn-gsd.sh`; (4) o mesmo caminho
    relativo ao `$PWD`. Não encontrando executável em nenhuma das quatro, `echo` em stderr
    nomeando o último caminho tentado e o comando que cria o fato, e `exit 1` — a doutrina
    da falha nomeada de cairn-gsd-state.py:176-178. Fechando, define
    `gsd_run() { "$CAIRN_GSD" "$@"; }`. Nenhum `node`, nenhum `npx`, nenhuma menção ao
    instalador do pacote externo (a fase 37 mata esse comando; um preâmbulo que o sugira
    manda o usuário para um caminho morto).

    UMA forma para os 34, colapsando as duas variantes upstream. O motivo vai escrito num
    comentário ao lado da constante: os 19 ramos da variante longa e os 3 da curta existiam
    para achar um runtime FORA do repo, e o binário do repo não tem essa incerteza; o append
    de PATH em `$CLAUDE_ENV_FILE` que só a longa fazia some junto porque exportava o
    diretório do runtime antigo.

    Subcomandos: `--print-form` (a linha, fonte única); `list` (os caminhos registrados e,
    para cada um, se está na forma nova, na antiga, ou sem preâmbulo); `check [<path>...]`
    (exit 3 + unified_diff quando algum registrado ainda está na forma antiga); `apply
    [<path>...]` (substitui a linha, write-only-when-changed no molde cairn-wrap.py:596-599).
    A detecção da linha a substituir é por PREFIXO de marcador — o marcador antigo (a
    variável que nomeia o runtime `.cjs`, grafia literal em PATTERNS §1) e o marcador novo —
    e nunca por número de linha: os 34 blocos moram em linhas diferentes.

    Recusa nominal, molde cairn-migrate.py:1861-1863, em três níveis: caminho fora de
    `cairn/gsd/`; caminho sob `cairn/gsd/contracts/` ou igual a `cairn/gsd/MANIFEST.json`;
    e caminho ausente de `cairn/gsd-adaptations.json`. A mensagem nomeia o caminho e diz que
    a adaptação se registra ANTES de existir no disco.

    Criar `cairn/gsd-adaptations.json` — o registro versionado, fora de `cairn/gsd/` porque
    um arquivo a mais lá dentro reprovaria `assert_tree_set_exact` (cairn-vendoring.bats:402).
    Schema `{schema_version: 1, adaptations: []}`, entradas `{path, phase, waves, reason}`
    ordenadas por path, com `path` na mesma grafia de `files[]` do MANIFEST.

    O registro nasce VAZIO e continua vazio ao fim desta task e da Task 2. O motivo é
    dependência, não estética: a partir da Task 2 o oráculo passa a exigir DIVERGÊNCIA
    obrigatória para todo caminho registrado, então um caminho registrado e ainda não
    adaptado reprova a suíte por desenho. Registro e adaptação nascem juntos, na Task 3.
    O tracer prova a forma pelo `--print-form`, sem escrever sob `cairn/gsd/`; a idempotência
    do `apply` é provada sobre arquivo de fixture com registro forjado em diretório
    temporário.

    `tests/cairn-preamble.bats`: os sete itens do behavior, cada um com o controle negativo
    do molde `assert_cut_holds`/`:465-474` — a rodada fora do repo cairn é a metade de
    liveness da resolução, e a recusa por caminho não registrado é a da allowlist. Os três
    gates de execução resolvem a raiz por `$CAIRN_REPO_ROOT` do helper, nunca por caminho de
    máquina.

    Em `tests/cairn-command-surfaces.bats`: registrar a razão escrita de `cairn-preamble.sh`
    no guard de alcançabilidade — é ferramenta de manutenção do transplante, não comando de
    usuário (mesma forma dos irmãos da 34/35, obrigatória para a suíte fechar).
  </action>
  <verify>
    <automated>R="$PWD" && FORM=$(bash "$R/cairn/scripts/cairn-preamble.sh" --print-form) && test "$(printf '%s' "$FORM" | wc -l)" -eq 0 && ! printf '%s' "$FORM" | grep -qE 'gsd-tools|npx' && (cd "$R" && env -u CLAUDE_PROJECT_DIR -u CAIRN_GSD bash -c "$FORM"'; gsd_run query init.debug' | jq -e '.project_root') && (cd "$R/cairn" && env -u CLAUDE_PROJECT_DIR -u CAIRN_GSD bash -c "$FORM"'; gsd_run query init.debug' | jq -e '.project_root') && T=$(mktemp -d) && git -C "$T" init -q && (cd "$T" && env -u CLAUDE_PROJECT_DIR -u CAIRN_GSD bash -c "$FORM"'; gsd_run query init.debug' >/dev/null 2>"$T/err"; test $? -eq 1) && grep -q 'cairn-gsd.sh' "$T/err" && N=$(mktemp -d) && git -C "$N" init -q && (cd "$R" && env -u CAIRN_GSD CLAUDE_PROJECT_DIR="$N" bash -c "$FORM"'; gsd_run query init.debug' | jq -e '.project_root') && test "$(jq -r '.adaptations | length' cairn/gsd-adaptations.json)" -eq 0 && bats tests/cairn-preamble.bats && bats tests/cairn-command-surfaces.bats && bats tests/cairn-vendoring.bats</automated>
  </verify>
  <done>A linha canônica existe numa fonte só; resolve o binário do repo da raiz, de um subdiretório e por variável de projeto apontando um repo qualquer (o elo 2 caindo para o 3 está provado); falha nomeada com exit 1 fora de qualquer repo cairn, com o ambiente decepado para que o exit 1 venha da resolução e não de herança de env. O reescritor aplica, checa (exit 3 + diff), é idempotente sobre fixture e recusa por nome três classes de caminho. O registro versionado existe e está VAZIO. A suíte de vendoring segue verde: nada foi escrito sob cairn/gsd/ ainda.</done>
</task>

<task type="auto">
  <name>Task 2: as duas guardas da allowlist — byte-paridade dois-sentidos (VEND-BYTES) e recusa do vendor (VEND-REVERT)</name>
  <files>tests/cairn-vendoring.bats, cairn/scripts/cairn-inventory.py</files>
  <read_first>
    - 36-PATTERNS.md §3 e §4 inteiros — o que exatamente quebra, o que NÃO quebra, e a ordem de preferência das três formas de evolução (allowlist dois-sentidos vence; baseline nova é descartada e por quê)
    - tests/cairn-vendoring.bats:160-175 (assert_tree_set_exact — o comm dois-sentidos que é o molde), :179-194 (assert_tree_bytes_match — o oráculo a evoluir), :420-448 (assert_cut_holds — raiz parametrizada), :465-474 (o controle negativo forjado), :497-507 (real_cache_or_skip + o teste da byte-paridade)
    - cairn/scripts/cairn-inventory.py:794-841 (cmd_vendor — a cópia por lista e a conferência de divergência que precisa exemplar os adaptados), :610-626 (add_corpus_flags), :268-312 (ensure_corpus)
    - cairn/scripts/cairn-migrate.py:1858-1882 — a recusa nominal por nome
  </read_first>
  <behavior>
    - `assert_tree_bytes_match <manifest> <dest> <corpus> <allowlist>` exige, para todo caminho de files[]: FORA da allowlist, `cmp -s` igual; DENTRO da allowlist, `cmp -s` DIFERENTE. Cada residuo é nomeado em stderr, dizendo de que lado falhou
    - controle negativo em árvore forjada: (a) um arquivo NÃO registrado alterado é mordido e nomeado; (b) um arquivo registrado revertido ao byte upstream é mordido e nomeado — a metade que prova que a perda de adaptação reprova
    - o conjunto de caminhos divergentes do cache é EXATAMENTE o conjunto de `path` do registro, comm nos dois sentidos (é este o gate que substitui PORCELAIN). Com o registro ainda vazio nesta task, os dois lados são vazios e o gate passa; ele ganha carga na Task 3
    - o registro consultado pelo vendor é resolvido A PARTIR DO `--dest` (`dest.parent / 'gsd-adaptations.json'`), com `--adaptations <path>` para sobrepor. NÃO a partir da raiz do projeto: `resolve_corpus` deriva a raiz de uma variável de ambiente que fica ausente dentro do bats, caindo no diretório corrente — que é a raiz do repo real. Com a resolução por raiz, uma rodada de fixture carregaria o registro REAL
    - `vendor` sobre a fixture sintética não encontra registro (o `--dest` fica sob o diretório temporário do teste), a allowlist fica vazia e os testes 5/6/7/10/11 passam sem edição. Isto é verificado, não presumido: a fixture cria `gsd-core/workflows/fast.md` e `agents/gsd-executor.md`, exatamente dois caminhos que o registro real vai conter ao fim da Task 3
    - `vendor` com registro não-vazio intersectando files[] → exit 6, stderr nomeando cada caminho, o registro e a flag deliberada
    - `vendor --clobber-adaptations` → copia tudo, e diz na saída quantas adaptações foram sobrescritas e qual comando as reaplica
    - as duas provas acima rodam contra uma CÓPIA da árvore em diretório temporário, nunca contra `cairn/gsd/`: o clobber é destrutivo por definição, e um verify que o exercite na árvore de produção reverte a fase se for reexecutado depois da Task 3 (por exemplo, ao repetir a Task 2 após uma falha adiante)
  </behavior>
  <action>
    Em `tests/cairn-vendoring.bats`: dar a `assert_tree_bytes_match` um quarto argumento —
    o caminho do registro de adaptações — e partir o laço em dois sentidos, no molde exato de
    `assert_tree_set_exact:160-175`. Um caminho registrado que passa no `cmp` é falha com
    mensagem própria ("a adaptação registrada sumiu — o arquivo voltou ao byte do upstream"),
    distinta da falha do lado de fora ("bytes divergem do corpus sem registro"). Ler o
    registro com `jq -r '.adaptations[].path'`; registro ausente = allowlist vazia, e aí o
    oráculo é exatamente o de hoje (compatibilidade com a fixture).

    Atualizar as três chamadas existentes, nas linhas 272, 288 e 505: as duas de fixture
    passam allowlist vazia; a real (505) passa
    `$CAIRN_REPO_ROOT/cairn/gsd-adaptations.json`. O comentário de topo do arquivo (a prosa
    que hoje afirma byte-identidade em ambos os sentidos) ganha a frase que descreve o novo
    invariante — o teste que mente é pior que o teste ausente, e essa suíte é o único teste
    de conteúdo `.md` sob cairn/gsd/.

    Acrescentar dois @test novos: o controle negativo de perda de adaptação (árvore forjada
    com um registrado revertido, e registro forjado no diretório temporário do teste) e o
    gate de exatidão do conjunto — divergentes-do-cache versus registro, `comm` nos dois
    sentidos, ambos gated por `real_cache_or_skip`. Esse segundo é a forma nova de PORCELAIN:
    vale antes e depois do commit, ao contrário do `git status` das fases 33-35, e por isso é
    ele que os planos seguintes citam. Com o registro vazio nesta task, ele compara vazio
    contra vazio e passa; a carga chega na Task 3.

    Em `cairn/scripts/cairn-inventory.py cmd_vendor`: acrescentar `--adaptations <path>` com
    default derivado do `--dest` (`dest.parent / "gsd-adaptations.json"`), NUNCA da raiz
    resolvida por `resolve_corpus`. A raiz de lá sai de uma variável de ambiente que fica
    ausente dentro do bats e cai no diretório corrente, que é a raiz do repo real — resolver
    por ela faria a rodada de fixture carregar o registro de produção e morrer com exit 6 nos
    testes 5, 6 e 7, que criam justamente dois caminhos que o registro vai conter. Com o
    default por `--dest`, a fixture aponta para o diretório temporário do teste, não acha
    registro, e a allowlist fica vazia.

    Depois de validar o corpus e ANTES da primeira cópia, carregar o registro (ausente =
    vazio) e interceptar a interseção com `files[]`. Havendo
    interseção sem `--clobber-adaptations`, `die` com EXIT_BAD_CORPUS (6, o mesmo código que
    a função já usa para "não vou medir/copiar sobre corpus que não confere"), nomeando cada
    caminho, o arquivo de registro e as duas saídas: a flag, ou reaplicar com
    `cairn-preamble.sh apply` depois. Com a flag, copiar tudo e imprimir a contagem
    sobrescrita mais o comando de reaplicação. A conferência de divergência pós-cópia
    (`:838-840`) fica intacta no caminho com flag — depois de clobber a árvore É idêntica ao
    cache, que é justamente o estado que a mensagem manda desfazer.

    Registrar a flag nova na `VENDOR_USAGE` e no docstring. Nenhum número de fechamento
    entra fora do bloco datado (o GUARD de contagem da própria suíte morde isso).
  </action>
  <verify>
    <automated>R="$PWD" && bats tests/cairn-vendoring.bats && python3 cairn/scripts/cairn-inventory.py vendor --help 2>&1 | grep -q -- --adaptations && test "$(jq -r '.adaptations | length' cairn/gsd-adaptations.json)" -eq 0 && python3 - "$R" <<'PY'
import json,os,shutil,subprocess,sys,pathlib,tempfile
root=pathlib.Path(sys.argv[1])
man=json.loads((root/"cairn/gsd/MANIFEST.json").read_text())
victim=man["files"][0]
tmp=pathlib.Path(tempfile.mkdtemp())
forged=tmp/"gsd-adaptations.json"
forged.write_text(json.dumps({"schema_version":1,"adaptations":[
    {"path":victim,"phase":"36","waves":[1],"reason":"forjado para provar a recusa"}]}))
sandbox=tmp/"gsd"
shutil.copytree(root/"cairn/gsd",sandbox)
env=os.environ|{"CLAUDE_PROJECT_DIR":str(root)}
run=lambda *a: subprocess.run([sys.executable,str(root/"cairn/scripts/cairn-inventory.py"),
    "vendor","--dest",str(sandbox),"--adaptations",str(forged),
    "--cache-dir",str(root/".cairn/cache/gsd-core-v1.10.0"),*a],
    capture_output=True,text=True,env=env)
p=run()
assert p.returncode==6, f"vendor devia recusar com 6, saiu {p.returncode}: {p.stderr[:300]}"
assert victim in p.stderr, "a recusa nao nomeia o caminho registrado"
assert "--clobber-adaptations" in p.stderr, "a recusa nao nomeia a saida deliberada"
q=run("--clobber-adaptations")
assert q.returncode==0, f"com a flag o vendor devia copiar, saiu {q.returncode}: {q.stderr[:300]}"
assert "cairn-preamble" in (q.stdout+q.stderr), "o clobber nao diz como reaplicar"
print("ok")
PY</automated>
  </verify>
  <done>A byte-paridade contra o cache virou oráculo dois-sentidos com controle negativo dos dois lados; o conjunto divergente-do-cache é conferido contra o registro por comm; o vendor recusa nominalmente e só sobrescreve sob flag que diz como reaplicar. As duas armadilhas que desfariam a fase em silêncio estão fechadas ANTES dos 34 arquivos divergirem.</done>
</task>

<task type="auto">
  <name>Task 3: os 34 blocos e os 2 fragments órfãos — registrados, trocados e medidos</name>
  <files>cairn/gsd-adaptations.json, cairn/gsd/gsd-core/workflows/*.md, cairn/gsd/gsd-core/workflows/*/steps/*.md, cairn/gsd/gsd-core/workflows/discuss-phase/modes/*.md, cairn/gsd/agents/*.md, cairn/gsd/commands/gsd/discuss-phase.md, cairn/gsd/skills/gsd-discuss-phase/SKILL.md, cairn/gsd/gsd-core/references/planner-load-graph-context.md, cairn/gsd/gsd-core/workflows/discuss-phase/modes/text.md, cairn/gsd/gsd-core/workflows/plan-phase/steps/adr-ingest-express-path.md</files>
  <read_first>
    - 36-PATTERNS.md §1, a tabela "Os 34 arquivos, agrupados" — grupo, contagem e a linha do bloco em cada um; e a CORREÇÃO AO RESEARCH §1 (steps+modes são 15, não 18; os outros 3 moram em commands/, skills/ e references/)
    - 36-CONTEXT.md D-01 e D-02 — a onda zero e a fronteira de escopo que esta task decide por PADRÃO
    - 36-PATTERNS.md §7 — shim ⟺ chamada nos agentes, sem exceção: os 8 com preâmbulo são os 8 que chamam o binário
  </read_first>
  <action>
    Registrar E aplicar na MESMA task — o registro sai de vazio direto para o estado final,
    porque o oráculo da Task 2 exige divergência para todo caminho registrado.

    Registrar os 34 caminhos com bloco de preâmbulo em `cairn/gsd-adaptations.json` (grafia
    relativa à raiz vendorizada, igual a `files[]`), `waves: [1]`, `reason` curta e uniforme
    citando D-01. Nos 3 fora de workflows/agents — `commands/gsd/discuss-phase.md`,
    `skills/gsd-discuss-phase/SKILL.md`, `gsd-core/references/planner-load-graph-context.md`
    — a `reason` diz por que entram apesar de D-02: a onda zero é por PADRÃO, não por
    diretório; D-02 delimita sítio de ESTADO; e um preâmbulo não trocado vira `exit 1` na
    fase 37, sendo que o de `planner-load-graph-context.md` é carregado pelo gsd-planner.

    Rodar `cairn-preamble.sh apply` sobre os 34 (sem lista explícita: o default é o
    registro). Conferir com `check` que a saída é exit 0 e que `list` reporta os 34 na forma
    nova, zero na antiga.

    O gate de revisabilidade NÃO é mais "1 linha removida e 1 adicionada por arquivo": com as
    10 conversões acima, seis arquivos passam a ter duas linhas mudadas, e o gate por numstat
    fica mutuamente inconsistente com o gate de escopo. O numstat era proxy da propriedade
    que interessa, e a propriedade se afirma direto: TODA linha mudada sob `cairn/gsd/` cita
    o runtime antigo (a que sai), a forma nova do preâmbulo, a função de despacho, ou o
    binário do repo (as que entram). É mais forte que o numstat — proíbe qualquer edição não
    relacionada, em qualquer quantidade de linhas — e admite as conversões.

    Eliminar a categoria "menção ao runtime morto" nos arquivos em escopo — as 10 linhas que
    citam o runtime FORA de bloco de preâmbulo, medidas em 8 arquivos. Não há exceção: a fase
    37 remove o plugin, e instrução apontando runtime inexistente é armadilha herdada. A
    conversão NÃO é uniforme; classificar antes de editar, em três grupos.

    Grupo A — SEIS linhas nomeiam comando que o modelo executaria; viram chamada por
    `gsd_run` na grafia contratada (conferir em `cairn/gsd/contracts/`, não de memória):
    `plan-phase.md:595` e `execute-phase.md:425` (ambas `query config-set`);
    `execute-phase.md:1561` (`query agent.classify-failure`, verbo entregue pela fase 35);
    `quick.md:642` (`query commit` — e a menção a comando "legacy" SAI: depois da 37 não
    existe legado); `discuss-phase/modes/text.md:21` (`query config-set`) e
    `plan-phase/steps/adr-ingest-express-path.md:12` (`query commit`), os dois fragments
    órfãos, que usam `gsd_run query …` como o resto do corpus e NÃO a variável do preâmbulo,
    já que não carregam bloco.

    Grupo B — UMA linha afirma algo FALSO desde a fase 35: `verify-work.md:634` diz que
    `audit-open` só existe em CJS até ser registrado. Medido: o verbo responde pelo binário
    com exit 0. A frase inteira é reescrita para a verdade nova, não só rebatizada — trocar o
    nome do runtime e manter a afirmação errada é pior que não tocar.

    Grupo C — TRÊS comentários de design/ambiente, onde o sentido se preserva sem nomear o
    runtime morto: `execute-phase/steps/codebase-drift-gate.md:11` e
    `execute-phase/steps/executor-isolation-dispatch.md:53` e `:92`. Cada um passa a nomear o
    binário do repo (`cairn-gsd.sh`), que é do que o comentário fala.

    Os 2 fragments órfãos entram no registro com `waves: [1]` e `reason` própria dizendo que
    são citação em instrução, não preâmbulo. Registro final: 36 entradas.

    Os QUATRO arquivos FORA de escopo — `commands/gsd/plan-phase.md`,
    `skills/gsd-plan-phase/SKILL.md`, `references/execute-phase-between-wave-reset.md` e
    `references/universal-anti-patterns.md` — NÃO entram: são superfície de comando/skill e
    references, fora do escopo de massa de D-02, e nenhum carrega bloco de preâmbulo. Vão
    para o SUMMARY como lacuna MEDIDA, com nome e linha, e para o registro de divergências
    junto com a lacuna de references consolidada no plano 07.

    Daí os dois gates por nome do runtime terem valores diferentes e ambos exatos: sob
    `workflows/` e `agents/` o resultado é ZERO — a categoria foi eliminada, não excetuada —
    e na árvore inteira é QUATRO, exatamente os de fora do escopo. Um gate tree-wide em zero
    seria insatisfazível por decisão, e um gate só por marcador de bloco deixaria as 10
    menções passarem despercebidas.

    Medir o ANTES pela ferramenta canônica e registrar no SUMMARY: `cairn-inventory.sh
    closure --json` sobre o cache dá o fecho do upstream (arquivos, linhas, sites) e é o
    baseline. Medir o DEPOIS com o seam de INV-ESCOPO, que a ferramenta já expõe e os
    testes já exercitam (`tests/cairn-inventory.bats:37-46`): copiar `cairn/gsd/` para um
    diretório temporário, `git init` + commit + `git tag v1.10.0`, e rodar `closure` com
    `--source <esse repo> --expect-commit <o sha dele> --cache-dir <tmp>`. Sem esse seam a
    contagem "depois" é immensurável pela ferramenta canônica, que sempre resolve o corpus
    para o cache e exige HEAD == TAG_COMMIT. Registrar os dois números lado a lado e a razão
    do seam, no molde da regra herdada: contagem sai da ferramenta canônica ou declara por
    que não.

    Registrar também, no SUMMARY, o fato que a onda zero NÃO resolve e que a fase 07 fecha:
    trocar o preâmbulo não remove `node` do caminho executado — 7 sítios chamam `node -e`
    fora do preâmbulo (armadilha NODE-SOBREVIVE, PATTERNS: execute-phase 4, plan-phase 1,
    gsd-verifier 2).
  </action>
  <verify>
    <automated>R="$PWD" && test "$(jq -r '.adaptations | length' cairn/gsd-adaptations.json)" -eq 36 && test "$(grep -rl '_GSD_SHIM_NAME=' cairn/gsd --include='*.md' | wc -l | tr -d ' ')" -eq 0 && test "$(grep -rlF "$(bash cairn/scripts/cairn-preamble.sh --print-form | cut -c1-40)" cairn/gsd --include='*.md' | wc -l | tr -d ' ')" -eq 34 && test "$(grep -rl 'gsd-tools\.cjs' cairn/gsd --include='*.md' | wc -l | tr -d ' ')" -eq 4 && test "$(grep -rl 'gsd-tools\.cjs' cairn/gsd/gsd-core/workflows cairn/gsd/agents --include='*.md' | wc -l | tr -d ' ')" -eq 0 && test "$(grep -rh -m1 'CAIRN_GSD' $(grep -rlF "$(bash cairn/scripts/cairn-preamble.sh --print-form | cut -c1-40)" cairn/gsd --include='*.md') | sort -u | wc -l | tr -d ' ')" -eq 1 && bash cairn/scripts/cairn-preamble.sh check && test -z "$(git diff -U0 -- cairn/gsd/ | grep '^[-+]' | grep -v '^[+-][+-]' | grep -vE 'gsd-tools\.cjs|CAIRN_GSD|gsd_run|cairn-gsd')" && bats tests/cairn-vendoring.bats && bats tests/cairn-preamble.bats && T=$(mktemp -d) && cp -R cairn/gsd/. "$T/src" && git -C "$T/src" init -q && git -C "$T/src" add -A && git -C "$T/src" -c user.email=t@e -c user.name=t commit -qm m && git -C "$T/src" tag v1.10.0 && S=$(git -C "$T/src" rev-parse HEAD) && CLAUDE_PROJECT_DIR="$R" python3 cairn/scripts/cairn-inventory.py closure --source "$T/src" --expect-commit "$S" --cache-dir "$T/cache" --json | jq -e '.totals.files > 0'</automated>
  </verify>
  <done>Os 34 blocos estão na forma nova, idêntica em todos os arquivos que a carregam (uma linha só no sort -u), mais os 2 fragments órfãos e as 10 menções fora de bloco convertidas — 36 entradas registradas com motivo. Zero menção ao runtime antigo sob workflows e agentes: a categoria foi eliminada, não excetuada; restam 4 arquivos fora do escopo, medidos nominalmente para o SUMMARY. Toda linha mudada sob cairn/gsd/ cita o runtime antigo, a forma nova, a função de despacho ou o binário do repo — nenhuma edição carona. A suíte inteira está verde COM o cache presente, o que hoje significa que o oráculo dois-sentidos está de fato mordendo, não skipando. O seam de medição foi exercitado e os números antes/depois estão no SUMMARY.</done>
</task>

</tasks>

<verification>
- `bats tests/cairn-vendoring.bats` verde com o cache em `.cairn/cache/gsd-core-v1.10.0` presente (24+2 testes; os antes-skipados 20/21/22 rodando de verdade)
- `bats tests/cairn-preamble.bats` e `bats tests/cairn-command-surfaces.bats` verdes
- Suíte inteira (`bats tests/`) verde
- `cairn-preamble.sh check` exit 0; `list` reporta 34 na forma nova e 0 na antiga
- O conjunto de caminhos que divergem do cache == o conjunto de `path` do registro, 36 entradas (comm dois-sentidos, no bats)
- `cairn-inventory.py vendor` recusa com exit 6 nomeando os caminhos; com `--clobber-adaptations` copia e diz como reaplicar; o registro consultado vem do `--dest`
- Zero menção ao runtime antigo sob `cairn/gsd/gsd-core/workflows/` e `cairn/gsd/agents/` (categoria eliminada, 10 linhas convertidas); exatamente 4 arquivos remanescentes na árvore, nomeados no SUMMARY como lacuna medida
- Toda linha mudada sob `cairn/gsd/` cita o runtime antigo, a forma nova, `gsd_run` ou o binário do repo — o gate substituto do numstat 1/1
- As provas de recusa e de clobber do vendor rodam contra cópia em diretório temporário; `cairn/gsd/` nunca é destino de vendor durante a verificação
</verification>

<success_criteria>
- D-01 cumprido: o caminho que o usuário executa resolve o binário do repo, provado rodando em três posições, não declarado por grep
- VEND-BYTES e VEND-REVERT fechados na mesma onda da primeira edição — a suíte não mente quando o cache existe, e uma re-vendorização não desfaz a fase em silêncio
- PORCELAIN substituído por um gate que sobrevive ao commit
- A árvore adaptada tem um registro versionado com motivo por caminho, lido por três consumidores
</success_criteria>

<output>
Criar `.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-01-SUMMARY.md`. Registrar:
a forma exata da linha nova e a justificativa do colapso das duas variantes; a decisão de
escopo por PADRÃO para os 3 blocos fora de workflows/agents; as duas contagens (34 por
marcador, 40 por nome do runtime) com o destino de cada um dos 6 da diferença; a
classificação das 10 menções fora de bloco em três grupos, com a frase antiga e a nova de
`verify-work.md:634` lado a lado (era afirmação falsa desde a fase 35); os números
antes/depois com o comando de cada um e a razão do seam de medição; e o inventário dos
sítios `node -e` que sobrevivem à onda zero (NODE-SOBREVIVE, medido: 14 no corpus — 9 em
escopo e 5 em references), com o plano que fecha cada um.
</output>
