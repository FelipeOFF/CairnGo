---
phase: 36-workflows-steps-e-agentes-falam-bd
plan: 06
type: execute
wave: 6
depends_on: [36-05]
files_modified:
  - cairn/gsd/gsd-core/references/execute-phase-quota-recovery.md
  - cairn/gsd/agents/gsd-phase-researcher.md
  - cairn/gsd/gsd-core/references/planner-load-graph-context.md
  - cairn/gsd/agents/gsd-verifier.md
  - tests/fixtures/gsd-goldens/divergences.json
  - tests/cairn-prompt-state.bats
  - cairn/gsd-adaptations.json
autonomous: true
requirements: [ADAPT-05]
beads:
  ADAPT-05: CairnGo-zjfa
must_haves:
  truths:
    - "D-04 aplicado: o verbo de recuperação de quota não existe no universo de 87 e o sítio consumia o JSON em cinco filtros seguidos sem tolerar falha — o caminho passa a descrever recuperação MANUAL, com zero código novo, e o comportamento degradado fica escrito em vez de quebrar no meio de um pipe"
    - "o arquivo de references tocado por D-04 é uma exceção NOMEADA a D-02, registrada com motivo no registro de adaptações e no SUMMARY: D-02 mantém os 40 references fora como escopo de massa, e D-04 nomeia um deles porque o sítio quebra sem tolerância"
    - "ADAPT-05 tem os TRÊS itens decididos por escrito, nenhum por silêncio: intel (plano 05), graphify (aqui) e o verbo de recuperação de quota (aqui, por D-04)"
    - "graphify responde no binário com exit 0 e payload nomeado de indisponibilidade; os dois sítios passam a tratar essa resposta como o caso de 'siga sem contexto de grafo' que a prosa já previa para grafo ausente"
    - "os 16 agentes têm número medido e PUBLICADO: zero ocorrências nas três famílias de estado nos 16 — a adaptação deles foi inteira na onda zero, e as menções a `.planning/` que sobram são caminho de documento"
    - "os dois sítios de node do gsd-verifier têm decisão escrita e FICAM: verificam a aplicação do usuário (uma resposta de API, um export de módulo), não o runtime do GSD — convertê-los seria adaptar o projeto verificado, não a camada prompt"
    - "o saldo de NODE-SOBREVIVE publicado é o MEDIDO, não o do mapa: 14 sítios no corpus (o mapa listava 7, omitindo os 2 do fragment de isolamento de executor e os 5 de references) — 9 em escopo e 5 fora por D-02"
    - "as divergências que a fase decide NÃO corrigir estão registradas com nome de arquivo e linha: o verbo de reversão de requisito já tolerante por desenho upstream, e o descompasso de grafia do subcomando de worktree num references que esta fase não toca"
  artifacts:
    - cairn/gsd/gsd-core/references/execute-phase-quota-recovery.md
    - tests/fixtures/gsd-goldens/divergences.json
    - tests/cairn-prompt-state.bats
  key_links:
    - "sítio de recuperação de quota → sem verbo no universo → prosa de recuperação manual: o pipe de cinco filtros deixa de existir em vez de consumir vazio"
    - "os dois sítios de graphify → subcomando do binário → payload {available:false, reason} exit 0 → o ramo 'siga sem grafo' que a prosa já tinha"
    - "tabela de adaptados do oráculo ← os 16 agentes, fechando a metade de agentes da cobertura que o plano 07 termina com os workflows"
  prohibitions:
    - statement: "Nenhuma task implementa o verbo de recuperação de quota — D-04 é falha nomeada e prosa manual, com implementação futura sendo somar handler + golden"
    - statement: "Nenhuma task adapta em massa os 40 references — D-02 os mantém fora; o único tocado aqui é a exceção nomeada de D-04, mais o de contexto de grafo que já entrou no registro pela onda zero"
    - statement: "Nenhuma task converte os dois sítios de node do gsd-verifier — eles verificam o projeto do usuário; a decisão é escrita, não silenciosa"
    - statement: "Nenhuma task toca execute-phase nem o agente executor — plano 07"
---

<objective>
ADAPT-05 fechado nos dois itens que faltam, e os 16 agentes medidos e publicados.

D-04 é o item que morde: o verbo de recuperação de quota não existe no universo de 87 e o
sítio consome a saída dele em cinco filtros encadeados, sem `|| true`, sem `2>/dev/null` —
é o único sítio do corpus que quebra no meio de um pipe quando o binário responde o que já
responde hoje. A decisão travada é escrever a recuperação manual e sumir com o caminho
automático, zero código novo. Isso obriga tocar UM arquivo de references, o que D-02 deixou
fora como massa: entra como exceção nomeada, com motivo registrado, não como abertura de
escopo.

A medição dos 16 agentes é o achado barato: zero ocorrências nas três famílias de estado.
A adaptação deles aconteceu inteira na onda zero, e o número zero vai publicado com o
comando, pela regra herdada.

Purpose: fechar as decisões pendentes por escrito — nenhuma por silêncio — e deixar só
execute-phase e seu agente para o último plano.
Output: caminho de quota manual, graphify tratando a resposta real, 16 agentes medidos e
sob o oráculo, divergências registradas com nome e linha.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-CONTEXT.md
@.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-PATTERNS.md
@.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-RESEARCH.md
@.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-04 — o caminho de recuperação de quota passa a descrever recuperação manual</name>
  <files>cairn/gsd/gsd-core/references/execute-phase-quota-recovery.md, tests/fixtures/gsd-goldens/divergences.json, cairn/gsd-adaptations.json</files>
  <read_first>
    - cairn/gsd/gsd-core/references/execute-phase-quota-recovery.md inteiro — o bloco que chama o verbo, os cinco filtros que consomem a saída, e os três ramos de prosa que dependem dos valores extraídos, mais as opções manuais que o arquivo já descreve logo abaixo
    - 36-RESEARCH.md §4 — a tabela dos dois verbos fora do universo, a coluna de tolerância no sítio, e por que só um deles importa
    - 36-CONTEXT.md D-04 e D-02 — a decisão travada e a fronteira que esta task cruza por exceção nomeada
    - 36-PATTERNS.md "Falha nomeada com o comando que cria o fato" — o molde da mensagem
  </read_first>
  <behavior>
    - o bloco que chamava o verbo e os cinco filtros somem; nada no arquivo consome saída de um verbo que não existe
    - os três ramos de prosa que dependiam dos valores extraídos são substituídos por uma descrição de recuperação manual, apoiada nas opções manuais que o próprio arquivo já traz abaixo
    - o texto diz em voz alta que a escalada automática de provider não existe nesta instalação e por quê, e nomeia o que fazer no lugar
    - o contador de tentativa de quota, se ainda tiver uso fora do trecho removido, permanece; se só existia para alimentar o verbo, sai junto
    - o arquivo entra no registro de adaptações com motivo que cita D-04 e explica a exceção a D-02
  </behavior>
  <action>
    Remover o bloco de chamada e os cinco filtros encadeados que consumiam a saída, e
    reescrever os três ramos de prosa que dependiam deles. No lugar entra uma descrição
    curta de recuperação manual, ancorada nas opções manuais que o arquivo já descreve logo
    abaixo do trecho removido — não inventar procedimento novo quando o arquivo já tem um.

    O texto novo declara o motivo em uma linha: a escalada automática de provider é um verbo
    que esta instalação não serve; o comportamento degradado é seguir pelo caminho manual, e
    a implementação futura seria somar handler e golden. É exatamente o padrão de falha
    nomeada da casa aplicado a prosa em vez de a código.

    Registrar duas coisas em `tests/fixtures/gsd-goldens/divergences.json`, no schema do
    arquivo: a decisão de D-04 sobre o verbo de recuperação de quota; e, na mesma passada, as
    duas divergências que a fase conhece e decide NÃO corrigir — o verbo de reversão de
    requisito, que já é tolerante a falha por desenho upstream e por isso não quebra nada, e
    o descompasso de grafia do subcomando de worktree num references que esta fase não toca
    (o CONTEXT manda registrar quando o arquivo não é tocado, e ele não é).

    Acrescentar o caminho ao registro de adaptações com a razão da exceção nomeada: D-02
    mantém os 40 references fora como escopo de massa; este entra sozinho porque o sítio não
    tolera falha e D-04 o nomeia.
  </action>
  <verify>
    <automated>R="$PWD" && F=cairn/gsd/gsd-core/references/execute-phase-quota-recovery.md && test "$(grep -c 'resolve-execution' "$F")" -eq 0 && test "$(grep -c 'ESC_JSON\|ESCALATED\|EXHAUSTED\|ESC_FROM\|ESC_TO\|ESC_TRIED' "$F")" -eq 0 && jq -e '[.divergences[] | select(.aspect|test("quota|resolve-execution"))] | length >= 1' tests/fixtures/gsd-goldens/divergences.json && jq -e '[.divergences[] | select(.aspect|test("revert-phase|set-baseref"))] | length >= 2' tests/fixtures/gsd-goldens/divergences.json && jq -e '[.adaptations[] | select(.path=="gsd-core/references/execute-phase-quota-recovery.md")] | length == 1' cairn/gsd-adaptations.json && bats tests/cairn-gsd.bats && bats tests/cairn-vendoring.bats</automated>
  </verify>
  <done>O único sítio do corpus que quebraria no meio de um pipe não existe mais; a recuperação manual está descrita com o motivo em voz alta; a exceção a D-02 está nomeada no registro; e as duas divergências que a fase decide não corrigir estão escritas com nome e razão em vez de silêncio.</done>
</task>

<task type="auto">
  <name>Task 2: graphify — os dois sítios tratam a resposta real do binário (ADAPT-05)</name>
  <files>cairn/gsd/agents/gsd-phase-researcher.md, cairn/gsd/gsd-core/references/planner-load-graph-context.md, tests/fixtures/gsd-goldens/divergences.json, cairn/gsd-adaptations.json</files>
  <read_first>
    - cairn/gsd/agents/gsd-phase-researcher.md linhas 555-585 — o passo de contexto de grafo: a checagem de existência do arquivo de grafo, a consulta de frescor, a consulta por capacidade e o ramo final que já manda seguir sem contexto quando não há grafo
    - cairn/gsd/gsd-core/references/planner-load-graph-context.md — o mesmo passo, versão do planejador, já com o preâmbulo trocado pela onda zero
    - cairn/gsd/contracts/*.json, verbo graphify — os subcomandos declarados e a nota de que a capability foi cortada
    - 36-CONTEXT.md, discrição de ADAPT-05 — decisão explícita registrada, nunca silêncio
  </read_first>
  <behavior>
    - a consulta de frescor e a consulta por capacidade continuam sendo feitas ao binário, sem invenção de verbo
    - quando a resposta traz indisponibilidade nomeada, o passo segue sem contexto de grafo pelo ramo que a prosa já previa para grafo ausente, dizendo em uma linha por quê
    - a checagem de existência do arquivo de grafo permanece: é documento, e é ela que já evita a consulta na maioria dos casos
    - a decisão — não implementar o subsistema de grafo, tratar a indisponibilidade — fica registrada em divergences.json
  </behavior>
  <action>
    Nos dois arquivos, acrescentar ao passo de contexto de grafo o tratamento da resposta de
    indisponibilidade: a resposta traz a razão nomeada e sai com sucesso, então o passo lê a
    razão, anota-a junto da nota de frescor que a prosa já prevê, e segue pelo ramo de
    "continuar sem contexto de grafo" que já existe no fim do trecho. Nenhum verbo novo,
    nenhuma reestruturação do passo — uma condição a mais no ramo que já estava escrito.

    Registrar a decisão em `tests/fixtures/gsd-goldens/divergences.json`: o subsistema de
    grafo não é implementado no cairn; o binário responde indisponibilidade nomeada com
    sucesso; os sítios tratam. Com isso os três itens de ADAPT-05 estão escritos — intel no
    plano 05, e graphify e a recuperação de quota aqui.

    Somar a onda 6 às entradas dos dois arquivos no registro de adaptações.
  </action>
  <verify>
    <automated>R="$PWD" && for F in cairn/gsd/agents/gsd-phase-researcher.md cairn/gsd/gsd-core/references/planner-load-graph-context.md; do grep -q 'graphify status' "$F" || exit 1; grep -qi 'available' "$F" || exit 1; done && jq -e '[.divergences[] | select(.verb=="graphify")] | length >= 1' tests/fixtures/gsd-goldens/divergences.json && jq -e '[.divergences[] | select(.verb=="intel" or .verb=="graphify" or (.aspect|test("quota|resolve-execution")))] | length >= 3' tests/fixtures/gsd-goldens/divergences.json && bats tests/cairn-gsd.bats && bats tests/cairn-vendoring.bats</automated>
  </verify>
  <done>Os dois sítios de grafo tratam a resposta que o binário de fato dá, sem verbo novo e sem assumir payload; os três itens de ADAPT-05 estão decididos por escrito no arquivo de divergências.</done>
</task>

<task type="auto">
  <name>Task 3: os 16 agentes medidos, publicados e sob o oráculo</name>
  <files>cairn/gsd/agents/gsd-verifier.md, tests/cairn-prompt-state.bats, cairn/gsd-adaptations.json</files>
  <read_first>
    - 36-PATTERNS.md §7 — a tabela dos 8 agentes com preâmbulo e suas contagens de chamada, e o fato de que shim e chamada coincidem sem exceção; e a nota de que o desembrulho de prefixo de arquivo é inerte, dívida a registrar e não a corrigir
    - cairn/gsd/agents/gsd-verifier.md linhas 420-440 — os dois sítios de node, e o que cada um verifica
    - 36-03-SUMMARY.md — as três regex de família, para medir com o mesmo instrumento
    - 36-PATTERNS.md, armadilha NODE-SOBREVIVE — a lista dos sete sítios, para não confundir infra com exemplo
  </read_first>
  <behavior>
    - os 16 agentes são medidos pelas três famílias com o mesmo instrumento dos workflows; o resultado esperado é zero em todas, e o número vai publicado com o comando
    - os 16 entram na tabela de adaptados do oráculo, e é essa tabela que impede que uma leitura de estado entre num agente depois
    - os dois sítios de node do gsd-verifier permanecem, com uma linha escrita ao lado de cada um dizendo por que: verificam a aplicação do usuário, não o runtime do GSD
    - o desembrulho de prefixo de arquivo, inerte porque o binário nunca emite esse prefixo, é registrado como dívida e não corrigido
  </behavior>
  <action>
    Medir os 16 agentes pelas três famílias e publicar o número no SUMMARY com o comando.
    Encontrando zero — que é o esperado pela medição do mapa — a conclusão a registrar é que
    a adaptação dos agentes foi inteira na onda zero, e que as menções a `.planning/` que
    sobram nos agentes são caminho de documento.

    Se a medição encontrar qualquer sítio de fato, convertê-lo pelo critério já fixado nos
    planos 03 a 05, e publicar.

    Acrescentar os 16 caminhos à tabela de adaptados do oráculo. Com os agentes fechados,
    falta só a metade de workflows, que o plano 07 termina.

    No gsd-verifier, escrever ao lado dos dois sítios de node a razão de ficarem: um confere
    a resposta de uma API da aplicação verificada, o outro confere um export de módulo dela;
    são técnica de verificação do projeto do usuário, e trocá-las adaptaria o projeto
    verificado em vez da camada prompt. Registrar essa decisão no SUMMARY com o saldo MEDIDO,
    não com o número herdado do mapa: o corpus tem 14 sítios, não 7 — plan-phase 1 (fechado
    no plano 05), execute-phase 4 mais 2 no fragment de isolamento de executor (plano 07),
    gsd-verifier 2 (estes, por decisão escrita), e 5 em references (dois em checkpoints, três
    no de sondagem sem especificação), fora por D-02 e registrados como lacuna no plano 07.
    Nove em escopo, cinco fora. Recontar antes de publicar, com o comando ao lado.

    Registrar como dívida — não corrigir — o desembrulho de prefixo de arquivo que aparece em
    dez arquivos e é inerte porque o binário nunca emite esse prefixo.
  </action>
  <verify>
    <automated>R="$PWD" && cd cairn/gsd/agents && T=0 && for f in *.md; do a=$(grep -cE '(cat|grep|head|tail|wc|test -f|\[ -f)[^|]*\.planning/STATE\.md' "$f"); b=$(grep -cE 'STATE_PATH|\{state_path\}|STATE_FILE|state_raw' "$f"); c=$(grep -cE '(Update|Read|Write|Check) STATE\.md' "$f"); T=$((T+a+b+c)); done && test "$T" -eq 0 && cd "$R" && test "$(ls cairn/gsd/agents/*.md | wc -l | tr -d ' ')" -eq 16 && test "$(grep -c 'node -e' cairn/gsd/agents/gsd-verifier.md)" -eq 2 && bats tests/cairn-prompt-state.bats && bats tests/</automated>
  </verify>
  <done>Os 16 agentes estão medidos com número publicado — zero nas três famílias — e sob o oráculo, que passa a impedir regressão. Os dois sítios de node que ficam têm a razão escrita ao lado, e a dívida do desembrulho inerte está registrada em vez de corrigida por impulso.</done>
</task>

</tasks>

<verification>
- `bats tests/` inteiro verde
- Oráculo de bytes dois-sentidos verde; o registro de adaptações agora inclui a exceção nomeada de references
- `tests/fixtures/gsd-goldens/divergences.json` com os três itens de ADAPT-05 e as duas divergências não corrigidas, todas com nome de sítio
- Nenhum sítio do corpus consome a saída de um verbo fora do universo sem tolerância
</verification>

<success_criteria>
- ADAPT-05 fechado: três itens, três decisões escritas, nenhuma por silêncio
- D-04 aplicado com zero código novo e a exceção a D-02 nomeada em vez de escorregada
- Os 16 agentes medidos e publicados, inclusive o zero, e protegidos contra regressão pelo oráculo
</success_criteria>

<output>
Criar `.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-06-SUMMARY.md`. Registrar:
o texto novo do caminho de recuperação manual; as entradas acrescentadas a divergences.json
com o texto de cada uma; a medição dos 16 agentes com o comando e o número; a decisão
escrita sobre os dois sítios de node do verificador; e o saldo atualizado de NODE-SOBREVIVE.
</output>
