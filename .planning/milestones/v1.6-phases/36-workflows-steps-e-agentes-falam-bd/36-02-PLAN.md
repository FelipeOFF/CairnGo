---
phase: 36-workflows-steps-e-agentes-falam-bd
plan: 02
type: execute
wave: 2
depends_on: [36-01]
files_modified:
  - cairn/scripts/cairn-gsd-init.py
  - tests/fixtures/gsd-goldens/init-autonomous.golden.json
  - tests/fixtures/gsd-goldens/init-debug.golden.json
  - tests/fixtures/gsd-goldens/init-execute-phase.golden.json
  - tests/fixtures/gsd-goldens/init-plan-phase.golden.json
  - tests/fixtures/gsd-goldens/init-quick.golden.json
  - tests/fixtures/gsd-goldens/init-verify-work.golden.json
  - tests/fixtures/gsd-goldens/divergences.json
  - tests/cairn-gsd.bats
autonomous: true
requirements: [ADAPT-01]
beads:
  ADAPT-01: CairnGo-m6po
must_haves:
  truths:
    - "D-03 escrito e aplicado uniformemente: a composição por section_manifest + steps é PRESERVADA, e o motivo medido fica registrado no código (não só no SUMMARY) — achatar inlinaria 21 arquivos / 806 linhas (+12,4% sobre as 6.502 lidas sempre) para adaptar 10 das 38 chamadas dos fragments, com o estado em markdown 65-vs-8 a favor da raiz"
    - "os 6 sítios de cairn-gsd-init.py emitem `null`, nunca lista vazia: `null` é o valor 'degradado / lê tudo' que a camada prompt especifica (execute-phase.md:92), e uma lista vazia não é nem null nem objeto com .included — debug.md:31 declara textualmente que os dois não são a mesma coisa"
    - "os 6 goldens foram regravados EDITANDO o campo .expect.stdout, jamais por --record: o recorder executa o runtime upstream (que emite lista vazia) e trocaria provenance para recorded, mudando a comparação de FORMA para BYTES"
    - "provenance derived-from-contract e source.commit 68a04ccf8ef74803bdb651e12c3b85b218bbccdf permanecem intactos nos 6 goldens; o envelope segue validado pelo guard de schema e o de timestamp"
    - "a entrada `section-manifest-empty` de divergences.json foi REESCRITA (ela deixou de descrever o estado), não duplicada — a divergência agora é 'null em vez de manifesto preenchido', com a fase que preenche nomeada"
    - "um @test novo prova o TIPO nos 6 verbos de init: section_manifest presente e null; lista vazia reprova com mensagem nomeada"
  artifacts:
    - cairn/scripts/cairn-gsd-init.py
    - tests/fixtures/gsd-goldens/divergences.json
    - tests/cairn-gsd.bats
  key_links:
    - "cairn-gsd-init.py (6 sítios) → stdout do bundle de init → .expect.stdout dos 6 goldens (string JSON escapada) → comparação por FORMA (jq -S) do runner de cairn-gsd.bats:238-254"
    - "section_manifest null → os 21 gates literais da camada prompt ('If section_manifest is null or <id> is in its included list') → o superset seguro: todos os steps lidos"
  prohibitions:
    - statement: "Nenhuma task roda cairn-gsd-record.py sobre os cenários de init — reintroduziria o bug do upstream e trocaria a provenance (e ainda exigiria cache, node e um build do runtime)"
    - statement: "Nenhuma task preenche o manifesto com included/excluded de verdade — é incremento medido de outra fase (deferred do CONTEXT), e misturar com a correção de tipo mata a prova de qualquer um dos dois"
    - statement: "Nenhuma task achata fragments em workflow raiz nem mexe nos marcadores de seção — D-03 é preservar"
    - statement: "Nenhuma task escreve sob cairn/gsd/ — o gate desta fase é o oráculo dois-sentidos contra o cache (36-01), e nada aqui adiciona caminho ao registro de adaptações"
---

<objective>
ADAPT-01 fechado nos dois movimentos que D-03 trava: a decisão sobre fragments fica ESCRITA
com o motivo medido e aplicada uniformemente (preservar a composição), e o bug de tipo que
veio junto é corrigido — os 6 sítios que emitem lista vazia passam a emitir `null`, com os
6 goldens regravados pelo caminho certo.

A correção é de ~6 linhas e o risco inteiro está em COMO regravar o golden: o recorder da
casa grava executando o runtime upstream, que é justamente quem emite o valor errado, e
ainda trocaria a provenance de derived-from-contract para recorded — o que muda a
comparação de forma para bytes. Além disso o valor mora dentro de `.expect.stdout` como
string JSON escapada, então `jq` direto no campo não alcança.

Purpose: tirar da frente, cedo e barato, o único bug do binário que a camada prompt lê como
gate, antes que os planos 03-07 comecem a converter sítios que dependem dele.
Output: 6 sítios corrigidos com a decisão D-03 escrita ao lado, 6 goldens editados por
substring, divergences.json reescrita, e um teste que prova o tipo.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-CONTEXT.md
@.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-PATTERNS.md
@.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: os 6 sítios emitem null, com a decisão D-03 escrita ao lado</name>
  <files>cairn/scripts/cairn-gsd-init.py, tests/cairn-gsd.bats</files>
  <read_first>
    - 36-PATTERNS.md §6 inteiro — os 6 sítios com linha, os 4 fatos que decidem como regravar, a entrada de divergência que já cita a fase, e os 21 gates literais da camada prompt
    - 36-RESEARCH.md §2 — os números que fundamentam preservar (21 arquivos / 806 linhas / +12,4%; 10 de 38 chamadas; 65-vs-8) e a ressalva justa do cético sobre o efeito da lista vazia ser inferência
    - cairn/scripts/cairn-gsd-init.py linhas 560-620 e 780-860 — os 6 sítios em contexto (um em atribuição, cinco em literal de dicionário)
    - cairn/gsd/gsd-core/workflows/execute-phase.md:85,92 — a especificação do campo e o superset seguro
    - cairn/gsd/gsd-core/workflows/debug.md:31 — a declaração de que null e included vazio não são a mesma coisa
  </read_first>
  <behavior>
    - `query init.debug` → o payload tem a chave `section_manifest` e o valor é null (não lista, não ausente)
    - o mesmo para init.autonomous, init.execute-phase, init.plan-phase, init.quick, init.verify-work — os 6 verbos que emitem o campo
    - um verbo de init que NÃO emite o campo continua não emitindo (a correção não espalha a chave)
    - o teste que prova isso reprova com mensagem nomeada quando o valor volta a ser lista vazia
  </behavior>
  <action>
    Trocar os 6 sítios de `cairn-gsd-init.py` (linhas 574, 583, 614, 786, 823, 856) de lista
    vazia para `None`. Nenhuma outra mudança de payload.

    Ao lado do primeiro sítio, um comentário-decisão curto de proveniência, no molde dos
    comentários datados da casa, dizendo três coisas: (1) o valor correto é null, que a
    camada prompt define como o superset seguro — todos os steps lidos; (2) lista vazia não
    é nenhum dos dois valores especificados, e o par null-vs-included-vazio está declarado
    em debug.md:31; (3) a composição por manifesto é PRESERVADA por D-03, com o motivo
    medido em uma linha (achatar custaria 21 arquivos / 806 linhas inlinadas para adaptar 10
    de 38 chamadas, e o estado em markdown está 65-vs-8 a favor da raiz) e o preenchimento
    de verdade nomeado como incremento de outra fase. Os outros 5 sítios apontam para esse
    comentário em vez de repeti-lo.

    Em `tests/cairn-gsd.bats`, um @test tabular que roda os 6 verbos de init num fixture
    mínimo e afirma, para cada um, presença da chave e valor null, nomeando o verbo na falha.
    A liveness do teste é a própria tabela: um sétimo verbo de init sem o campo entra na
    tabela como controle negativo do lado "não espalha a chave".
  </action>
  <verify>
    <automated>R="$PWD" && test "$(grep -c 'section_manifest' cairn/scripts/cairn-gsd-init.py)" -eq 6 && test "$(grep 'section_manifest' cairn/scripts/cairn-gsd-init.py | grep -c '\[\]')" -eq 0 && T=$(mktemp -d) && cd "$T" && git init -q && mkdir -p .planning && echo '{}' > .planning/config.json && for v in init.debug init.autonomous init.quick init.verify-work; do CLAUDE_PROJECT_DIR="$T" bash "$R/cairn/scripts/cairn-gsd.sh" query "$v" | jq -e 'has("section_manifest") and .section_manifest == null' >/dev/null || exit 1; done && cd "$R" && bats tests/cairn-gsd.bats -f 'section_manifest'</automated>
  </verify>
  <done>Os 6 sítios emitem null; o payload dos verbos de init carrega a chave com o valor especificado; a decisão D-03 está escrita no código, com o número que a sustenta, e não só no documento de fase.</done>
</task>

<task type="auto">
  <name>Task 2: os 6 goldens regravados por edição de substring, e a divergência reescrita</name>
  <files>tests/fixtures/gsd-goldens/init-autonomous.golden.json, tests/fixtures/gsd-goldens/init-debug.golden.json, tests/fixtures/gsd-goldens/init-execute-phase.golden.json, tests/fixtures/gsd-goldens/init-plan-phase.golden.json, tests/fixtures/gsd-goldens/init-quick.golden.json, tests/fixtures/gsd-goldens/init-verify-work.golden.json, tests/fixtures/gsd-goldens/divergences.json, tests/cairn-gsd.bats</files>
  <read_first>
    - 36-PATTERNS.md §6, os quatro fatos verificados — em especial o (2), por que o recorder é a ferramenta errada, e o (4), o campo estar dentro de uma string JSON escapada
    - tests/cairn-gsd.bats:216-254 (o ramo recorded por bytes vs o ramo derived-from-contract por forma), :329-341 (o validador de envelope do golden), :344 (o guard de timestamp)
    - tests/fixtures/gsd-goldens/divergences.json — o schema `{aspect, cairn, family, reason, upstream, verb}` sob `.divergences`, 55 entradas, e a entrada `section-manifest-empty` que cita esta fase nominalmente
    - cairn/scripts/cairn-gsd-record.py:2-9 — a proveniência do recorder, para não ser tentado
  </read_first>
  <action>
    Nos 6 goldens, editar a substring dentro de `.expect.stdout` — o valor serializado do
    campo passa de lista vazia para null. Como o stdout é uma string JSON escapada, a edição
    é textual sobre o arquivo (a chave aparece escapada, na forma com a barra invertida antes
    das aspas), nunca `jq` sobre um caminho que não existe. `provenance`,
    `schema_version`, `scenario` e `source` ficam byte a byte como estão — a comparação
    destes goldens é por FORMA, e trocar a provenance mudaria o runner de ramo.

    Depois da edição, provar o envelope: os 5 campos de topo continuam presentes, o
    `.expect.stdout` continua parseando como JSON, e o guard de timestamp segue verde.

    Reescrever — não duplicar — a entrada `section-manifest-empty` de `divergences.json`. Ela
    hoje descreve "emitido lista vazia … a fase 36 decide se preenche"; passa a descrever o
    estado real: o campo é emitido null, o valor degradado que a camada prompt especifica
    como superset seguro; as seções vivem nos workflows vendorizados e a composição foi
    preservada por D-03; preencher included/excluded de verdade é incremento medido, fora
    desta fase. Manter o schema e a ordenação do arquivo; nenhuma entrada nova.

    Rodar a suíte inteira de goldens: os 6 cenários de init passam a comparar contra o valor
    novo e nada mais muda.
  </action>
  <verify>
    <automated>for f in tests/fixtures/gsd-goldens/init-*.golden.json; do jq -e '.provenance == "derived-from-contract" and .source.commit == "68a04ccf8ef74803bdb651e12c3b85b218bbccdf"' "$f" >/dev/null || exit 1; jq -r '.expect.stdout' "$f" | jq -e 'if has("section_manifest") then .section_manifest == null else true end' >/dev/null || exit 1; done && test "$(grep -l 'section_manifest' tests/fixtures/gsd-goldens/*.golden.json | wc -l | tr -d ' ')" -eq 6 && test "$(grep -h 'section_manifest' tests/fixtures/gsd-goldens/*.golden.json | grep -c '\[\]')" -eq 0 && jq -e '[.divergences[] | select(.aspect=="section-manifest-empty")] | length == 1' tests/fixtures/gsd-goldens/divergences.json && jq -e '.divergences | length == 55' tests/fixtures/gsd-goldens/divergences.json && bats tests/cairn-gsd.bats && bats tests/cairn-vendoring.bats</automated>
  </verify>
  <done>Os 6 goldens carregam o valor correto sem terem passado pelo recorder, com provenance e proveniência de commit intactas e envelope validado; a divergência descreve de novo o estado real do binário, com o incremento futuro nomeado; a suíte de goldens está verde.</done>
</task>

</tasks>

<verification>
- `bats tests/cairn-gsd.bats` verde offline, incluindo o teste novo de tipo
- `bats tests/` inteiro verde
- Nenhum golden mudou de provenance; `git diff` dos 6 goldens toca só a linha do campo
- Oráculo dois-sentidos do 36-01 verde e inalterado: o conjunto de caminhos divergentes do cache continua sendo exatamente as 36 entradas do registro (nada foi adaptado sob cairn/gsd/ neste plano). O gate porcelain-vazio das fases 33-35 NÃO é usado — o 36-01 o proíbe nominalmente
</verification>

<success_criteria>
- ADAPT-01 fechado: decisão escrita com motivo, aplicada uniformemente, e o bug de tipo que ela expôs corrigido
- Os 21 gates literais da camada prompt passam a ler o valor que a própria camada especifica
- A regravação dos goldens não introduziu o bug do upstream nem trocou o regime de comparação
</success_criteria>

<output>
Criar `.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-02-SUMMARY.md`. Registrar:
o caminho exato usado para editar o campo dentro da string escapada; a confirmação de que o
recorder não foi executado e por quê; o texto novo da entrada `section-manifest-empty`; e a
ressalva do cético que continua de pé — que lista vazia FAZER os steps serem pulados é
inferência sobre prosa lida por modelo, e o que se corrigiu foi o tipo, que estava errado
dos dois jeitos.
</output>
