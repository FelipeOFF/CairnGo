---
phase: 36-workflows-steps-e-agentes-falam-bd
plan: 05
type: execute
wave: 5
depends_on: [36-04]
files_modified:
  - cairn/gsd/gsd-core/workflows/plan-phase.md
  - cairn/gsd/gsd-core/workflows/plan-phase/steps/chunked-planning-mode.md
  - cairn/gsd/gsd-core/workflows/plan-phase/steps/prd-express-path.md
  - cairn/gsd/gsd-core/workflows/plan-phase/steps/stall-detection-helpers.md
  - cairn/gsd/gsd-core/workflows/verify-work.md
  - cairn/gsd/gsd-core/workflows/verify-work/steps/mvp-uat-framing.md
  - cairn/gsd/gsd-core/workflows/autonomous.md
  - cairn/gsd/gsd-core/workflows/autonomous/steps/converge-fail-fast.md
  - tests/cairn-prompt-state.bats
  - tests/fixtures/gsd-goldens/divergences.json
  - cairn/gsd-adaptations.json
autonomous: true
requirements: [ADAPT-03, ADAPT-05]
beads:
  ADAPT-03: CairnGo-z782
  ADAPT-05: CairnGo-zjfa
must_haves:
  truths:
    - "o helper que parseia TODO o JSON de init do plan-phase deixa de rodar em `node -e`: trocar o preâmbulo não removeu node do caminho executado (armadilha NODE-SOBREVIVE), e esse sítio é o mais crítico dos sete porque toda leitura de campo do bundle passa por ele"
    - "o caminho de estado deixa de ser âncora de diretório: o caminho do artefato de intel era montado a partir dele por dirname, então remover a variável sem substituir a âncora quebraria o gate de intel silenciosamente"
    - "as três leituras mecânicas do arquivo de estado em autonomous (duas idênticas mais a instrução explícita de reler) viram consulta ao binário; a métrica publica quantas eram e quantas ficaram"
    - "o predicado de existência do arquivo de estado muda de SIGNIFICADO, não só de fonte: com o bd dono do fato, 'não há estado' passa a ser 'não há portador de estado no bd', com a falha nomeada dizendo o comando que cria o fato — a mensagem antiga mandava rodar um comando que não cria mais nada"
    - "ADAPT-05, item intel: o gate de api-surface tem decisão explícita e escrita — o binário responde ao subcomando com um payload nomeado de indisponibilidade e exit 0, e o sítio passa a tratar essa resposta em vez de assumir que o artefato foi gerado"
    - "nenhum sítio adaptado passa a escrever em ROADMAP.md ou REQUIREMENTS.md por outro caminho; o que já era commit de artefato de planejamento continua como está, e é o arquivo de estado que sai das listas de commit"
  artifacts:
    - cairn/gsd/gsd-core/workflows/plan-phase.md
    - cairn/gsd/gsd-core/workflows/verify-work.md
    - cairn/gsd/gsd-core/workflows/autonomous.md
    - tests/fixtures/gsd-goldens/divergences.json
  key_links:
    - "bundle de init → helper de leitura de campo → todo campo consumido pelo plan-phase: um único ponto, e por isso o único que precisa mudar de motor"
    - "gate de intel → subcomando api-surface do binário → payload {available:false, reason} com exit 0 → o sítio decide seguir sem o artefato, em vez de consumir um payload que não veio"
    - "predicado de existência de estado → falha nomeada com o comando que cria o portador no bd (molde cairn-gsd-state.py:176-178)"
  prohibitions:
    - statement: "Nenhuma task remove a variável de caminho de estado sem substituir a âncora de diretório que dela dependia — a quebra seria silenciosa e o gate de intel some sem erro"
    - statement: "Nenhuma task implementa verbo novo no binário: ADAPT-05 aqui é DECISÃO escrita sobre um subcomando que já responde, não código"
    - statement: "Nenhuma task toca execute-phase nem agentes — planos 06 e 07"
    - statement: "Nenhuma task acrescenta ROADMAP.md ou REQUIREMENTS.md a lista de commit onde já não estavam"
---

<objective>
ADAPT-03, segunda metade, nos três workflows que sobraram antes do mais caro: `plan-phase`
(36 chamadas), `verify-work` (18) e `autonomous` (17). É aqui que a armadilha NODE-SOBREVIVE
morde de verdade — o helper que lê CADA campo do bundle de init do plan-phase roda em
`node -e`, então um teste de ponta a ponta que só validasse o preâmbulo declararia verde um
caminho que ainda quebra sem node. É aqui também que o predicado de existência de estado
muda de significado, e não só de fonte, porque com o bd dono do fato a pergunta deixa de ser
sobre um arquivo.

Junto vai o primeiro item de ADAPT-05: o gate de intel. O subcomando já responde no binário
com um payload nomeado de indisponibilidade e exit 0 — falta o sítio TRATAR essa resposta em
vez de seguir como se o artefato tivesse sido gerado.

Purpose: fechar os três workflows de custo médio e deixar o caminho crítico livre de node
antes que o plano 07 encare execute-phase, que tem quatro sítios do mesmo tipo.
Output: plan-phase, verify-work e autonomous adaptados com seus fragments; o gate de intel
decidido e escrito em divergences.json; a tabela do oráculo crescida e duas isenções do
plano 04 mortas.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-CONTEXT.md
@.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-PATTERNS.md
@.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-04-SUMMARY.md
@cairn/gsd/contracts/misc.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: plan-phase — o motor do helper de campo, a âncora de diretório, e o gate de intel (ADAPT-05)</name>
  <files>cairn/gsd/gsd-core/workflows/plan-phase.md, cairn/gsd/gsd-core/workflows/plan-phase/steps/chunked-planning-mode.md, cairn/gsd/gsd-core/workflows/plan-phase/steps/prd-express-path.md, cairn/gsd/gsd-core/workflows/plan-phase/steps/stall-detection-helpers.md, tests/fixtures/gsd-goldens/divergences.json, cairn/gsd-adaptations.json</files>
  <read_first>
    - cairn/gsd/gsd-core/workflows/plan-phase.md linhas 545-555 (o helper de leitura de campo e a variável de caminho de estado que ele alimenta), 640-660 (a seção do gate de intel e a âncora de diretório montada por dirname), 690-700 (a injeção do caminho em prompt de subagente), 1305-1355 (a seção que registra a conclusão do planejamento e o commit dos artefatos), 1485-1510 (as instruções de revisão e o checklist)
    - 36-PATTERNS.md §5b, primeira subclasse — por que a variável ali é TRANSPORTE e não leitura de fato, e por que isso muda o tratamento
    - 36-PATTERNS.md, armadilha NODE-SOBREVIVE — os sete sítios e o peso específico deste
    - 36-RESEARCH.md §4 e §7.4 — a decisão pendente de ADAPT-05 sobre o subcomando de intel
    - cairn/gsd/contracts/misc.json, verbo intel — os subcomandos declarados e a nota que aponta o sítio do universo
  </read_first>
  <behavior>
    - o helper de leitura de campo do bundle roda sem node: mesma assinatura, mesmo comportamento para campo ausente (string vazia) e para valor nulo
    - a âncora de diretório do artefato de intel para de depender da variável de caminho de estado e passa a sair do bundle de init diretamente
    - o gate de intel roda o subcomando e trata a resposta: com indisponibilidade nomeada, o passo diz por que está seguindo sem o artefato e continua; nunca consome um payload que não veio, nunca declara o artefato regenerado
    - a injeção do caminho em prompt de subagente some — é a isenção que o plano 04 abriu com contagem, e ela morre aqui
    - a seção de registro de conclusão do planejamento fala do fato registrado pelo verbo, não do arquivo de estado refletindo a fase
    - o arquivo de estado sai da lista de arquivos do commit de planejamento; os arquivos de plano e o roadmap continuam exatamente como estão
    - os três fragments são medidos pelas três famílias e convertidos só onde houver fato
  </behavior>
  <action>
    Trocar o motor do helper de leitura de campo do bundle: a mesma função passa a extrair o
    campo com a ferramenta de JSON que os workflows já usam à exaustão em outros sítios,
    preservando assinatura, o retorno vazio para campo ausente e o tratamento de nulo. Este é
    o sítio que a onda zero NÃO resolveu e o mais crítico dos sete, porque todo campo lido
    pelo plan-phase passa por ele.

    Substituir a âncora de diretório do artefato de intel: ela era montada com dirname sobre
    a variável de caminho de estado, então remover a variável sem tocar na âncora quebraria o
    gate de intel em silêncio. A âncora passa a sair do próprio bundle de init.

    Decidir e escrever ADAPT-05, item intel: o subcomando já responde no binário com exit 0 e
    um payload nomeado dizendo que a capability não está habilitada no cairn, com a razão. A
    decisão é NÃO implementar o subsistema — nenhum call site do corpus consome o artefato
    além da presença — e fazer o sítio tratar a resposta: ler a indisponibilidade, dizer em
    uma linha por que segue sem o artefato regenerado, e continuar. Registrar a decisão em
    `tests/fixtures/gsd-goldens/divergences.json` como entrada própria, no schema do arquivo,
    nomeando o sítio e a razão.

    Remover a injeção do caminho em prompt de subagente pelo mesmo tratamento fixado no plano
    04, e apagar a linha correspondente da tabela de isenções do oráculo — a conferência de
    contagem nos dois sentidos vai reprovar se a isenção sobreviver ao sítio.

    Na seção de registro de conclusão do planejamento: converter a prosa para o fato
    registrado pelo verbo, e retirar o arquivo de estado da lista de arquivos do commit.
    Deixar os arquivos de plano e o roadmap na lista exatamente como estão — o bookkeep do
    cairn é o dono do roadmap e esta task não muda quem escreve nele, só para de commitar o
    markdown de estado.

    Medir os três fragments antes de tocar; converter só fato; publicar os números,
    inclusive zeros. Somar a onda 5 às entradas correspondentes em
    `cairn/gsd-adaptations.json` e acrescentar os quatro caminhos à tabela de adaptados.
  </action>
  <verify>
    <automated>R="$PWD" && F=cairn/gsd/gsd-core/workflows/plan-phase.md && test "$(grep -c 'node -e' "$F")" -eq 0 && test "$(grep -cE 'STATE_PATH|\{state_path\}|STATE_FILE|state_raw' "$F")" -eq 0 && test "$(grep -cE '(Update|Read|Write|Check) STATE\.md' "$F")" -eq 0 && grep -q 'API_SURFACE_PATH' "$F" && test "$(grep -E 'gsd_run query commit' "$F" | grep -c 'STATE\.md')" -eq 0 && jq -e '[.divergences[] | select(.verb=="intel")] | length >= 1' tests/fixtures/gsd-goldens/divergences.json && bats tests/cairn-prompt-state.bats && bats tests/cairn-gsd.bats && bats tests/cairn-vendoring.bats</automated>
  </verify>
  <done>Nenhum sítio do plan-phase depende de node; a âncora do artefato de intel sobreviveu à remoção da variável; o gate de intel trata a indisponibilidade nomeada em vez de assumir o artefato; a isenção de injeção aberta no plano 04 morreu; o commit de planejamento não carrega mais o arquivo de estado.</done>
</task>

<task type="auto">
  <name>Task 2: verify-work — o fato de conclusão de fase e a última injeção em prompt</name>
  <files>cairn/gsd/gsd-core/workflows/verify-work.md, cairn/gsd/gsd-core/workflows/verify-work/steps/mvp-uat-framing.md, tests/cairn-prompt-state.bats, cairn/gsd-adaptations.json</files>
  <read_first>
    - cairn/gsd/gsd-core/workflows/verify-work.md linhas 100-110 (o find dos artefatos de UAT — documento), 285-295 (o caminho de escrita do artefato de UAT — documento), 500-510 (o commit do artefato), 605-620 (a transição automática que marca a fase completa no roadmap e no arquivo de estado), 705-715 (a injeção do caminho em prompt de subagente)
    - 36-PATTERNS.md §5c e §5d — o que é escrita de fato e o que é escrita de documento
    - cairn/gsd/contracts/estado.json e checagem.json — os verbos de conclusão de fase e de status de verificação disponíveis
    - 36-04-SUMMARY.md — a tabela de isenções com a linha deste arquivo e a contagem declarada
  </read_first>
  <behavior>
    - a transição automática de conclusão de fase registra o FATO pelo verbo; a atualização do roadmap continua sendo do bookkeep e o passo não passa a escrever nele por outro caminho
    - a injeção do caminho de estado em prompt de subagente some, e a isenção correspondente do oráculo morre junto
    - o find dos artefatos de UAT, o caminho de escrita do artefato e o commit dele permanecem: documento
    - o fragment é medido pelas três famílias, com número publicado
  </behavior>
  <action>
    Converter a transição automática de conclusão: a prosa que hoje descreve marcar a fase
    completa no roadmap e no arquivo de estado passa a nomear o verbo que registra o fato, e
    diz explicitamente que a projeção para o roadmap é do bookkeep — a regra de integração do
    CONTEXT proíbe que um workflow adaptado passe a escrever nesse arquivo por outra porta, e
    a forma mais fácil de violá-la é "melhorar" essa linha.

    Remover a injeção do caminho em prompt de subagente pelo tratamento do plano 04, e apagar
    a segunda linha da tabela de isenções — com as duas mortas, a subclasse invisível está
    zerada no corpus dos workflows.

    Não tocar no find dos artefatos de UAT, no caminho de escrita nem no commit deles: são
    documento, e a proibição de converter documento vale aqui como valeu no plano 03.

    Medir o fragment, converter só fato, publicar o número. Somar a onda 5 às entradas e
    acrescentar os dois caminhos à tabela de adaptados.
  </action>
  <verify>
    <automated>R="$PWD" && F=cairn/gsd/gsd-core/workflows/verify-work.md && test "$(grep -cE 'STATE_PATH|\{state_path\}|STATE_FILE|state_raw' "$F")" -eq 0 && test "$(grep -cE '(Update|Read|Write|Check) STATE\.md' "$F")" -eq 0 && test "$(grep -cE '(cat|grep|head|tail|wc|test -f|\[ -f)[^|]*\.planning/STATE\.md' "$F")" -eq 0 && grep -q 'UAT\.md' "$F" && bats tests/cairn-prompt-state.bats && bats tests/cairn-vendoring.bats</automated>
  </verify>
  <done>A conclusão de fase é registrada como fato pelo verbo, sem que o workflow passe a escrever no roadmap; a última injeção de caminho em prompt de subagente do corpus de workflows morreu, junto com sua isenção; os artefatos de UAT seguem tratados como documento.</done>
</task>

<task type="auto">
  <name>Task 3: autonomous — três leituras mecânicas e um predicado que muda de significado</name>
  <files>cairn/gsd/gsd-core/workflows/autonomous.md, cairn/gsd/gsd-core/workflows/autonomous/steps/converge-fail-fast.md, tests/cairn-prompt-state.bats, cairn/gsd-adaptations.json</files>
  <read_first>
    - cairn/gsd/gsd-core/workflows/autonomous.md linhas 95-130 (os campos parseados do bundle, o predicado de existência de estado e a mensagem de erro que ele dispara), 135-145 e 610-625 (as três leituras mecânicas do arquivo de estado, duas idênticas mais a instrução explícita de reler), 485-495 e 535-550 (as duas seções que mandam anexar ou atualizar uma seção do arquivo de estado), 680-690 e 735-745 (os caminhos de auditoria de milestone e de roadmap arquivado — documento), 810-820 (o checklist)
    - 36-PATTERNS.md §5a e §5b, terceira subclasse — por que o predicado muda de significado e não só de fonte
    - 36-PATTERNS.md "Falha nomeada com o comando que cria o fato" — o molde da mensagem nova
    - cairn/gsd/contracts/estado.json — o verbo que cria o portador de estado no bd, cujo nome entra na mensagem
  </read_first>
  <behavior>
    - as três leituras mecânicas viram consulta ao binário; a instrução de reler o estado passa a mandar reconsultar, não recatar o arquivo
    - o predicado de existência deixa de ser sobre o arquivo: quando não há portador de estado no bd, a mensagem nomeia esse fato e o comando exato que o cria — e não manda mais rodar o comando antigo, que não cria mais nada
    - as duas seções que mandam anexar ou atualizar uma seção do arquivo de estado passam a registrar o fato pelo verbo correspondente
    - os caminhos de auditoria de milestone e de roadmap arquivado permanecem: documento
    - o item de checklist sobre bloqueios passa a descrever a consulta ao bd
    - o fragment é medido e publicado
  </behavior>
  <action>
    Converter as três leituras mecânicas para a consulta ao binário. As duas primeiras são o
    mesmo padrão repetido em pontos distantes do arquivo — converter uma e esquecer a outra é
    o erro provável, e o oráculo é quem pega.

    Reescrever o predicado de existência de estado e sua mensagem. Este é o único sítio da
    fase em que a semântica muda, e a mudança precisa estar escrita: com o bd dono do fato, a
    pergunta "o arquivo existe?" vira "existe portador de estado?", e a resposta negativa vem
    com o comando que cria o portador, no molde da falha nomeada da casa. Manter o campo do
    bundle com o nome que tem — o bundle é contrato da fase 34 e não muda aqui; o que muda é
    o que o workflow faz com ele.

    Converter as duas seções de anexar/atualizar seção do arquivo de estado para o verbo que
    registra o fato correspondente, sem inventar verbo: se o vocabulário atual não cobrir
    exatamente o registro pedido, usar o verbo de atualização de estado com a chave adequada
    e registrar em divergences.json a distância, em vez de improvisar escrita em markdown.

    Não tocar nos caminhos de auditoria de milestone nem de roadmap arquivado. Atualizar o
    item de checklist. Medir o fragment, converter só fato, publicar o número. Somar a onda 5
    e acrescentar os dois caminhos à tabela de adaptados.
  </action>
  <verify>
    <automated>R="$PWD" && F=cairn/gsd/gsd-core/workflows/autonomous.md && test "$(grep -cE '(cat|grep|head|tail|wc|test -f|\[ -f)[^|]*\.planning/STATE\.md' "$F")" -eq 0 && test "$(grep -cE '(Update|Read|Write|Check) STATE\.md' "$F")" -eq 0 && test "$(grep -c 'STATE_CONTENT' "$F")" -eq 0 && grep -q 'state_exists' "$F" && bats tests/cairn-prompt-state.bats && bats tests/cairn-gsd.bats && bats tests/</automated>
  </verify>
  <done>As três leituras mecânicas somem; o predicado de existência pergunta pelo portador de estado e a falha nomeia o comando que o cria; as duas escritas de seção viraram fato registrado por verbo, com qualquer distância registrada em divergences.json; documento intacto.</done>
</task>

</tasks>

<verification>
- `bats tests/` inteiro verde
- Oráculo de bytes dois-sentidos verde; caminhos divergentes == registro de adaptações
- Tabela de isenções do oráculo sem nenhuma linha de injeção em prompt de subagente para workflows — as duas abertas no plano 04 morreram e a conferência dois-sentidos prova
- `grep -c 'node -e'` em plan-phase.md igual a zero. Saldo medido do corpus: 14 sítios no total — plan-phase 1 (fechado aqui), execute-phase 4 e execute-phase/steps/executor-isolation-dispatch.md 2 (plano 07), gsd-verifier 2 (decisão escrita no plano 06), references/checkpoints.md 2 e references/specless-probe-fallback.md 3 (fora por D-02, registrados como lacuna no plano 07). O mapa da fase listava 7 e omitia os 2 do fragment de isolamento e os 5 de references
</verification>

<success_criteria>
- ADAPT-03 fechado: os cinco workflows do bead (discuss-phase, plan-phase, verify-work, quick, autonomous) sob o critério
- ADAPT-05 com o primeiro dos três itens decidido, escrito em divergences.json e com o sítio tratando a resposta real do binário
- NODE-SOBREVIVE reduzida ao que os planos 06 e 07 fecham, com o sítio mais crítico já resolvido, e a contagem corrigida para o número medido (14 no corpus, 9 em escopo) em vez dos 7 herdados do mapa
</success_criteria>

<output>
Criar `.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-05-SUMMARY.md`. Registrar:
o motor novo do helper de leitura de campo e como a âncora de diretório foi preservada; o
texto da decisão de intel e a entrada nova em divergences.json; a mensagem nova do predicado
de existência de estado, palavra por palavra; os números por arquivo e por fragment; e o
estado da tabela de isenções depois das duas mortes.
</output>
