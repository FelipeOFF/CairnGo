---
phase: 36-workflows-steps-e-agentes-falam-bd
plan: 03
type: execute
wave: 3
depends_on: [36-02]
files_modified:
  - tests/cairn-prompt-state.bats
  - tests/cairn-command-surfaces.bats
  - cairn/gsd/gsd-core/workflows/fast.md
  - cairn/gsd/gsd-core/workflows/debug.md
  - cairn/gsd-adaptations.json
autonomous: true
requirements: [ADAPT-02]
beads:
  ADAPT-02: CairnGo-0yzd
must_haves:
  truths:
    - "a métrica de 'zero leitura de .planning/ como fonte de estado' NÃO é grep por nome de arquivo: o oráculo tem TRÊS famílias — leitura mecânica de estado, estado por variável (STATE_PATH / {state_path} / STATE_FILE / state_raw), e prosa imperativa sobre o arquivo de estado — porque 12 linhas medidas referenciam estado sem citar o nome do arquivo"
    - "`state_exists` fica FORA da família B por decisão escrita: é campo do bundle de init, fato que já vem do binário e contrato da fase 34 — pô-lo na família obrigaria quatro isenções permanentes (autonomous 2, execute-phase 2) para descrever comportamento correto, e isenção que nunca morre é ruído que treina a ignorar a tabela"
    - "leitura de DOCUMENTO continua legítima e não é convertida: PROJECT.md, REQUIREMENTS.md, ROADMAP.md, CONTEXT.md e os arquivos de sessão sob .planning/debug/ são documento, não fato (PATTERNS §5d)"
    - "cada isenção da família 'estado por variável' é declarada NA TABELA do oráculo com caminho, padrão, contagem e motivo escrito; uma isenção cuja contagem não bate mais também reprova — isenção morta é mentira igual a sítio não convertido"
    - "fast.md não pergunta mais ao markdown para decidir se chama o binário: o grep na tabela de quick tasks some e a chamada fala direto, com a falha nomeada do verbo aparecendo em vez de ser engolida"
    - "debug.md tem número medido e PUBLICADO mesmo sendo zero: as 13 menções a .planning/ do arquivo são caminhos de documento de sessão (.planning/debug/*.md), não estado — a conversão correta é nenhuma, e a regra herdada manda publicar o zero em vez de omitir"
    - "o par null-vs-included-vazio declarado em debug.md:31 sobrevive intacto à adaptação — é a especificação que o plano 36-02 acabou de honrar no binário"
  artifacts:
    - tests/cairn-prompt-state.bats
    - cairn/gsd/gsd-core/workflows/fast.md
  key_links:
    - "tabela do oráculo (arquivo × família × isenções) → mesmo laço apontado para uma cópia forjada = controle negativo; molde assert_cut_holds:422-448 + :465-474"
    - "fast.md → gsd_run quick-tasks-append → falha nomeada exit 1 do verbo quando a tabela não existe: o predicado saiu do markdown e virou resposta do binário"
    - "cairn/gsd-adaptations.json → os dois caminhos ganham a onda 3 em waves[]; o oráculo de bytes do 36-01 segue exigindo divergência para eles"
  prohibitions:
    - statement: "Nenhuma task converte leitura de documento (PROJECT/REQUIREMENTS/ROADMAP/CONTEXT/.planning/debug/*.md) — converter documento em verbo é regressão, não adaptação"
    - statement: "Nenhuma task faz um workflow adaptado passar a ESCREVER em ROADMAP.md ou REQUIREMENTS.md por outro caminho — o bookkeep do cairn é o dono (integration point do CONTEXT)"
    - statement: "Nenhuma task mede cobertura por grep de nome de arquivo — a métrica de uma família só declara verde uma cobertura que não existe (discrição travada no CONTEXT)"
    - statement: "Nenhuma task toca execute-phase, plan-phase, quick, discuss-phase, verify-work, autonomous ou agentes — cada um tem seu plano; misturar some com a medição por arquivo"
---

<objective>
ADAPT-02, a fatia barata que estabelece a REGRA de medição da fase inteira. Primeiro nasce o
oráculo que decide, por máquina, o que conta como "zero leitura de `.planning/` como fonte
de estado" — e ele não pode ser um grep por nome de arquivo, porque 12 linhas medidas
referenciam estado por variável e escapariam, declarando cobertura inexistente. Depois os
dois workflows mais baratos passam por ele: `fast.md`, cujo caso é o mais claro do corpus
(um `grep` no markdown decide se o binário é chamado, com a chamada na linha seguinte), e
`debug.md`, cujo resultado medido é ZERO conversões — as 13 menções a `.planning/` são
caminhos de documento de sessão, e a regra herdada manda publicar o zero em vez de omiti-lo.

Purpose: fixar a métrica antes de gastá-la nos seis workflows caros, com dois arquivos de
custo mínimo servindo de primeiro consumidor e de prova de que o oráculo morde os dois
lados.
Output: tests/cairn-prompt-state.bats com as três famílias e isenções com motivo, fast.md
convertido, debug.md medido e declarado, registro de adaptações atualizado.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-CONTEXT.md
@.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-PATTERNS.md
@.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-RESEARCH.md
@.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: o oráculo de estado — três famílias, tabela por arquivo, isenções com motivo, controle negativo</name>
  <files>tests/cairn-prompt-state.bats, tests/cairn-command-surfaces.bats</files>
  <read_first>
    - 36-PATTERNS.md §5 inteiro — os quatro padrões (a) leitura mecânica, (b) estado por variável com as TRÊS subclasses, (c) escrita/prosa imperativa, (d) leitura legítima de documento; e o bloco-armadilha de discuss-phase.md:238-240, onde três `cat` no mesmo fence têm semânticas diferentes e só o terceiro é fato
    - 36-PATTERNS.md "No Analog Found", linha do teste de conteúdo semântico de prompt — por que o único teste de conteúdo `.md` existente afirma o inverso, e qual é o substituto
    - tests/cairn-vendoring.bats:420-448 e :465-474 — o molde do oráculo tabular com raiz parametrizada e sua metade de liveness
    - tests/helpers.bash:31-32 — CAIRN_REPO_ROOT / CAIRN_SCRIPTS_DIR, a única forma de resolver caminho no harness
  </read_first>
  <behavior>
    - família A (leitura mecânica de fato): um comando de shell — cat, grep, head, tail, wc, `test -f`, `[ -f` — cujo argumento é o arquivo de estado sob `.planning/`. Zero ocorrências permitidas em arquivo adaptado
    - família B (estado por variável): as grafias STATE_PATH, {state_path}, STATE_FILE, state_raw — e NÃO state_exists, que é campo do bundle e portanto fato vindo do binário. Cada ocorrência remanescente das quatro grafias exige linha própria na tabela de isenções com caminho, padrão, contagem exata e motivo
    - um caso de teste prova a exclusão: uma linha que consome o campo de existência do bundle não casa a família B; se casasse, quatro sítios corretos (dois em autonomous, dois em execute-phase) exigiriam isenção permanente nos planos 05 e 07
    - família C (prosa imperativa sobre o arquivo de estado): verbo de instrução seguido do nome do arquivo de estado. Zero ocorrências permitidas em arquivo adaptado
    - leitura de documento não é família nenhuma: PROJECT.md, REQUIREMENTS.md, ROADMAP.md, CONTEXT.md e `.planning/debug/*.md` não casam com nenhum dos três padrões, e existe um caso de teste que prova isso (senão o oráculo empurraria regressão)
    - isenção morta reprova: contagem declarada diferente da contagem medida falha nomeando o caminho, o padrão, o esperado e o encontrado — nos DOIS sentidos
    - controle negativo: numa cópia forjada de um arquivo adaptado, com um sítio de cada família reinjetado, o MESMO laço morde os três e nomeia cada um
    - a tabela cobre só os arquivos declarados adaptados; um arquivo ainda não adaptado não é testado (a tabela cresce plano a plano e fecha no 36-07)
  </behavior>
  <action>
    Criar `tests/cairn-prompt-state.bats` — a suíte nova, sem tocar `tests/helpers.bash` (é
    carregado por trinta suítes; o precedente da casa é builder local por arquivo).

    Cabeçalho do arquivo declarando o que a suíte é e por que ela não existia: o único teste
    de conteúdo `.md` sob `cairn/gsd/` até a fase 36 era a byte-paridade contra o upstream, e
    ela afirma o oposto do que esta fase faz; esta suíte é a metade semântica, e a métrica
    dela é deliberadamente de TRÊS famílias porque a de uma família — nome de arquivo — dá
    verde com 12 linhas de estado por variável de pé.

    Uma função-oráculo `assert_no_state_facts <root> <path> ...` que recebe a RAIZ como
    argumento (para o controle negativo apontar o mesmo laço para uma árvore forjada), aplica
    as três famílias de regex e nomeia cada residuo em stderr com arquivo, linha e família.

    Uma tabela de ADAPTADOS e uma tabela de ISENÇÕES, ambas como dados no topo do arquivo, no
    formato tabular da casa (uma linha por registro, campos separados por barra vertical). A
    de isenções carrega caminho, padrão, contagem esperada e motivo, e é conferida nos dois
    sentidos: contagem menor que a declarada é isenção morta (o sítio sumiu e ninguém
    apagou a linha), contagem maior é sítio novo entrando pela porta dos fundos.

    Um caso de teste dedicado ao falso-positivo: um fixture sintético com leituras legítimas
    de documento nas cinco formas listadas no behavior, MAIS uma linha consumindo o campo de
    existência do bundle, provando que o oráculo NÃO morde nem documento nem fato vindo do
    binário. Sem ele, a suíte empurraria a conversão de documento em verbo, que o CONTEXT
    proíbe como regressão, e obrigaria quatro isenções permanentes nos planos 05 e 07.

    Um caso de teste de completude parcial: cada caminho da tabela de adaptados existe no
    disco e está registrado em `cairn/gsd-adaptations.json`. O fecho total — a tabela cobrir
    os 8 workflows e os 16 agentes — é do plano 36-07 e fica anotado em comentário aqui.

    Se `cairn-preamble.sh` já tiver entrado no guard de alcançabilidade, nada a fazer em
    `tests/cairn-command-surfaces.bats`; caso a suíte nova precise de algum script auxiliar,
    ele entra no guard com razão escrita, no mesmo molde.
  </action>
  <verify>
    <automated>R="$PWD" && bats tests/cairn-prompt-state.bats && bats tests/cairn-prompt-state.bats -f 'negativ' | grep -q '^ok 1' && bats tests/cairn-prompt-state.bats -f 'documento' | grep -q '^ok 1' && bats tests/cairn-prompt-state.bats -f 'bundle' | grep -q '^ok 1' && test "$(bats tests/cairn-prompt-state.bats --count)" -ge 5</automated>
  </verify>
  <done>O oráculo existe, tem as três famílias medidas, uma tabela de adaptados e uma de isenções com motivo conferidas nos dois sentidos, um caso que prova que documento não é mordido, e o controle negativo forjado que prova que as três famílias mordem de verdade.</done>
</task>

<task type="auto">
  <name>Task 2: fast.md — o predicado sai do markdown e vira resposta do binário</name>
  <files>cairn/gsd/gsd-core/workflows/fast.md, cairn/gsd-adaptations.json, tests/cairn-prompt-state.bats</files>
  <read_first>
    - cairn/gsd/gsd-core/workflows/fast.md, linhas 60-115 — os três sítios medidos: a prosa condicional em :66, o bloco de detecção em :76-77 com a chamada já convertida logo abaixo em :78, e o item de checklist em :108
    - 36-PATTERNS.md §5a — por que este é o caso mais claro do corpus: o workflow pergunta ao markdown para decidir se chama o binário
    - cairn/gsd/contracts/*.json, verbo quick-tasks-append — o contrato e a falha nomeada que ele já dá quando a tabela não existe (exit 1 com a razão)
    - 36-PATTERNS.md "Falha nomeada com o comando que cria o fato" — a doutrina que substitui o predicado em markdown
  </read_first>
  <behavior>
    - o bloco de detecção some: a chamada ao verbo é incondicional e a resposta do binário é o predicado
    - a falha do verbo aparece nomeada, com a razão que ele emite, em vez de ser trocada por um aviso genérico ou engolida
    - a prosa condicional e o item de checklist deixam de descrever o arquivo de estado como a coisa consultada e passam a descrever o fato pedido ao binário
    - o oráculo passa a listar fast.md como adaptado, com zero isenções
    - o ponta a ponta que ADAPT-02 pede é provado RODANDO, não por grep: extrair do próprio fast.md a linha de preâmbulo e o comando do verbo, executá-los num repositório de fixture com portador de estado no bd, e exigir exit 0 — é o caminho que o usuário executa, e é ele que o incidente de estado obsoleto atravessava
  </behavior>
  <action>
    Reescrever os três sítios de `fast.md`. O bloco em fence perde a linha de detecção e o
    condicional inteiro: a chamada ao verbo de append passa a ser a única instrução, e o
    tratamento de falha reproduz a razão que o verbo emite — a doutrina da casa é falha
    nomeada com o comando que cria o fato, nunca fallback markdown, nunca aviso genérico que
    esconde a razão.

    A prosa condicional acima do bloco e o item de checklist ao final passam a falar do FATO
    (a linha de tarefa rápida registrada) em vez do arquivo consultado, e nomeiam o verbo.
    Nenhuma outra linha do arquivo muda; a linha de preâmbulo já trocada pelo 36-01 fica como
    está.

    Acrescentar `gsd-core/workflows/fast.md` à tabela de adaptados do oráculo, sem isenção, e
    somar a onda 3 ao `waves[]` da entrada já existente em `cairn/gsd-adaptations.json`.

    Fechar o "verdes de ponta a ponta" que ADAPT-02 exige com uma rodada REAL, não com grep:
    montar um repositório de fixture com portador de estado no bd, extrair do próprio
    fast.md a linha de preâmbulo e a linha do verbo, executá-las em sequência e exigir exit
    0. Extrair do arquivo, e não reescrever o comando no teste, é o que faz a prova valer:
    um teste que digita o comando à mão passaria mesmo com o arquivo errado.
  </action>
  <verify>
    <automated>R="$PWD" && F=cairn/gsd/gsd-core/workflows/fast.md && test "$(grep -cE '(cat|grep|head|tail|wc|test -f|\[ -f)[^|]*\.planning/STATE\.md' "$F")" -eq 0 && test "$(grep -cE '(Update|Read|Write|Check) STATE\.md' "$F")" -eq 0 && test "$(grep -cE 'STATE_PATH|\{state_path\}|STATE_FILE|state_raw' "$F")" -eq 0 && grep -q 'quick-tasks-append' "$F" && jq -e '[.adaptations[] | select(.path=="gsd-core/workflows/fast.md") | .waves | index(3)] | length == 1' cairn/gsd-adaptations.json && T=$(mktemp -d) && git -C "$T" init -q && mkdir -p "$T/.planning" && echo '{}' > "$T/.planning/config.json" && (cd "$T" && bd init --prefix e2e >/dev/null 2>&1 || true) && PRE=$(grep -m1 'CAIRN_GSD' "$R/$F") && CMD=$(grep -m1 'quick-tasks-append' "$R/$F") && (cd "$T" && env -u CAIRN_GSD CLAUDE_PROJECT_DIR="$T" bash -c "$PRE"'; gsd_run query state.begin-phase 1 >/dev/null 2>&1; TASK=e2e; '"$CMD") && bats tests/cairn-prompt-state.bats && bats tests/cairn-vendoring.bats</automated>
  </verify>
  <done>fast.md não consulta mais o markdown para decidir chamar o binário; a falha do verbo chega ao usuário com a razão; o oráculo cobre o arquivo com zero isenções e a byte-paridade dois-sentidos segue verde. O ponta a ponta de ADAPT-02 está provado RODANDO: preâmbulo e verbo extraídos do próprio arquivo, executados num repositório de fixture, exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: debug.md — o número medido é zero, e o zero vai publicado</name>
  <files>cairn/gsd/gsd-core/workflows/debug.md, tests/cairn-prompt-state.bats, cairn/gsd-adaptations.json</files>
  <read_first>
    - cairn/gsd/gsd-core/workflows/debug.md — as 13 linhas que citam `.planning/`: :40, :71, :90, :107, :149, :151, :191, :196, :206, :241, :242, :246 e a nota de :28; classificar cada uma antes de tocar qualquer coisa
    - 36-PATTERNS.md §5d — leitura legítima de documento, a classe que NÃO muda
    - cairn/gsd/gsd-core/workflows/debug.md:28 e :31 — `debug_dir` já vem do bundle do binário, e :31 é a declaração de que null e included vazio não são a mesma coisa, honrada pelo plano 36-02
    - 36-CONTEXT.md, Specific Ideas — a regra herdada de publicar o número mesmo quando for zero
  </read_first>
  <behavior>
    - as 12 linhas com `.planning/debug/...` são caminho de arquivo de sessão de depuração: documento, não fato — nenhuma vira verbo
    - `debug_dir` continua vindo do bundle de init, que já é o binário; a nota de :28 sobre montar o caminho a partir dele em vez de literal continua valendo e fica intacta
    - a declaração do par null-vs-included-vazio em :31 permanece byte a byte
    - o oráculo lista debug.md como adaptado e mede zero residuo nas três famílias; o número de conversões — zero — é publicado no SUMMARY com o comando que o produziu
  </behavior>
  <action>
    Classificar as 13 menções uma a uma e registrar a classificação no SUMMARY. A medição
    esperada é que todas sejam documento de sessão, e nesse caso a conversão correta é
    NENHUMA: a adaptação de `debug.md` já aconteceu na onda zero (o preâmbulo) e o resto do
    arquivo nunca leu estado do markdown.

    Se — e somente se — a classificação encontrar um sítio que é fato, convertê-lo pelo mesmo
    critério do fast.md e registrar. Nada de converter documento para engordar o número.

    Acrescentar `gsd-core/workflows/debug.md` à tabela de adaptados do oráculo. Se a
    classificação confirmar zero, o arquivo entra sem isenção — e o teste passa a ser a prova
    permanente de que nenhuma leitura de estado entrou depois.

    Somar a onda 3 ao `waves[]` da entrada de debug.md em `cairn/gsd-adaptations.json` apenas
    se o arquivo tiver sido de fato editado nesta task; sendo zero conversões, a entrada fica
    como o 36-01 deixou, e o SUMMARY diz isso em voz alta.

    Publicar o número no SUMMARY com o comando: sítios de estado convertidos em debug.md, e
    a razão — as menções a `.planning/` do arquivo são caminho de documento. O roadmap orçou
    esta fase por contagem de `.planning/` por workflow (debug: 13, a segunda maior), e um
    SUMMARY que omitisse o zero deixaria a próxima fase pensando que sobrou trabalho.
  </action>
  <verify>
    <automated>R="$PWD" && F=cairn/gsd/gsd-core/workflows/debug.md && test "$(grep -cE '(cat|grep|head|tail|wc|test -f|\[ -f)[^|]*\.planning/STATE\.md' "$F")" -eq 0 && test "$(grep -cE '(Update|Read|Write|Check) STATE\.md' "$F")" -eq 0 && test "$(grep -c 'section_manifest' "$F")" -ge 1 && bats tests/cairn-prompt-state.bats && bats tests/cairn-vendoring.bats && bats tests/</automated>
  </verify>
  <done>As 13 menções de debug.md estão classificadas uma a uma; o arquivo está sob o oráculo com zero residuo nas três famílias; o número de conversões — inclusive sendo zero — está publicado com o comando que o mediu.</done>
</task>

</tasks>

<verification>
- `bats tests/cairn-prompt-state.bats` verde, com o controle negativo forjado e o caso de falso-positivo de documento
- `bats tests/` inteiro verde
- Oráculo de bytes dois-sentidos do 36-01 verde: os caminhos divergentes do cache continuam sendo exatamente os registrados
- fast.md e debug.md na tabela de adaptados; isenções, se houver, com motivo escrito e contagem conferida nos dois sentidos
</verification>

<success_criteria>
- ADAPT-02 fechado: fast e debug sob o critério, sem leitura nem escrita de `.planning/` como fonte de estado, com o preâmbulo já apontando o binário desde a onda zero
- A métrica da fase existe, é de três famílias, e morde nos dois sentidos — nenhum plano seguinte pode declarar cobertura por grep de nome de arquivo
- O zero de debug.md está publicado, não omitido
</success_criteria>

<output>
Criar `.planning/phases/36-workflows-steps-e-agentes-falam-bd/36-03-SUMMARY.md`. Registrar:
as três regex de família exatamente como ficaram; a classificação linha a linha das 13
menções de debug.md; os números de conversão de cada arquivo com o comando; e a lista de
isenções abertas, se alguma, com o plano que as fecha.
</output>
