---
phase: 33-o-binario-python-familias-triviais
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cairn/scripts/cairn-gsd.py
  - cairn/scripts/cairn-gsd.sh
  - cairn/scripts/cairn-gsd-record.py
  - cairn/scripts/cairn-gsd-record.sh
  - tests/cairn-gsd.bats
  - tests/fixtures/gsd-goldens/scenarios.json
  - tests/fixtures/gsd-goldens/*.golden.json
  - tests/fixtures/gsd-goldens/divergences.json
autonomous: true
requirements: [TRIV-01, TRIV-04]
beads:
  TRIV-01: CairnGo-basl
  TRIV-04: CairnGo-lcca
must_haves:
  truths:
    - "`cairn/scripts/cairn-gsd.sh query config-get <chave>` responde num repo fixture com o valor e o exit code do contrato config.json da fase 31 — 0 quando resolve (ou --default/schema default aplica), 1 para CONFIG_KEY_NOT_FOUND sem default (D-01, TRIV-01)"
    - "config-get resolve na cadeia do contrato — config do escopo, depois --default, depois default do schema vindo do manifest de config resolvido em cadeia declarada: cairn/gsd/gsd-core/config-defaults.manifest.json se existir (pós-fase-32), senão o config-defaults.manifest.json do clone em cache verificado por commit, senão falha nomeada pedindo cache/rede — e quando a chave existe em mais de uma camada vence a primeira (TRIV-01)"
    - "config-set com valor rejeitado deixa .planning/config.json byte-idêntico ao anterior; config-set repetido com o mesmo valor é idempotente — a segunda gravação não muda um byte"
    - "verbo do universo dos 87 cuja família está fora das 5 triviais morre com mensagem nomeando a família e a fase que a entrega, nunca resposta vazia; verbo fora do universo morre exit 2 (uso do dispatcher)"
    - "os goldens versionados em tests/fixtures/gsd-goldens/ carregam provenance declarada (recorded ou derived-from-contract) mais source{repo,tag,commit} da fase 31; o diferencial bats roda verde offline e um golden adulterado reprova (D-02, TRIV-04)"
    - "o --record é skip-gated na ordem cache+HEAD verificado, node no PATH, build do runtime ok; gravação interrompida ou falha não deixa golden parcial — escrita em temp com rename atômico por golden (D-02)"
    - "um cenário do diferencial cobre valor não-ASCII sem escape unicode na saída python; a ordem de chaves do stdout JSON é estável e toda divergência deliberada do upstream está na tabela de divergências da fase (tests/fixtures/gsd-goldens/divergences.json)"
    - statement: "duas execuções concorrentes de config-set não corrompem .planning/config.json — o arquivo termina JSON válido"
      verification: backstop
  artifacts:
    - cairn/scripts/cairn-gsd.py
    - cairn/scripts/cairn-gsd.sh
    - cairn/scripts/cairn-gsd-record.py
    - cairn/scripts/cairn-gsd-record.sh
    - tests/cairn-gsd.bats
    - tests/fixtures/gsd-goldens/scenarios.json
    - tests/fixtures/gsd-goldens/divergences.json
  key_links:
    - "cairn-gsd.py roteia por spellings[] derivados de cairn/gsd/contracts/contracts.json .verbs — o contrato é a fonte do roteamento, não uma tabela paralela"
    - "tests/cairn-gsd.bats itera tests/fixtures/gsd-goldens/scenarios.json e compara o envelope inteiro (stdout, exit code, stderr quando o cenário registrar) contra o golden do cenário"
    - "cairn-gsd-record.py usa o mesmo cache .cairn/cache/gsd-core-v1.10.0 com a mesma verificação de HEAD de cairn-inventory.py — forma copiada, módulo nunca importado"
  prohibitions:
    - statement: "Nenhum golden declara provenance recorded sem ter saído de execução real do gsd-tools.cjs do clone verificado por commit — provenance nunca é promovida na mão"
    - statement: "O recorder nunca executa o binário real com cwd no repositório de trabalho — toda gravação roda em fixture repo descartável"
    - statement: "Nenhuma task da fase escreve sob cairn/gsd/** — a fase 32 vendoriza essa árvore em worktree paralelo com invariante de exatidão nos dois sentidos; para a 33, cairn/gsd/ é somente-leitura"
---

<objective>
Tracer da fase: o dispatcher único `cairn/scripts/cairn-gsd.py` (D-01) nasce com UM verbo
de ponta a ponta — `config-get`, o mais chamado do corpus (32+2 sítios) — atravessando
todas as camadas que a fase toca: roteamento por spellings do contrato, handler, wrapper
.sh, cenário no manifesto, golden versionado com provenance (D-02) e diferencial bats
verde. Em seguida a família config completa (TRIV-01: manifest resolvido em cadeia
declarada + config-set) e o recorder skip-gated que re-grava goldens do binário real da
tag (TRIV-04, metade mecânica).

Purpose: provar a arquitetura inteira do harness com os melhores tokens antes de expandir
às outras 4 famílias — as fases 34 e 35 herdam este formato pronto.
Output: dispatcher + wrapper + recorder + harness diferencial + goldens de config +
resolução do manifest de config em cadeia declarada + divergences.json da fase.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/33-o-binario-python-familias-triviais/33-CONTEXT.md
@.planning/phases/33-o-binario-python-familias-triviais/33-PATTERNS.md
@cairn/gsd/contracts/contracts.json
@cairn/gsd/contracts/config.json
</context>

## Artifacts this phase produces (escopo deste plano)

| Artefato | O que é | Flags/superfície |
|---|---|---|
| `cairn/scripts/cairn-gsd.py` | Dispatcher único das famílias triviais (D-01) | argv = superfície do gsd_run: `query <verbo> ...`; exits 0/1 por contrato do verbo, 2 uso do dispatcher, 4 verbo de família não implementada (nomeia família + fase) |
| `cairn/scripts/cairn-gsd.sh` | Wrapper fino (D-01) | reenuncia usage + exits no header, exec python3 |
| `cairn/scripts/cairn-gsd-record.py` + `.sh` | Recorder de goldens (D-02) | `[--cache-dir <dir>] [--source <url|path>] [--expect-commit <sha>] [--only <scenario-id>]`; exits 0 ok, 2 uso, 5 dependência ausente (node/build), 6 corpus inválido |
| `tests/cairn-gsd.bats` | Harness diferencial (TRIV-04) | runner iterando o manifesto de cenários; skip-gates padrão da casa |
| `tests/fixtures/gsd-goldens/scenarios.json` | Manifesto de cenários (verbo, argv, fixture declarativa, compare, mask) | fonte única para o bats E o recorder |
| `tests/fixtures/gsd-goldens/<id>.golden.json` | Golden por cenário: `{scenario, source, provenance, expect:{exit_code, stdout, stderr}}` | provenance `recorded` \| `derived-from-contract` |
| resolução do manifest de defaults (TRIV-01) | cadeia declarada no dispatcher: `cairn/gsd/gsd-core/config-defaults.manifest.json` se existir (pós-fase-32) → clone em cache verificado por commit → falha nomeada | nada é escrito sob `cairn/gsd/` — árvore da fase 32 |
| `tests/fixtures/gsd-goldens/divergences.json` | Tabela única de divergências deliberadas da fase | `{family, verb, aspect, upstream, cairn, reason}` |

Verbos implementados neste plano: `config-get`, `config-set` (família config, 38+2 sítios).

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — cairn-gsd responde config-get de ponta a ponta, com golden e diferencial verdes</name>
  <files>cairn/scripts/cairn-gsd.py, cairn/scripts/cairn-gsd.sh, tests/cairn-gsd.bats, tests/fixtures/gsd-goldens/scenarios.json, tests/fixtures/gsd-goldens/config-get-*.golden.json</files>
  <read_first>
    - .planning/phases/33-o-binario-python-familias-triviais/33-PATTERNS.md (o mapa inteiro; em especial a tensão de exit codes, "Roteador ≠ subparsers" e a tabela dos 9 verbos)
    - cairn/gsd/contracts/contracts.json (.verbs — o índice verbo→família dos 87; .source.commit)
    - cairn/gsd/contracts/config.json (o contrato de config-get: camadas, exit codes, spellings, secrets mascarados)
    - cairn/scripts/cairn-config.py L219-221 e L349-351 (EXIT_* + die()), L401-419 (leitura defensiva), L432-439 (dotted_get), L555-579 (effective — a FORMA da resolução em camadas), L582-589 (scalar_text), L767-770 (resolve_root)
    - cairn/scripts/cairn-inventory.py L454-563 (separação modelo/render + serialização estável)
    - cairn/scripts/cairn-inventory.sh (o wrapper inteiro — 16 linhas, copiar trocando nomes)
    - tests/cairn-inventory.bats L16-25 (disciplina de asserção), L31-107 (builders locais + helper de invocação + jq), L326-375 (GUARD com controle negativo)
    - tests/gsd-contracts.bats L38-50 (loop com acumulação de falhas), L35-50 (serialização punida)
    - tests/helpers.bash (CAIRN_REPO_ROOT, CAIRN_SCRIPTS_DIR, make_tmp_repo)
  </read_first>
  <action>
    Criar `cairn/scripts/cairn-gsd.py` (D-01): docstring pt-BR com semântica de exits
    registrada (0 = caminho do contrato do verbo; 1 = erro contratado do verbo; 2 =
    EXIT_USAGE, erro de uso do próprio dispatcher — flag do dispatcher desconhecida,
    verbo fora do universo dos 87; 4 = EXIT_UNIMPLEMENTED, verbo do universo cuja
    família está fora das 5 triviais), `die()` com prefixo `[cairn-gsd]` em stderr.
    O roteamento carrega `cairn/gsd/contracts/contracts.json` (path relativo ao script:
    `Path(__file__).resolve().parent.parent / "gsd" / "contracts"`, leitura estrita —
    arquivo nosso, die em falta/inválido) e monta a tabela spelling→handler a partir de
    `.verbs` + spellings dos contratos por família: `query config-get` → handler;
    verbo do universo sem handler → die exit 4 nomeando família e fase (estado,
    roadmap-phase, worktree, init e misc → fase 34; checagem → fase 35 — CHECK-01; e a
    exceção por verbo: os 5 órfãos de CHECK-03 — audit-open, review-lane,
    agent.classify-failure, task.is-behavior-adding, run-with-timeout —, família misc
    em contracts.json mas entregues pela fase 35, → fase 35), NUNCA resposta
    vazia; token não presente no universo → die exit 2. Flags desconhecidas DENTRO de um
    verbo são ignoradas best-effort (espírito do --force-isolation do contrato); flag
    desconhecida ANTES do verbo é uso do dispatcher, exit 2.

    Handler `config-get` no escopo do tracer (a cadeia completa vem na Task 2): argv
    `<key.path>`, flags `--default <v>` e `--raw`; resolve `.planning/config.json` do
    escopo por dotted-path com gate de own-property (copiar a forma de dotted_get e da
    leitura defensiva de cairn-config.py), depois `--default`, depois erro
    CONFIG_KEY_NOT_FOUND exit 1 conforme config.json L37-38. Sem `--raw` imprime
    String(valor) via scalar_text (valor cru, sem aspas em string); com `--raw` imprime
    o valor como JSON com ensure_ascii=False. Chaves secretas saem SEMPRE mascaradas
    (isSecretKey/maskSecret do contrato — reproduzir a máscara na forma da casa).
    Separação modelo/render de cairn-inventory.py: handler devolve (payload, exit_code),
    serialização numa função só.

    Criar `cairn/scripts/cairn-gsd.sh`: cópia da forma de cairn-inventory.sh trocando
    nomes; header reenuncia usage + exits (0/1 por contrato, 2 uso, 4 não implementado
    nesta fase) e aponta o .py como fonte; GUARD da casa vale: nenhuma contagem viva no
    header.

    Criar `tests/fixtures/gsd-goldens/scenarios.json` (layout à discrição, decidido
    aqui): `{schema_version, source:{repo,tag,commit (68a04ccf8ef74803bdb651e12c3b85b218bbccdf)}, scenarios:[...]}`;
    cada cenário `{id, verb, argv (lista, começando em "query"/"loop"), fixture:{git:bool,
    planning_config:objeto|null, files:{path:conteúdo}}, compare:"bytes"|"json",
    expect_stderr:bool, mask:{jq_path:regex}|null}`. `compare:"bytes"` para verbos kind
    text, `"json"` (igualdade via jq -S sobre o stdout parseado) para kind json — a
    escolha protege o diferencial de divergência de formatação do JSON.stringify sem
    esconder divergência de conteúdo. Regra do comparador por provenance: byte-igualdade
    literal só vale quando o golden tem provenance `recorded`; golden
    `derived-from-contract` compara SEMPRE por forma — exit code exato + stdout
    normalizado (jq -S para kind json; texto com newline final normalizado para kind
    text) — porque um golden derivado do contrato não pode prometer os bytes do
    binário real. Cenários do tracer (config-get): chave resolvida
    do escopo; chave ausente com --default; chave ausente sem default (exit 1); valor
    não-ASCII (acentos pt-BR) provando ensure_ascii=False; --raw sobre subárvore.

    Criar os goldens do tracer `tests/fixtures/gsd-goldens/config-get-<cenário>.golden.json`:
    `{schema_version, scenario, source:{repo,tag,commit}, provenance:"derived-from-contract",
    expect:{exit_code, stdout, stderr:null}}` — provenance honesta: o cache está ausente
    nesta máquina e a gravação real é a Task 3; NENHUM timestamp no golden (doutrina de
    determinismo da casa). Serialização house: indent 2, sort_keys, newline final.

    Criar `tests/cairn-gsd.bats` com o núcleo do harness (TRIV-04): bloco de disciplina
    de asserção copiado de cairn-inventory.bats L16-25; builders de fixture LOCAIS ao
    arquivo (precedente L31-34) que interpretam o spec declarativo `fixture` do manifesto
    (git init com identidade determinística via GIT_CONFIG_* env — seam da fase 02 —,
    escrita de .planning/config.json e files); helper `gsd()` invocando cairn-gsd.sh no
    cwd do fixture; runner que itera scenarios.json via jq com acumulação de falhas
    (forma de gsd-contracts.bats L38-50) e compara por cenário: exit code EXATO, stdout
    por compare cruzado com a provenance do golden (recorded: bytes/json conforme
    compare; derived-from-contract: sempre forma normalizada), stderr quando
    expect_stderr, aplicando mask (substitui o valor no jq_path por marcador após validar
    o regex) nos DOIS lados antes de comparar. Controle negativo obrigatório: um golden
    copiado e adulterado no stdout TEM que reprovar no comparador (análogo
    gsd-contracts.bats L286-302). Testes dos exit codes do dispatcher: `query state.load`
    → status 4 com stderr citando a família estado e a fase 34; verbo inexistente no
    universo → status 2.
  </action>
  <reversibility rating="costly">D-01: o caminho cairn/scripts/cairn-gsd.py + .sh é o alvo do preâmbulo shim da fase 36 e do harness — mover depois toca os dois.</reversibility>
  <verify>
    <automated>bats tests/cairn-gsd.bats</automated>
  </verify>
  <acceptance_criteria>
    - `bats tests/cairn-gsd.bats` verde offline (sem rede, sem cache) nesta máquina
    - `cd $(mktemp -d) && git init -q . && printf '{"model_profile":"quality"}' > .planning/config.json` (mkdir antes) e `cairn-gsd.sh query config-get model_profile` imprime `quality` com status 0; `query config-get chave.inexistente` sai status 1; `query config-get chave.inexistente --default x` imprime `x` status 0
    - `cairn-gsd.sh query state.load` sai status 4 e o stderr nomeia `estado` e `fase 34`; `cairn-gsd.sh query verbo-que-nao-existe` sai status 2
    - `jq -e '.scenarios | length >= 5' tests/fixtures/gsd-goldens/scenarios.json` e todo golden passa `jq -e '.provenance == "derived-from-contract" and .source.commit == "68a04ccf8ef74803bdb651e12c3b85b218bbccdf"'`
    - o teste de controle negativo (golden adulterado reprova) existe e passa
  </acceptance_criteria>
  <done>Um verbo real atravessa dispatcher → wrapper → cenário → golden → diferencial, tudo commitado e verde; a falha nomeada e os exits 2/4 do dispatcher estão testados.</done>
</task>

<task type="auto">
  <name>Task 2: A família config completa — manifest resolvido em cadeia, camada de schema default e config-set</name>
  <files>cairn/scripts/cairn-gsd.py, tests/cairn-gsd.bats, tests/fixtures/gsd-goldens/scenarios.json, tests/fixtures/gsd-goldens/config-set-*.golden.json, tests/fixtures/gsd-goldens/divergences.json</files>
  <precondition>Cache do clone restaurável: rede disponível para `cairn/scripts/cairn-inventory.sh --refresh` popular `.cairn/cache/gsd-core-v1.10.0` com HEAD 68a04ccf8ef74803bdb651e12c3b85b218bbccdf, OU o cache já presente e verificado — sem o clone o caminho (b) da cadeia do manifest não resolve e a camada de schema-default de TRIV-01 não é exercitável de verdade nesta execução (os testes da camada são skip-gated pelo cache, mas a acceptance exige tê-la exercitado ao menos uma vez aqui).</precondition>
  <read_first>
    - cairn/gsd/contracts/config.json (config-set: coerção, validação, exit codes; config-get: cadeia completa L34, secrets)
    - cairn/scripts/cairn-config.py L507-552 (coerce_value — "a rejected value leaves the file untouched"), L426 (escrita estável), L555-579 (effective)
    - cairn/scripts/cairn-inventory.py L229-273 (ensure_corpus — verificação de HEAD antes de qualquer uso do cache)
    - .planning/research/v1.6-transplante-gsd.md §4 (config ~80 linhas + manifest de 106)
    - .planning/phases/33-o-binario-python-familias-triviais/33-PATTERNS.md (seções config-get/config-set e "Serialização estável")
  </read_first>
  <action>
    DECISÃO registrada (ordem do checker): a fase 33 NÃO escreve nada sob cairn/gsd/**
    — a fase 32 roda EM PARALELO em worktree separado e vendoriza a árvore inteira com
    invariante de exatidão nos dois sentidos (arquivo a mais sob cairn/gsd/ reprova a
    suíte dela, e o merge das duas branches colidiria). O manifest de defaults NÃO é
    vendorizado; o dispatcher o resolve em CADEIA DECLARADA: (a)
    `cairn/gsd/gsd-core/config-defaults.manifest.json` se existir — passa a valer
    quando a 32 mergear; senão (b) o `config-defaults.manifest.json` do clone em cache
    `.cairn/cache/gsd-core-v1.10.0`, com a MESMA verificação de HEAD ==
    68a04ccf8ef74803bdb651e12c3b85b218bbccdf de ensure_corpus do cairn-inventory
    (forma copiada, módulo nunca importado), localizado por find no clone (caminho a
    confirmar; o PATTERNS registra que não foi confirmado — registrar no summary);
    senão (c) die nomeado pedindo cache/rede ("rode cairn-inventory.sh uma vez com
    rede"). Restaurar o cache nesta execução (cairn-inventory.sh, que já resolve clone
    + verificação de commit — reusar o comando, não duplicar a lógica) para exercitar
    o caminho (b) de verdade. Seam de teste: o resolver aceita override do caminho via
    env CAIRN_GSD_CONFIG_MANIFEST, para os testes provarem a precedência da cadeia sem
    criar arquivo sob cairn/gsd/.

    Completar `config-get` com a cadeia inteira do contrato: config do escopo →
    `--default` → default do schema lido do manifest resolvido pela cadeia acima
    (quando a camada é alcançada e nenhuma fonte da cadeia existe, o die é o do passo
    (c), nomeado — nunca silêncio nem default inventado) → CONFIG_KEY_NOT_FOUND exit 1.
    A camada "root do workstream" (#2702) NÃO entra: o cairn não usa workstreams —
    declarar essa divergência em `tests/fixtures/gsd-goldens/divergences.json`, a
    tabela ÚNICA de divergências da fase (formato: `{schema_version, divergences:
    [{family, verb, aspect, upstream, cairn, reason}]}`, serialização da casa), fora
    de cairn/gsd/ porque essa árvore é somente-leitura para a 33; nunca improvisada —
    regra do CONTEXT. Qualquer outra divergência deliberada que a implementação
    decidir (ex.: forma da máscara de secret) entra na mesma tabela.

    Implementar `config-set` pelo contrato: argv `<key.path> <valor>`, flag `--raw`;
    coerção upstream ('true'/'false' → bool; 'null' → UNSET da chave, #2046 — deletar,
    nunca persistir null; número finito → number e Infinity NÃO coage, #1581; '['/'{' →
    json.loads com fallback string; project_code string verbatim). Validação ANTES de
    escrever — valor rejeitado deixa o arquivo intocado (forma de coerce_value);
    escrita estável da casa para o arquivo (indent 2, sort_keys, newline). Saída texto
    `chave=valor` (ou `chave unset`), `--raw` emite o resultado como JSON; erros do
    contrato (chave/valor ausente, CONFIG_INVALID_KEY, valor fora do domínio) saem
    exit 1. Validação de chave: sem o VALID_CONFIG_KEYS do upstream disponível, o
    domínio de chaves válidas deriva do manifest resolvido pela cadeia + padrões dinâmicos que o
    contrato nomeia (agent_skills.<agent>, features.<nome>); se isso divergir do
    upstream em algum caso, a divergência vai declarada na tabela.

    Cenários novos no manifesto + goldens derived-from-contract: config-set grava e
    relê (config-get confirma); 'null' remove a chave; valor rejeitado deixa o arquivo
    byte-intocado (o teste captura sha256 antes/depois); idempotência (set repetido com
    mesmo valor → arquivo byte-idêntico); chave com default de schema resolvida sem
    config no escopo (prova a camada do manifest — o cenário ganha o campo opcional
    `requires:"config-manifest"` no schema do manifesto, e o runner o skipa com a
    mensagem padrão da casa quando a cadeia não resolve). Testes bats da cadeia:
    skip-gated pelo cache, o resolver devolve o manifest do clone com o commit
    verificado; o seam de env prova a precedência (override vence o cache — a mesma
    ordem do caminho (a)) sem tocar cairn/gsd/; e o die nomeado do passo (c) é testado
    apontando seam/cache para caminho inexistente, com a mensagem pedindo cache/rede.
  </action>
  <verify>
    <automated>bats tests/cairn-gsd.bats && jq -e '.divergences | type == "array"' tests/fixtures/gsd-goldens/divergences.json && test -z "$(git status --porcelain cairn/gsd/)"</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain cairn/gsd/` vazio ao fim do plano — nenhum arquivo novo ou alterado sob cairn/gsd/ (a árvore é da fase 32); a cadeia (a)→(b)→(c) está documentada no docstring do cairn-gsd.py com a nota de que o caminho (a) passa a valer quando a 32 mergear
    - num fixture sem a chave no escopo, `query config-get <chave-com-default-de-schema>` devolve o default do manifest com status 0 (exercitado com o cache presente nesta execução; o teste é skip-gated pelo cache); o die nomeado do passo (c) é testado e a mensagem pede cache/rede
    - `query config-set workflow.research false` imprime `workflow.research=false` e o config-get subsequente devolve `false`; `query config-set workflow.research null` remove a chave; valor fora do domínio sai status 1 e o sha256 do arquivo não muda
    - config-set duas vezes com o mesmo valor: sha256 idêntico entre as duas gravações
    - `jq -e '.divergences[] | select(.aspect | test("workstream"))' tests/fixtures/gsd-goldens/divergences.json` encontra a divergência declarada da camada workstream
    - `bats tests/cairn-gsd.bats` verde
  </acceptance_criteria>
  <done>TRIV-01 satisfeito: a família config responde em python com o manifest de defaults resolvido em cadeia declarada — o "importado" de TRIV-01 chega pelo caminho (a) quando a 32 mergear; até lá o clone verificado responde —, na forma do contrato, com divergência deliberada declarada na tabela própria da fase e não improvisada.</done>
</task>

<task type="auto">
  <name>Task 3: O recorder — cairn-gsd-record grava goldens do binário real, skip-gated e atômico</name>
  <files>cairn/scripts/cairn-gsd-record.py, cairn/scripts/cairn-gsd-record.sh, tests/cairn-gsd.bats, tests/fixtures/gsd-goldens/*.golden.json</files>
  <read_first>
    - .planning/phases/33-o-binario-python-familias-triviais/33-PATTERNS.md (seção "Modo --record" inteira: os 3 fatos medidos e a ordem dos gates)
    - cairn/scripts/cairn-inventory.py L184 (CACHE_RELPATH), L219-273 (run_git + ensure_corpus — a doutrina de verificação de HEAD a copiar), L108-118 (EXIT_NO_HELPER=5, EXIT_BAD_CORPUS=6)
    - tests/cairn-inventory.bats L430-435 (skip-gating com a mensagem padrão), L203-216 (expectativa recomputada, nunca digitada)
    - tests/gsd-contracts.bats L453-474 (o precedente de re-executar o comando gravado e comparar com o gravado)
    - tests/helpers.bash (make_tmp_repo)
  </read_first>
  <action>
    Criar `cairn/scripts/cairn-gsd-record.py` + wrapper `.sh` (D-02): re-grava os goldens
    executando o gsd-tools.cjs REAL do clone, cenário a cenário, a partir do MESMO
    manifesto scenarios.json que o bats consome. Flags: `--cache-dir`, `--source`,
    `--expect-commit`, `--only <scenario-id>` — os mesmos seams de teste do
    cairn-inventory (permitem corpus fixture local, zero rede nos testes). Gates na
    ordem do PATTERNS, cada um com exit próprio e mensagem nomeada: (1) cache presente
    com HEAD == expect-commit, senão exit 6 (EXIT_BAD_CORPUS, forma de ensure_corpus
    copiada); (2) `node` no PATH, senão exit 5 (EXIT_NO_HELPER); (3) probe do bin — uma
    invocação barata do gsd-tools.cjs; se falhar por módulos ./lib ausentes, tentar o
    build do runtime do clone (ensure-runtime-build, localizar o entrypoint no
    gsd-core/bin do cache) e re-probar; build falhou → exit 5 nomeando o passo. NUNCA
    goldens parciais em falha: cada golden é escrito em arquivo temp irmão e renomeado
    atomicamente (precedente staging da fase 02), e falha no meio de um cenário aborta
    sem tocar os goldens já commitados dos cenários restantes.

    Execução por cenário: montar o fixture declarativo num diretório descartável
    (make_tmp_repo é o precedente; o recorder py monta o seu com identidade git via env
    GIT_CONFIG_*), rodar `node <cache>/gsd-core/bin/gsd-tools.cjs <argv sem o prefixo
    do wrapper>` com cwd NO FIXTURE — jamais no repo real (config-set/commit mutariam
    estado de verdade) —, capturar stdout/exit/stderr, aplicar `mask` do cenário
    (validar o valor contra o regex, substituir pelo marcador) e escrever o golden com
    provenance `recorded`, source{repo,tag,commit}, SEM timestamp (o teste de
    reprodução byte-a-byte exige determinismo).

    Testes no cairn-gsd.bats: (a) OFFLINE com corpus fixture — um repo local fake com um
    gsd-tools.cjs stub (script node de uma linha imprimindo envelope canned) e commit
    esperado do próprio fixture, provando gates, mask, escrita atômica (falha injetada
    no stub → nenhum golden parcial aparece) e provenance recorded; (b) skip-gated
    contra o corpus REAL (mensagem padrão: "cache do clone ausente — rode
    cairn-inventory.sh uma vez com rede" + gate de node): para goldens com provenance
    recorded, `--record` os reproduz byte-a-byte — a expectativa recomputada da casa;
    goldens derived-from-contract NÃO prometem os bytes do binário real — para eles a
    comparação é por forma (exit code + stdout normalizado) e a gravação real os
    promove a recorded.

    Tentativa real nesta execução: com o cache restaurado pela Task 2, rodar
    `cairn-gsd-record.sh` de verdade. Se o build do runtime passar, os goldens de config
    são re-gravados com provenance recorded e commitados assim; se o build falhar, o
    exit 5 nomeado é registrado no SUMMARY como skip documentado, os goldens permanecem
    derived-from-contract e a re-gravação fica pendente por bead — nunca provenance
    forjada.
  </action>
  <verify>
    <automated>bats tests/cairn-gsd.bats</automated>
  </verify>
  <acceptance_criteria>
    - com corpus fixture stub: `cairn-gsd-record.sh --cache-dir <fixture> --expect-commit <sha-do-fixture> --only <cenário>` grava golden com `provenance == "recorded"` e mask aplicada; HEAD divergente sai status 6; sem node no PATH (PATH restrito no teste) sai status 5
    - falha injetada no stub no meio da gravação: nenhum arquivo `*.golden.json` parcial ou temp sobra no diretório de goldens
    - os testes contra o corpus real skipam com a mensagem padrão quando cache/node ausentes, e o teste de reprodução existe: byte-a-byte só para provenance recorded; derived-from-contract compara por forma (json normalizado + exit code)
    - a tentativa real de gravação foi feita e o resultado (recorded OU skip nomeado com o passo que falhou) está registrado no SUMMARY
    - `bats tests/cairn-gsd.bats` verde
  </acceptance_criteria>
  <done>D-02 mecânica completa: goldens commitados são a fonte do diferencial, o --record re-grava do binário real quando o corpus existe, e nenhum caminho produz golden parcial ou provenance desonesta.</done>
</task>

</tasks>

<verification>
- `bats tests/cairn-gsd.bats` verde offline (skips visíveis e nomeados onde o corpus real falta)
- `bats tests/` inteiro sem regressão nas suítes existentes
- `cairn/scripts/cairn-gsd.sh query config-get model_profile` responde no repo real (a chave existe em .planning/config.json)
</verification>

<success_criteria>
- Tracer completo: config-get atravessa todas as camadas com golden e diferencial verdes
- TRIV-01: família config no contrato, com o manifest de defaults resolvido em cadeia declarada (cairn/gsd/gsd-core → clone em cache verificado → falha nomeada) e zero escrita sob cairn/gsd/
- Harness (metade mecânica de TRIV-04): manifesto de cenários + goldens com provenance + recorder skip-gated com escrita atômica
- Divergências deliberadas declaradas na tabela da fase (tests/fixtures/gsd-goldens/divergences.json), nunca improvisadas
</success_criteria>

<output>
Criar `.planning/phases/33-o-binario-python-familias-triviais/33-01-SUMMARY.md` ao concluir, registrando: caminho de origem do manifest no clone (caminho (b) da cadeia), resultado da tentativa real de --record (recorded ou skip nomeado), e as entradas adicionadas a tests/fixtures/gsd-goldens/divergences.json.
</output>
