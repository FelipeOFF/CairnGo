---
phase: 33-o-binario-python-familias-triviais
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - cairn/scripts/cairn-gsd.py
  - tests/cairn-gsd.bats
  - tests/fixtures/gsd-goldens/scenarios.json
  - tests/fixtures/gsd-goldens/*.golden.json
  - tests/fixtures/gsd-goldens/divergences.json
autonomous: true
requirements: [TRIV-02, TRIV-03]
beads:
  TRIV-02: CairnGo-3ea6
  TRIV-03: CairnGo-i75i
must_haves:
  truths:
    - "`query commit` emite envelope JSON {committed, hash, reason, skipped} com reason explícito — committed | skipped_commit_docs_false | skipped_gitignored | nothing_to_commit | staging_failed | commit_failed — e exit 0 mesmo com committed:false; só mensagem ausente sem --amend sai exit 1 (TRIV-02)"
    - "os skips do commit são caminho de sucesso intencional: commit_docs=false devolve skipped_commit_docs_false e .planning gitignored devolve skipped_gitignored — nunca contornados"
    - "`query agent-skills <agent>` devolve o bloco XML cru por default e o IR tipado com --json; sem agent-type devolve bloco vazio com exit 0; reasons resolved | not_configured | configured_empty | configured_unresolved (TRIV-02)"
    - "`loop render-hooks <point>` valida o point contra o vocabulário canônico de 12 pontos e devolve {activeHooks, point, rendered, warnings}; point inválido sai exit 1; --active-cap imprime exatamente true ou false e capId desconhecido é false, não erro (TRIV-03)"
    - "a tabela nativa de hooks nasce das entradas por point de cairn/capability/capability.json — o capability-registry de 7k linhas não é reimplementado (TRIV-03)"
    - "dispatch-isolation, dispatch-should-flatten e resolve-dispatch-type são fail-closed total: qualquer erro resolve para none / true / eco do requested, sempre exit 0; dispatch-isolation persiste .gsd/dispatch-isolation-sentinel.json e falha de escrita nunca falha o verbo (TRIV-03)"
    - "`query resolve-model <agent>` devolve {effort, model, profile, unknown_agent}; agente fora dos perfis sai exit 0 com unknown_agent:true (TRIV-03)"
    - "cada um dos 8 verbos deste plano tem cenários no manifesto e goldens com provenance declarada, e o diferencial bats roda verde offline"
  artifacts:
    - cairn/scripts/cairn-gsd.py
    - tests/fixtures/gsd-goldens/scenarios.json
    - tests/cairn-gsd.bats
  key_links:
    - "o handler de commit fala com git por subprocess no cwd resolvido, com a mensagem sanitizada (chars invisíveis e marcadores de injection) antes do -m"
    - "o handler de loop lê cairn/capability/capability.json (contributions/gates → points) para montar activeHooks"
    - "os handlers de dispatch devolvem o default seguro em QUALQUER exceção — o try/except externo é a implementação do fail-closed do contrato"
  prohibitions:
    - statement: "Nenhuma task da fase escreve sob cairn/gsd/** — a fase 32 vendoriza essa árvore em worktree paralelo com invariante de exatidão nos dois sentidos; os contratos das famílias são somente-leitura e toda divergência declarada vai em tests/fixtures/gsd-goldens/divergences.json"
---

<objective>
Expansão a partir do tracer: as quatro famílias restantes entram no dispatcher que o
plano 01 provou — commit e skills (TRIV-02), loop-hooks e dispatch/model (TRIV-03) — cada
verbo na forma exata do contrato da fase 31, com cenários no manifesto e goldens no
harness. Ao fim, os 10 verbos das 5 famílias triviais respondem.

Purpose: cobrir os ~96 sítios das famílias baratas (commit 15+8, skills 13, loop 17,
dispatch/model 14+1) sobre a arquitetura já verde.
Output: cairn-gsd.py com os 8 verbos novos + cenários + goldens + diferencial cobrindo
todos.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-o-binario-python-familias-triviais/33-CONTEXT.md
@.planning/phases/33-o-binario-python-familias-triviais/33-PATTERNS.md
@.planning/phases/33-o-binario-python-familias-triviais/33-01-SUMMARY.md
@cairn/gsd/contracts/commit.json
@cairn/gsd/contracts/skills.json
@cairn/gsd/contracts/loop-hooks.json
@cairn/gsd/contracts/dispatch-model.json
</context>

## Artifacts this phase produces (escopo deste plano)

| Artefato | O que entra neste plano |
|---|---|
| `cairn/scripts/cairn-gsd.py` | 8 handlers novos: `commit`, `commit-to-subrepo`, `agent-skills`, `loop render-hooks`, `dispatch-isolation`, `dispatch-should-flatten`, `resolve-model`, `resolve-dispatch-type` |
| `tests/fixtures/gsd-goldens/scenarios.json` | cenários por verbo/situação das 4 famílias (goldens irmãos `<id>.golden.json`) |
| `tests/cairn-gsd.bats` | diferencial cobrindo os 8 verbos + asserção do sentinel de dispatch-isolation |
| `tests/fixtures/gsd-goldens/divergences.json` | entradas de divergência (family commit/skills/dispatch-model) onde a implementação divergir de propósito — os contratos sob `cairn/gsd/` ficam intocados (árvore da fase 32) |

Flags por verbo (do contrato): commit `--files/--amend/--no-verify/--raw`;
commit-to-subrepo `--files/--raw`; agent-skills `--json`; loop render-hooks
`--raw/--active-cap/--config-dir/--runtime`; dispatch-isolation
`--raw/--json/--phase/--plan/--cwd-target/--prompt/--force-isolation`;
dispatch-should-flatten `--raw/--json`; resolve-model `--raw/--pick`;
resolve-dispatch-type `--requested/--raw/--json`.

## Flagged assumptions (probe spec-less, §C — nunca drop silencioso)

O edge-probe não classificou aresta para TRIV-02 nem TRIV-03 (`unclassified —
review manually`, 2 linhas). Resolução registrada aqui, não descartada: as arestas
reais dessas famílias vêm do próprio contrato — para commit, os seis reasons de skip e
a mensagem sanitizada; para skills, os quatro reasons; para loop, point inválido e
capId desconhecido; para dispatch, o fail-closed sob runtime desconhecido — e cada uma
recebe cenário no manifesto neste plano. Verificação: as acceptance criteria das duas
tasks exigem cenário por reason/ramo contratado.

<tasks>

<task type="auto">
  <name>Task 1: commit, commit-to-subrepo e agent-skills respondem pelo contrato</name>
  <files>cairn/scripts/cairn-gsd.py, tests/cairn-gsd.bats, tests/fixtures/gsd-goldens/scenarios.json, tests/fixtures/gsd-goldens/commit-*.golden.json, tests/fixtures/gsd-goldens/agent-skills-*.golden.json</files>
  <read_first>
    - cairn/gsd/contracts/commit.json (os dois verbos inteiros: reasons, flags, posicionais unidos com espaço, exit codes)
    - cairn/gsd/contracts/skills.json (Resolution envelope, reasons, fallback de persona #2454, exit 0 sempre)
    - .planning/phases/33-o-binario-python-familias-triviais/33-PATTERNS.md (tabela dos 9 verbos; leitura defensiva; scalar_text)
    - cairn/scripts/cairn-gsd.py (o dispatcher como o plano 01 deixou — o arquivo que esta task estende)
    - tests/fixtures/gsd-goldens/scenarios.json (o schema de cenário estabelecido no plano 01)
    - cairn/scripts/cairn-config.py L401-419 (leitura defensiva para tudo que o handler lê e não é dono)
  </read_first>
  <action>
    Estender cairn-gsd.py com o handler `commit` (TRIV-02): posicionais entre o verbo e
    a primeira flag unidos com espaço formam a mensagem (routeCommit); sanitizar a
    mensagem (remover chars invisíveis e marcadores de injection — o contrato registra a
    exigência, a lista concreta fica na implementação e qualquer diferença do upstream
    vai à tabela de divergências da fase — tests/fixtures/gsd-goldens/divergences.json,
    family commit); cadeia de decisão do envelope na ordem do
    contrato: config commit_docs=false → reason skipped_commit_docs_false; .planning
    gitignored (git check-ignore) → skipped_gitignored; `--files` staging individual e
    falha de staging → staging_failed com index revertido; nada staged →
    nothing_to_commit; git commit (com --amend/--no-verify quando pedidos) → committed
    com hash, ou commit_failed. Envelope {committed, hash, reason, skipped} — skipped só
    aparece nos envelopes de skip; exit 0 em TODOS esses caminhos, exit 1 apenas
    mensagem ausente sem --amend. Sem --raw o texto é o hash (ou 'committed'); --raw
    emite o envelope JSON.

    Handler `commit-to-subrepo`: agrupar --files pelo sub-repo dono (subir do arquivo ao
    .git mais próximo), commitar um a um, `repos.<nome>` com {committed, hash, files} ou
    {committed:false, hash:null, files, reason, error?}; committed de topo é OR dos
    repos; exit 0 sempre.

    Handler `agent-skills`: ler config agent_skills.<agent-type> (leitura defensiva),
    resolver paths de skill, montar o bloco XML e o envelope Resolution {value:{block,
    skills_count}, configured, reason, warnings}; default imprime o bloco cru (pode ser
    vazio), --json imprime o IR; sem agent-type → bloco vazio exit 0; configured_empty
    ganha warning em stderr. O fallback de persona (#2454) em host claude não dispara —
    se a implementação optar por não portá-lo, declarar na tabela de divergências da
    fase (family skills) com o motivo (host único claude).

    Cenários + goldens (provenance derived-from-contract, ou recorded se o corpus da
    execução do plano 01 seguir disponível e o record rodar): commit real committed:true
    com mask do hash (`mask: {".hash": "^[0-9a-f]{7,40}$"}`); commit_docs=false →
    skip; nada a commitar; mensagem ausente → exit 1; mensagem com posicionais
    múltiplos unidos; agent-skills resolved com fixture de skill, not_configured, sem
    agent-type, e --json. O diferencial compara os envelopes com compare:"json".
  </action>
  <verify>
    <automated>bats tests/cairn-gsd.bats</automated>
  </verify>
  <acceptance_criteria>
    - num fixture com mudança staged, `query commit "msg" --raw` devolve envelope com `committed == true`, hash casando `^[0-9a-f]{7,40}$` e status 0; com commit_docs=false o reason é skipped_commit_docs_false e status 0; sem nada a commitar reason nothing_to_commit e status 0; sem mensagem status 1
    - cada um dos seis reasons do contrato tem cenário no manifesto OU teste bats direto (staging_failed/commit_failed podem ser bats direto com fixture que força a falha)
    - `query agent-skills` sem argumento imprime bloco vazio com status 0; `--json` devolve IR com reason e skills_count; fixture configurado resolve com skills_count >= 1
    - goldens novos passam o guard de provenance e serialização estabelecido no plano 01
    - `bats tests/cairn-gsd.bats` verde offline
  </acceptance_criteria>
  <done>TRIV-02 satisfeito: commit e skills respondem em python sobre git e manifest estático, envelope por contrato, skips como sucesso intencional.</done>
</task>

<task type="auto">
  <name>Task 2: loop render-hooks como tabela nativa e os 4 verbos de dispatch/model em host único</name>
  <files>cairn/scripts/cairn-gsd.py, tests/cairn-gsd.bats, tests/fixtures/gsd-goldens/scenarios.json, tests/fixtures/gsd-goldens/loop-*.golden.json, tests/fixtures/gsd-goldens/dispatch-*.golden.json, tests/fixtures/gsd-goldens/resolve-*.golden.json</files>
  <read_first>
    - cairn/gsd/contracts/loop-hooks.json (spelling top-level `loop`, os 12 CANONICAL_POINTS, --active-cap limpo, fail-open de gates com warning)
    - cairn/gsd/contracts/dispatch-model.json (os 4 verbos inteiros — fail-closed, sentinel incondicional, identidade do resolve-dispatch-type, unknown_agent)
    - cairn/capability/capability.json (contributions[].point e gates[] — a fonte da tabela nativa)
    - o manifest de config resolvido pela cadeia do plano 01 — cairn/gsd/gsd-core/config-defaults.manifest.json se existir, senão o do clone em cache verificado (se contiver model_profiles, é a fonte preferida do resolve-model)
    - .planning/phases/33-o-binario-python-familias-triviais/33-PATTERNS.md ("Efeito colateral contratado que não pode sumir" + leitura defensiva fail-closed)
    - cairn/scripts/cairn-gsd.py (o roteador — loop entra como spelling top-level, não sob query)
  </read_first>
  <action>
    Handler `loop render-hooks <point>` (TRIV-03, tabela nativa): validar o point contra
    o vocabulário canônico de 12 (discuss/plan/execute/verify/ship × pre/post +
    execute:wave:pre/post) — inválido/ausente exit 1; montar activeHooks da TABELA
    NATIVA: leitura defensiva de cairn/capability/capability.json mapeando
    contributions[].point e gates para os points (o capability-registry NÃO é
    reimplementado — é exatamente o que o research §4 corta); repo sem capability →
    envelope {activeHooks:[], point, rendered:"", warnings:[]} exit 0. `--raw` emite o
    envelope JSON (a forma dominante nos 8); `--active-cap <capId>` imprime exatamente
    `true` ou `false` + newline, sem envelope, capId desconhecido é false; `--config-dir`
    e `--runtime` aceitos nas duas formas (com espaço e com =) e resolvidos com
    path.resolve — no host único influenciam só a resolução de diretório.

    Handlers dispatch/model (TRIV-03), todos com try/except externo implementando o
    fail-closed do contrato: `dispatch-isolation` resolve runtime (GSD_RUNTIME >
    config.runtime > claude) e devolve o modo do vocabulário {harness-worktree,
    orchestrator-worktree, none} — qualquer erro, runtime desconhecido ou valor fora do
    vocabulário → none, sempre exit 0; `--force-isolation` com valor fora do
    vocabulário é IGNORADO; --raw imprime o modo cru, --json o envelope {runtime,
    isolation, exec, harnessFlag}. EFEITO COLATERAL contratado: persistir a decisão em
    .gsd/dispatch-isolation-sentinel.json (mode + harnessFlag + phase/plan) em toda
    invocação, e falha de escrita NUNCA falha o verbo. Em host único claude sem registry
    multi-host, o modo naturalmente resolvido é none — se essa simplificação divergir do
    upstream em cenário observável, declarar na tabela de divergências da fase
    (family dispatch-model). `dispatch-should-flatten`: fail-closed imprime true; host
    claude resolve false pelo registry — sem registry no cairn, decidir o valor de host
    único e DECLARAR a base (contrato: claude é named-dispatch → shouldFlatten false no
    caminho são; qualquer erro → true). `resolve-model <agent-type>`: MODEL_PROFILES do manifest de
    config resolvido pela cadeia do plano 01 quando disponível e contiver
    model_profiles, senão tabela copiada do clone com source_ref em comentário no
    próprio cairn-gsd.py (fallback offline — resolve-model não pode depender de
    cache); envelope {effort, model, profile, unknown_agent}; sem --raw o texto é só
    o model id; --pick model extrai a chave; agente desconhecido exit 0 com
    unknown_agent:true; agent-type ausente exit 1. `resolve-dispatch-type`: identidade
    em host único — ecoa --requested inalterado (ausente → coder), sempre exit 0;
    --json emite {runtime, requested, resolved, dispatch}.

    Cenários + goldens: loop com point válido (envelope vazio em fixture cru), point
    inválido (exit 1), --active-cap desconhecido (false); dispatch-isolation --raw
    (none) e --json; dispatch-should-flatten --raw; resolve-model conhecido e
    desconhecido; resolve-dispatch-type --requested gsd-planner. Teste bats direto
    (fora do golden): após dispatch-isolation, .gsd/dispatch-isolation-sentinel.json
    existe no fixture e carrega o mode; com o diretório .gsd travado (chmod), o verbo
    ainda sai 0 — a falha de escrita não falha o verbo.
  </action>
  <verify>
    <automated>bats tests/cairn-gsd.bats</automated>
  </verify>
  <acceptance_criteria>
    - `loop render-hooks plan:post --raw` num fixture devolve envelope com as 4 chaves e status 0; `loop render-hooks ponto-invalido` sai status 1; `--active-cap nao-existe` imprime `false` com status 0
    - `query dispatch-isolation --raw` imprime um de harness-worktree|orchestrator-worktree|none com status 0 e grava o sentinel; com .gsd ilegível o status segue 0
    - `query dispatch-should-flatten --raw` imprime exatamente true ou false com status 0; sob erro forçado (config corrompida no fixture) imprime true
    - `query resolve-model gsd-planner --raw` devolve as 4 chaves do shape; agente inventado devolve unknown_agent true com status 0; sem argumento status 1
    - `query resolve-dispatch-type --requested gsd-verifier --raw` imprime `gsd-verifier`; sem --requested imprime `coder`
    - os 8 verbos do plano têm cenário no manifesto e golden com provenance válida; `bats tests/cairn-gsd.bats` verde offline
  </acceptance_criteria>
  <done>TRIV-03 satisfeito: loop-hooks responde por tabela nativa e dispatch/model devolve o contrato em host único, fail-closed provado por teste.</done>
</task>

</tasks>

<verification>
- `bats tests/cairn-gsd.bats` verde offline — os 10 verbos das 5 famílias com cenário e golden
- `bats tests/` inteiro sem regressão
- divergências deliberadas novas (se houver) presentes em tests/fixtures/gsd-goldens/divergences.json com a family da família tocada
- nada escrito sob cairn/gsd/ — `git status --porcelain cairn/gsd/` vazio
</verification>

<success_criteria>
- TRIV-02: commit, commit-to-subrepo e agent-skills na forma dos contratos
- TRIV-03: loop render-hooks (tabela nativa) e os 4 verbos dispatch/model fail-closed em host único
- Sentinel de dispatch-isolation reproduzido — o efeito colateral contratado não sumiu
- Todos os reasons/ramos contratados com cenário ou teste direto (resolução das flagged assumptions do probe)
</success_criteria>

<output>
Criar `.planning/phases/33-o-binario-python-familias-triviais/33-02-SUMMARY.md` ao concluir, registrando as divergências declaradas (persona fallback, base do should-flatten, modo natural do isolation em host único) e a decisão da fonte de MODEL_PROFILES.
</output>
