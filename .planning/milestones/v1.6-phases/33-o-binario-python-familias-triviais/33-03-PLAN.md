---
phase: 33-o-binario-python-familias-triviais
plan: 03
type: execute
wave: 3
depends_on: [33-01, 33-02]
files_modified:
  - tests/cairn-gsd.bats
  - cairn/scripts/cairn-gsd.py
autonomous: true
requirements: [TRIV-04]
beads:
  TRIV-04: CairnGo-lcca
must_haves:
  truths:
    - "a cobertura é provada nos dois sentidos contra o universo do contrato: todo verbo de contracts.json .verbs com família nas 5 triviais tem handler no dispatcher, e todo handler corresponde a verbo do universo — comm vazio dos dois lados, com controle negativo em que um verbo forjado vaza (TRIV-04)"
    - "a soma de call_sites das 5 famílias é recomputada dos próprios JSONs de contrato pelo teste (109 = 97 workflows8 + 12 agents — os ~96 sítios do CONTEXT), nunca digitada de cabeça"
    - "verbo de família estado, roadmap-phase, worktree, init ou misc morre nomeando a família e a fase 34 — exceto os 5 órfãos de CHECK-03 (audit-open, review-lane, agent.classify-failure, task.is-behavior-adding, run-with-timeout), família misc em contracts.json mas entregues pela fase 35, que morrem nomeando a fase 35; família checagem (incl. user-story.validate) morre nomeando a fase 35 (CHECK-01) — testado por família e pelos 5 órfãos, nunca resposta vazia"
    - "a fase 34 acrescenta um verbo criando somente entrada no scenarios.json, golden e handler: o runner do bats itera o manifesto e não carrega lista própria de verbos — provado por teste que injeta um cenário extra num manifesto fixture e o vê executado (TRIV-04, reutilizável)"
    - "todo golden referencia cenário existente no manifesto e todo cenário tem golden; JSON commitado do harness segue a serialização da casa (indent 2, sort_keys, newline final)"
    - "skip-gated contra o corpus real: o --record reproduz byte-a-byte os goldens com provenance recorded — goldens derived-from-contract comparam por forma (json normalizado + exit code), nunca por bytes — e a lista de verbos de contracts.json bate com cairn-inventory.sh --json"
    - "a suíte bats inteira do repositório sai verde"
  artifacts:
    - tests/cairn-gsd.bats
  key_links:
    - "o guard de cobertura cruza contracts.json .verbs (comitado, offline) com a tabela de handlers exposta pelo dispatcher — a prova não depende de rede"
    - "o dispatcher expõe seus verbos implementados de forma enumerável (die de uso lista, ou flag interna) para o guard não manter lista paralela"
  prohibitions:
    - statement: "Verbo não implementado nunca responde envelope vazio, default inventado ou sucesso silencioso — a falha é sempre nomeada com família e fase"
    - statement: "Nenhuma task da fase escreve sob cairn/gsd/** — a fase 32 vendoriza essa árvore em worktree paralelo com invariante de exatidão nos dois sentidos; contracts.json é lido, nunca tocado"
---

<objective>
Fechamento de TRIV-04: o harness deixa de ser uma suíte e vira um CONTRATO — cobertura
provada contra o universo do inventário nos dois sentidos, falha nomeada testada família
a família, guards com controle negativo, e o seam de reuso pelo qual as fases 34 e 35
acrescentam verbos sem tocar o runner.

Purpose: o produto durável da fase é o harness; este plano é o que o torna herdável.
Output: tests/cairn-gsd.bats completo com guards de cobertura, reuso e integridade;
suíte inteira do repo verde.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-o-binario-python-familias-triviais/33-CONTEXT.md
@.planning/phases/33-o-binario-python-familias-triviais/33-PATTERNS.md
@.planning/phases/33-o-binario-python-familias-triviais/33-01-SUMMARY.md
@.planning/phases/33-o-binario-python-familias-triviais/33-02-SUMMARY.md
@cairn/gsd/contracts/contracts.json
</context>

## Artifacts this phase produces (escopo deste plano + visão consolidada da fase)

Este plano entrega os guards finais do harness. Consolidado da fase 33:

| Categoria | Artefato |
|---|---|
| Dispatcher | `cairn/scripts/cairn-gsd.py` + `cairn-gsd.sh` — 10 verbos: config-get, config-set, commit, commit-to-subrepo, agent-skills, loop render-hooks, dispatch-isolation, dispatch-should-flatten, resolve-model, resolve-dispatch-type |
| Recorder | `cairn/scripts/cairn-gsd-record.py` + `.sh` (`--cache-dir/--source/--expect-commit/--only`) |
| Harness | `tests/cairn-gsd.bats` — diferencial + cobertura + guards, herdado pelas fases 34-35 |
| Dados | `tests/fixtures/gsd-goldens/scenarios.json` + `*.golden.json` (provenance recorded \| derived-from-contract) |
| Manifest de config | resolvido em cadeia declarada (plano 01): `cairn/gsd/gsd-core/config-defaults.manifest.json` se existir (pós-fase-32) → clone em cache verificado por commit → falha nomeada; zero escrita sob `cairn/gsd/` |
| Divergências | `tests/fixtures/gsd-goldens/divergences.json` — tabela única `{family, verb, aspect, upstream, cairn, reason}`; os contratos sob `cairn/gsd/` ficam intocados |

<tasks>

<task type="auto">
  <name>Task 1: Cobertura nos dois sentidos contra o universo, e a falha nomeada por família</name>
  <files>tests/cairn-gsd.bats, cairn/scripts/cairn-gsd.py</files>
  <read_first>
    - tests/gsd-contracts.bats L257-302 (cobertura total sem tolerância via comm -23/comm -13 + controle negativo do verbo forjado — o molde exato desta task)
    - cairn/gsd/contracts/contracts.json (.verbs — universo dos 87; .universe.scopes — sites por escopo)
    - cairn/scripts/cairn-gsd.py (a tabela de roteamento como os planos 01-02 deixaram)
    - tests/cairn-inventory.bats L326-375 (a doutrina: todo guard tem a metade de vivacidade)
    - .planning/phases/33-o-binario-python-familias-triviais/33-PATTERNS.md (seção "Verbo não implementado nesta fase" e a tabela dos 9 verbos com sítios)
  </read_first>
  <action>
    Expor no dispatcher uma enumeração dos verbos implementados que o guard consome sem
    manter lista paralela — a forma mais barata na casa: uma flag interna
    `--list-implemented` (fora dos contratos, documentada no docstring como superfície
    de teste) que imprime um verbo por linha e sai 0. Guard de cobertura no bats, molde
    de gsd-contracts.bats: extrair de contracts.json os verbos cuja família ∈ {config,
    commit, skills, loop-hooks, dispatch-model} via jq, comparar com a enumeração do
    dispatcher via comm — os DOIS sentidos vazios (verbo trivial sem handler reprova;
    handler sem verbo no universo reprova). Controle negativo obrigatório: um universo
    fixture com um verbo trivial forjado a mais TEM que reprovar o guard.

    Guard dos ~96 sítios: recomputar dos próprios JSONs das 5 famílias a soma de
    call_sites (workflows8 e agents) via jq e assertar 97 e 12 — a expectativa vem de
    grep/jq sobre o artefato, nunca digitada (doutrina de cairn-inventory.bats
    L203-216; os números ficam no bloco datado MEASURED do cabeçalho do teste se
    citados em comentário).

    Falha nomeada testada por família e pelo mapa do ROADMAP: um representante de cada
    família não-trivial — state.load (estado), roadmap.get-phase (roadmap-phase),
    worktree.create (worktree), init.plan-phase (init), frontmatter.get (misc) →
    status 4 com stderr citando a família e `fase 34` (CORE-01..05);
    verify.plan-structure e user-story.validate (checagem) → status 4 citando
    `fase 35` (CHECK-01); e a exceção por verbo: os 5 órfãos de CHECK-03 — audit-open,
    review-lane, agent.classify-failure, task.is-behavior-adding, run-with-timeout —,
    todos família misc em contracts.json mas entregues pela fase 35, → status 4
    citando `fase 35` (testar os 5, não um representante).
    Os fantasmas da fase 31 (phase.list-artifacts, plan.task-structure) caem nesse
    mesmo caminho — teste explícito confirmando que respondem falha nomeada, não
    resposta inventada. Verbo fora do universo → status 2. Skip-gated: com o corpus
    real presente, a lista de verbos do inventário vivo (cairn-inventory.sh --json |
    jq '.verbs') bate com contracts.json .verbs — o cross-check da cobertura contra o
    inventário, na mensagem padrão de skip da casa.
  </action>
  <verify>
    <automated>bats tests/cairn-gsd.bats</automated>
  </verify>
  <acceptance_criteria>
    - o guard de cobertura passa e seu controle negativo (verbo forjado no universo fixture) reprova
    - `jq` recomputa 97 (workflows8) e 12 (agents) somando call_sites das 5 famílias, dentro do teste
    - os representantes de família saem status 4 nomeando família e fase certas (estado, roadmap-phase, worktree, init e misc → fase 34; checagem, incl. user-story.validate → fase 35); os 5 órfãos de CHECK-03 saem status 4 nomeando a fase 35; os 2 fantasmas idem; verbo inexistente sai status 2
    - o cross-check contra o inventário vivo existe e skipa com a mensagem padrão quando o cache falta
    - `bats tests/cairn-gsd.bats` verde offline
  </acceptance_criteria>
  <done>Cobertura da fase provada contra o universo do contrato nos dois sentidos, com vivacidade, e nenhum verbo fora das 5 famílias responde sem nomear quem o entrega.</done>
</task>

<task type="auto">
  <name>Task 2: O contrato de reuso do harness — integridade golden↔cenário e o seam das fases 34-35</name>
  <files>tests/cairn-gsd.bats</files>
  <read_first>
    - tests/cairn-gsd.bats (o runner como ficou — esta task o blinda)
    - tests/gsd-contracts.bats L35-50 (serialização punida: JSON sem newline reprova) e L453-474 (re-execução do gravado)
    - tests/cairn-release.bats L436-440 e L497-520 (a lição do golden: ele precisa carregar o que o diferencial pode perder)
    - tests/fixtures/gsd-goldens/scenarios.json (o manifesto completo das 5 famílias)
  </read_first>
  <action>
    Guards de integridade do par manifesto↔goldens, cada um com metade de vivacidade:
    (1) todo cenário do manifesto tem golden irmão e todo *.golden.json referencia
    cenário existente (os dois sentidos via comm; controle negativo: golden órfão
    fixture reprova); (2) todo golden carrega provenance ∈ {recorded,
    derived-from-contract}, source.commit == o commit da fase 31, e expect com
    exit_code inteiro e stdout string (jq -e por arquivo, loop com acumulação de
    falhas); (3) serialização da casa em scenarios.json e em todo golden — newline
    final e chaves ordenadas (reserializar com jq -S e comparar; JSON cru sem newline
    reprova, molde de gsd-contracts.bats); (4) nenhum golden carrega timestamp (grep
    filtrado por chave, não por prosa — recorded_at/date/time como CHAVE reprova).

    O seam de reuso (a herança das fases 34-35): teste que constrói um manifesto
    fixture com um cenário extra apontando um verbo já implementado e roda o runner
    sobre ele (o runner aceita override do caminho do manifesto via variável de
    ambiente — acrescentar esse seam se o plano 01 não o deixou), provando que cenário
    novo executa SEM mudança no runner; e o inverso: cenário no manifesto sem golden
    reprova. Documentar o seam no cabeçalho do cairn-gsd.bats em um bloco curto "como
    a fase 34 adiciona um verbo" (entrada no scenarios.json + golden + handler; nada
    mais), porque é esse bloco que as fases seguintes vão ler primeiro.

    Fechamento: rodar a suíte inteira do repositório e confirmar zero regressão nas
    suítes existentes; se o cairn-gsd.py citar contagens no docstring, o bloco datado
    MEASURED VERSUS ASSUMED da casa se aplica (convenção de cairn-inventory.py, e o
    GUARD correspondente se a contagem for viva).
  </action>
  <verify>
    <automated>bats tests/</automated>
  </verify>
  <acceptance_criteria>
    - guards 1-4 passam e cada controle negativo (golden órfão, JSON sem newline, chave de timestamp) reprova no fixture adulterado
    - o teste do seam executa um cenário injetado via manifesto fixture sem editar o runner, e cenário sem golden reprova
    - o cabeçalho do cairn-gsd.bats contém o bloco "como a fase 34 adiciona um verbo"
    - `bats tests/` inteiro verde (todas as suítes do repo, não só a nova)
  </acceptance_criteria>
  <done>TRIV-04 satisfeito por inteiro: o harness compara cada verbo python com o gsd-tools real da tag via goldens íntegros, e as fases 34-35 o herdam com o seam documentado e provado.</done>
</task>

</tasks>

<verification>
- `bats tests/` inteiro verde
- `comm` de cobertura vazio nos dois sentidos; controles negativos reprovando
- Falha nomeada por família com fase certa (34/35, incluindo os 5 órfãos de CHECK-03 → fase 35); fantasmas da fase 31 cobertos
</verification>

<success_criteria>
- TRIV-04: harness diferencial completo, cobertura provada contra o universo (~96 sítios recomputados dos contratos), reutilizável pelas fases 34-35 com seam provado por teste
- Nenhum caminho do dispatcher responde vazio, inventado ou silencioso para verbo não implementado
</success_criteria>

<output>
Criar `.planning/phases/33-o-binario-python-familias-triviais/33-03-SUMMARY.md` ao concluir, registrando o estado final da cobertura (verbos × famílias), o seam de reuso e qualquer pendência de re-gravação de goldens (provenance ainda derived-from-contract).
</output>
