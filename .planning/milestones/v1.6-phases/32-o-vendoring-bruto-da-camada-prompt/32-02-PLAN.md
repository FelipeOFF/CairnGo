---
phase: 32-o-vendoring-bruto-da-camada-prompt
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - tests/cairn-vendoring.bats
autonomous: true
requirements: [VEND-01, VEND-03, VEND-04]
beads:
  VEND-01: CairnGo-l17c
  VEND-03: CairnGo-01qa
  VEND-04: CairnGo-jpca
must_haves:
  truths:
    - "`tests/cairn-vendoring.bats` prova em fixture miniatura que o fecho converge até ponto fixo e que `closure --write` grava bytes idênticos ao `--json` (D-02, VEND-01)"
    - "Manifest gravado == saída viva do subcomando, comparados por comm nos DOIS sentidos — nenhum lado com resíduo (VEND-01)"
    - "Todo arquivo de files[] do manifest real é byte-idêntico ao mesmo caminho no clone da tag, e nada existe sob cairn/gsd/ além de files[] + MANIFEST.json + contracts/** — skip-gated pelo cache real (D-01, VEND-03)"
    - "A ausência de gsd-write-guard.js sob cairn/gsd/ é assertada por teste NOMEADO que explica a colisão com o bd como dono do estado (VEND-04)"
    - "Os blocos do corte (tests, docs, src, instalador, multi-host, capabilities, scripts de dev, eslint-rules) são provados ausentes por teste tabular sobre lista de caminhos proibidos (VEND-04)"
    - "Todo guard novo tem controle negativo pareado que prova que ele MORDE: byte corrompido é pego pelo diff, arquivo proibido forjado é pego pelo laço de ausência"
  artifacts:
    - path: "tests/cairn-vendoring.bats"
      provides: "prova executável de VEND-01/03/04 — fidelidade, derivação do manifest e o corte"
      contains: "the cut holds"
      min_lines: 120
  key_links:
    - from: "tests/cairn-vendoring.bats"
      to: "cairn/gsd/MANIFEST.json"
      via: "re-executa closure e compara com o manifest gravado por comm nos dois sentidos — expectativa recomputada, nunca digitada"
      pattern: "comm -"
    - from: "tests/cairn-vendoring.bats"
      to: ".cairn/cache/gsd-core-v1.10.0"
      via: "asserções contra o clone real skip-gated pela presença do cache (padrão real_inv)"
      pattern: "skip"
  prohibitions:
    - statement: "Nenhum teste digita uma contagem tirada da árvore real sem o comando de reprodução ao lado — expectativas de fixture vêm do fixture, expectativas reais são recomputadas do manifest/comando (disciplina da casa)"
---

<objective>
A suíte que transforma as afirmações da fase em prova: o manifest é derivado (o
bats re-roda o subcomando e compara, D-02), a árvore vendorizada é byte-idêntica
ao clone na lista de inclusão nos dois sentidos (D-01, VEND-03), e o corte segura
— gsd-write-guard.js e os blocos excluídos estão ausentes por teste (VEND-04).
Cada guard nasce com seu controle negativo.

Purpose: um guard que só passa não prova nada; a fase 36 vai editar a árvore e
esta suíte é o que denuncia qualquer divergência não-intencional da base.
Output: tests/cairn-vendoring.bats.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-o-vendoring-bruto-da-camada-prompt/32-CONTEXT.md
@.planning/phases/32-o-vendoring-bruto-da-camada-prompt/32-PATTERNS.md
@.planning/phases/32-o-vendoring-bruto-da-camada-prompt/32-01-SUMMARY.md
@tests/cairn-inventory.bats
@tests/gsd-contracts.bats
</context>

<assuncoes_sinalizadas>
Probe specless (fase sem SPEC): o edge probe classificou VEND-01, VEND-03 e
VEND-04 como `unclassified/unresolved` — nenhum edge auto-resolvido. As truths
goal-backward do frontmatter carregam os critérios; o verifier trata o que não
confirmar como `human_needed`, nunca passe silencioso.
</assuncoes_sinalizadas>

<tasks>

<task type="auto">
  <name>Task 1: fixture miniatura — fecho, manifest derivado, cópia fiel e controles negativos</name>
  <files>tests/cairn-vendoring.bats</files>
  <read_first>
    - tests/cairn-inventory.bats:16-34 (cabeçalho com notas de estilo de asserção — repetir o bloco; fixtures locais ao arquivo, helpers.bash intocado), 37-107 (new_corpus_fixture: repo git local com a tag e mini-corpus sintético + helpers inv/ijq — o modelo exato a estender), 337-375 (GUARD + controle negativo em par)
    - tests/gsd-contracts.bats:229-249 (comparação de conjuntos por comm nos dois sentidos com resíduos impressos no stderr), 286-302 (controle negativo com artefato forjado)
    - tests/helpers.bash:31-33 (CAIRN_TESTS_DIR/CAIRN_REPO_ROOT/CAIRN_SCRIPTS_DIR exportados)
    - .planning/phases/32-o-vendoring-bruto-da-camada-prompt/32-01-SUMMARY.md (nomes/flags finais dos subcomandos como implementados)
  </read_first>
  <action>
    Criar tests/cairn-vendoring.bats com o cabeçalho de notas de estilo da casa
    (status exato com -eq, grep -qF para substrings, nenhuma contagem da árvore
    real sem comando ao lado) e load 'helpers'. Copiar o builder
    new_corpus_fixture de cairn-inventory.bats para dentro DESTE arquivo
    (fixtures são locais ao arquivo — helpers.bash não é tocado) e estendê-lo
    com uma cadeia de referências para provar o ponto fixo em miniatura: um
    workflow do fixture menciona gsd-core/references/r1.md, r1.md menciona
    gsd-core/templates/t1.md, t1.md não menciona nada; mais um arquivo
    gsd-core/references/orfao.md que NINGUÉM menciona. Testes sobre o fixture:

    1. closure converge: a lista contém o workflow, r1.md e t1.md (fecho de 2
       saltos) e NÃO contém orfao.md; duas execuções emitem bytes idênticos.
    2. manifest derivado: closure --write num path temporário e diff contra a
       saída do --json — vazio; newline final via tail -c1 | od (forma exata de
       gsd-contracts.bats:44-47).
    3. comparação por conjuntos: files[] do manifest gravado vs saída viva por
       comm -23 / comm -13, os dois resíduos vazios, e em falha os dois lados
       impressos no stderr (forma de gsd-contracts.bats:229-249).
    4. vendor fiel no fixture: vendor com --manifest/--dest para dirs
       temporários; cada arquivo copiado byte-idêntico (cmp) e nada além da
       lista no dest.
    5. controle negativo do diff: corromper um byte numa cópia temporária e
       provar que a comparação MORDE (status não-zero, arquivo nomeado na
       saída) — um guard que só passa não prova nada.
    6. controle negativo da ausência: montar árvore simulada em
       BATS_TEST_TMPDIR com um arquivo proibido injetado e provar que o laço
       de ausência da task 2 o pega (a função de asserção recebe a raiz como
       argumento para ser apontável ao dir forjado — modelo
       gsd-contracts.bats:292-301).
    7. contrato flat preservado: a invocação sem subcomando sobre o fixture
       segue funcionando (run com --json e status 0), e exit codes exatos dos
       caminhos de erro do vendor (manifest ausente -eq 2, commit divergente
       -eq 6).
  </action>
  <verify>
    <automated>bats tests/cairn-vendoring.bats && bats tests/cairn-inventory.bats tests/gsd-contracts.bats</automated>
  </verify>
  <acceptance_criteria>
    - `bats tests/cairn-vendoring.bats` passa com os testes de fixture rodando SEM cache real (nenhum skip nos testes de fixture)
    - O teste de ponto fixo prova inclusão por referência transitiva (2 saltos) e exclusão do órfão — expectativas vindas do próprio fixture, nunca literais da árvore real
    - Os dois controles negativos falham na direção certa: corrupção de byte → status não-zero com o path na saída; arquivo forjado → laço de ausência retorna 1 e o nome forjado aparece via grep -qF
    - Status codes assertados com -eq exato (2 e 6 nos caminhos de erro do vendor)
    - `bats tests/cairn-inventory.bats tests/gsd-contracts.bats` seguem verdes (nenhuma edição em suítes existentes nem em helpers.bash)
  </acceptance_criteria>
  <done>Fecho, derivação do manifest, fidelidade da cópia e vivacidade dos guards provados em miniatura, sem depender de rede ou do cache real.</done>
</task>

<task type="auto">
  <name>Task 2: os artefatos reais — fidelidade skip-gated, o corte segura, guard de números</name>
  <files>tests/cairn-vendoring.bats</files>
  <read_first>
    - tests/cairn-inventory.bats:377-435 (GUARD de docstring com os substantivos vigiados e o helper real_inv com skip-gating pelo cache — copiar o skip literal), 422-428 (valor medido assertado com a divergência anotada ao lado)
    - tests/gsd-contracts.bats:38-49 (laço com acumulador fails e return 1 explícito), 175-199 (identidade de fonte entre artefatos irmãos — modelo para manifest vs contracts)
    - .planning/research/v1.6-transplante-gsd.md §3 (a tabela do que fica de fora — a fonte da lista de caminhos proibidos)
  </read_first>
  <action>
    Acrescentar ao mesmo arquivo os testes sobre os artefatos commitados e o
    clone real:

    1. Sempre rodam (sem cache): MANIFEST.json existe, parseia, source
       {repo,tag,commit} idêntico ao de cairn/gsd/contracts/contracts.json
       (identidade de fonte entre irmãos, modelo gsd-contracts.bats:184-199);
       files[] ordenado e não-vazio; newline final; todo path de files[] existe
       sob cairn/gsd/ (fidelidade de presença não precisa do cache).
    2. Sempre rodam — o corte (VEND-04): teste NOMEADO
       "the cut holds: gsd-write-guard.js is NOT vendored (collides with bd as
       state owner)" assertando [ ! -e ] e find por gsd-write-guard* vazio sob
       cairn/gsd/; e o teste tabular iterando a lista de caminhos/padrões
       proibidos derivada do research §3 — tests/, docs/, src/, install.js,
       scripts/ de dev, eslint-rules/, capabilities/, .github/, examples/,
       .changeset/, e os blocos multi-host (host-integration-sdk, adapter-*,
       runtime-*) — com acumulador fails e os sobreviventes impressos no
       stderr (shape do laço ORPHAN_PAIRS, gsd-contracts.bats:333-360). A
       função de asserção recebe a raiz como argumento (é a mesma que o
       controle negativo da task 1 morde).
    3. Sempre rodam: sob cairn/gsd/ não existe NADA além de files[] do
       manifest + MANIFEST.json + contracts/** — find recursivo comparado por
       comm nos dois sentidos com files[]; resíduo em qualquer sentido falha
       nomeando os paths (é o sentido inverso de VEND-01/VEND-03: "exatamente
       os arquivos dela").
    4. Skip-gated pelo cache real (copiar o helper e o skip literal de
       real_inv, cairn-inventory.bats:430-435): todo arquivo de files[]
       byte-idêntico entre cairn/gsd/<path> e o cache (laço com cmp e
       acumulador); manifest gravado == closure re-executado contra o cache
       real (comm nos dois sentidos); LICENSE byte-idêntico ao do clone; e
       totals.files do manifest bate com a contagem recomputada de files[] —
       o comando de reprodução ao lado de cada asserção, e qualquer
       divergência do 160/28.071 do research anotada ao lado do valor medido,
       nunca o contrário.
    5. Guard de números do vendoring: estender o padrão do GUARD de docstring
       (cairn-inventory.bats:377-420) NESTA suíte para os substantivos do
       fecho — "arquivos" e "linhas" com valor numérico fora do bloco datado
       MEASURED VERSUS ASSUMED do cairn-inventory.py falham; par de vivacidade
       obrigatório: injetar num arquivo temporário uma contagem forjada fora
       do bloco e provar que o guard a pega (modelo
       cairn-inventory.bats:365-375). O guard novo vive aqui — as suítes
       existentes não são editadas.
  </action>
  <verify>
    <automated>bats tests/cairn-vendoring.bats && bats tests/cairn-inventory.bats tests/gsd-contracts.bats && bats tests/</automated>
  </verify>
  <acceptance_criteria>
    - `bats tests/cairn-vendoring.bats` passa; com o cache real presente, zero skips reportados; sem cache, só os testes da faixa 4 skipam com a mensagem que ensina a reproduzir
    - O teste nomeado do write-guard e o tabular do corte passam contra a árvore real, e o controle negativo prova que o mesmo laço morde a árvore forjada
    - `comm` dos dois sentidos entre find(cairn/gsd/) e files[] devolve vazio nos dois lados (exclusões: MANIFEST.json e contracts/**)
    - Identidade de fonte: source.commit do MANIFEST.json == source.commit do contracts.json == 68a04ccf8ef74803bdb651e12c3b85b218bbccdf, por jq, sem literal digitado duas vezes no teste (lido de um e comparado ao outro)
    - Nenhuma contagem da árvore real digitada sem o comando de reprodução ao lado (disciplina da casa, verificável por leitura do diff da suíte)
    - Suíte completa do repo (`bats tests/`) sem regressão nova
  </acceptance_criteria>
  <done>VEND-03 e VEND-04 provados por teste contra os artefatos reais; o sentido inverso de VEND-01 fechado; guards com vivacidade provada; zero regressão.</done>
</task>

</tasks>

<verification>
- `bats tests/cairn-vendoring.bats` verde com cache real (zero skip) e verde sem cache (skips só na faixa de fidelidade real, com mensagem de reprodução)
- Os quatro pilares provados: manifest derivado (comm 2 sentidos), fidelidade byte a byte (cmp por files[]), exatidão do conjunto (find vs files[]), corte ausente (nomeado + tabular)
- Todo guard com controle negativo pareado que morde
- `bats tests/` do repo inteiro sem regressão
</verification>

<success_criteria>
- VEND-01 (sentido "exatamente os arquivos dela"): find vs manifest sem resíduo em nenhum sentido
- VEND-03: diff vazio por teste, nos dois sentidos, skip-gated pelo cache real
- VEND-04: gsd-write-guard.js por teste nomeado + blocos do corte por teste tabular, ambos com controle negativo
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- `tests/cairn-vendoring.bats` — a prova executável da fase: fecho em miniatura, manifest derivado, fidelidade byte a byte, exatidão do conjunto vendorizado, ausência do corte, e o guard de números do vendoring — cada guard com seu controle negativo
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/32-o-vendoring-bruto-da-camada-prompt/32-02-SUMMARY.md` when done
</output>
