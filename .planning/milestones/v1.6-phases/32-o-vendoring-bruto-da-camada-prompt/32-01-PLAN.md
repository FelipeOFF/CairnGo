---
phase: 32-o-vendoring-bruto-da-camada-prompt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cairn/scripts/cairn-inventory.py
  - cairn/scripts/cairn-inventory.sh
  - cairn/gsd/MANIFEST.json
  - cairn/gsd/LICENSE
  - "cairn/gsd/** (árvore vendorizada — ~160 arquivos espelhando o clone da tag)"
  - README.md
autonomous: true
requirements: [VEND-01, VEND-02]
beads:
  VEND-01: CairnGo-l17c
  VEND-02: CairnGo-dj7z
must_haves:
  truths:
    - "`cairn-inventory.sh closure --json` emite a lista do fecho transitivo dos 8 workflows sobre o corpus pinado (HEAD == 68a04cc), determinística entre duas execuções (D-02, VEND-01)"
    - "`cairn/gsd/MANIFEST.json` gravado é byte-idêntico à saída viva de `closure --json` (D-02, VEND-01)"
    - "Cada caminho de `files[]` do manifest existe sob `cairn/gsd/` com conteúdo byte-idêntico ao mesmo caminho relativo no clone da tag (D-01, VEND-01)"
    - "`cairn/gsd/LICENSE` é byte-idêntico ao LICENSE do clone (MIT License / Copyright (c) 2026 Open GSD) — sem empilhar copyright (VEND-02)"
    - "README §License & credits credita open-gsd/gsd-core com a tag v1.10.0 e o commit 68a04cc (VEND-02)"
    - "A invocação flat existente (`cairn-inventory.sh --json`, sem subcomando) segue intacta — `tests/cairn-inventory.bats` e `tests/gsd-contracts.bats` passam sem edição"
  artifacts:
    - path: "cairn/gsd/MANIFEST.json"
      provides: "lista de inclusão derivada do inventário (D-02) — artefato consumido pelo vendoring e pelo teste"
      contains: "68a04ccf8ef74803bdb651e12c3b85b218bbccdf"
    - path: "cairn/gsd/LICENSE"
      provides: "MIT do upstream intacto"
      contains: "MIT License"
    - path: "cairn/gsd/gsd-core/workflows/fast.md"
      provides: "prova do espelhamento da árvore (menor workflow do fecho — caminho relativo idêntico ao clone, D-01)"
    - path: "cairn/scripts/cairn-inventory.py"
      provides: "subcomandos closure e vendor sobre o mesmo ensure_corpus da fase 31"
      contains: "closure"
    - path: "README.md"
      provides: "crédito com tag e commit do pin"
      contains: "68a04cc"
  key_links:
    - from: "cairn/scripts/cairn-inventory.py"
      to: "cairn/gsd/MANIFEST.json"
      via: "closure --write grava com o MESMO serializer do --json (indent 2, sort_keys, newline final)"
      pattern: "sort_keys"
    - from: "cairn/gsd/MANIFEST.json"
      to: "cairn/gsd/"
      via: "vendor lê files[] e copia do cache SÓ depois de ensure_corpus validar o HEAD"
      pattern: "ensure_corpus"
    - from: "cairn/scripts/cairn-inventory.sh"
      to: "cairn/scripts/cairn-inventory.py"
      via: "exec python3 preservando argv — o subcomando passa pelo wrapper sem mudança de contrato"
      pattern: "exec python3"
  prohibitions:
    - statement: "Nenhum arquivo vendorizado sofre transformação de conteúdo na cópia — sem normalização de whitespace, sem conversão /gsd:→/gsd-, sem frontmatter injetado; qualquer adaptação é a fase 36 (o ruído de instalador do research §2.2 é exatamente o que esta fase existe para NÃO produzir)"
    - statement: "cairn/gsd/LICENSE nunca é alterado nem empilha copyright — entra byte-idêntico ao LICENSE do clone, e o crédito no README nunca atribui o trabalho do upstream ao CairnGo"
    - statement: "gsd-write-guard.js nunca entra em cairn/gsd/, nem por cópia acidental de diretório inteiro — a cópia é por lista de arquivos, nunca por copytree de diretório do clone"
---

<objective>
Vendoring bruto da camada prompt do GSD v1.10.0: o subcomando de fecho transitivo
no `cairn-inventory` emite a lista de inclusão, ela vira `cairn/gsd/MANIFEST.json`
versionado (D-02), e a mecânica `vendor` copia exatamente esses arquivos do clone
pinado para `cairn/gsd/`, espelhando os caminhos relativos do clone (D-01), com o
LICENSE MIT intacto e o crédito no README com tag v1.10.0 e commit 68a04cc.

Purpose: a fase 36 edita esses arquivos em lugar e precisa medir a própria mudança
contra uma base byte-idêntica ao upstream; sem manifest derivado, a lista de
inclusão vira juízo de momento.
Output: subcomandos `closure` e `vendor` no inventory, MANIFEST.json, árvore
vendorizada (~160 arquivos + LICENSE), crédito no README.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/32-o-vendoring-bruto-da-camada-prompt/32-CONTEXT.md
@.planning/phases/32-o-vendoring-bruto-da-camada-prompt/32-PATTERNS.md
@.planning/research/v1.6-transplante-gsd.md
@cairn/scripts/cairn-inventory.py
</context>

<assuncoes_sinalizadas>
Probe specless (fase sem SPEC): o edge probe classificou VEND-01 e VEND-02 como
`unclassified/unresolved` — nenhum edge auto-resolvido. As truths goal-backward do
frontmatter carregam os critérios de aceitação; o verifier trata o que não
confirmar como `human_needed`, nunca passe silencioso. As proibições do
frontmatter vêm da recall in-prompt (§B) e são flagged-unverified por construção.
</assuncoes_sinalizadas>

<tasks>

<task type="tracer">
  <name>Task 1: subcomando closure + manifest + vendor — um caminho de ponta a ponta</name>
  <reversibility rating="costly">D-01: a árvore espelhada nasce aqui e a fase 36 edita esses arquivos em lugar; reorganizar depois quebra o diff contra o clone e o mapa mental dos sítios medidos.</reversibility>
  <precondition>git disponível e (rede até github.com OU cache válido em .cairn/cache/gsd-core-v1.10.0) — ensure_corpus clona na ausência do cache e morre com exit 5 sem rede.</precondition>
  <files>cairn/scripts/cairn-inventory.py, cairn/gsd/MANIFEST.json, cairn/gsd/ (árvore vendorizada), cairn/gsd/LICENSE</files>
  <read_first>
    - cairn/scripts/cairn-inventory.py:108-118 (taxonomia de exit codes com comentário de porquê), 196-205 (die + read_text), 208-216 (parser FLAT atual — o contrato a preservar), 229-273 (ensure_corpus — REUSAR, nunca duplicar), 276-288 (workflows8_files), 340-348 (agents_files), 454-559 (separação build_model/render + serialização da casa)
    - cairn/scripts/cairn-config.py:736-778 (padrão de subparsers da casa — considerado e descartado nesta task, ver action)
    - .planning/phases/32-o-vendoring-bruto-da-camada-prompt/32-PATTERNS.md (seções cairn-inventory.py, MANIFEST.json e mecânica de cópia)
    - .planning/research/v1.6-transplante-gsd.md §2.1 (método do fecho: 3 passadas de grep até ponto fixo; composição de referência 160/28.071) e §3 (o que entra e o que fica de fora)
    - cairn/gsd/contracts/config.json (envelope source{repo,tag,commit} a espelhar no manifest)
  </read_first>
  <action>
    Adicionar dois subcomandos ao cairn-inventory.py sem tocar no contrato flat
    existente. Rota de dispatch: rota (b) do PATTERNS — em main(), se sys.argv[1]
    for "closure" ou "vendor", despachar para um parser dedicado do subcomando;
    caso contrário seguir para build_parser() flat inalterado. A rota (a)
    (add_subparsers com default) foi considerada — é o precedente da casa em 12
    scripts — mas descartada aqui porque duas suítes (cairn-inventory.bats,
    gsd-contracts.bats) chamam a invocação sem subcomando e um subparser opcional
    exigiria duplicar as flags flat no top-level; (b) preserva o contrato
    byte a byte. Registrar essa escolha em comentário curto no dispatch.

    Subcomando `closure` (flags: --json, --write PATH, e as mesmas --cache-dir /
    --source / --expect-commit / --refresh do flat): resolve o corpus via
    ensure_corpus exatamente como main() faz hoje (cairn-inventory.py:548-556 —
    valida HEAD contra 68a04ccf8ef74803bdb651e12c3b85b218bbccdf, die 6 em
    divergência). Sementes do fecho: workflows8_files(root) (8 .md + subdirs de
    steps/modes), agents_files(root) (AGENTS_SCOPE, 16 nomes na fase 31), e os
    shims 1:1 dos 8 workflows — para cada nome de WORKFLOWS_8, incluir
    commands/<match>.md e skills/<match>/SKILL.md quando existirem no clone
    (descobrir o layout real com ls no cache; zero match para um workflow não é
    erro fatal, é registrado no summary do modelo). Rejeição registrada: o
    research §3 propõe fechar shims via `requires:` (9 commands, 34 shims), em
    contradição interna com o próprio §2.1 e com o Goal da fase, que enumeram 8
    shims 1:1 — o goal vence; o plano segue 8 shims 1:1. Guarda derivada dessa
    rejeição: depois do ponto fixo, varrer cada SKILL.md do conjunto por
    declarações `requires:` e die (não-zero) nomeando o SKILL.md e o command
    ausente se alguma apontar para command fora da lista vendorizada — o fecho
    não pode prometer um shim que não entrega. Fecho: iterar sobre o
    conjunto corrente com grep por caminhos referenciados casando o padrão
    (gsd-core/)?(references|templates|contexts)/<path>.md — normalizando as
    grafias ~/.claude/gsd-core/..., @-prefixadas e nuas para o caminho relativo
    à raiz do clone (gsd-core/references/..., etc.) — mantendo só caminhos que
    existem no cache, até ponto fixo (método do research §2.1, "3 passadas até
    ponto fixo"). Ler arquivos sempre via read_text() (linhas 201-205), nunca
    path.read_text cru. Acrescentar a entrada fixa "LICENSE" ao conjunto (o MIT
    entra pela mesma lista, D-02 + VEND-02).

    Modelo (separação build_model/render da casa): schema_version 1; source
    {repo "open-gsd/gsd-core", tag "v1.10.0", commit
    "68a04ccf8ef74803bdb651e12c3b85b218bbccdf"} idêntico ao envelope de
    cairn/gsd/contracts/config.json; derived_from {command
    "cairn/scripts/cairn-inventory.sh closure --json", date date-only
    "YYYY-MM-DD", sem hora — duas execuções no mesmo dia emitem bytes
    idênticos, critério de determinismo}; files[] com
    caminhos relativos à raiz do clone, ordenados; totals {files, lines} (linhas
    por len(splitlines()) via read_text). --json imprime o modelo com
    json.dumps(indent=2, sort_keys=True, ensure_ascii=False) — o print dá o
    newline final; --write PATH grava EXATAMENTE os mesmos bytes (mesmo
    serializer + "\n" explícito no write). Render humano sem --json: só
    posiciona strings do modelo, nada é computado no render.

    Números de referência: o research §2.1 fecha em 160 arquivos/28.071 linhas.
    O número MEDIDO vence e fica registrado (CONTEXT, Specific Ideas).
    Divergências esperadas e pré-explicadas: AGENTS_SCOPE da fase 31 tem 16
    nomes onde o research contou 13 agents, e o conjunto de shims pode diferir
    da composição de referência — se der ≠160/28.071, não é defeito, é o medido
    vencendo; a task 2 registra o valor com o comando ao lado.

    Subcomando `vendor` (flags: --manifest PATH default cairn/gsd/MANIFEST.json,
    --dest DIR default cairn/gsd/, ambos resolvidos contra CLAUDE_PROJECT_DIR
    como main() faz, mais as flags de corpus): roda ensure_corpus PRIMEIRO
    (nada de cópia sobre corpus não verificado — regra do PATTERNS); manifest
    ausente → die 2 nomeando o path; manifest.source.commit diferente do commit
    esperado → die 6 nomeando os dois shas (mesma semântica do EXIT_BAD_CORPUS).
    Copia cada entrada de files[] do cache para dest preservando o caminho
    relativo (shutil.copy2 + mkdir de parents) — cópia POR LISTA, arquivo a
    arquivo, nunca copytree de diretório do clone (proibição: é assim que um
    hook indesejado entraria). NUNCA rmtree no dest: cairn/gsd/contracts/ da
    fase 31 mora lá e não é tocado nesta fase. Depois da cópia, verificar cada
    arquivo com filecmp.cmp(shallow=False) contra o cache e die 6 se algum
    divergir; imprimir contagem copiada no stdout. Erros sempre via die() com o
    prefixo [cairn-inventory].

    Rodar de ponta a ponta no corpus real: closure --write no manifest, vendor,
    e a prova do caminho único no menor workflow do fecho (fast, 110 linhas no
    research) — diff de gsd-core/workflows/fast.md e do shim correspondente
    entre cairn/gsd/ e o cache. Commit ao ficar verde.
  </action>
  <verify>
    <automated>cairn/scripts/cairn-inventory.sh closure --json | jq -e '.files | length > 0 and (.files == (.files | sort))' && cairn/scripts/cairn-inventory.sh closure --write cairn/gsd/MANIFEST.json && cairn/scripts/cairn-inventory.sh vendor && diff cairn/gsd/gsd-core/workflows/fast.md .cairn/cache/gsd-core-v1.10.0/gsd-core/workflows/fast.md && bats tests/cairn-inventory.bats tests/gsd-contracts.bats</automated>
  </verify>
  <acceptance_criteria>
    - `cairn/scripts/cairn-inventory.sh closure --json` sai 0 e emite JSON com `.files` não-vazio, ordenado, e `.source.commit == "68a04ccf8ef74803bdb651e12c3b85b218bbccdf"` (jq -e)
    - Duas execuções consecutivas de `closure --json` produzem bytes idênticos (determinismo: `diff <(...) <(...)` vazio)
    - `diff <(cairn/scripts/cairn-inventory.sh closure --json) cairn/gsd/MANIFEST.json` vazio (manifest == saída viva, D-02)
    - `cairn/scripts/cairn-inventory.sh vendor` sai 0; `diff cairn/gsd/gsd-core/workflows/fast.md .cairn/cache/gsd-core-v1.10.0/gsd-core/workflows/fast.md` vazio (caminho único provado de ponta a ponta)
    - `cmp cairn/gsd/LICENSE .cairn/cache/gsd-core-v1.10.0/LICENSE` sai 0 (LICENSE intacto via lista)
    - `tail -c1 cairn/gsd/MANIFEST.json | od -An -c` termina em \n (newline final da casa)
    - Invocação flat intacta: `bats tests/cairn-inventory.bats tests/gsd-contracts.bats` passa sem nenhuma edição nas suítes
    - `cairn/scripts/cairn-inventory.sh vendor --manifest /caminho/inexistente.json` sai exatamente 2; manifest com commit divergente sai exatamente 6
    - Nenhum SKILL.md vendorizado declara `requires:` apontando para command fora de files[] do manifest — a guarda do closure reprova (die não-zero) se declarar
  </acceptance_criteria>
  <done>Pipeline closure → manifest → vendor → diff provado de ponta a ponta no corpus real com o workflow fast; manifest gravado igual à saída viva; suítes existentes verdes; commit feito.</done>
</task>

<task type="auto">
  <name>Task 2: expansão — fidelidade do fecho inteiro, números medidos no bloco datado, usage do wrapper</name>
  <files>cairn/scripts/cairn-inventory.py, cairn/scripts/cairn-inventory.sh, cairn/gsd/ (árvore completa)</files>
  <read_first>
    - cairn/scripts/cairn-inventory.py:41-97 (bloco MEASURED VERSUS ASSUMED — a forma exata: cada número datado com o comando ao lado, seção ASSUMED obrigatória)
    - tests/cairn-inventory.bats:377-420 (o GUARD que falha se contagem viva existir fora do bloco datado no .py ou em qualquer lugar do .sh) e 422-428 (precedente: valor medido assertado com divergência anotada ao lado, nunca o contrário)
    - cairn/scripts/cairn-inventory.sh (14 linhas — só a linha de Usage muda)
  </read_first>
  <action>
    Varrer a fidelidade do fecho INTEIRO, não só o caminho do tracer: script
    inline python3 que lê cairn/gsd/MANIFEST.json e compara cada entrada de
    files[] byte a byte (filecmp.cmp shallow=False) entre cairn/gsd/<path> e
    .cairn/cache/gsd-core-v1.10.0/<path>, acumulando falhas e saindo não-zero se
    houver qualquer divergência ou arquivo ausente. Sentido inverso: find de
    todos os arquivos sob cairn/gsd/ EXCLUINDO MANIFEST.json e contracts/**
    (vizinhos da fase 31, fora do vendoring por decisão do CONTEXT), comparado
    via comm nos dois sentidos com files[] do manifest — os dois lados vazios.
    Qualquer resíduo em qualquer sentido é corrigido pela mecânica (nunca à
    mão): regerar manifest e re-rodar vendor.

    Registrar os números medidos do fecho no docstring do cairn-inventory.py,
    DENTRO do bloco datado MEASURED VERSUS ASSUMED, na forma da casa: a contagem
    de arquivos e de linhas do fecho com data e o comando ao lado (jq .totals
    sobre closure --json), e — se divergirem de 160/28.071 do research §2.1 — a
    divergência anotada ao lado do valor medido, nunca o contrário (o medido
    vence, CONTEXT). Nenhum número fora do bloco; nomear também no ASSUMED o que
    segue assumido (ex.: que o layout de shims descoberto cobre todos os 8).

    Atualizar SÓ a linha de Usage do cairn-inventory.sh com os dois subcomandos
    novos e suas flags principais. Nenhum número entra no .sh — o GUARD
    cairn-inventory.bats:402-407 varre o wrapper inteiro por contagens.
  </action>
  <verify>
    <automated>python3 -c "
import filecmp,json,sys
from pathlib import Path
m=json.loads(Path('cairn/gsd/MANIFEST.json').read_text())
cache=Path('.cairn/cache/gsd-core-v1.10.0'); dest=Path('cairn/gsd')
fails=[p for p in m['files'] if not (dest/p).is_file() or not filecmp.cmp(dest/p,cache/p,shallow=False)]
tree={str(p.relative_to(dest)) for p in dest.rglob('*') if p.is_file()}
tree-={'MANIFEST.json'}; tree={p for p in tree if not p.startswith('contracts/')}
extra=tree-set(m['files']); missing=set(m['files'])-tree
sys.exit(1 if (fails or extra or missing) else 0)
" && bats tests/cairn-inventory.bats</automated>
  </verify>
  <acceptance_criteria>
    - O sweep python3 acima sai 0: todo arquivo de files[] é byte-idêntico ao cache, e sob cairn/gsd/ não existe nada além de files[] + MANIFEST.json + contracts/** (diff vazio nos DOIS sentidos, VEND-01/VEND-03 base)
    - `jq -r '.totals.files' cairn/gsd/MANIFEST.json` reproduz a contagem registrada no docstring; grep do valor no cairn-inventory.py o encontra apenas DENTRO do bloco MEASURED VERSUS ASSUMED
    - `bats tests/cairn-inventory.bats` passa (o GUARD de docstring aceita o bloco novo; nada de contagem no .sh)
    - `grep -c 'closure\|vendor' cairn/scripts/cairn-inventory.sh` >= 1 na linha de Usage e `bats tests/gsd-contracts.bats` segue verde
  </acceptance_criteria>
  <done>Fecho inteiro provado byte-idêntico nos dois sentidos; números medidos datados com comando ao lado no bloco da casa; wrapper documenta os subcomandos sem números.</done>
</task>

<task type="auto">
  <name>Task 3: crédito no README com tag e commit do pin</name>
  <files>README.md</files>
  <read_first>
    - README.md:118-123 (seção License & credits existente — o tom e o formato do crédito ao fork original)
    - README.md:75 (precedente de como o README fala do upstream com tag pinada — a tabela de plugins NÃO muda nesta fase; a saída do marketplace é a fase 37)
    - .planning/research/v1.6-transplante-gsd.md §5 (obrigação MIT: copyright + permission notice junto da porção substancial; forma proposta do crédito)
  </read_first>
  <action>
    Acrescentar à seção License & credits existente (decisão do CONTEXT, Claude's
    Discretion resolvida: o crédito entra ali, não em seção nova) uma ou duas
    frases no mesmo tom do crédito ao fork original: o GSD embutido em cairn/gsd/
    deriva de open-gsd/gsd-core (link para o repo), MIT, pinado na tag v1.10.0,
    commit 68a04cc — registrando tag e commit exatos para syncs futuros
    (research §5), e apontando que o LICENSE do upstream vive intacto em
    cairn/gsd/LICENSE. Não tocar na tabela de plugins nem em qualquer outra
    seção; não alterar o crédito existente ao fork.
  </action>
  <verify>
    <automated>grep -q 'open-gsd/gsd-core' README.md && grep -q 'v1.10.0' README.md && grep -q '68a04cc' README.md && awk '/## License & credits/,0' README.md | grep -q 'cairn/gsd/LICENSE'</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/## License & credits/,0' README.md` contém open-gsd/gsd-core, v1.10.0, 68a04cc e cairn/gsd/LICENSE (VEND-02)
    - O crédito ao fork eventually-consistent-code permanece intacto (grep segue encontrando a linha existente)
    - `git diff README.md` toca só a seção License & credits — a tabela de plugins (linha 75) não muda
  </acceptance_criteria>
  <done>README credita o upstream com tag e commit na seção de créditos existente, sem tocar no resto.</done>
</task>

</tasks>

<verification>
- `diff <(cairn/scripts/cairn-inventory.sh closure --json) cairn/gsd/MANIFEST.json` vazio (a lista é derivada, não digitada — D-02)
- Sweep de fidelidade nos dois sentidos (task 2) sai 0 sobre o fecho inteiro
- `cmp cairn/gsd/LICENSE .cairn/cache/gsd-core-v1.10.0/LICENSE` sai 0
- `bats tests/cairn-inventory.bats tests/gsd-contracts.bats` verdes sem edição (contrato flat preservado)
- README: tag + commit + link na seção de créditos
</verification>

<success_criteria>
- VEND-01: lista gerada pelo subcomando do inventário; cairn/gsd/ recebe exatamente os arquivos dela (dois sentidos)
- VEND-02: LICENSE MIT intacto em cairn/gsd/LICENSE; README credita com tag e commit
- Números do fecho medidos e registrados no bloco datado com o comando ao lado; se ≠160/28.071, o medido vence com a divergência anotada
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- `cairn/scripts/cairn-inventory.py` — subcomandos `closure` (fecho transitivo → lista) e `vendor` (cópia por manifest), sobre o mesmo ensure_corpus da fase 31
- `cairn/gsd/MANIFEST.json` — lista de inclusão versionada, derivada e testável (schema_version, source, derived_from, files[], totals)
- `cairn/gsd/**` — árvore vendorizada byte a byte espelhando os caminhos relativos do clone v1.10.0 (~160 arquivos; o medido vence)
- `cairn/gsd/LICENSE` — MIT do upstream intacto
- `README.md` — crédito open-gsd/gsd-core com tag v1.10.0 e commit 68a04cc
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/32-o-vendoring-bruto-da-camada-prompt/32-01-SUMMARY.md` when done
</output>
