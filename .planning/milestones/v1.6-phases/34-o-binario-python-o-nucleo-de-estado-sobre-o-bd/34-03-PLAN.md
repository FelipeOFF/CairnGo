---
phase: 34-o-binario-python-o-nucleo-de-estado-sobre-o-bd
plan: 03
type: execute
wave: 3
depends_on: [34-02]
files_modified:
  - cairn/scripts/cairn-gsd-state.py
  - tests/cairn-gsd.bats
  - tests/fixtures/gsd-goldens/scenarios.json
  - tests/fixtures/gsd-goldens/*.golden.json
  - tests/fixtures/gsd-goldens/divergences.json
autonomous: true
requirements: [CORE-05, CORE-02, CORE-03, CORE-04]
beads:
  CORE-05: CairnGo-5uuw
  CORE-02: CairnGo-897j
  CORE-03: CairnGo-u7z9
  CORE-04: CairnGo-av47
must_haves:
  truths:
    - "os 12 verbos da família roadmap-phase respondem em python na forma de roadmap-phase.json, verbo a verbo (CORE-05)"
    - "a regra documento-vs-fato vale em todo verbo misto: o payload que É documento (seção do ROADMAP, lista de PLANs, frontmatters) vem do arquivo; todo campo que É fato de estado (current_phase, phase_status) vem do label projetado do portador; progress é DERIVADO de fatos do portador (phase/plan) + contagem de artefatos — nunca de prosa de STATE.md, nunca dimensão nova no vocabulário D-02 (CORE-02)"
    - "phase.complete é transição auditada: phase_status:complete (e o avanço para a fase seguinte) via set-state com ator e motivo; reentrada com a fase já complete é idempotente (CORE-03)"
    - "verbo misto com fato ausente falha nomeando o fato e o comando — roadmap.analyze num repo sem portador não inventa current_phase (CORE-04)"
    - "o guard de cobertura conhece roadmap-phase: os 12 verbos aparecem no --list-implemented agregado, comm vazio"
    - "cairn-gsd-state.py permanece <= 1500 linhas (D-01: ~1.5k é teto, não alvo)"
  artifacts:
    - cairn/scripts/cairn-gsd-state.py
    - tests/fixtures/gsd-goldens/scenarios.json
    - tests/cairn-gsd.bats
  key_links:
    - "roadmap.analyze → portador bd para current_phase (label projetado) e progress derivado (fatos do portador + contagem de artefatos) + parse de ROADMAP.md para a lista de fases: duas fontes, cada uma dona do seu tipo de dado, nenhuma respondendo pelo da outra"
    - "phase.complete → set-state no portador + edições de documento (ROADMAP checkbox, REQUIREMENTS) na forma que o shape do contrato descreve"
    - "parsers de fase/plano → moldes de cairn-bookkeep (parse_roadmap L958) e cairn-status (regexes L369-499) — cópia de forma, não reimplementação de cabeça"
  prohibitions:
    - statement: "Nenhum campo de FATO de estado no shape de um verbo roadmap-phase é respondido lendo .planning/STATE.md — fato vem do portador bd ou o verbo falha nomeado"
    - statement: "Nenhuma transição de dimensão escreve no bd por caminho que não seja set-state com ator e motivo"
    - statement: "Nenhuma task escreve sob cairn/gsd/** — divergência deliberada vai em tests/fixtures/gsd-goldens/divergences.json"
---

<objective>
A família roadmap-phase (12 verbos, ~600 linhas estimadas no research §4) entra no
cairn-gsd-state.py: os leitores de documento (find-phase, phases.list, phase.list-plans,
phase.list-artifacts, roadmap.get-phase, phase-plan-index, roadmap.analyze,
phase.mvp-mode) e os verbos de transição/mutação (phase, phase.complete,
roadmap.annotate-dependencies, roadmap.update-plan-progress). O ponto fino da fase mora
aqui: ROADMAP.md é DOCUMENTO e continua sendo lido como payload; current_phase e
phase_status são FATO e vêm do bd — o verbo que mistura os dois obedece às duas regras ao
mesmo tempo (CORE-05, D-02).

Purpose: fechar os 20+ sítios de roadmap/phase dos 8 workflows, mantendo a fonte única de
fato que o milestone existe para impor.
Output: família roadmap-phase completa no irmão de estado, cenários/goldens por verbo,
guard estendido.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-o-binario-python-o-nucleo-de-estado-sobre-o-bd/34-CONTEXT.md
@.planning/phases/34-o-binario-python-o-nucleo-de-estado-sobre-o-bd/34-PATTERNS.md
@.planning/phases/34-o-binario-python-o-nucleo-de-estado-sobre-o-bd/34-02-SUMMARY.md
@cairn/gsd/contracts/roadmap-phase.json
</context>

## Artifacts this phase produces (escopo deste plano)

| Artefato | O que entra neste plano |
|---|---|
| `cairn/scripts/cairn-gsd-state.py` | 12 handlers novos — 8 leitores de documento + 4 mistos/transições da família roadmap-phase |
| Regra documento-vs-fato | codificada em helper único (um resolvedor de fato por label + parsers de documento por molde bookkeep/status), consumido por todos os handlers mistos |
| Cenários + goldens | por verbo/situação; fixtures com .planning/ROADMAP.md e PLANs reais montados por fixture.files + portador bd por fixture.bd |
| `tests/cairn-gsd.bats` | guard de cobertura com roadmap-phase no filtro |

## Flagged assumptions (probe spec-less — escopo deste plano)

Do registro consolidado no 34-01-PLAN.md: CORE-05 unclassified resolve-se por cobertura
derivada do inventário (os 12 verbos entram no guard aqui; o fecho total é o plano 05) e
CORE-04 vale nos verbos mistos (critérios abaixo). Nenhum item descartado.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: os 8 leitores — find-phase, phases.list, phase.list-plans, phase.list-artifacts, roadmap.get-phase, phase-plan-index, roadmap.analyze, phase.mvp-mode</name>
  <files>cairn/scripts/cairn-gsd-state.py, tests/cairn-gsd.bats, tests/fixtures/gsd-goldens/scenarios.json, tests/fixtures/gsd-goldens/find-phase*.golden.json, tests/fixtures/gsd-goldens/phases-list*.golden.json, tests/fixtures/gsd-goldens/roadmap-*.golden.json, tests/fixtures/gsd-goldens/phase-*.golden.json</files>
  <read_first>
    - cairn/gsd/contracts/roadmap-phase.json (os 8 verbos: shape, flags, notes — roadmap.analyze carrega current_phase/progress_percent, que são FATO; os demais campos são documento)
    - cairn/scripts/cairn-bookkeep.py L958 (parse_roadmap) e L1104-1150 (parse_state_frontmatter — forma de parse de frontmatter para phase-plan-index)
    - cairn/scripts/cairn-status.py L369-499 (o bloco de regexes de fase/plano/summary — PHASE_DIR_PREFIX, PLAN_FILE, SUMMARY_FILE)
    - cairn/scripts/cairn-gsd-state.py (o resolvedor de portador e o envelope, como os planos 01-02 deixaram)
  </read_first>
  <behavior>
    - roadmap.get-phase 34 num fixture com ROADMAP.md real → {found:true, section, goal, phase_name, phase_number, ...} do documento
    - find-phase/phases.list/phase.list-plans/phase.list-artifacts → enumeração de diretórios/arquivos pelo shape do contrato
    - phase-plan-index → waves/depends_on/has_checkpoints parseados dos frontmatters dos PLANs do fixture
    - roadmap.analyze → lista de fases do documento + current_phase/progress DO PORTADOR; sem portador → falha nomeada
    - phase.mvp-mode → composição config + modo do roadmap, pelo shape
  </behavior>
  <action>
    Implementar os 8 leitores. Documento é documento: ROADMAP.md, diretórios de fase, PLANs
    e SUMMARYs são lidos do filesystem pelos moldes de parse da casa (parse_roadmap,
    regexes de cairn-status, parse de frontmatter de bookkeep) — leniência de parse dos
    moldes, die nomeado quando o arquivo requerido pelo contrato falta e o contrato não
    define fallback. Fato é fato: em roadmap.analyze, current_phase (e qualquer campo de
    posição corrente) vem dos labels do portador (`phase:N` etc.) pelo resolvedor do
    plano 01; progress_percent é DERIVADO — fatos do portador (phase/plan) + contagem de
    artefatos — jamais um label `progress:*` novo (D-02 é one-way, 5 dimensões fixas).
    Sem portador, falha nomeada prescrevendo state.begin-phase
    (CORE-04); a lista/contagem de fases e milestones vem do documento. Escrever o
    resolvedor de fato UMA vez como helper e consumi-lo em todo handler que precise de
    posição corrente — nenhuma segunda implementação da consulta.

    Cenários + goldens derived-from-contract por verbo: fixtures combinam fixture.files
    (ROADMAP.md mínimo com 2-3 fases, diretório de fase com PLANs de frontmatter real) e
    fixture.bd (portador com phase/phase_status semeados). Cenário adversarial de
    roadmap.analyze: fixture com ROADMAP completo e SEM portador → exit 1 nomeado, provando
    que o documento não responde pelo fato.
  </action>
  <verify>
    <automated>bats tests/cairn-gsd.bats</automated>
  </verify>
  <acceptance_criteria>
    - `query roadmap.get-phase 34 --raw` num fixture devolve found:true com section/goal não vazios e as chaves do shape; fase inexistente devolve o ramo de erro do contrato (não traceback)
    - `query phase-plan-index 34 --raw` devolve waves/plans coerentes com os frontmatters do fixture
    - `query roadmap.analyze --raw` com portador phase:34 devolve current_phase 34; SEM portador sai exit 1 com stderr nomeando o fato e o comando
    - os 8 verbos têm >= 1 cenário no manifesto com golden provenance derived-from-contract
    - `bats tests/cairn-gsd.bats` verde offline, sem regressão nos planos anteriores
  </acceptance_criteria>
  <done>Os 8 leitores respondem: documento do arquivo, fato do bd, cada fonte dona do seu tipo — provado pelo cenário adversarial.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: os 4 mistos/transições — phase, phase.complete, roadmap.annotate-dependencies, roadmap.update-plan-progress — e o guard</name>
  <files>cairn/scripts/cairn-gsd-state.py, tests/cairn-gsd.bats, tests/fixtures/gsd-goldens/scenarios.json, tests/fixtures/gsd-goldens/phase-complete*.golden.json, tests/fixtures/gsd-goldens/roadmap-annotate*.golden.json, tests/fixtures/gsd-goldens/roadmap-update*.golden.json</files>
  <read_first>
    - cairn/gsd/contracts/roadmap-phase.json (os 4 verbos: phase.complete tem o shape mais largo da família — next_phase/roadmap_updated/requirements_updated/auto_pruned; phase carrega policy/checks/uat_files; update-plan-progress carrega status/verification_stale_check_indeterminate)
    - cairn/scripts/cairn-gsd-state.py (o caminho de transição único dos planos 01-02 — phase.complete o reusa, não duplica)
    - cairn/scripts/cairn-map.py (mecanismo split_markers/replace-only-inner referenciado pela casa para edição de documento entre marcadores, se o shape exigir edição de ROADMAP)
    - tests/cairn-gsd.bats L530-561 (o filtro do guard — adicionar roadmap-phase)
  </read_first>
  <behavior>
    - phase.complete <N> → set-state phase_status:complete com ator/motivo; avanço de posição para a fase seguinte pelo MESMO caminho de transição; edições de documento que o shape declara (checkbox do ROADMAP) aplicadas; envelope completo
    - phase.complete reentrado (fase já complete) → idempotente, sem transição espúria
    - roadmap.update-plan-progress → status como fato (bd) + contagens de PLAN/SUMMARY como documento
    - roadmap.annotate-dependencies → mutação de documento (ROADMAP), sem tocar dimensão nenhuma
    - phase → policy/checks compostos de arquivos de verificação (documentos) pelo shape
  </behavior>
  <action>
    Implementar os 4 handlers. phase.complete é a transição mais gorda da fase: valida a
    fase alvo, transiciona phase_status=complete e avança a posição (phase=N+1,
    phase_status=planned, quando há próxima) — TODAS as transições pelo caminho único de
    set-state com ator e motivo (CORE-03); as mutações de DOCUMENTO que o shape declara
    (roadmap_updated: checkbox da fase; requirements_updated) são edições de arquivo
    escopadas, na forma da casa (edição mínima, nunca rewrite do arquivo inteiro);
    is_last_phase/next_phase_name vêm do parse do ROADMAP (documento). Reentrada
    idempotente por teste. roadmap.update-plan-progress compõe status (fato, portador) com
    plan_count/summary_count (documento, contagem de arquivos) e atualiza o que o contrato
    manda no documento. roadmap.annotate-dependencies e phase são majoritariamente
    documento — implementar pelo shape e notes do contrato; onde um campo upstream perder o
    sentido no cairn (ex. verification_stale_check_indeterminate, se o mecanismo upstream
    não existir aqui), a semântica adotada é declarada em divergences.json (family
    roadmap-phase), nunca improvisada.

    Cenários + goldens por verbo e por ramo (complete feliz, complete reentrado, complete
    da última fase). Guard: adicionar `roadmap-phase` ao filtro de famílias do bats e
    manter comm vazio nos dois sentidos. Gate D-01: asserção de teto de linhas do irmão.
  </action>
  <verify>
    <automated>bats tests/cairn-gsd.bats && test "$(wc -l < cairn/scripts/cairn-gsd-state.py)" -le 1500</automated>
  </verify>
  <acceptance_criteria>
    - `query phase.complete 34 --raw` num fixture completo: projeta `phase_status:complete`, avança posição, marca o checkbox no ROADMAP do fixture, e o envelope carrega as chaves do shape; segunda invocação: exit 0 idempotente, labels byte-iguais
    - a trilha de auditoria da transição de complete carrega ator e motivo (mesma asserção do plano 01)
    - `query roadmap.update-plan-progress ... --raw` devolve status do portador e contagens do filesystem — cenário com os dois dessincronizados prova qual fonte responde qual campo
    - os 12 verbos da família aparecem em --list-implemented; filtro do guard inclui roadmap-phase; comm vazio nos dois sentidos
    - `wc -l cairn/scripts/cairn-gsd-state.py` <= 1500 (D-01)
    - `bats tests/cairn-gsd.bats` verde offline
  </acceptance_criteria>
  <done>CORE-05 (metade roadmap-phase) fechado: 12/12 verbos em python, transições auditadas, documento e fato cada um na sua fonte, guard cobrindo a família.</done>
</task>

</tasks>

<verification>
- `bats tests/cairn-gsd.bats` verde offline — 22 verbos do irmão de estado (10 estado + 12 roadmap-phase) com cenário e golden
- cenários adversariais (documento presente/fato ausente e vice-versa) provam a regra documento-vs-fato nos dois sentidos
- `git status --porcelain cairn/gsd/` vazio
</verification>

<success_criteria>
- CORE-05: família roadmap-phase completa em python na forma dos contratos
- CORE-02/03: transições de phase.complete auditadas por set-state, consulta só por label
- CORE-04: fato ausente nomeado nos verbos mistos
- D-01: irmão de estado dentro do teto ~1.5k
</success_criteria>

<output>
Criar `.planning/phases/34-o-binario-python-o-nucleo-de-estado-sobre-o-bd/34-03-SUMMARY.md`
ao concluir, registrando as divergências declaradas da família e a contagem final de linhas
do irmão de estado.
</output>
