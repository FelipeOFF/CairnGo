---
phase: 34-o-binario-python-o-nucleo-de-estado-sobre-o-bd
plan: 04
type: execute
wave: 4
depends_on: [34-03]
files_modified:
  - cairn/scripts/cairn-gsd-init.py
  - tests/cairn-gsd.bats
  - tests/fixtures/gsd-goldens/scenarios.json
  - tests/fixtures/gsd-goldens/*.golden.json
  - tests/fixtures/gsd-goldens/divergences.json
autonomous: true
requirements: [CORE-05]
beads:
  CORE-05: CairnGo-5uuw
must_haves:
  truths:
    - "os 6 verbos worktree e os 9 bundles init respondem em python na forma de worktree.json e init.json, verbo a verbo (CORE-05)"
    - "cada bundle init.<workflow> serve os campos que os call sites consomem, na composição medida em contracts.json measurements.init_budget (orçamento re-derivado da 31: 490 linhas somadas nos 9, per_shape registrado) — o delta real é registrado no SUMMARY (CORE-05)"
    - "fato de estado dentro de bundle init vem do irmão de estado via run_sibling_json — a consulta por label tem UMA implementação na casa, nunca duas que possam discordar"
    - "worktree.create com falha no meio desfaz a criação parcial (molde rollback de cairn-parallel) — nunca worktree órfã de branch nem branch órfã de worktree"
    - "`--pick <campo>` extrai a chave do bundle na semântica medida do binário (String do valor, undefined para chave ausente)"
    - "cairn-gsd-init.py permanece <= 1500 linhas (D-01)"
    - "o guard de cobertura conhece worktree e init: os 15 verbos aparecem no --list-implemented agregado, comm vazio"
  artifacts:
    - cairn/scripts/cairn-gsd-init.py
    - tests/fixtures/gsd-goldens/scenarios.json
    - tests/cairn-gsd.bats
  key_links:
    - "dispatcher → cairn-gsd-init.py: a tabela FAMILY_SCRIPT do plano 01 já roteia worktree/init pra cá — o exec passa a encontrar o arquivo"
    - "verbos worktree → git via run_git com die nomeado; parse de `git worktree list --porcelain` pelo molde worktree_entries"
    - "bundles init → cairn-gsd-state.py via run_sibling_json para posição corrente/status — nunca query bd própria"
  prohibitions:
    - statement: "Nenhum bundle init responde fato de estado lendo .planning/STATE.md nem reimplementando a consulta de label — fato vem do irmão de estado ou a composição falha nomeada"
    - statement: "Nenhuma task escreve sob cairn/gsd/** — divergência deliberada vai em tests/fixtures/gsd-goldens/divergences.json"
---

<objective>
O segundo irmão do D-01 nasce: `cairn-gsd-init.py` com as famílias worktree (6 verbos,
~250 linhas estimadas, semântica reaproveitada de cairn-parallel por cópia de forma) e
init (9 bundles de contexto, orçamento re-derivado na fase 31: 490 linhas com rationale
por shape em init.json). O dispatcher já roteia pra cá desde o plano 01 — este plano faz o
exec encontrar o arquivo (CORE-05).

Purpose: os 13 sítios init + 12 worktree dos 8 workflows passam a ser respondidos em
python, com os fatos de estado dentro dos bundles vindo do irmão de estado — uma
implementação de consulta, não duas.
Output: cairn-gsd-init.py com 15 verbos, cenários/goldens, guard estendido.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-o-binario-python-o-nucleo-de-estado-sobre-o-bd/34-CONTEXT.md
@.planning/phases/34-o-binario-python-o-nucleo-de-estado-sobre-o-bd/34-PATTERNS.md
@.planning/phases/34-o-binario-python-o-nucleo-de-estado-sobre-o-bd/34-01-SUMMARY.md
@cairn/gsd/contracts/worktree.json
@cairn/gsd/contracts/init.json
@cairn/gsd/contracts/contracts.json
</context>

## Artifacts this phase produces (escopo deste plano)

| Artefato | O que entra neste plano |
|---|---|
| `cairn/scripts/cairn-gsd-init.py` (novo) | skeleton na forma da casa + 6 handlers worktree + 9 handlers init |
| Bundles init | init.autonomous (15), init.debug (20), init.execute-phase (140), init.manager (55), init.milestone-op (30), init.phase-op (45), init.plan-phase (105), init.quick (45), init.verify-work (35) — orçamento per_shape de contracts.json |
| Cenários + goldens | por verbo/situação; worktree com fixtures git reais; init com fixture.files + fixture.bd |
| `tests/cairn-gsd.bats` | guard de cobertura com worktree e init no filtro |

## Flagged assumptions (probe spec-less — escopo deste plano)

Do registro consolidado no 34-01-PLAN.md: CORE-05 unclassified — resolvido aqui por
cobertura derivada de worktree.json/init.json (cada verbo com cenário) e pelo orçamento
per_shape citado nos critérios. Nenhum item descartado.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: o irmão nasce com a família worktree</name>
  <files>cairn/scripts/cairn-gsd-init.py, tests/cairn-gsd.bats, tests/fixtures/gsd-goldens/scenarios.json, tests/fixtures/gsd-goldens/worktree-*.golden.json</files>
  <read_first>
    - cairn/gsd/contracts/worktree.json (os 6 verbos: shape, flags, notes — record-agent e create carregam manifest_path/entry; base-check carrega shouldDegrade)
    - cairn/scripts/cairn-parallel.py L694 (run_git), L731 (is_linked_worktree), L745 (worktree_entries — parse do --porcelain), L788 (worktree_entry_at), L797 (branch_exists), L806 (phase_slug), L831 (phase_layout — convenção de nome/caminho por fase), L990 (rollback — desfazer criação parcial)
    - cairn/scripts/cairn-gsd-state.py (o skeleton do plano 01 — cabeçalho, exits, die, HANDLERS, envelope: copiar a MESMA forma, D-01)
    - cairn/scripts/cairn-gsd.py (FAMILY_SCRIPT — conferir que worktree/init apontam pra este arquivo; nada a mudar no dispatcher)
  </read_first>
  <behavior>
    - worktree.create <fase> num repo git real → cria worktree + branch pela convenção phase_layout, grava entry no manifesto; envelope {cwd, entry, manifest_path, ok, ...}
    - worktree.create com branch já existente → ramo de erro do contrato com hint, e NADA criado pela metade (rollback)
    - worktree.base-check → forkRef/forkSha/shouldDegrade coerentes com o repo do fixture
    - worktree.reap-orphans / cleanup-wave → enumeração e limpeza pelo shape
    - worktree.record-agent → entry no manifesto, idempotente para o mesmo agente
  </behavior>
  <action>
    Criar cairn-gsd-init.py com a MESMA forma do irmão de estado (docstring-contrato,
    EXIT_OK/CONTRACT/USAGE/UNIMPLEMENTED, die com TAG_PREFIX, HANDLERS, envelope copiado,
    main recebendo verbo canônico; verbo da família sem handler → exit 4 nomeando verbo e
    fase 34). Implementar os 6 verbos worktree reaproveitando a semântica de
    cairn-parallel por cópia de forma: run_git com die nomeado para todo subprocess git;
    worktree_entries para o parse do porcelain; phase_slug/phase_layout para a convenção
    de nome; rollback para desfazer criação parcial em worktree.create (worktree criada
    sem branch, ou o inverso, nunca sobrevive a uma falha). O manifesto de agentes
    (record-agent) é escrita de metadata de arquivo na forma que o contrato descreve —
    não é fato de estado, não passa pelo bd. Onde a semântica do cairn divergir do
    upstream de propósito (host único, ausência de multi-runtime), declarar em
    divergences.json (family worktree).

    Cenários + goldens derived-from-contract: worktree exige fixtures git reais
    (fixture.git já existe no builder) — create feliz, create com branch duplicada
    (rollback provado por bats direto: após a falha, `git worktree list` e `git branch`
    não carregam o nome), base-check, record-agent idempotente. Paths variáveis mascarados.
  </action>
  <verify>
    <automated>bats tests/cairn-gsd.bats</automated>
  </verify>
  <acceptance_criteria>
    - `query worktree.create <fase> --raw` num fixture git sai 0, `git worktree list --porcelain` mostra a árvore nova na convenção phase_layout, e o envelope tem as chaves do shape
    - na falha forçada (branch pré-existente), exit != 0 com o ramo do contrato E `git worktree list` + `git branch` sem resíduo parcial — o rollback é provado, não presumido
    - `query worktree.base-check --raw` devolve forkRef/forkSha reais do fixture
    - os 6 verbos têm >= 1 cenário com golden provenance derived-from-contract; `bats tests/cairn-gsd.bats` verde offline
    - o skeleton passa nos mesmos guards de forma do irmão de estado (exits 0/1/2/4 documentados na docstring; envelope --raw/default na semântica medida)
  </acceptance_criteria>
  <done>cairn-gsd-init.py existe na forma da casa e a família worktree responde com a semântica de cairn-parallel — o exec do dispatcher encontra o segundo irmão.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: os 9 bundles init no orçamento re-derivado, com fato vindo do irmão de estado</name>
  <files>cairn/scripts/cairn-gsd-init.py, tests/cairn-gsd.bats, tests/fixtures/gsd-goldens/scenarios.json, tests/fixtures/gsd-goldens/init-*.golden.json</files>
  <read_first>
    - cairn/gsd/contracts/init.json (bundle_shapes por verbo: output.shape, source_ref, python_estimate_rationale — a lista de campos que cada call site consome; a flag --pick)
    - cairn/gsd/contracts/contracts.json (measurements.init_budget — método e per_shape: o orçamento que o CORE-05 cita)
    - cairn/scripts/cairn-bookkeep.py L720-746 (sibling + run_sibling_json — o molde de chamar o irmão degradando pra falha nomeada, com o racional 'duas implementações que podem discordar é a doença' no próprio docstring)
    - cairn/scripts/cairn-gsd.py L1179-1263 (build_agent_skills_block — análogo de composição de bundle a partir de config + filesystem com diagnostics)
    - .planning/phases/34-o-binario-python-o-nucleo-de-estado-sobre-o-bd/34-01-SUMMARY.md (o vocabulário D-02 e o design do portador — o que o irmão de estado responde)
  </read_first>
  <behavior>
    - `query init.plan-phase 34 --raw` → bundle com os campos de init.json (planner_model, phase_dir, has_research, has_context, ...) compostos de config + filesystem + posição corrente via irmão de estado
    - `query init.plan-phase 34 --pick planner_model` → String do valor; chave ausente → undefined (semântica medida)
    - bundle num repo sem portador bd → os campos de posição falham nomeados (a composição não inventa default)
    - init.execute-phase (o shape mais largo, 140 linhas estimadas) → campos consumidos servidos; helpers compartilhados (locator de fase/planos, leitor de manifest) contados uma vez
  </behavior>
  <action>
    Implementar os 9 handlers init.* como composições de bundle: cada um serve SÓ os campos
    que os call sites consomem (a lista exata está em init.json bundle_shapes — é o
    contrato da 31, não a superfície inteira do upstream). Helpers compartilhados
    (locator de fase/diretório, resolução de modelos por perfil, leitor de manifest de
    config) escritos UMA vez e consumidos pelos 9 — é assim que o orçamento de 490 fecha.
    Campo de posição corrente/status dentro de bundle vem do irmão de estado via
    run_sibling_json (sibling() + subprocess do python do próprio interpretador) — se o
    irmão falhar nomeando fato ausente, o bundle propaga a falha nomeada (CORE-04 por
    composição), nunca preenche default. Campos de documento (existência de RESEARCH,
    CONTEXT, diretórios) vêm do filesystem. --pick na semântica medida do binário.
    Divergências de campo (ex. campos que o upstream emitia e nenhum call site consome —
    as descobertas summaries_total/uat_path nunca emitidos dos SUMMARYs da 31/33) ficam
    registradas: o que o contrato já declara não re-declarar; o que for decisão nova vai
    em divergences.json (family init).

    Cenários + goldens derived-from-contract: um cenário por bundle no fixture padrão
    (config + fase 34 montada + portador semeado) + o cenário --pick + o cenário sem
    portador (falha nomeada propagada). Guard: adicionar worktree e init ao filtro de
    famílias do bats, comm vazio nos dois sentidos.
  </action>
  <verify>
    <automated>bats tests/cairn-gsd.bats && test "$(wc -l < cairn/scripts/cairn-gsd-init.py)" -le 1500</automated>
  </verify>
  <acceptance_criteria>
    - cada um dos 9 verbos init.* tem cenário com golden derived-from-contract cobrindo os campos de bundle_shapes; `jq` sobre a saída real confirma presença dos campos consumidos do shape
    - `query init.plan-phase 34 --pick planner_model` imprime String do valor; `--pick chave-inexistente` imprime undefined
    - num fixture sem portador bd, o bundle sai exit != 0 com a falha nomeada PROPAGADA do irmão de estado (a mensagem cita o fato e o comando)
    - os 15 verbos (6+9) aparecem em --list-implemented; filtro do guard inclui worktree e init; comm vazio nos dois sentidos
    - `wc -l cairn/scripts/cairn-gsd-init.py` <= 1500 (D-01); o delta vs o orçamento 490+250 é registrado no SUMMARY (zero e negativo são resultados publicáveis)
    - `bats tests/cairn-gsd.bats` verde offline
  </acceptance_criteria>
  <done>CORE-05 (metades worktree e init) fechado: 15 verbos em python no orçamento re-derivado, fato de estado com uma implementação só na casa.</done>
</task>

</tasks>

<verification>
- `bats tests/cairn-gsd.bats` verde offline — 15 verbos novos com cenário e golden
- rollback de worktree.create provado por teste de resíduo, não por leitura de código
- falha nomeada propagada do irmão de estado provada em pelo menos um bundle
- `git status --porcelain cairn/gsd/` vazio
</verification>

<success_criteria>
- CORE-05: worktree e init completos em python, orçamento re-derivado citado e delta registrado
- Uma implementação de consulta de fato na casa (irmão de estado), consumida por composição
- D-01: segundo irmão dentro do teto ~1.5k
</success_criteria>

<output>
Criar `.planning/phases/34-o-binario-python-o-nucleo-de-estado-sobre-o-bd/34-04-SUMMARY.md`
ao concluir, registrando o delta de linhas vs orçamento (por shape, quando desviar) e as
divergências declaradas de worktree/init.
</output>
