---
phase: 34-o-binario-python-o-nucleo-de-estado-sobre-o-bd
plan: 02
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - cairn/scripts/cairn-gsd-state.py
  - tests/cairn-gsd.bats
  - tests/fixtures/gsd-goldens/scenarios.json
  - tests/fixtures/gsd-goldens/*.golden.json
  - tests/fixtures/gsd-goldens/divergences.json
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03, CORE-04]
beads:
  CORE-01: CairnGo-l8e2
  CORE-02: CairnGo-897j
  CORE-03: CairnGo-u7z9
  CORE-04: CairnGo-av47
must_haves:
  truths:
    - "os 10 verbos da família estado respondem do bd na forma de payload de estado.json, verbo a verbo (CORE-01)"
    - "toda transição de dimensão dos 8 verbos novos passa por set-state com ator e motivo, mapeada pela constante VERB_DIMENSIONS — state.update e state.advance-plan projetam a dimensão que o campo nomeia; state.planned-phase projeta phase_status:planned; state.record-session projeta session (CORE-03, D-02)"
    - "o caso canônico current_phase 18 é idempotente por teste: com um .planning/STATE.md obsoleto dizendo Phase 18 e o portador bd dizendo phase:34, state.record-metric atribui à fase 34 nas DUAS invocações e nenhuma invocação altera label nenhum do portador (CORE-03)"
    - "blockers e decisions viram fatos consultáveis por label projetado (`gsd-blocker`, `gsd-decision`) — nunca prosa em markdown, nunca metadata aninhado como chave (CORE-02)"
    - "verbo de estado com fato ausente falha nomeando o fato e o comando que o cria, em todos os 8 verbos novos que leem fato (CORE-04)"
    - "goldens de verbos que carregam tempo (record-session, record-metric) usam mask — nenhuma chave de timestamp sobrevive nos goldens (doutrina de determinismo da 33)"
    - "o guard de cobertura conhece a família estado: os 10 verbos de estado.json aparecem no --list-implemented agregado, comm vazio nesse sentido"
  artifacts:
    - cairn/scripts/cairn-gsd-state.py
    - tests/fixtures/gsd-goldens/scenarios.json
    - tests/cairn-gsd.bats
  key_links:
    - "VERB_DIMENSIONS → set-state: o handler consulta a constante para saber QUAL dimensão projeta — nenhum handler carrega vocabulário próprio"
    - "state.record-metric → label phase:N do portador: a fase corrente vem do bd, o STATE.md obsoleto do fixture é ignorado por construção"
    - "state.update-progress → derivação: percent/completed/total calculados de fatos consultáveis na leitura, nunca persistidos como prosa"
  prohibitions:
    - statement: "Nenhum verbo de estado lê .planning/STATE.md, ROADMAP.md ou SUMMARYs como fonte ou fallback de FATO de estado — fato vem do bd ou o verbo falha nomeado"
    - statement: "Nenhuma transição de dimensão escreve no bd por caminho que não seja set-state com ator e motivo"
    - statement: "Nenhuma task escreve sob cairn/gsd/** — divergência deliberada vai em tests/fixtures/gsd-goldens/divergences.json"
---

<objective>
Expansão a partir do tracer: os 8 verbos restantes da família estado entram no
cairn-gsd-state.py que o plano 01 provou — os escritores de dimensão (state.update,
state.advance-plan, state.planned-phase, state.record-session), os fatos de coleção
(state.add-blocker, state.add-decision), o derivado (state.update-progress) e o caso que
batiza o CORE-03: state.record-metric relendo o fato do bd, não prosa obsoleta. Ao fim, a
família estado inteira (10 verbos) responde do bd e entra no guard de cobertura.

Purpose: fechar os ~26 sítios de estado dos 8 workflows sobre a arquitetura já verde,
com o teste canônico de idempotência que a fase inteira existe para tornar impossível
de regredir.
Output: família estado completa + cenários/goldens por verbo + caso 18 por teste + guard
estendido.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-o-binario-python-o-nucleo-de-estado-sobre-o-bd/34-CONTEXT.md
@.planning/phases/34-o-binario-python-o-nucleo-de-estado-sobre-o-bd/34-PATTERNS.md
@.planning/phases/34-o-binario-python-o-nucleo-de-estado-sobre-o-bd/34-01-SUMMARY.md
@cairn/gsd/contracts/estado.json
</context>

## Artifacts this phase produces (escopo deste plano)

| Artefato | O que entra neste plano |
|---|---|
| `cairn/scripts/cairn-gsd-state.py` | 8 handlers novos: `state.update`, `state.advance-plan`, `state.update-progress`, `state.record-session`, `state.planned-phase`, `state.add-blocker`, `state.add-decision`, `state.record-metric` |
| Labels novos consultáveis | `gsd-blocker` e `gsd-decision` (beads de coleção); dimensões `plan` e `session` passam a ser escritas (o vocabulário segue o D-02 do plano 01 — nada novo no schema) |
| Cenários + goldens | por verbo/situação, incluindo o par adversarial do caso 18; masks nos verbos com tempo |
| `tests/cairn-gsd.bats` | teste canônico de idempotência do caso 18 + guard de cobertura com a família estado no filtro |

## Flagged assumptions (probe spec-less — escopo deste plano)

Do registro consolidado no 34-01-PLAN.md, este plano resolve: CORE-03 ordering (o caso 18,
Task 2), CORE-02 encoding para valores livres (session normalizado YYYY-MM-DD com mask no
golden, Task 1), e CORE-04 unclassified para os 8 verbos novos (falha nomeada por verbo,
critérios das duas tasks). Nenhum item descartado.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: os escritores de dimensão — update, advance-plan, update-progress, record-session</name>
  <files>cairn/scripts/cairn-gsd-state.py, tests/cairn-gsd.bats, tests/fixtures/gsd-goldens/scenarios.json, tests/fixtures/gsd-goldens/state-update*.golden.json, tests/fixtures/gsd-goldens/state-advance*.golden.json, tests/fixtures/gsd-goldens/state-record-session*.golden.json</files>
  <read_first>
    - cairn/gsd/contracts/estado.json (os 4 verbos: shape, flags, notes — state.update valida nome de campo contra injeção de regex, campo não mapeado sai {updated:false, reason} exit 0; advance-plan carrega previous_plan/current_plan/total_plans; update-progress carrega bar/percent)
    - cairn/scripts/cairn-gsd-state.py (o irmão como o plano 01 deixou — VERB_DIMENSIONS, portador, run_bd, resolve_actor, envelope)
    - .planning/phases/34-o-binario-python-o-nucleo-de-estado-sobre-o-bd/34-PATTERNS.md (Shared Patterns: escrita SEMPRE set-state; molde write_lease para metadata que NÃO é transição)
    - cairn/scripts/cairn-status.py L385-387 (regexes PLAN_FILE/SUMMARY_FILE — a forma de identificar plano NN-MM)
  </read_first>
  <behavior>
    - state.update campo mapeado (ex. status→phase_status) → set-state projeta o label novo; {updated:[campo], reason} exit 0
    - state.update campo não mapeado em VERB_DIMENSIONS → {updated:false, reason} exit 0 (contrato), sem escrita nenhuma no bd
    - state.advance-plan → dimensão plan transiciona para o NN-MM seguinte; envelope com previous_plan/current_plan
    - state.record-session → dimensão session=YYYY-MM-DD via set-state; golden com mask na data
    - state.update-progress → percent/completed/total derivados na leitura; portador ausente → falha nomeada
  </behavior>
  <action>
    Implementar os 4 handlers no cairn-gsd-state.py, todos sobre o portador e a constante
    VERB_DIMENSIONS do plano 01 (D-02): a transição SEMPRE via set-state com ator
    (resolve_actor) e motivo que nomeia o verbo e o valor (ex.: "state.advance-plan
    02→03"). state.update traduz o nome de campo do contrato para a dimensão pelo mapa da
    constante — nome validado com a mesma disciplina do upstream (validateFieldName:
    caracteres seguros, nunca interpolado em regex); campo que não projeta dimensão devolve
    o envelope de recusa do contrato com exit 0, sem tocar o bd. state.advance-plan lê a
    dimensão plan corrente do label, computa o próximo NN-MM (forma dos regexes de
    cairn-status) e transiciona; total_plans vem da contagem de PLANs do diretório da fase
    (documento — contagem de arquivo é legítima; o FATO plan corrente é do bd).
    state.update-progress não escreve dimensão nenhuma (progress não está no vocabulário
    D-02 — é derivado): calcula completed/total/percent/bar de fatos consultáveis
    (dimensões do portador + contagem de SUMMARYs como artefatos) e devolve o shape; se o
    significado de "updated" divergir do upstream (que persistia em frontmatter), declarar
    em divergences.json family estado. state.record-session transiciona session=YYYY-MM-DD
    e registra a descrição da atividade como metadata NÃO-transição (molde write_lease —
    payload, não label).

    Cenários + goldens derived-from-contract por verbo e por ramo do behavior; mask na
    data de session. Fato ausente (portador inexistente) tem cenário nomeado para
    advance-plan e update-progress — a mensagem prescreve state.begin-phase.
  </action>
  <verify>
    <automated>bats tests/cairn-gsd.bats</automated>
  </verify>
  <acceptance_criteria>
    - `query state.update status executing --raw` (grafia exata das flags conforme contrato) projeta `phase_status:executing` no portador — `bd list -l phase_status:executing --json | jq length` == 1 — e o envelope lista o campo em updated
    - campo não mapeado: envelope {updated:false, reason} exit 0 e NENHUM label novo no portador (contagem de labels antes == depois)
    - `query state.advance-plan --raw` num fixture com plan 34-01 corrente e dois PLANs no diretório devolve previous_plan/current_plan coerentes e projeta `plan:34-02`
    - `query state.record-session ... --raw` projeta `session:<YYYY-MM-DD>` e o golden passa free_of_timestamp_keys via mask
    - `query state.update-progress --raw` devolve bar/percent/completed/total numéricos coerentes com o fixture; sem portador, exit 1 nomeando fato + comando
    - cada um dos 4 verbos tem >= 1 cenário no manifesto com golden provenance derived-from-contract; `bats tests/cairn-gsd.bats` verde offline
  </acceptance_criteria>
  <done>Os 4 escritores respondem do bd e escrevem só por set-state; dimensão plan e session ativas; derivado calculado na leitura, nunca persistido como prosa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: coleções, planned-phase e o caso canônico current_phase 18</name>
  <files>cairn/scripts/cairn-gsd-state.py, tests/cairn-gsd.bats, tests/fixtures/gsd-goldens/scenarios.json, tests/fixtures/gsd-goldens/state-add*.golden.json, tests/fixtures/gsd-goldens/state-planned*.golden.json, tests/fixtures/gsd-goldens/state-record-metric*.golden.json</files>
  <read_first>
    - cairn/gsd/contracts/estado.json (add-blocker/add-decision: shape com created/reason; planned-phase: warning no shape; record-metric: a nota inteira — o upstream appendava linha em tabela markdown achada por SHAPE de header; essa semântica de DESTINO muda de propósito aqui)
    - .planning/research/v1.6-transplante-gsd.md §4 (as regras do bd e o racional do caso 18)
    - cairn/scripts/cairn-gsd-state.py (handlers e helpers como a Task 1 deixou)
    - tests/cairn-gsd.bats L530-561 (trivial_verbs_of + o teste comm nos dois sentidos — o filtro a estender)
  </read_first>
  <behavior>
    - state.add-blocker "texto" → bead novo com label `gsd-blocker`; consulta `bd list -l gsd-blocker` encontra; envelope {added, blocker, created, error, reason}
    - state.add-decision idem com `gsd-decision`
    - state.planned-phase <N> → set-state phase=N + phase_status:planned; envelope do contrato
    - state.record-metric com STATE.md dizendo Phase 18 e portador dizendo phase:34 → o registro atribui a 34; segunda invocação idêntica: mesmo resultado, labels do portador byte-iguais antes/depois
  </behavior>
  <action>
    Implementar os 4 handlers restantes. add-blocker/add-decision criam FATOS de coleção:
    bead novo via bd (molde run_bd) com o label projetado da coleção (`gsd-blocker` /
    `gsd-decision`) — a criação registra ator (bd create com o ator resolvido, na forma que
    o bd 1.1.0 aceitar) e o texto vira o título/corpo do bead; não são dimensões, não
    passam por VERB_DIMENSIONS, mas a CHAVE de consulta segue sendo label projetado
    (CORE-02). planned-phase transiciona phase e phase_status=planned via set-state
    (reaproveitando o caminho do begin-phase — uma implementação de transição, não duas).

    state.record-metric (o caso 18, CORE-03): resolve a fase e o plano correntes dos labels
    do portador (`phase:N`, `plan:NN-MM`) — NUNCA de .planning/STATE.md; registra a métrica
    (duration/tasks/files, flags do contrato) como metadata não-transição associada à
    fase/plano corrente (molde write_lease — payload consultável, não label; métrica não é
    dimensão D-02). A nota do contrato descreve o destino upstream (tabela markdown em
    STATE.md): a mudança de destino é divergência declarada em divergences.json (family
    estado, aspect source-of-truth), citando que a tabela Performance Metrics deixa de ser
    escrita por este verbo.

    O teste canônico (bats direto + cenário): fixture com `.planning/STATE.md` contendo a
    prosa obsoleta `Phase: 18` (o replay real do incidente) e portador bd com `phase:34` —
    invocar state.record-metric DUAS vezes; asserções: (a) ambas atribuem a 34 (o envelope
    e o fato gravado citam 34, nunca 18); (b) `bd list -l gsd-state --json | jq .[]
    .labels` é byte-igual antes e depois de cada invocação — leitura de fato não
    transiciona nada; (c) o STATE.md do fixture permanece byte-igual — o verbo não escreve
    markdown. Estender o guard de cobertura: a função de filtro do bats (trivial_verbs_of)
    ganha a família `estado` na lista do jq, e o teste comm continua vazio nos dois
    sentidos com o --list-implemented agregado.
  </action>
  <verify>
    <automated>bats tests/cairn-gsd.bats</automated>
  </verify>
  <acceptance_criteria>
    - `query state.add-blocker "X" --raw` sai 0 e `bd list -l gsd-blocker --all --json | jq length` >= 1; idem decision; os envelopes têm as chaves do contrato
    - `query state.planned-phase 35 --raw` projeta `phase:35` e `phase_status:planned` (labels correntes únicos por dimensão)
    - caso 18: as três asserções (atribui a 34 nas duas invocações; labels do portador byte-iguais; STATE.md do fixture intocado) passam num teste bats nomeado que cita current_phase 18
    - divergences.json ganhou a entrada do destino de record-metric (family estado, aspect source-of-truth, upstream/cairn/reason preenchidos)
    - os 10 verbos de estado.json aparecem em `--list-implemented`; o filtro do guard inclui estado e `comm` é vazio nos dois sentidos
    - `bats tests/cairn-gsd.bats` verde offline; cenários das fases 33 e do plano 01 sem regressão
  </acceptance_criteria>
  <done>CORE-03 fechado por teste no caso que o batizou; família estado completa no bd, no manifesto e no guard.</done>
</task>

</tasks>

<verification>
- `bats tests/cairn-gsd.bats` verde offline — 10/10 verbos de estado com cenário e golden
- o teste canônico do caso 18 existe, é nomeado, e falha se qualquer handler voltar a ler prosa de STATE.md (prova: o fixture adversarial é parte do teste)
- `git status --porcelain cairn/gsd/` vazio
</verification>

<success_criteria>
- CORE-01: 10/10 verbos de estado na forma de estado.json
- CORE-02: toda chave nova de consulta é label projetado (dimensões + gsd-blocker/gsd-decision)
- CORE-03: caso current_phase 18 idempotente por teste; toda transição com ator e motivo
- CORE-04: fato ausente falha nomeado em todos os leitores novos
</success_criteria>

<output>
Criar `.planning/phases/34-o-binario-python-o-nucleo-de-estado-sobre-o-bd/34-02-SUMMARY.md`
ao concluir, registrando o destino escolhido para métricas e sessão (payload não-transição)
e as divergências declaradas novas.
</output>
