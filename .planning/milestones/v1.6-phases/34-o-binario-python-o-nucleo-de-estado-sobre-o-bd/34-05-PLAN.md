---
phase: 34-o-binario-python-o-nucleo-de-estado-sobre-o-bd
plan: 05
type: execute
wave: 5
depends_on: [34-04]
files_modified:
  - cairn/scripts/cairn-gsd-state.py
  - cairn/scripts/cairn-gsd-init.py
  - tests/cairn-gsd.bats
  - tests/fixtures/gsd-goldens/scenarios.json
  - tests/fixtures/gsd-goldens/*.golden.json
  - tests/fixtures/gsd-goldens/divergences.json
autonomous: true
requirements: [CORE-05, CORE-04]
beads:
  CORE-05: CairnGo-5uuw
  CORE-04: CairnGo-av47
must_haves:
  truths:
    - "os 24 verbos misc da fase respondem em python na forma de misc.json — 7 no irmão de estado (summary-extract, todo.match-phase, requirements.mark-complete, quick-tasks-append, history-digest, research-plan, research-store) e 17 no de init, exatamente como a partição registrada no plano 01 roteia (discrição do CONTEXT, cumprida)"
    - "os 5 órfãos da 35 (audit-open, review-lane, agent.classify-failure, task.is-behavior-adding, run-with-timeout) continuam exit 4 nomeando a fase 35 — exclusão nominal no guard, nunca cobertura fingida"
    - "a cobertura é derivada do inventário: o filtro do guard soma as 10 famílias implementadas (5 triviais da 33 + 5 desta fase, menos os órfãos) e o comm contra o --list-implemented agregado é vazio NOS DOIS SENTIDOS, com controle negativo de verbo forjado (CORE-05)"
    - "verbo misc que precisa de fato ausente falha nomeando o fato e o comando (CORE-04)"
    - "`bats tests/` inteiro verde — regressão zero em toda a suíte da casa, não só no arquivo da fase"
    - "os dois irmãos fecham a fase <= 1500 linhas cada (D-01)"
  artifacts:
    - cairn/scripts/cairn-gsd-state.py
    - cairn/scripts/cairn-gsd-init.py
    - tests/fixtures/gsd-goldens/scenarios.json
    - tests/cairn-gsd.bats
  key_links:
    - "dispatcher MISC_STATE_VERBS → o roteamento por verbo do plano 01 passa a encontrar handler nos dois irmãos"
    - "misc de planning-docs → parsers de bookkeep/status por cópia de forma (parse_roadmap, plan_id_matches, parse_state_frontmatter)"
    - "guard de cobertura → contracts.json .verbs (o universo) x --list-implemented agregado (a superfície) — a lista escrita à mão não existe"
  prohibitions:
    - statement: "Nenhum verbo misc responde fato de estado lendo .planning/STATE.md — fato vem do bd (diretamente no irmão de estado, por composição no de init) ou falha nomeado"
    - statement: "Nenhuma task escreve sob cairn/gsd/** — divergência deliberada vai em tests/fixtures/gsd-goldens/divergences.json"
---

<objective>
O fecho da fase: os 24 verbos misc (29 do contrato menos os 5 órfãos da 35) entram nos
dois irmãos pela partição registrada no plano 01, e o guard de cobertura passa a derivar a
superfície inteira do inventário — 10 famílias, comm vazio nos dois sentidos, controle
negativo. Ao fim, todo verbo das famílias estado, roadmap-phase, worktree, init e misc
responde em python sobre o bd, e a única coisa que o dispatcher ainda nega, nomeando a
fase, é o que pertence à 35.

Purpose: fechar os últimos ~40 sítios do corpus dos 8 workflows e tornar a cobertura da
fase um fato derivado, não uma afirmação (CORE-05; goal da fase).
Output: misc completo nos dois irmãos, guard total, regressão completa da suíte.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-o-binario-python-o-nucleo-de-estado-sobre-o-bd/34-CONTEXT.md
@.planning/phases/34-o-binario-python-o-nucleo-de-estado-sobre-o-bd/34-PATTERNS.md
@.planning/phases/34-o-binario-python-o-nucleo-de-estado-sobre-o-bd/34-01-SUMMARY.md
@cairn/gsd/contracts/misc.json
@cairn/gsd/contracts/contracts.json
</context>

## Artifacts this phase produces (escopo deste plano)

| Artefato | O que entra neste plano |
|---|---|
| `cairn/scripts/cairn-gsd-state.py` | 7 handlers misc de planning-docs: `summary-extract`, `todo.match-phase`, `requirements.mark-complete`, `quick-tasks-append`, `history-digest`, `research-plan`, `research-store` |
| `cairn/scripts/cairn-gsd-init.py` | 17 handlers misc genéricos: `frontmatter.get`, `frontmatter.set`, `frontmatter.validate`, `git.base-branch`, `graphify`, `intel`, `is`, `learnings.copy`, `learnings.query`, `normalize-test-command`, `package-legitimacy`, `classify-confidence`, `plan.task-structure`, `estimate-check`, `teams-status`, `websearch`, `windows` |
| `tests/cairn-gsd.bats` | guard de cobertura FINAL: 10 famílias no filtro, exclusão nominal dos 5 órfãos, comm bidirecional, controle negativo |
| Cenários + goldens | por verbo/situação dos 24; masks onde houver tempo (audit-like: scanned_at não existe aqui — os órfãos ficam fora; windows/ledger e research-store/stale mascarados se carregarem tempo) |

## Flagged assumptions (probe spec-less — escopo deste plano)

Do registro consolidado no 34-01-PLAN.md: CORE-05 unclassified fecha aqui — a cobertura
derivada do inventário é exatamente a resolução (guard comm bidirecional + controle
negativo, Task 3). Nenhum item do probe terminou descartado no plan set: os 10 itens têm
resolução registrada entre os planos 01-05.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: misc de planning-docs no irmão de estado (7 verbos)</name>
  <files>cairn/scripts/cairn-gsd-state.py, tests/cairn-gsd.bats, tests/fixtures/gsd-goldens/scenarios.json, tests/fixtures/gsd-goldens/summary-extract*.golden.json, tests/fixtures/gsd-goldens/todo-match*.golden.json, tests/fixtures/gsd-goldens/requirements-mark*.golden.json, tests/fixtures/gsd-goldens/quick-tasks*.golden.json, tests/fixtures/gsd-goldens/history-digest*.golden.json, tests/fixtures/gsd-goldens/research-*.golden.json</files>
  <read_first>
    - cairn/gsd/contracts/misc.json (os 7 verbos: shape, flags, notes — requirements.mark-complete tem o shape mais largo; research-store carrega hit/stale; quick-tasks-append carrega row/variant)
    - cairn/scripts/cairn-bookkeep.py L958 (parse_roadmap), L1186 (plan_id_matches), L1104-1150 (parse de frontmatter) — os parsers que estes verbos reaproveitam por cópia de forma
    - cairn/scripts/cairn-status.py L369-499 (regexes de fase/plano/summary)
    - cairn/scripts/cairn-gsd-state.py (como o plano 03 deixou — respeitar o teto D-01 ao somar ~200 linhas)
  </read_first>
  <behavior>
    - summary-extract <path> → one_liner/decisions/patterns/tech_added parseados de um SUMMARY real do fixture
    - todo.match-phase → matches/todo_count contra os todos do fixture
    - requirements.mark-complete → ids marcados na tabela de REQUIREMENTS.md do fixture (mutação de documento escopada), write_set/table_unmatched pelo shape
    - quick-tasks-append → row/variant pelo shape; o registro segue a forma que a casa já usa para quick tasks (issues bd — research §4), qualquer diferença de destino vs upstream declarada
    - history-digest → decisions/phases/tech_stack compostos dos SUMMARYs (documentos)
    - research-plan/research-store → items e entry/hit/stale sobre .planning/research (documentos de cache)
  </behavior>
  <action>
    Implementar os 7 handlers no irmão de estado. Todos operam sobre planning-docs
    (SUMMARYs, REQUIREMENTS, ROADMAP, research) — DOCUMENTOS, lidos e editados pelos moldes
    de parse da casa; nenhum deles transiciona dimensão. Onde o verbo upstream tocava
    STATE.md (quick-tasks-append appendava tabela markdown — research §4: quick tasks já
    são issues bd na casa), o destino do cairn segue o precedente da casa e a diferença é
    declarada em divergences.json (family misc, aspect source-of-truth). Mutações de
    documento (requirements.mark-complete) são edições escopadas na linha/célula alvo,
    nunca rewrite do arquivo. Cenários + goldens derived-from-contract por verbo, com
    fixtures de documento realistas (SUMMARY com as seções que summary-extract espera;
    REQUIREMENTS.md com tabela real).
  </action>
  <verify>
    <automated>bats tests/cairn-gsd.bats && test "$(wc -l < cairn/scripts/cairn-gsd-state.py)" -le 1500</automated>
  </verify>
  <acceptance_criteria>
    - `query summary-extract <path> --raw` num SUMMARY de fixture devolve one_liner não vazio e as chaves do shape
    - `query requirements.mark-complete CORE-01 --raw` num REQUIREMENTS de fixture marca o checkbox da linha alvo e SÓ dela (diff do arquivo mostra 1 linha mudada), com write_set/total no envelope
    - `query history-digest --raw` devolve decisions/phases/tech_stack coerentes com os SUMMARYs do fixture
    - os 7 verbos têm >= 1 cenário com golden derived-from-contract; `wc -l cairn/scripts/cairn-gsd-state.py` <= 1500 (D-01)
    - `bats tests/cairn-gsd.bats` verde offline
  </acceptance_criteria>
  <done>Os 7 misc de planning-docs respondem no irmão de estado com os parsers da casa; o roteamento por verbo do plano 01 (MISC_STATE_VERBS) está inteiramente coberto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: misc genérico no irmão de init (17 verbos)</name>
  <files>cairn/scripts/cairn-gsd-init.py, tests/cairn-gsd.bats, tests/fixtures/gsd-goldens/scenarios.json, tests/fixtures/gsd-goldens/frontmatter-*.golden.json, tests/fixtures/gsd-goldens/git-base-branch*.golden.json, tests/fixtures/gsd-goldens/learnings-*.golden.json, tests/fixtures/gsd-goldens/misc-*.golden.json</files>
  <read_first>
    - cairn/gsd/contracts/misc.json (os 17 verbos: shape, flags, notes — vários têm shape [] ou notes que definem a semântica de host único: websearch com WebSearch nativa do host, graphify/intel guardados por capability; a nota de CADA verbo decide se ele responde, degrada ou declara indisponibilidade — nunca silêncio)
    - cairn/scripts/cairn-bookkeep.py L1104-1150 (parse_state_frontmatter — o molde de frontmatter.get/set/validate)
    - cairn/scripts/cairn-gsd.py L784-799 (run_git com timeout e die nomeado — o molde de git.base-branch)
    - cairn/scripts/cairn-gsd-init.py (como o plano 04 deixou — teto D-01 ao somar ~500 linhas)
  </read_first>
  <behavior>
    - frontmatter.get/set/validate → sobre arquivos arbitrários do fixture (PLAN.md), shape do contrato; set é edição escopada do frontmatter
    - git.base-branch → branch base real do fixture git
    - learnings.query/copy → count/learnings/tag e created/skipped/total pelo shape
    - is / normalize-test-command / plan.task-structure / estimate-check / classify-confidence / task-adjacentes → puros, entrada→saída pelo contrato
    - websearch / graphify / intel / package-legitimacy / teams-status / windows → a semântica de host único que a nota do contrato define, com indisponibilidade DECLARADA no envelope quando for o caso
  </behavior>
  <action>
    Implementar os 17 handlers no irmão de init, cada um pela shape + notes de misc.json —
    a nota do contrato é a decisão da 31 sobre o que o verbo significa no cairn; onde ela
    declarar semântica de host único ou indisponibilidade (websearch: o host tem WebSearch
    nativa; graphify/intel: guardados por capability no upstream), o envelope declara
    (available:false/reason ou equivalente do shape) — nunca resposta vazia sem explicação,
    nunca reimplementação dos subsistemas que o research §4 cortou. frontmatter.* pelo
    molde parse_state_frontmatter, com set fazendo edição escopada só da chave alvo.
    git.base-branch pelo molde run_git. Verbos puros (is, normalize-test-command,
    plan.task-structure, estimate-check, classify-confidence) são funções entrada→saída
    diretas do contrato. Divergências deliberadas de host único em divergences.json
    (family misc). Cenários + goldens derived-from-contract por verbo; mask no que
    carregar tempo (windows/ledger, research-like).
  </action>
  <verify>
    <automated>bats tests/cairn-gsd.bats && test "$(wc -l < cairn/scripts/cairn-gsd-init.py)" -le 1500</automated>
  </verify>
  <acceptance_criteria>
    - `query frontmatter.get <plan.md> <campo> --raw` devolve o valor real do fixture; `frontmatter.set` muda SÓ a chave alvo (diff de 1 linha); `frontmatter.validate --schema plan` devolve valid/missing/present coerentes
    - `query git.base-branch --raw` num fixture git devolve a branch base real
    - cada verbo de indisponibilidade declarada (websearch e os que a nota definir) devolve o envelope com a razão — cenário no manifesto provando a forma
    - os 17 verbos têm >= 1 cenário com golden derived-from-contract; `wc -l cairn/scripts/cairn-gsd-init.py` <= 1500 (D-01)
    - `bats tests/cairn-gsd.bats` verde offline
  </acceptance_criteria>
  <done>Os 17 misc genéricos respondem no irmão de init pela letra dos contratos, com toda semântica de host único declarada em tabela.</done>
</task>

<task type="auto">
  <name>Task 3: o guard total — cobertura derivada do inventário, nos dois sentidos</name>
  <files>tests/cairn-gsd.bats</files>
  <read_first>
    - tests/cairn-gsd.bats L530-561 (trivial_verbs_of + o teste comm — o mecanismo a generalizar), L563+ (o controle negativo do verbo forjado — o molde a preservar)
    - cairn/scripts/cairn-gsd.py L116-122 (ORPHANS_PHASE_35 — a lista de exclusão nominal; o guard NÃO a duplica à mão: deriva dela ou a referencia)
    - cairn/gsd/contracts/contracts.json (.verbs — o universo de 87 verbos contra o qual o comm roda)
  </read_first>
  <action>
    Generalizar o guard de cobertura: o filtro de famílias passa a somar as 10 famílias
    implementadas (config, commit, skills, loop-hooks, dispatch-model, estado,
    roadmap-phase, worktree, init, misc), com os 5 verbos de ORPHANS_PHASE_35 excluídos
    NOMINALMENTE — a exclusão cita cada verbo e a fase 35, derivada da constante do
    dispatcher (um dono para a lista), não de uma segunda lista à mão. O teste comm
    compara o conjunto derivado do inventário com o --list-implemented agregado e exige
    vazio NOS DOIS SENTIDOS: verbo contratado sem handler é furo; handler sem contrato é
    superfície fantasma. O controle negativo (verbo forjado injetado no universo faz o
    teste falhar) é adaptado para o filtro novo. Acrescentar um teste bats de falha nomeada
    por família futura: `query verify.plan-structure` (família checagem) segue exit 4
    nomeando a fase 35 — o dispatcher nega o que não é dele, com endereço.

    Regressão total: rodar `bats tests/` inteiro (não só o arquivo da fase) e o teto D-01
    nos dois irmãos como asserção final da fase.
  </action>
  <verify>
    <automated>bats tests/cairn-gsd.bats && bats tests/</automated>
  </verify>
  <acceptance_criteria>
    - o comm derivado-do-inventário x --list-implemented é vazio nos dois sentidos para as 10 famílias, com os 5 órfãos excluídos por nome
    - o controle negativo falha quando um verbo forjado entra no universo (provado invertido: o teste do guard o detecta)
    - `query audit-open` e `query verify.plan-structure` saem exit 4 nomeando a fase 35
    - `bats tests/` inteiro verde — zero regressão na suíte completa da casa
    - `wc -l` dos dois irmãos <= 1500 cada (D-01)
  </acceptance_criteria>
  <done>A cobertura da fase é um fato derivado do inventário e provado por teste bidirecional — o que responde, responde por contrato; o que não responde, nega nomeando a fase dona.</done>
</task>

</tasks>

<verification>
- `bats tests/` inteiro verde offline
- 61 verbos da fase (10 estado + 12 roadmap-phase + 6 worktree + 9 init + 24 misc) com cenário e golden; 5 órfãos negados com endereço
- `git status --porcelain cairn/gsd/` vazio
- divergences.json consolidado: toda mudança deliberada de semântica da fase declarada em tabela
</verification>

<success_criteria>
- CORE-05 fechado: as famílias da fase respondem em python, cobertura derivada do inventário
- CORE-04 valendo em misc: fato ausente nomeado, indisponibilidade declarada
- D-01 provado no fecho: dois irmãos <= ~1.5k cada
- A fase 35 recebe o harness pronto: fixture.bd, goldens por forma, guard generalizado — o mesmo contrato de reuso que a 33 entregou pra 34
</success_criteria>

<output>
Criar `.planning/phases/34-o-binario-python-o-nucleo-de-estado-sobre-o-bd/34-05-SUMMARY.md`
ao concluir, registrando a partição misc final (7/17, com qualquer ajuste que a execução
tenha exigido), as contagens de linha dos dois irmãos vs orçamento, e o inventário de
divergências declaradas da fase inteira.
</output>
