---
phase: 34-o-binario-python-o-nucleo-de-estado-sobre-o-bd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cairn/scripts/cairn-gsd.py
  - cairn/scripts/cairn-gsd-state.py
  - tests/cairn-gsd.bats
  - tests/fixtures/gsd-goldens/scenarios.json
  - tests/fixtures/gsd-goldens/*.golden.json
  - tests/fixtures/gsd-goldens/divergences.json
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03, CORE-04]
beads:
  CORE-01: CairnGo-l8e2
  CORE-02: CairnGo-897j
  CORE-03: CairnGo-u7z9
  CORE-04: CairnGo-av47
must_haves:
  truths:
    - "`query state.begin-phase <N>` escreve no bd exclusivamente via `bd set-state <carrier> dim=val --actor --reason` — a transição projeta os labels `phase:<N>` e `phase_status:executing` no bead portador, e a trilha de auditoria carrega ator e motivo (CORE-03, D-02)"
    - "`query state.load` responde do bd consultando SÓ por label projetado e devolve as 6 chaves do contrato estado.json — config, config_exists, debug_dir, roadmap_exists, state_exists, state_raw (CORE-01, CORE-02)"
    - "repo com bd inicializado mas SEM bead portador: state.load sai exit 1 nomeando o fato ausente E o comando que o cria (`state.begin-phase`), mesmo com um .planning/STATE.md válido presente no fixture — a presença do markdown não muda o resultado, provando que não há fallback de fonte (CORE-04)"
    - "dois beads portadores no mesmo repo: falha nomeada de ambiguidade, nunca escolha silenciosa; zero: falha nomeada prescrevendo o comando (CORE-04 — resolução da aresta adjacency do probe)"
    - "após duas transições consecutivas de phase_status, exatamente UM label `phase_status:*` está presente no carrier (o corrente) — consulta pelo valor antigo não encontra nada (CORE-02 — resolução da aresta ordering do probe)"
    - "o dispatcher roteia estado/roadmap-phase/worktree/init/misc por exec pros irmãos via tabela família→script (misc por verbo), preservando stdout/stderr/exit; `--list-implemented` agrega dispatcher + irmãos existentes (D-01)"
    - "reentrada de state.begin-phase com a fase já executing é idempotente — não duplica label nem cria transição espúria (CORE-03, semântica #3127 do contrato)"
    - "o vocabulário D-02 (5 dimensões e seus valores) está numa fonte só — a constante VERB_DIMENSIONS de cairn-gsd-state.py — e é impresso verbatim no SUMMARY como registro da decisão one-way"
  artifacts:
    - cairn/scripts/cairn-gsd-state.py
    - cairn/scripts/cairn-gsd.py
    - tests/fixtures/gsd-goldens/scenarios.json
    - tests/fixtures/gsd-goldens/divergences.json
    - tests/cairn-gsd.bats
  key_links:
    - "dispatcher → os.execv(sys.executable, [sys.executable, sibling, verbo_canônico] + rest) — o roteamento spelling→verbo fica SÓ no dispatcher (build_routes), nenhuma tabela paralela no irmão"
    - "irmão de estado → bd pelo molde run_bd (cairn-lease.py:287-299): FileNotFoundError e exit != 0 viram die nomeado, nunca traceback"
    - "set-state → label projetado → consulta: o label é o índice; o event bead é a auditoria; os dois só existem pelo caminho set-state"
    - "fixture.bd do harness → bd init -q --prefix --non-interactive + seeds declarativos no cenário → o diferencial roda offline sem rede"
  prohibitions:
    - statement: "Nenhum verbo de estado lê .planning/STATE.md, ROADMAP.md ou SUMMARYs como fonte ou fallback de FATO de estado — fato vem do bd ou o verbo falha nomeado; a leniência de parse de resolve_active_phase (cairn-lease.py:241-263) é molde de FORMA, a FONTE dele é exatamente o que esta fase proíbe"
    - statement: "Nenhuma transição de dimensão escreve no bd por caminho que não seja set-state com ator e motivo — escrita direta de issue vira root sem razão e mata a auditoria (regra medida do research §4)"
    - statement: "Nenhuma task escreve sob cairn/gsd/** — contratos são somente-leitura; toda divergência deliberada vai em tests/fixtures/gsd-goldens/divergences.json, nunca improvisada"
---

<objective>
Tracer da fase 34: UM caminho de estado de ponta a ponta — o dispatcher passa a rotear as
cinco famílias pesadas por exec pros irmãos (D-01), o irmão `cairn-gsd-state.py` nasce com
o vocabulário de dimensões (D-02) numa fonte só, e o par escrita+leitura mais fino que
prova a arquitetura inteira: `state.begin-phase` populando o bd via set-state com ator e
motivo, `state.load` respondendo do bd por label projetado na forma do contrato, e a falha
nomeada quando o fato não existe. Golden derived-from-contract e diferencial verdes antes
de qualquer expansão.

Purpose: provar as três regras não opcionais do bd (label projetado como chave; set-state
com ator/motivo; falha nomeada) num commit, antes de gastar contexto nos 59 verbos
restantes.
Output: dispatcher roteando por exec, cairn-gsd-state.py com begin-phase + load, harness
com fixture.bd, goldens e divergências declaradas.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-o-binario-python-o-nucleo-de-estado-sobre-o-bd/34-CONTEXT.md
@.planning/phases/34-o-binario-python-o-nucleo-de-estado-sobre-o-bd/34-PATTERNS.md
@cairn/gsd/contracts/contracts.json
@cairn/gsd/contracts/estado.json
</context>

## Artifacts this phase produces (escopo deste plano)

| Artefato | O que entra neste plano |
|---|---|
| `cairn/scripts/cairn-gsd-state.py` (novo) | skeleton na forma da casa (docstring-contrato, exits 0/1/2/4, die, HANDLERS, envelope copiado) + constante `VERB_DIMENSIONS` (D-02) + handlers `state.begin-phase` e `state.load` |
| `cairn/scripts/cairn-gsd.py` (mod) | tabela família→irmão (estado/roadmap-phase → cairn-gsd-state.py; worktree/init → cairn-gsd-init.py; misc por verbo — partição do plano 05), exec no ponto de inserção do PATTERNS, `--list-implemented` agregado |
| Dimensões D-02 (schema permanente, one-way) | `phase` (número corrente), `phase_status` ∈ {planned, executing, verified, complete}, `plan` (NN-MM), `verification` ∈ {passed, failed, pending}, `session` (última atividade, YYYY-MM-DD) — labels projetados `dim:valor`, ASCII, sem espaços |
| Bead portador (design de consulta) | label âncora `gsd-state`, único por repo; criado por state.begin-phase; 0 ou >1 portadores → falha nomeada |
| `tests/cairn-gsd.bats` + goldens | extensão `fixture.bd` no builder local, cenários do tracer, entradas novas em divergences.json (family estado) |

## Flagged assumptions — probe spec-less (§A/§C, nunca drop silencioso)

O edge-probe devolveu 10 itens, todos `unresolved` (unclassified para CORE-01/04/05;
adjacency/empty/encoding/ordering para CORE-02/03). Resolução registrada aqui, não
descartada — as arestas reais vêm das três regras medidas do bd e viram cenários:

| Item do probe | Resolução (vira cenário/critério neste plano set) |
|---|---|
| CORE-01 unclassified | forma por contrato, verbo a verbo — cada verbo tem golden derived-from-contract comparado por FORMA (plano 01-03) |
| CORE-02 empty | bd sem portador → falha nomeada, nunca envelope vazio (cenário `state-load` sem fato, este plano) |
| CORE-02 adjacency | dois portadores → falha nomeada de ambiguidade (bats direto, este plano) |
| CORE-02 encoding | labels `dim:valor` ASCII sem espaços — vocabulário fixo de VERB_DIMENSIONS; valor livre (session) normalizado para YYYY-MM-DD |
| CORE-02 ordering | label projetado é valor-corrente-único: após transição, consulta pelo valor antigo não encontra (bats, este plano) |
| CORE-03 adjacency | reentrada de begin-phase com fase já executing: idempotente, sem label duplicado (cenário, este plano) |
| CORE-03 empty | transição sem ator resolvível: resolve_actor cai para USER; sem motivo o verbo falha usage — nunca transição anônima (critério, este plano) |
| CORE-03 ordering | caso canônico current_phase 18 — duas invocações de record-metric atribuem à fase do bd e não alteram dimensão nenhuma (plano 02) |
| CORE-04 unclassified | mensagem nomeia o FATO e o COMANDO, com STATE.md presente no fixture para provar ausência de fallback (cenário, este plano) |
| CORE-05 unclassified | cobertura derivada do inventário — guard comm nos dois sentidos fecha no plano 05 |

<tasks>

<task type="tracer">
  <name>Task 1: state.begin-phase → bd → state.load, de ponta a ponta pelo dispatcher</name>
  <files>cairn/scripts/cairn-gsd.py, cairn/scripts/cairn-gsd-state.py</files>
  <precondition>`bd --version` responde no PATH (os fixtures e a verificação fazem `bd init` real; bd 1.1.0 confirmado no ambiente em 2026-08-10)</precondition>
  <reversibility rating="one-way">D-02: os labels projetados são o índice de consulta permanente do bd deste e de todo repo que adotar o cairn; renomear dimensão depois exige migração de labels em todo acervo. Run autônomo: o checkpoint vira registro explícito — o vocabulário exato das 5 dimensões entra verbatim na constante, neste plano e no SUMMARY.</reversibility>
  <read_first>
    - .planning/phases/34-o-binario-python-o-nucleo-de-estado-sobre-o-bd/34-PATTERNS.md (o mapa inteiro — todos os moldes abaixo têm arquivo:linha lá)
    - cairn/scripts/cairn-gsd.py L107-122 (PHASE_BY_FAMILY + ORPHANS_PHASE_35 — o que a tabela nova substitui), L2015-2041 (--list-implemented e o loop de despacho — o ponto de inserção do exec), L70-142 (cabeçalho/exits/die a copiar), L292-336 + L874-906 (envelope emit/stringify/output_like_binary a copiar, não reimplementar)
    - cairn/scripts/cairn-lease.py L287-299 (molde run_bd), L266-281 (resolve_actor), L241-263 (resolve_active_phase — copiar a leniência de parse, NUNCA a fonte: é o contraexemplo)
    - cairn/scripts/cairn-status.py L594-623 (as_str_list + parse de labels defensivo)
    - cairn/gsd/contracts/estado.json (entradas state.begin-phase e state.load: shape, flags, notes — o "nunca falha" do load upstream é divergência declarada, ver Task 2)
    - cairn/gsd/contracts/contracts.json (.verbs — o índice verbo→família que a tabela de roteamento consome)
  </read_first>
  <action>
    No dispatcher (cairn-gsd.py): substituir as entradas estado/roadmap-phase/worktree/init/misc
    de PHASE_BY_FAMILY por uma tabela FAMILY_SCRIPT — estado e roadmap-phase →
    `cairn-gsd-state.py`; worktree e init → `cairn-gsd-init.py`; misc roteia POR VERBO
    (constante MISC_STATE_VERBS com os 7 verbos de planning-docs — summary-extract,
    todo.match-phase, requirements.mark-complete, quick-tasks-append, history-digest,
    research-plan, research-store — pro irmão de estado; os demais 17 pro de init; partição
    é a discrição do CONTEXT, registrada aqui e implementada nos planos 04-05). Os 5
    ORPHANS_PHASE_35 continuam exatamente como estão: exit 4 nomeando a fase 35. No ponto
    de inserção (entre HANDLERS.get e o die de EXIT_UNIMPLEMENTED): resolver o irmão pelo
    molde sibling() de cairn-bookkeep.py:720-721 e fazer
    `os.execv(sys.executable, [sys.executable, str(sibling), verb] + argv[n:])` — passar o
    VERBO canônico já resolvido, não o spelling (precedente shell: cairn-gsd.sh L13).
    Irmão ainda inexistente no disco → cair no die exit 4 atual nomeando a fase 34 (estado
    transitório entre ondas). `--list-implemented`: o dispatcher roda cada irmão EXISTENTE
    com --list-implemented e concatena com as próprias chaves, ordenado — mantém o contrato
    de superfície única e o bats intocado.

    Criar cairn-gsd-state.py na forma da casa (D-01): docstring-contrato (Usage, exits um a
    um, semântica medida com data, seams de teste, regra cairn/gsd/ somente-leitura),
    EXIT_OK/CONTRACT/USAGE/UNIMPLEMENTED idênticos ao dispatcher, die com TAG_PREFIX
    próprio, HANDLERS, envelope copiado (emit/js_string/stringify/output_like_binary). O
    main recebe o verbo canônico como argv[1]; verbo da família ainda sem handler → die
    exit 4 nomeando o verbo e a fase 34. No topo, a constante VERB_DIMENSIONS (D-02, molde
    PHASE_BY_FAMILY com racional em comentário) — a ÚNICA fonte do vocabulário: dimensões
    `phase`, `phase_status` (planned|executing|verified|complete), `plan` (NN-MM),
    `verification` (passed|failed|pending), `session` (YYYY-MM-DD), com o mapa
    verbo→dimensões-que-escreve. Registrar em comentário: os verbos gsd MAPEIAM para essas
    dimensões; o vocabulário do upstream não vira schema.

    Design do portador (resolve as arestas adjacency/empty do probe): um bead único por
    repo com label âncora `gsd-state` carrega as 5 dimensões como labels projetados
    `dim:valor`. Consulta SEMPRE por label (`bd list -l <label> --all --limit 0 --json`,
    molde cairn-lease.py:315, ou `bd query "label=dim:valor"`) — nunca por metadata
    aninhado (rc 0 silencioso, medido). Zero portadores num verbo de leitura → die exit 1
    nomeando o fato e prescrevendo `cairn-gsd.sh query state.begin-phase <N>`; dois ou
    mais → die exit 1 nomeando a ambiguidade e os ids.

    handler state.begin-phase (o escritor): cria o portador se ausente (registrado no
    envelope, semântica DWIM do contrato) e transiciona `phase=<N>` e
    `phase_status=executing` EXCLUSIVAMENTE via `bd set-state <id> dim=val --actor <ator>
    --reason <motivo>` pelo molde run_bd; ator pelo molde resolve_actor
    (BEADS_ACTOR > git user.name > USER); motivo nomeia o verbo e o gatilho (ex.:
    "state.begin-phase 34 via cairn-gsd"). Reentrada com a fase já executing: idempotente
    (#3127) — não duplica label, updated reflete só o que mudou. Envelope no shape do
    contrato: {error, phase, phase_name, plan_count, updated}; phase_name e plan_count vêm
    de onde o contrato os define (documentos de roadmap são documento, não fato — ler o
    documento aqui é legítimo; o FATO phase/phase_status vive no bd).

    handler state.load (o leitor): resolve o portador por label e monta o shape do contrato
    {config, config_exists, debug_dir, roadmap_exists, state_exists, state_raw} — config
    continua vindo de .planning/config.json (config é documento), debug_dir absoluto,
    roadmap_exists é existência de arquivo; state_exists e state_raw passam a derivar dos
    FATOS do bd (state_raw = rendering determinístico das dimensões do portador; a mudança
    de significado é divergência declarada na Task 2). Portador ausente → falha nomeada
    (diverge do "nunca falha" upstream — declarada, não descoberta). Sem --raw =
    JSON.stringify(v, null, 2); --raw = String(valor); sem newline final.
  </action>
  <verify>
    <automated>R="$PWD" && T=$(mktemp -d) && cd "$T" && git init -q && git commit -q --allow-empty -m x && bd init -q --prefix trc --non-interactive && mkdir -p .planning && echo '{}' > .planning/config.json && P="$R/cairn/scripts/cairn-gsd.sh" && "$P" query state.load --raw 2>err.txt; test $? -eq 1 && grep -q "state.begin-phase" err.txt && "$P" query state.begin-phase 1 --raw && bd list -l phase_status:executing --json | jq -e 'length == 1' && "$P" query state.load --raw | jq -e 'has("config") and has("state_exists") and has("state_raw")'</automated>
  </verify>
  <acceptance_criteria>
    - num repo temporário com git + `bd init`: `query state.begin-phase 1 --raw` sai 0; `bd list -l gsd-state --all --json` devolve exatamente 1 issue cujos labels contêm `phase:1` e `phase_status:executing` — o label projetado é a prova do caminho set-state
    - a trilha de auditoria existe: após a transição há evidência de evento com ator e motivo no bd (asserção pelo comando bd que a versão 1.1.0 expõe — bd show/list do event bead), e o motivo contém o nome do verbo
    - `query state.load --raw` no mesmo repo sai 0 com exatamente as 6 chaves do contrato; no repo SEM portador sai exit 1 com stderr nomeando o fato ausente E o comando `state.begin-phase`
    - reentrada: `query state.begin-phase 1` uma segunda vez sai 0 sem duplicar labels (`bd list -l gsd-state --json` segue com UM label `phase_status:*`)
    - transição dupla (begin-phase seguido de uma segunda transição de status por set-state semeado) deixa UM único label `phase_status:*` no portador; `bd list -l phase_status:executing` após a mudança não encontra o portador
    - `./cairn/scripts/cairn-gsd.sh --list-implemented` inclui state.load e state.begin-phase; `query state.update` (ainda sem handler) sai exit 4 nomeando o verbo e a fase 34
    - `bats tests/cairn-gsd.bats` segue verde (nenhum cenário da 33 regride com o roteamento novo)
  </acceptance_criteria>
  <done>O caminho fino inteiro — dispatcher → exec → irmão → bd set-state → label projetado → consulta por label → envelope do contrato — funciona num repo real e está commitado; as três regras não opcionais estão provadas em UM verbo de escrita e UM de leitura.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fixture.bd no harness, cenários do tracer e divergências declaradas</name>
  <files>tests/cairn-gsd.bats, tests/fixtures/gsd-goldens/scenarios.json, tests/fixtures/gsd-goldens/state-*.golden.json, tests/fixtures/gsd-goldens/divergences.json</files>
  <read_first>
    - tests/cairn-gsd.bats L43-53 (o seam: cenário novo = entrada no manifesto + golden irmão, zero mudança no runner), L75-95 (build_scenario_fixture — o builder a estender), L132-144 (apply_mask_file), L181-215 (comparador: bytes para recorded, FORMA para derived-from-contract), L284-349 (guards de provenance, serialização da casa e free_of_timestamp_keys)
    - tests/fixtures/gsd-goldens/scenarios.json (shape de cenário estabelecido) e um golden derived-from-contract existente, se houver, senão config-get-scope-hit.golden.json (shape de golden)
    - tests/fixtures/gsd-goldens/divergences.json (molde da entrada workstream-root-layer: aspect/family/upstream/cairn/reason)
    - tests/cairn-lease.bats L34 e tests/helpers.bash L353 (molde bd em fixture: `bd init -q --prefix X --non-interactive`)
    - cairn/gsd/contracts/estado.json (notes de state.load — o texto exato do "nunca falha" upstream que a divergência cita)
  </read_first>
  <behavior>
    - Cenário `state-load-fato-ausente`: fixture.bd init sem seed → exit 1, stderr nomeia fato + comando
    - Cenário `state-load-md-presente-fato-ausente`: fixture com files[".planning/STATE.md"] válido E bd sem portador → MESMO exit 1 nomeado (prova comportamental de fonte única)
    - Cenário `state-begin-phase-cria-portador`: fixture.bd limpo → exit 0, envelope do contrato
    - Cenário `state-load-do-bd`: fixture.bd com seeds (create portador + set-state phase/phase_status) → exit 0, 6 chaves
    - Cenário `state-begin-phase-reentrada`: seeds já executing → exit 0 idempotente
  </behavior>
  <action>
    Estender build_scenario_fixture (no builder LOCAL do cairn-gsd.bats — helpers.bash não
    é tocado, precedente declarado em L65-67) com a chave `fixture.bd`: objeto declarativo
    com `init` (bool → `bd init -q --prefix <prefixo-do-cenário> --non-interactive`) e
    `seed` (lista de argvs bd executados em ordem — `bd create ...`, `bd set-state ...`) —
    o cenário descreve, o builder executa, nada de shell embutido no manifesto. Ids de
    issue e datas que variem entre execuções recebem `mask` no cenário (molde
    apply_mask_file: valor validado contra regex e trocado por `<masked>`).

    Escrever os 5 cenários do behavior com goldens irmãos `<id>.golden.json`, provenance
    `derived-from-contract` (comparação por FORMA, L194-215) — o recorder é inaplicável a
    verbos bd: cairn-gsd-record.py grava do binário real, que lê markdown; um verbo que
    responde do bd não tem o que gravar dele. Esse registro entra como entrada informativa
    em divergences.json, junto com as duas divergências semânticas da fase (family estado,
    molde workstream-root-layer): (1) aspect source-of-truth de state.load — upstream
    "nunca falha, STATE.md ausente sai state_exists:false"; cairn: portador ausente é falha
    nomeada com comando prescrito (CORE-04); (2) aspect source-of-truth de state_raw —
    upstream: markdown cru de STATE.md; cairn: rendering determinístico das dimensões do
    portador (D-02). Reason de cada uma cita a regra do milestone (duas fontes é a doença).

    Testes bats diretos (fora de golden, no molde dos testes de sentinel da 33): a aresta
    adjacency — fixture com DOIS beads portando `gsd-state` → state.load exit 1 nomeando
    ambiguidade; a aresta ordering — após seed com transição dupla de phase_status, `bd
    list -l <valor-antigo>` vazio e `-l <valor-novo>` com 1; e o guard de que os goldens
    novos passam house_serialization_ok e free_of_timestamp_keys.
  </action>
  <verify>
    <automated>bats tests/cairn-gsd.bats</automated>
  </verify>
  <acceptance_criteria>
    - os 5 cenários do behavior existem no manifesto com goldens irmãos provenance derived-from-contract, e o runner diferencial os cobre SEM nenhuma linha mudada no runner (só builder + manifesto)
    - o cenário com STATE.md presente e bd sem portador falha exit 1 com a MESMA mensagem nomeada do cenário sem STATE.md — diff das duas stderr limpo fora do prefixo de path
    - divergences.json ganhou >= 3 entradas novas de family estado (as duas semânticas + o registro do recorder inaplicável), cada uma com aspect/upstream/cairn/reason preenchidos
    - bats direto: dois portadores → exit 1 nomeando ambiguidade; transição dupla → label antigo não encontra, novo encontra exatamente 1
    - `bats tests/cairn-gsd.bats` verde offline, incluindo todos os cenários da fase 33 (regressão zero)
  </acceptance_criteria>
  <done>O harness da 33 fala bd: cenário declarativo com fixture.bd, goldens por forma de contrato, e toda mudança deliberada de semântica declarada em tabela — nada para ser descoberto em produção.</done>
</task>

</tasks>

<verification>
- `bats tests/cairn-gsd.bats` verde offline — tracer coberto por cenário e por bats direto
- sequência manual de ponta a ponta num repo temporário (o verify da Task 1) sai limpa
- `git status --porcelain cairn/gsd/` vazio — contratos intocados
- nenhum handler novo contém leitura de .planning/STATE.md como fonte de fato (prova comportamental: cenário md-presente-fato-ausente)
</verification>

<success_criteria>
- CORE-02 e CORE-03 provados no par begin-phase/load: label projetado como única chave, set-state com ator/motivo como única escrita
- CORE-01 provado em state.load: as 6 chaves do contrato, envelope na semântica medida
- CORE-04 provado com o fixture adversarial (markdown presente, fato ausente → falha nomeada)
- D-01 e D-02 materializados: dois pontos de roteamento (FAMILY_SCRIPT + MISC_STATE_VERBS) e uma fonte de vocabulário (VERB_DIMENSIONS)
- Arquitetura provada com ~2 verbos antes dos ~59 restantes — o custo de um erro de arquitetura é um commit, não dez
</success_criteria>

<output>
Criar `.planning/phases/34-o-binario-python-o-nucleo-de-estado-sobre-o-bd/34-01-SUMMARY.md`
ao concluir. O SUMMARY DEVE imprimir verbatim o vocabulário D-02 decidido (as 5 dimensões,
seus valores permitidos e a forma do label projetado) — é o registro explícito da decisão
one-way no lugar do checkpoint de parada — e registrar a partição misc 7/17 e a decisão de
não criar wrappers .sh próprios pros irmãos (toda entrada passa por cairn-gsd.sh; irmãos
são detalhe de implementação do dispatcher — PATTERNS, nota .sh).
</output>
