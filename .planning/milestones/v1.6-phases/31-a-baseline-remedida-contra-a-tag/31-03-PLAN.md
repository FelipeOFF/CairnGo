---
phase: 31-a-baseline-remedida-contra-a-tag
plan: "03"
type: execute
wave: 3
depends_on: ["31-02"]
files_modified:
  - cairn/gsd/contracts/estado.json
  - cairn/gsd/contracts/roadmap-phase.json
  - cairn/gsd/contracts/worktree.json
  - cairn/gsd/contracts/init.json
  - cairn/gsd/contracts/checagem.json
  - cairn/gsd/contracts/misc.json
  - cairn/gsd/contracts/contracts.json
  - tests/gsd-contracts.bats
autonomous: true
requirements: [REM-03]
beads: [CairnGo-b87b]
must_haves:
  truths:
    - "As seis famílias restantes (estado, roadmap-phase, worktree, init, checagem, misc) têm arquivo de contrato sob o schema do plano 02, com source_ref por verbo apontando a implementação na tag (REM-03)"
    - "A grafia dupla de verification.status está representada no contrato: a entrada em checagem.json lista as duas spellings (verification.status e query verification status), como o research §2.3 exige do parser"
    - "Os quatro verbos com sítio real cujas fontes ficaram fora da soma de orçamento (audit-open, review-lane, agent.classify-failure, task.is-behavior-adding) têm contrato com source_ref em src (audit.cts, command-aliases.cts/task-command-router.cts, review-lane descriptor/invocation/runner) — eles têm fonte, diferente dos 4 órfãos do plano 04"
    - "A cobertura fecha: todo verbo do universo do inventário tem entrada no índice do agregado e no arquivo da sua família, e o teste que prova isso roda contra a saída real de cairn-inventory.sh (skip-gated quando o cache não existe)"
    - "contracts.json indexa as 11 famílias com arquivos existentes e consistência bidirecional verde"
    - statement: "Amostra de contratos confere com a implementação da tag: para cada uma das seis famílias, ao menos um verbo re-conferido abrindo o source_ref citado no clone"
      verification: backstop
  artifacts:
    - path: cairn/gsd/contracts/estado.json
      provides: "contratos dos verbos de estado (state.load/update/begin-phase/record-*/advance-plan/add-*)"
      contains: "state.load"
    - path: cairn/gsd/contracts/roadmap-phase.json
      provides: "contratos da família roadmap/phase (20+ sítios)"
    - path: cairn/gsd/contracts/worktree.json
      provides: "contratos da família worktree (12 sítios)"
    - path: cairn/gsd/contracts/init.json
      provides: "contratos dos verbos init.* — os 9 bundles lidos por --pick pelos 8 workflows"
    - path: cairn/gsd/contracts/checagem.json
      provides: "contratos da família de checagem (verify.*, uat.*, verification.status nas duas grafias, check) e dos verbos de checagem fora da soma"
      contains: "verification.status"
    - path: cairn/gsd/contracts/misc.json
      provides: "contratos dos verbos atômicos restantes do universo"
  key_links:
    - from: cairn/gsd/contracts/contracts.json
      to: cairn/gsd/contracts/estado.json
      via: "índice verbs e mapa families do agregado"
      pattern: "estado.json"
    - from: tests/gsd-contracts.bats
      to: cairn/scripts/cairn-inventory.sh
      via: "teste de cobertura que roda o inventário real e compara o universo com o índice do agregado"
      pattern: "cairn-inventory"
  prohibitions:
    - "Nenhuma entrada de contrato escrita de memória ou por plausibilidade: toda entrada nasce da leitura da implementação no clone da tag e do uso real nos call sites"
    - "A medição nunca lê ~/.claude nem runtime instalado: source com source, sempre o clone cacheado"
    - "cairn/scripts/cairn-doctor.py não é modificado por nenhum plano desta fase (D-03)"
  assumptions:
    - "edge-probe (specless fallback): REM-03 saiu unclassified/unresolved do engine determinístico; as arestas foram autoradas manualmente nas truths/prohibitions acima e ficam flagged para o verificador — nunca dropadas"
---

<objective>
Completar a tabela versionada do REM-03: os contratos das seis famílias que
faltam — estado, roadmap/phase, worktree, init, checagem e misc — extraídos da
implementação da tag no schema que o plano 02 fixou (D-02), e o fechamento
mecânico da cobertura: todo verbo que o inventário encontra no fecho dos 8
workflows e dos agentes tem contrato, provado por teste que roda o próprio
inventário.

Purpose: estas famílias são o coração das fases 34 (estado sobre o bd) e 35
(checagem): sem contrato por verbo, "mesma forma de payload" (CORE-01) não tem
referência contra a qual ser verificado.

Output: `cairn/gsd/contracts/{estado,roadmap-phase,worktree,init,checagem,misc}.json`,
agregado atualizado e `tests/gsd-contracts.bats` com o teste de cobertura fechada.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-a-baseline-remedida-contra-a-tag/31-CONTEXT.md
@.planning/phases/31-a-baseline-remedida-contra-a-tag/31-PATTERNS.md
@.planning/research/v1.6-transplante-gsd.md
@cairn/gsd/contracts/contracts.json
@tests/gsd-contracts.bats
</context>

## Artifacts this phase produces (parcela deste plano)

- **Arquivos de família**: `cairn/gsd/contracts/estado.json`, `roadmap-phase.json`, `worktree.json`, `init.json`, `checagem.json`, `misc.json` — mesmos campos obrigatórios do schema do plano 02.
- **Entrada de contrato com spellings dupla**: `verification.status` em `checagem.json` com `spellings` contendo as duas grafias.
- **Índice completo**: `contracts.json` com `verbs` cobrindo o universo inteiro do inventário e `universe` atualizado (contagens e data).
- **Teste de cobertura fechada** em `tests/gsd-contracts.bats`: universo do inventário == índice do agregado (skip-gated pelo cache).

<tasks>

<task type="auto">
  <name>Task 1: Estado, roadmap/phase e worktree extraídos da tag</name>
  <precondition>o cache do clone existe (`git -C .cairn/cache/gsd-core-v1.10.0 rev-parse HEAD` sai 0) ou há rede para `cairn/scripts/cairn-inventory.sh` recriá-lo</precondition>
  <files>cairn/gsd/contracts/estado.json, cairn/gsd/contracts/roadmap-phase.json, cairn/gsd/contracts/worktree.json, cairn/gsd/contracts/contracts.json</files>
  <read_first>
    - Saída de `cairn/scripts/cairn-inventory.sh --json` (o universo: quais verbos state.*/roadmap.*/phase.*/worktree.* os 8 e os agentes chamam, e onde)
    - cairn/gsd/contracts/config.json (o exemplar do schema em uso — a forma a copiar)
    - No clone: o router de gsd-core/bin/gsd-tools.cjs para localizar handlers; src/*.cts de estado (~6.472 linhas), roadmap/phase (~5.945) e worktree (~2.426) — SÓ as regiões dos handlers dos verbos do universo, localizadas por grep, nunca o arquivo inteiro
    - .planning/research/v1.6-transplante-gsd.md §4 (a tabela de famílias e onde mora cada implementação)
  </read_first>
  <action>
    Mesmo método da Task 2 do plano 02, sobre as três famílias de estado: para
    cada verbo do universo atribuído a estado, roadmap-phase ou worktree pela
    assignment_rule do agregado, localizar o handler por grep dirigido, ler a
    região, e registrar a entrada completa (verb, spellings, invocation, input,
    output com shape das chaves de topo — os campos extraídos por --pick nos
    call sites são evidência primária do shape —, exit_codes, source_ref
    path+lines, call_sites, notes). Os verbos de estado incluem a família
    state.load/state.update/begin-phase/record-*/advance-plan/add-* que o
    research §4 lista; o que o inventário achar além disso entra também — o
    universo medido manda, não a lista do research. Atualizar o índice verbs do
    contracts.json a cada família fechada.
  </action>
  <verify>
    <automated>bats tests/gsd-contracts.bats</automated>
  </verify>
  <acceptance_criteria>
    - `bats tests/gsd-contracts.bats` sai 0 (os três arquivos novos passam no schema)
    - `jq -e '.verbs[] | select(.verb == "state.load")' cairn/gsd/contracts/estado.json` sai 0
    - `jq -e '[.verbs[] | select((.source_ref.path // "") == "" or (.source_ref.lines // "") == "")] | length == 0'` sai 0 para os três arquivos novos
    - Todo verbo do universo do inventário atribuído a estas três famílias tem entrada (cruzamento jq entre `cairn/scripts/cairn-inventory.sh --json | jq -r '.verbs | keys[]'` e o índice do agregado)
  </acceptance_criteria>
  <done>As três famílias de estado têm contrato completo com source_ref, indexadas no agregado.</done>
</task>

<task type="auto">
  <name>Task 2: Init, checagem e misc — incluindo a grafia dupla e os verbos fora da soma</name>
  <files>cairn/gsd/contracts/init.json, cairn/gsd/contracts/checagem.json, cairn/gsd/contracts/misc.json, cairn/gsd/contracts/contracts.json</files>
  <read_first>
    - Saída de `cairn/scripts/cairn-inventory.sh --json` (os verbos init.*/verify.*/uat.*/verification.*/check e o resto do universo; as spellings registradas por sítio)
    - No clone: src/init.cts (3.782 linhas — ler a composição de bundle por verbo init.*, não o arquivo inteiro); o módulo de checagem (~7.390 linhas, handlers dos verbos do universo); audit.cts + audit-command-router.cts (audit-open), command-aliases.cts + task-command-router.cts (agent.classify-failure, task.is-behavior-adding), review-lane descriptor/invocation/runner (review-lane) — os ~4,6k de fontes que o research §6 risco 5 nomeou fora da soma, mas que TÊM fonte em src
    - .planning/research/v1.6-transplante-gsd.md §2.3 (grafia dupla) e §6 risco 5 (a lista dos verbos fora da soma e seus arquivos)
  </read_first>
  <action>
    Extrair as três famílias restantes no mesmo método. Pontos que não podem
    passar batidos: (1) em checagem.json, a entrada de verification.status
    carrega spellings com AS DUAS grafias — `verification.status` e
    `query verification status` — porque o corpus usa ambas (research §2.3) e o
    binário python da fase 35 precisa aceitar as duas (CHECK-01); (2) os quatro
    verbos com sítio real e fonte fora da soma de orçamento — audit-open,
    review-lane, agent.classify-failure, task.is-behavior-adding — entram nas
    famílias que a assignment_rule determinar (checagem ou misc), com source_ref
    para seus arquivos src reais; eles NÃO recebem provenance de bundle (têm
    fonte em src — os órfãos sem fonte são os do plano 04); (3) init.json
    registra cada verbo init.* que os 8 chamam, com output.shape listando as
    chaves de topo do bundle que os call sites leem por --pick (o detalhamento
    shape a shape e a estimativa derivada são o plano 04 — aqui o contrato
    verbo a verbo). Fechar o índice verbs do contracts.json com o universo
    inteiro e atualizar universe com as contagens e a data.
  </action>
  <verify>
    <automated>bats tests/gsd-contracts.bats</automated>
  </verify>
  <acceptance_criteria>
    - `bats tests/gsd-contracts.bats` sai 0
    - `jq -e '.verbs[] | select(.verb == "verification.status") | .spellings | length >= 2' cairn/gsd/contracts/checagem.json` sai 0
    - Os quatro verbos fora da soma têm entrada: `jq -e '.verbs[.verbs | map(.verb) | index("audit-open")]' ` encontra audit-open em checagem.json ou misc.json, e o mesmo para review-lane, agent.classify-failure e task.is-behavior-adding — cada um com source_ref.path começando com src/
    - `jq -e '[.verbs[] | select((.source_ref.path // "") == "" or (.source_ref.lines // "") == "")] | length == 0'` sai 0 para os três arquivos novos
  </acceptance_criteria>
  <done>As 11 famílias existem em disco; a grafia dupla e os verbos fora da soma estão contratados com fonte em src.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: O fechamento — cobertura do universo provada contra o inventário real</name>
  <files>tests/gsd-contracts.bats, cairn/gsd/contracts/contracts.json</files>
  <read_first>
    - tests/cairn-trend.bats linhas 365-396 (o molde: expectativa recomputada, nunca digitada)
    - tests/gsd-contracts.bats (o estado atual da suíte)
    - cairn/gsd/contracts/contracts.json (o índice completo)
  </read_first>
  <behavior>
    - Consistência interna (offline): todo verbo do índice verbs do agregado existe no arquivo da família apontada, e todo verbo presente em qualquer arquivo de família consta do índice — diferença vazia nos dois sentidos
    - Cobertura real (skip-gated pelo cache): o conjunto de verbos de `cairn/scripts/cairn-inventory.sh --json` (chaves de .verbs) menos os verbos do índice do agregado é vazio, e o inverso lista no máximo os 4 órfãos do plano 04 ainda não escritos — o teste nomeia essa tolerância com comentário citando REM-04, e o plano 04 a remove
    - Controle negativo: um verbo forjado injetado no universo simulado não passa (o teste rejeita cobertura furada)
  </behavior>
  <action>
    RED: escrever os testes acima em tests/gsd-contracts.bats. O de cobertura
    real começa com skip quando `.cairn/cache/gsd-core-v1.10.0` não existe
    (mesma linha de skip do cairn-inventory.bats) e recomputa os dois conjuntos
    com jq — nenhuma lista de verbos digitada à mão no teste. O controle
    negativo roda a mesma comparação contra um universo simulado (arquivo
    temporário com um verbo a mais) e afirma que a diferença NÃO é vazia.

    GREEN: fechar o que o vermelho apontar — tipicamente verbos do universo sem
    entrada (adicionar à família certa com source_ref) ou entradas órfãs no
    índice (corrigir). Atualizar universe no agregado com os totais finais por
    escopo e a data.
  </action>
  <verify>
    <automated>bats tests/gsd-contracts.bats</automated>
  </verify>
  <acceptance_criteria>
    - `bats tests/gsd-contracts.bats` sai 0 com o cache real presente (o teste de cobertura roda de verdade nesta máquina)
    - `cairn/scripts/cairn-inventory.sh --json | jq -r '.verbs | keys[]' | sort` comparado com `jq -r '.verbs | keys[]' cairn/gsd/contracts/contracts.json | sort` — diferença restrita aos 4 órfãos do plano 04 (run-with-timeout, user-story.validate, dispatch-isolation, dispatch-should-flatten), se aparecerem no universo
    - O controle negativo de cobertura furada passa (rejeita o verbo forjado)
  </acceptance_criteria>
  <done>A cobertura do REM-03 é um fato mecânico: universo medido e tabela versionada coincidem, com a única tolerância nomeada (os órfãos do plano 04) e um controle negativo provando que o teste morde.</done>
</task>

</tasks>

<verification>
- `bats tests/gsd-contracts.bats` — schema, consistência interna e cobertura real verdes.
- `bats tests/cairn-inventory.bats` — regressão do inventário.
- Spot-check do backstop: um verbo por família nova re-conferido contra o source_ref no clone.
</verification>

<success_criteria>
- REM-03 completo: cada verbo chamado pelos 8 workflows (e pelos agentes que eles despacham) tem contrato de entrada e saída registrado, extraído da implementação da tag, em tabela versionada — e a cobertura é provada por teste contra o inventário real, não afirmada.
- A grafia dupla de verification.status está contratada; os verbos fora da soma de orçamento têm contrato com fonte em src.
</success_criteria>

<output>
Criar `.planning/phases/31-a-baseline-remedida-contra-a-tag/31-03-SUMMARY.md` ao concluir, registrando o total de verbos contratados por família e qualquer verbo do universo cuja atribuição de família precisou de decisão anotada.
</output>
