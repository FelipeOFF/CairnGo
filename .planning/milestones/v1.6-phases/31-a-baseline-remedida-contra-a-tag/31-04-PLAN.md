---
phase: 31-a-baseline-remedida-contra-a-tag
plan: "04"
type: execute
wave: 4
depends_on: ["31-03"]
files_modified:
  - cairn/gsd/contracts/misc.json
  - cairn/gsd/contracts/checagem.json
  - cairn/gsd/contracts/dispatch-model.json
  - cairn/gsd/contracts/init.json
  - cairn/gsd/contracts/contracts.json
  - tests/gsd-contracts.bats
autonomous: true
requirements: [REM-04, REM-05]
beads: [CairnGo-6g0i, CairnGo-zto8]
must_haves:
  truths:
    - "Os 4 verbos sem fonte em src (run-with-timeout, user-story.validate, dispatch-isolation, dispatch-should-flatten) têm contrato no MESMO schema das demais entradas, com campo provenance declarando a extração do gsd-tools.cjs bakeado e source_ref com linhas em gsd-core/bin/gsd-tools.cjs (D-04, REM-04)"
    - "Nenhuma entrada não-órfã carrega provenance de bundle, e nenhuma entrada órfã aponta source_ref para src/ — a distinção é testada nos dois sentidos"
    - "A estimativa de init está re-derivada pelos 9 shapes de bundle lidos por --pick: cada shape tem os campos consumidos enumerados a partir dos call sites reais, a composição mapeada em src/init.cts, e a estimativa python por shape somada com o método escrito (REM-05; substitui o ~500 refutado pelo research §6 risco 6)"
    - "O instalador descartável tem UM número oficial com filtro declarado: a lista de padrões de arquivo do filtro e o comando find/wc exato estão gravados junto do número, com o subconjunto estreito nomeado como componente (REM-05; fecha o 7.726 refutado — esperados ≈10.331 amplo / 6.049 estreito, corrigidos se a medição divergir)"
    - "A tolerância de cobertura do plano 03 morreu: universo do inventário e índice do agregado coincidem sem exceção nomeada"
    - statement: "A estimativa de init re-derivada é defensável: cada um dos 9 shapes tem composição e contagem justificadas shape a shape, conferíveis contra src/init.cts e os call sites citados"
      verification: backstop
  artifacts:
    - path: cairn/gsd/contracts/misc.json
      provides: "contrato do órfão run-with-timeout com provenance de bundle"
      contains: "run-with-timeout"
    - path: cairn/gsd/contracts/checagem.json
      provides: "contrato do órfão user-story.validate com provenance de bundle"
      contains: "user-story.validate"
    - path: cairn/gsd/contracts/dispatch-model.json
      provides: "contratos dos órfãos dispatch-isolation e dispatch-should-flatten com provenance de bundle"
      contains: "dispatch-isolation"
    - path: cairn/gsd/contracts/init.json
      provides: "os 9 bundle shapes com campos consumidos por --pick e estimativa python por shape"
      contains: "bundle_shapes"
    - path: cairn/gsd/contracts/contracts.json
      provides: "measurements datados: init_budget (estimativa re-derivada com método) e installer_cut (número com filtro e comando)"
      contains: "installer_cut"
  key_links:
    - from: cairn/gsd/contracts/misc.json
      to: ".cairn/cache/gsd-core-v1.10.0/gsd-core/bin/gsd-tools.cjs"
      via: "source_ref.path/lines das entradas órfãs apontando o bundle bakeado"
      pattern: "gsd-tools.cjs"
    - from: cairn/gsd/contracts/contracts.json
      to: cairn/gsd/contracts/init.json
      via: "measurements.init_budget derivado dos bundle_shapes registrados na família init"
      pattern: "init_budget"
    - from: tests/gsd-contracts.bats
      to: cairn/gsd/contracts/contracts.json
      via: "asserções jq sobre provenance dos 4 órfãos e sobre as chaves de measurements"
      pattern: "provenance"
  prohibitions:
    - "Nenhuma redução de escopo: os 4 órfãos entram no schema pleno (input/output/exit_codes extraídos do bundle), nunca como stub ou entrada vazia com provenance"
    - "A medição nunca lê ~/.claude nem runtime instalado: a semântica dos órfãos sai do gsd-tools.cjs DO CLONE, não do binário instalado na máquina"
    - "cairn/scripts/cairn-doctor.py não é modificado por nenhum plano desta fase (D-03)"
  assumptions:
    - "edge-probe (specless fallback): REM-04 e REM-05 saíram unclassified/unresolved do engine determinístico; as arestas foram autoradas manualmente nas truths/prohibitions acima e ficam flagged para o verificador — nunca dropadas"
---

<objective>
O trabalho sujo que o research nomeou e nenhuma soma cobria: extrair do
gsd-tools.cjs bakeado do clone a semântica dos 4 verbos sem fonte em src e
registrá-la no mesmo schema com proveniência (D-04, REM-04); re-derivar a
estimativa de init pelos 9 shapes de bundle que os workflows leem por --pick,
substituindo o ~500 mal fundamentado; e dar ao instalador descartável um número
com filtro declarado, fechando os dois números da abertura que não reproduziram
(REM-05).

Purpose: a fase 35 implementa os órfãos com a semântica registrada aqui
(CHECK-03), e a fase 34 orça init pelo número re-derivado (CORE-05). Sem este
plano, o orçamento do milestone segue apoiado em números refutados.

Output: entradas órfãs em misc/checagem/dispatch-model, `init.json` com os 9
shapes, `contracts.json` com measurements datados, e a suíte fechando a
cobertura sem tolerância.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-a-baseline-remedida-contra-a-tag/31-CONTEXT.md
@.planning/research/v1.6-transplante-gsd.md
@cairn/gsd/contracts/contracts.json
@tests/gsd-contracts.bats
</context>

## Artifacts this phase produces (parcela deste plano)

- **Entradas órfãs** (schema pleno + campos extras): `provenance` = "extraído do gsd-tools.cjs bakeado" e `source_ref` = {path: "gsd-core/bin/gsd-tools.cjs", lines} em: `run-with-timeout` (misc.json), `user-story.validate` (checagem.json), `dispatch-isolation` e `dispatch-should-flatten` (dispatch-model.json).
- **Campo** `bundle_shapes` em `init.json`: por shape — verbo init.* de origem, campos consumidos por --pick (com os call sites), composição em src/init.cts (source_ref), estimativa python.
- **Chaves de measurements** em `contracts.json`: `measurements.init_budget` {total_estimated_lines, method, per_shape, date} e `measurements.installer_cut` {official_number, filter_patterns[], command, narrow_subset{number, patterns, command}, date}.
- **Testes novos** em `tests/gsd-contracts.bats`: proveniência dos 4 órfãos (dois sentidos), chaves de measurements, cobertura sem tolerância.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Os 4 órfãos extraídos do bundle bakeado, com proveniência</name>
  <precondition>o cache do clone existe (`git -C .cairn/cache/gsd-core-v1.10.0 rev-parse HEAD` sai 0) ou há rede para `cairn/scripts/cairn-inventory.sh` recriá-lo</precondition>
  <files>cairn/gsd/contracts/misc.json, cairn/gsd/contracts/checagem.json, cairn/gsd/contracts/dispatch-model.json, cairn/gsd/contracts/contracts.json, tests/gsd-contracts.bats</files>
  <read_first>
    - .planning/phases/31-a-baseline-remedida-contra-a-tag/31-CONTEXT.md §decisions D-04 (o schema com proveniência, sem caso especial no harness)
    - .planning/research/v1.6-transplante-gsd.md §4 (os 4 verbos sem fonte em src; `grep -c run-with-timeout gsd-core/bin/gsd-tools.cjs` = 10 no clone) e §6 risco 5
    - No clone: gsd-core/bin/gsd-tools.cjs (4.028 linhas) — localizar por grep as regiões de run-with-timeout, user-story.validate, dispatch-isolation e dispatch-should-flatten e ler SÓ essas regiões
    - Saída de `cairn/scripts/cairn-inventory.sh --json` (os call sites reais dos 4 — run-with-timeout tem 4 sítios nos workflows)
    - tests/gsd-contracts.bats (a suíte que os testes novos estendem)
  </read_first>
  <behavior>
    - Cada um dos 4 órfãos tem entrada no arquivo da sua família com todos os campos obrigatórios do schema MAIS provenance e source_ref.path == "gsd-core/bin/gsd-tools.cjs" com lines não vazio
    - Sentido inverso 1: nenhuma entrada com provenance tem source_ref.path começando com src/
    - Sentido inverso 2: nenhuma entrada sem provenance tem source_ref.path apontando gsd-tools.cjs
    - O índice do agregado ganha os 4 verbos, cada um na família certa
  </behavior>
  <action>
    RED: escrever primeiro os testes bats do behavior — asserções jq nomeando os
    4 verbos, a igualdade do path do bundle e os dois sentidos inversos varrendo
    todos os arquivos de família em glob.

    GREEN: no clone, localizar cada verbo no gsd-tools.cjs por grep e extrair a
    semântica lendo a região do handler: o que o verbo recebe (argv/flags — para
    run-with-timeout, o comando e o timeout que ele envolve), o que devolve
    (stdout/JSON/exit codes — para os dispatch-*, o payload de decisão que o
    workflow consome), e qualquer efeito colateral visível no código. Registrar
    a entrada completa em misc.json (run-with-timeout), checagem.json
    (user-story.validate) e dispatch-model.json (dispatch-isolation,
    dispatch-should-flatten), cada uma com provenance exatamente
    "extraído do gsd-tools.cjs bakeado" e source_ref {path: "gsd-core/bin/gsd-tools.cjs",
    lines: "Lx-Ly"} — a referência de linha que D-04 exige. Os call sites do
    inventário entram em call_sites e como evidência do output em notes quando o
    workflow inspeciona o payload. Atualizar o índice verbs do agregado.
  </action>
  <verify>
    <automated>bats tests/gsd-contracts.bats</automated>
  </verify>
  <acceptance_criteria>
    - `bats tests/gsd-contracts.bats` sai 0
    - `jq -e '.verbs[] | select(.verb == "run-with-timeout") | (.provenance | length > 0) and (.source_ref.path == "gsd-core/bin/gsd-tools.cjs")' cairn/gsd/contracts/misc.json` sai 0; equivalentes para user-story.validate (checagem.json), dispatch-isolation e dispatch-should-flatten (dispatch-model.json)
    - `jq -e '[.verbs[] | select(has("provenance") and ((.source_ref.path // "") | startswith("src/")))] | length == 0'` sai 0 em todos os arquivos de família (glob)
    - `jq -e '[.verbs[] | select((has("provenance") | not) and (.source_ref.path == "gsd-core/bin/gsd-tools.cjs"))] | length == 0'` sai 0 em todos os arquivos de família (glob)
    - As linhas citadas existem: para cada órfão, `sed -n '<primeira linha do range>p' .cairn/cache/gsd-core-v1.10.0/gsd-core/bin/gsd-tools.cjs` contém o nome do verbo ou o identificador do handler
  </acceptance_criteria>
  <done>Os 4 órfãos têm dono: semântica registrada no schema pleno, proveniência declarada, linhas citadas conferíveis — o harness da fase 33 não precisa de caso especial (D-04).</done>
</task>

<task type="auto">
  <name>Task 2: Init re-derivado pelos 9 bundle shapes e o número do instalador com filtro declarado</name>
  <files>cairn/gsd/contracts/init.json, cairn/gsd/contracts/contracts.json, tests/gsd-contracts.bats</files>
  <read_first>
    - .planning/research/v1.6-transplante-gsd.md §4 (init: 9 bundles, 13 sítios; a refutação do "boa parte é host-install") e §6 riscos 3 e 6 (instalador 6.049-10.331; init ~500 mal fundamentado)
    - Saída de `cairn/scripts/cairn-inventory.sh --json` filtrada para os sites de verbos init.* (os 13 sítios e seus arquivos/linhas nos 8)
    - No clone: as linhas dos call sites init.* nos workflows (para capturar os campos --pick literais); src/init.cts (3.782 linhas — ler a composição por bundle, dirigido pelos verbos, não o arquivo inteiro)
    - cairn/gsd/contracts/init.json (as entradas de verbo que o plano 03 escreveu)
  </read_first>
  <action>
    Parte 1 — init (REM-05, risco 6): a partir do inventário, enumerar os call
    sites de init.* nos 8 e abrir cada um no clone para capturar os campos
    --pick literais que o workflow consome. Consolidar em init.json o campo
    bundle_shapes: um objeto por shape (os 9 que os workflows leem) com o verbo
    de origem, os campos consumidos (e o call site arquivo:linha de cada um), a
    composição correspondente em src/init.cts (source_ref com linhas) e a
    estimativa de linhas python para servir AQUELES campos sobre o bd. A
    estimativa total = soma por shape, gravada em contracts.json em
    measurements.init_budget {total_estimated_lines, method (uma frase por
    passo: pick→shape→composição→estimativa), per_shape, date} — o número que
    substitui o ~500 e que a fase 34 orça (CORE-05).

    Parte 2 — instalador (REM-05, risco 3): computar no clone os dois filtros
    que o research nomeou — o estreito (src/install*.cts + installer-migrations)
    e o amplo (o estreito + agent-install-check + runtime-artifact-*) — cada um
    com comando find/wc exato executado e registrado. Adotar o AMPLO como número
    oficial do corte (o corte descarta tudo que é instalador; o estreito fica
    nomeado como componente), gravando em measurements.installer_cut
    {official_number, filter_patterns, command, narrow_subset{number, patterns,
    command}, date}. Esperados ≈10.331 e 6.049; se a medição divergir, o número
    MEDIDO vale e o comando ao lado prova (a mesma cláusula do plano 01).

    Fechamento: remover do bats a tolerância aos 4 órfãos no teste de cobertura
    (plano 03) — universo e índice agora coincidem sem exceção — e acrescentar
    asserções jq das chaves de measurements.
  </action>
  <verify>
    <automated>bats tests/gsd-contracts.bats</automated>
  </verify>
  <acceptance_criteria>
    - `bats tests/gsd-contracts.bats` sai 0 com o cache real presente, incluindo o teste de cobertura SEM a tolerância aos órfãos
    - `jq -e '.bundle_shapes | length == 9' cairn/gsd/contracts/init.json` sai 0 (ou o número medido de shapes, corrigido com o comando de enumeração citado em notes se os call sites reais divergirem de 9)
    - `jq -e '.measurements.init_budget | (.total_estimated_lines > 0) and (.method | length > 0) and (.per_shape | length > 0)' cairn/gsd/contracts/contracts.json` sai 0
    - `jq -e '.measurements.installer_cut | (.official_number > 0) and (.filter_patterns | length > 0) and (.command | length > 0) and (.narrow_subset.number > 0)' cairn/gsd/contracts/contracts.json` sai 0
    - O comando gravado em measurements.installer_cut.command, re-executado no clone, devolve official_number
  </acceptance_criteria>
  <done>REM-05 fechado: init tem estimativa re-derivada shape a shape com método escrito, o instalador tem um número oficial com filtro e comando reproduzível, e a cobertura da fase não tem mais tolerância nenhuma.</done>
</task>

</tasks>

<verification>
- `bats tests/gsd-contracts.bats` — proveniência dos órfãos (dois sentidos), measurements e cobertura sem tolerância, tudo verde com o cache real.
- `bats tests/cairn-inventory.bats` — regressão do inventário.
- Re-execução manual do comando de installer_cut no clone confirma o número gravado.
</verification>

<success_criteria>
- REM-04: os 4 verbos sem fonte em src têm semântica extraída do gsd-tools.cjs bakeado e registrada no mesmo schema, com proveniência e referência de linha (D-04).
- REM-05: a estimativa de init está re-derivada pelos 9 shapes de bundle lidos por --pick, e o instalador descartável tem número com filtro declarado e comando reproduzível.
- A fase fecha com a tabela versionada cobrindo 100% do universo medido, sem exceções.
</success_criteria>

<output>
Criar `.planning/phases/31-a-baseline-remedida-contra-a-tag/31-04-SUMMARY.md` ao concluir, registrando: as linhas do bundle citadas por órfão, a estimativa de init re-derivada (e o delta contra o ~500 refutado), e o número oficial do instalador com o filtro (e o delta contra 7.726/6.049/10.331).
</output>
