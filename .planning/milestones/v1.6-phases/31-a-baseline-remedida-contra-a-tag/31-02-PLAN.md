---
phase: 31-a-baseline-remedida-contra-a-tag
plan: "02"
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - cairn/gsd/contracts/contracts.json
  - cairn/gsd/contracts/config.json
  - cairn/gsd/contracts/commit.json
  - cairn/gsd/contracts/skills.json
  - cairn/gsd/contracts/loop-hooks.json
  - cairn/gsd/contracts/dispatch-model.json
  - tests/gsd-contracts.bats
autonomous: true
requirements: [REM-03]
beads: [CairnGo-b87b]
must_haves:
  truths:
    - "cairn/gsd/contracts/ existe com o agregado contracts.json e os arquivos das cinco famílias triviais (config, commit, skills, loop-hooks, dispatch-model), todos sob o MESMO schema (D-02)"
    - "Cada entrada de verbo carrega verb, invocation, input (argv/flags), output (kind + exit_codes) e source_ref com path e linhas apontando a implementação na tag v1.10.0 — nenhuma entrada sem source_ref (REM-03: extraído da implementação, nunca de memória)"
    - "contracts.json indexa verbo → família/arquivo e família → arquivo, bidirecionalmente consistente com os arquivos de família por teste"
    - "tests/gsd-contracts.bats valida o schema de todo arquivo em cairn/gsd/contracts/ com jq (D-02: o bats valida o schema) e passa"
    - "Todo verbo do universo do inventário (cairn-inventory.sh --json) pertencente às cinco famílias deste plano tem entrada de contrato"
    - statement: "Amostra de contratos confere com a implementação da tag: para cada família, ao menos um verbo re-conferido abrindo o source_ref citado no clone e comparando input/output registrados"
      verification: backstop
  artifacts:
    - path: cairn/gsd/contracts/contracts.json
      provides: "agregado: identidade da fonte (repo/tag/commit), mapa famílias→arquivo, índice verbos→família, regra de atribuição registrada em prosa no próprio dado"
      contains: "open-gsd/gsd-core"
    - path: cairn/gsd/contracts/config.json
      provides: "contratos da família mais chamada (config-get/config-set e demais config.* do universo)"
      contains: "config-get"
    - path: cairn/gsd/contracts/commit.json
      provides: "contratos da família commit (cmdCommit, commands.cts)"
    - path: cairn/gsd/contracts/skills.json
      provides: "contratos da família skills/agent-skills (resolution.cts)"
    - path: cairn/gsd/contracts/loop-hooks.json
      provides: "contratos da família loop (loop-resolver.cts), incluindo loop render-hooks"
      contains: "render-hooks"
    - path: cairn/gsd/contracts/dispatch-model.json
      provides: "contratos da família dispatch/model em host único"
    - path: tests/gsd-contracts.bats
      provides: "validação de schema por jq, consistência agregado↔famílias"
  key_links:
    - from: cairn/gsd/contracts/contracts.json
      to: cairn/gsd/contracts/config.json
      via: "mapa families e índice verbs do agregado apontando arquivo por família"
      pattern: "config.json"
    - from: tests/gsd-contracts.bats
      to: cairn/gsd/contracts/contracts.json
      via: "jq sobre todos os arquivos de cairn/gsd/contracts/"
      pattern: "gsd/contracts"
    - from: cairn/gsd/contracts/config.json
      to: ".cairn/cache/gsd-core-v1.10.0/src"
      via: "source_ref.path/lines de cada verbo citando a implementação na tag"
      pattern: "source_ref"
  prohibitions:
    - "Nenhuma entrada de contrato escrita de memória ou por plausibilidade: toda entrada nasce da leitura da implementação no clone da tag e do uso real nos call sites"
    - "A medição nunca lê ~/.claude nem runtime instalado: source com source, sempre o clone cacheado"
    - "cairn/scripts/cairn-doctor.py não é modificado por nenhum plano desta fase (D-03)"
  assumptions:
    - "edge-probe (specless fallback): REM-03 saiu unclassified/unresolved do engine determinístico; as arestas foram autoradas manualmente nas truths/prohibitions acima e ficam flagged para o verificador — nunca dropadas"
---

<objective>
Fazer nascer `cairn/gsd/contracts/` — a tabela versionada do REM-03 — na forma
que D-02 travou: JSON, um arquivo por família mais um agregado, schema único
servido por um só leitor. Este plano fixa o schema (a interface que as fases
33-35 e o harness diferencial consomem — D-02 é reversibility costly), cria o
agregado e extrai os contratos das cinco famílias triviais: config, commit,
skills, loop-hooks e dispatch/model.

Purpose: a fase 33 implementa exatamente estas cinco famílias em python; sem
contrato de entrada/saída extraído da tag, o harness diferencial não tem contra
o que comparar.

Output: `cairn/gsd/contracts/{contracts,config,commit,skills,loop-hooks,dispatch-model}.json`
+ `tests/gsd-contracts.bats`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-a-baseline-remedida-contra-a-tag/31-CONTEXT.md
@.planning/phases/31-a-baseline-remedida-contra-a-tag/31-PATTERNS.md
@.planning/research/v1.6-transplante-gsd.md
@cairn/capability/capability.json
@tests/capability.bats
</context>

## Artifacts this phase produces (parcela deste plano)

- **Diretório** `cairn/gsd/contracts/` (primeiro conteúdo de `cairn/gsd/`, o destino do vendoring da fase 32 — research §5).
- **Schema de contrato** (interface consumida pelas fases 33-35 e pelo harness; campos obrigatórios por arquivo de família): `schema_version` (1), `family`, `source{repo,tag,commit}`, `verbs[]` com — por verbo — `verb`, `spellings[]`, `invocation`, `input{argv[],flags[],stdin}`, `output{kind,shape,exit_codes{}}`, `source_ref{path,lines}`, `call_sites{workflows8,agents}`, `notes`. Campo adicional `provenance` reservado aos órfãos do plano 04 (D-04).
- **Agregado** `cairn/gsd/contracts/contracts.json`: `schema_version`, `source{repo,tag,commit}`, `families{nome→arquivo}` (as 11 famílias do research §4: estado, config, commit, skills, init, loop-hooks, worktree, checagem, roadmap-phase, dispatch-model, misc), `verbs{verbo→{family,file}}`, `universe{derived_from}`, `assignment_rule` (a regra de atribuição por prefixo, em prosa no próprio dado).
- **Arquivos de família deste plano**: `config.json`, `commit.json`, `skills.json`, `loop-hooks.json`, `dispatch-model.json`.
- **Suíte** `tests/gsd-contracts.bats`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: O schema, o agregado e a suíte que os valida</name>
  <reversibility rating="costly">D-02 — o caminho cairn/gsd/contracts/ e o schema viram insumo do binário python e do harness das fases 33-35; mudar depois toca todos os consumidores.</reversibility>
  <precondition>o cache do clone existe (`git -C .cairn/cache/gsd-core-v1.10.0 rev-parse HEAD` sai 0) ou há rede para `cairn/scripts/cairn-inventory.sh` recriá-lo</precondition>
  <files>cairn/gsd/contracts/contracts.json, tests/gsd-contracts.bats</files>
  <read_first>
    - .planning/phases/31-a-baseline-remedida-contra-a-tag/31-PATTERNS.md §"cairn/gsd/contracts/*.json" e §"Schema dos contratos validado com jq"
    - cairn/capability/capability.json (o precedente: identidade e versão no topo, decisões registradas em prosa NO dado — as NOTEs)
    - tests/capability.bats linhas 55-95 (o molde jq de validação de schema)
    - .planning/research/v1.6-transplante-gsd.md §4 (a tabela de famílias) e §2.3 (grafia dupla)
    - .planning/phases/31-a-baseline-remedida-contra-a-tag/31-CONTEXT.md §decisions (D-02, D-04 e a discrição de layout)
  </read_first>
  <behavior>
    - Todo arquivo de cairn/gsd/contracts/ é JSON válido (jq empty) terminado em newline
    - Todo arquivo de família tem schema_version 1, family, source com repo/tag/commit e verbs array não vazio
    - Toda entrada de verbo tem verb, invocation, input.argv, input.flags, output.exit_codes e source_ref.path + source_ref.lines não vazios
    - contracts.json lista as 11 famílias em families; todo arquivo listado que já existir em disco valida; todo verbo do índice verbs aponta família cujo arquivo existe e o contém — e todo verbo presente num arquivo de família consta do índice (bidirecional; famílias ainda não escritas pelos planos 03-04 não quebram o teste: a consistência é checada sobre os arquivos presentes)
    - contracts.json carrega assignment_rule e universe.derived_from não vazios
  </behavior>
  <action>
    RED: escrever `tests/gsd-contracts.bats` primeiro, no molde do
    capability.bats — cabeçalho com as regras da casa, sem fixtures de rede: a
    suíte valida os JSONs commitados. Os testes iteram `cairn/gsd/contracts/*.json`
    em glob (para valerem também para os arquivos que os planos 03-04 criarem)
    e asseguram os comportamentos do behavior acima via `jq -e`.

    GREEN: criar `cairn/gsd/contracts/contracts.json` com: `schema_version: 1`;
    `source` = {repo: "open-gsd/gsd-core", tag: "v1.10.0", commit: o sha lido de
    `git -C .cairn/cache/gsd-core-v1.10.0 rev-parse HEAD`}; `families` mapeando
    as 11 famílias do research §4 para seus arquivos (estado.json, config.json,
    commit.json, skills.json, init.json, loop-hooks.json, worktree.json,
    checagem.json, roadmap-phase.json, dispatch-model.json, misc.json — os das
    famílias dos planos 03-04 ficam declarados no mapa desde já, é o contrato de
    layout); `verbs` começando com os verbos das cinco famílias deste plano
    (preenchido pela Task 2); `universe.derived_from` = a linha de comando do
    inventário que produz o universo (`cairn/scripts/cairn-inventory.sh --json`)
    com a data; `assignment_rule` em prosa registrando a regra de atribuição por
    prefixo (config-* → config; commit → commit; skills/agent-skills → skills;
    loop → loop-hooks; dispatch-*/model* → dispatch-model; state.* → estado;
    roadmap.*/phase.* → roadmap-phase; worktree.* → worktree;
    verify.*/uat.*/verification.*/check → checagem; init.* → init; resto →
    misc; ambiguidade resolvida caso a caso e anotada em notes — o precedente de
    gravar o porquê junto do quê é a NOTE do capability.json). Serialização com
    indent 2, chaves ordenadas e newline final, como o cairn-review.py grava
    JSON (diffs estáveis).
  </action>
  <verify>
    <automated>bats tests/gsd-contracts.bats</automated>
  </verify>
  <acceptance_criteria>
    - `bats tests/gsd-contracts.bats` sai 0
    - `jq -e '.families | length == 11' cairn/gsd/contracts/contracts.json` sai 0
    - `jq -r '.source.commit' cairn/gsd/contracts/contracts.json` == `git -C .cairn/cache/gsd-core-v1.10.0 rev-parse HEAD`
    - `jq -e '.assignment_rule | length > 0' cairn/gsd/contracts/contracts.json` sai 0
  </acceptance_criteria>
  <done>O schema está fixado por teste executável, o agregado existe com identidade da fonte e regra de atribuição escrita, e qualquer arquivo futuro em contracts/ é validado pelo mesmo bats.</done>
</task>

<task type="auto">
  <name>Task 2: As cinco famílias triviais extraídas da tag</name>
  <files>cairn/gsd/contracts/config.json, cairn/gsd/contracts/commit.json, cairn/gsd/contracts/skills.json, cairn/gsd/contracts/loop-hooks.json, cairn/gsd/contracts/dispatch-model.json, cairn/gsd/contracts/contracts.json</files>
  <read_first>
    - Saída de `cairn/scripts/cairn-inventory.sh --json` (o universo de verbos e os call sites por verbo — é o filtro do que precisa de contrato)
    - No clone (.cairn/cache/gsd-core-v1.10.0): a região de roteamento de gsd-core/bin/gsd-tools.cjs (localizar por grep o dispatch dos verbos destas famílias); src/*.cts alvo — o módulo de config (~2.590 linhas, ler só os handlers), commands.cts linhas 793-1049 (cmdCommit, medição do research), resolution.cts (64 linhas, inteiro), loop-resolver.cts (635 linhas, handlers), o módulo de dispatch/model (~2.154 linhas, handlers)
    - .planning/research/v1.6-transplante-gsd.md §4 (linhas upstream por família — o guia de onde está cada implementação)
  </read_first>
  <action>
    Para cada verbo do universo do inventário atribuído às cinco famílias deste
    plano (config, commit, skills, loop-hooks, dispatch-model — pela
    assignment_rule do agregado): localizar o handler na tag (grep dirigido no
    router do gsd-tools.cjs e no src/*.cts da família; ler SÓ a região do
    handler, nunca o arquivo inteiro — o research §4 mapeia os arquivos) e
    registrar a entrada completa no arquivo da família: verb, spellings,
    invocation (a forma chamada nos workflows, ex.: `gsd_run query config-get <key>`),
    input (argv posicionais, flags com seus valores possíveis, stdin quando
    houver), output (kind: json | text | exit-only; shape com as chaves de topo
    do payload — os campos que os call sites extraem por --pick são evidência
    direta e entram no shape; exit_codes com significado), source_ref
    {path relativo à raiz do clone, lines "Lx-Ly"}, call_sites {workflows8, agents}
    com as contagens do inventário, e notes para o que precisar de porquê.
    config-get e config-set entram obrigatoriamente (são os 38 sítios da família
    mais chamada); loop-hooks registra `loop render-hooks` (17 sítios nos 8);
    dispatch-model registra o contrato em host único (o research: dispatch em
    host único devolve o role inalterado — a nota vai em notes com o source_ref
    da lógica multi-host que o justifica). Ao fechar cada família, atualizar o
    índice verbs do contracts.json com verbo → {family, file}.
  </action>
  <verify>
    <automated>bats tests/gsd-contracts.bats</automated>
  </verify>
  <acceptance_criteria>
    - `bats tests/gsd-contracts.bats` sai 0 (schema de todos os cinco arquivos novos válido)
    - `jq -e '.verbs[] | select(.verb == "config-get")' cairn/gsd/contracts/config.json` e o mesmo para config-set saem 0
    - `jq -e '[.verbs[] | select((.source_ref.path // "") == "" or (.source_ref.lines // "") == "")] | length == 0' cairn/gsd/contracts/config.json` sai 0, e o mesmo para os outros quatro arquivos de família (nenhuma entrada sem source_ref)
    - Para as cinco famílias: todo verbo do universo do inventário atribuído à família pela assignment_rule tem entrada — conferido com `cairn/scripts/cairn-inventory.sh --json | jq -r '.verbs | keys[]'` cruzado com `jq -r '.verbs | keys[]' cairn/gsd/contracts/contracts.json` (diferença restrita a verbos das famílias dos planos 03-04)
    - `jq -e '.verbs[] | select(.verb == "loop") | .notes // .invocation' cairn/gsd/contracts/loop-hooks.json` menciona render-hooks
  </acceptance_criteria>
  <done>As cinco famílias triviais têm contrato completo extraído da tag, com source_ref linha a linha, e o agregado as indexa.</done>
</task>

</tasks>

<verification>
- `bats tests/gsd-contracts.bats` — schema e consistência agregado↔famílias verdes.
- `bats tests/cairn-inventory.bats` — regressão: o inventário segue verde.
- Spot-check do backstop: um verbo por família re-conferido abrindo o source_ref citado no clone.
</verification>

<success_criteria>
- REM-03 (parcial, famílias triviais): cinco famílias com contrato de entrada e saída registrado, extraído da implementação da tag, em tabela versionada validada por teste.
- D-02 implementado: JSON em cairn/gsd/contracts/, um arquivo por família + agregado, schema único, bats validando.
</success_criteria>

<output>
Criar `.planning/phases/31-a-baseline-remedida-contra-a-tag/31-02-SUMMARY.md` ao concluir, registrando o schema fixado (campos e regra de atribuição) e quantos verbos cada família cobriu.
</output>
