---
phase: 31-a-baseline-remedida-contra-a-tag
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - cairn/scripts/cairn-inventory.py
  - cairn/scripts/cairn-inventory.sh
  - tests/cairn-inventory.bats
  - .gitignore
autonomous: true
requirements: [REM-01, REM-02]
beads: [CairnGo-vdfj, CairnGo-i3l1]
must_haves:
  truths:
    - "cairn/scripts/cairn-inventory.sh --json devolve, por sítio, arquivo (relativo à raiz do clone), linha, escopo e verbo normalizado, medido exclusivamente sobre o clone cacheado da tag v1.10.0 (REM-01)"
    - "A métrica adotada é uma só e está declarada literalmente no artefato: a regex larga vive como constante comentada no .py e como escalar metric.regex no --json; a regex de calibração existe apenas como reprodução documentada do método 147-vs-189 (REM-02)"
    - "Primeira execução clona com rede (aceito por D-01); com cache válido a execução é 100% offline, provado por teste que roda com --source apontando caminho inexistente e cache pré-construído"
    - "Cache cujo HEAD difere do commit pinado da tag morre com exit 6 nomeando os dois shas; o inventário nunca mede corpus não verificado"
    - "Toda ocorrência bruta de gsd_run é classificada em calls, shim_preambles ou other dentro da fatia do seu escopo (accounting.workflows8 e accounting.agents são objetos separados); other é enumerado sítio a sítio no JSON; a identidade total_raw = calls + shim_preambles + other é assertada por bats em CADA escopo, com controle negativo"
    - "Os números de aceitação do research (534 chamadas/116 verbos no corpus pela calibração; 189 sítios nos 8 pela métrica larga; 64 sítios/42 verbos nos agentes; 17 sítios de loop render-hooks; contabilidade 651 = 189 + 460 + 2, medida só na fatia workflows8) são reproduzidos contra o cache real ou corrigidos com o comando junto, e registrados no bloco datado MEASURED VERSUS ASSUMED"
    - "A lista de agentes do escopo é a constante AGENTS_SCOPE declarada no script, com o comando de derivação registrado no bloco datado; o número reportado deriva dela (o research balizou 12 ou 13 — o inventário fixa a lista)"
    - "Todo token numérico da saída humana existe como escalar no --json (GUARD numbers_not_in_json com controle negativo de forja)"
    - statement: "Os sítios enumerados nos other_sites de cada fatia do accounting no corpus real são de fato prosa (julgamento humano sobre cada sítio, nomeado no bloco datado)"
      verification: backstop
  artifacts:
    - path: cairn/scripts/cairn-inventory.py
      provides: "inventário executável do corpus GSD v1.10.0: clone cacheado com commit pinado, métrica larga declarada, classificação completa, modelo único com --json"
      contains: "MEASURED VERSUS ASSUMED"
    - path: cairn/scripts/cairn-inventory.sh
      provides: "wrapper exec python3 no padrão da casa, contrato reapresentado no cabeçalho sem contagens vivas"
      contains: "exec python3"
    - path: tests/cairn-inventory.bats
      provides: "suíte offline com fixtures sintéticos locais, GUARDs de proveniência e asserções skip-gated contra o cache real"
    - path: .gitignore
      provides: "cache do clone fora do controle de versão (D-01), comentário nomeando dono e chave de invalidação"
      contains: ".cairn/cache"
  key_links:
    - from: cairn/scripts/cairn-inventory.sh
      to: cairn/scripts/cairn-inventory.py
      via: "exec python3 no wrapper"
      pattern: "exec python3"
    - from: cairn/scripts/cairn-inventory.py
      to: ".cairn/cache/gsd-core-v1.10.0"
      via: "git clone --depth 1 --branch v1.10.0 via subprocess + validação de HEAD contra TAG_COMMIT"
      pattern: "rev-parse"
    - from: tests/cairn-inventory.bats
      to: cairn/scripts/cairn-inventory.sh
      via: "run via CAIRN_SCRIPTS_DIR de helpers.bash"
      pattern: "cairn-inventory"
  prohibitions:
    - "A medição nunca lê ~/.claude nem qualquer runtime instalado: o corpus é exclusivamente o clone cacheado da tag (source com source — regra permanente do Goal; risco 12 do research)"
    - "O corpus clonado nunca entra no controle de versão (D-01: o corpus nunca entra no repo)"
    - "cairn/scripts/cairn-doctor.py não é modificado por nenhum plano desta fase (D-03: o doctor fica intocado)"
  assumptions:
    - "edge-probe (specless fallback): REM-01 e REM-02 saíram unclassified/unresolved do engine determinístico; as arestas foram autoradas manualmente nas truths e prohibitions acima e ficam flagged para o verificador — nunca dropadas silenciosamente"
---

<objective>
Transformar a remedição do research em comando executável: `cairn-inventory`, o
inventário do corpus GSD na tag pinada v1.10.0, sítio a sítio (arquivo, linha,
escopo, verbo), sob UMA métrica declarada — a regex larga que cobre query e os
subcomandos loop/check/run-with-timeout/graphify. Implementa D-01 (clone
on-demand em cache com o commit da tag como chave de invalidação), D-03 (script
novo no padrão da casa; o doctor fica intocado) e fecha REM-01 e REM-02.

Purpose: nenhuma fase do milestone pode ser orçada sobre número que não
reproduz. Este comando é a fonte dos números que as fases 32-38 citam, e o
universo de verbos que ele emite é o insumo direto dos contratos (planos 02-04).

Output: `cairn/scripts/cairn-inventory.py` + `cairn-inventory.sh` +
`tests/cairn-inventory.bats` + entrada de cache no `.gitignore`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-a-baseline-remedida-contra-a-tag/31-CONTEXT.md
@.planning/phases/31-a-baseline-remedida-contra-a-tag/31-PATTERNS.md
@.planning/research/v1.6-transplante-gsd.md
@cairn/scripts/cairn-trend.py
@cairn/scripts/cairn-review.py
@cairn/scripts/cairn-trend.sh
@tests/cairn-trend.bats
</context>

## Artifacts this phase produces (parcela deste plano)

- **Script** `cairn/scripts/cairn-inventory.py` — CLI read-only, stdlib-only.
  - Flags: `--json`, `--cache-dir <dir>`, `--source <url|path>`, `--expect-commit <sha>`, `--refresh`.
  - Exit codes: `0` ok · `2` usage · `5` dependência indisponível (git ausente; clone falhou e não há cache) · `6` corpus inválido (HEAD do cache não confere com o commit esperado da tag).
  - Constantes públicas do módulo: `SOURCE_URL` (https://github.com/open-gsd/gsd-core), `TAG` (v1.10.0), `TAG_COMMIT` (sha peeled da tag, pinado na implementação), `BROAD_RE` (métrica adotada), `CALIBRATION_RE`, `WORKFLOWS_8`, `AGENTS_SCOPE`, `EXIT_OK/EXIT_USAGE/EXIT_NO_HELPER/EXIT_BAD_CORPUS`.
- **Wrapper** `cairn/scripts/cairn-inventory.sh` (executável).
- **Suíte** `tests/cairn-inventory.bats`.
- **Interface JSON** (consumida pelos planos 02-04 e pelo harness da fase 33): chaves de topo `source{repo,tag,commit,cache_dir,cache_state}`, `metric{name,regex,calibration_regex}`, `scopes{workflows8.files[],agents.files[],corpus.roots[]}`, `sites[]{file,line,scope,command,verb,subcommand,spelling,raw}`, `verbs{<verbo>:{count,scopes[],spellings[]}}`, `summary{workflows8{sites,verbs},agents{sites,verbs},corpus_calibration{calls,verbs},loop_render_hooks_sites}`, `accounting{workflows8{total_raw,calls,shim_preambles,other,other_sites[]},agents{total_raw,calls,shim_preambles,other,other_sites[]}}` (contabilidade por escopo: a identidade vale dentro de cada fatia).
- **Entrada `.gitignore`**: `.cairn/cache/`.

<tasks>

<task type="tracer">
  <name>Task 1: Fatia ponta a ponta — clone cacheado da tag, varredura larga dos 8, --json por sítio</name>
  <precondition>git instalado e, quando não houver cache local, rede alcançando github.com (aceito por D-01) — checável com `git ls-remote https://github.com/open-gsd/gsd-core refs/tags/v1.10.0`</precondition>
  <files>cairn/scripts/cairn-inventory.py, cairn/scripts/cairn-inventory.sh, tests/cairn-inventory.bats, .gitignore</files>
  <read_first>
    - .planning/phases/31-a-baseline-remedida-contra-a-tag/31-PATTERNS.md (inteiro — é o mapa de análogos deste plano)
    - cairn/scripts/cairn-trend.py (docstring, constantes de exit code, die(), build_model/main/render — o análogo primário)
    - cairn/scripts/cairn-review.py (fetch_one, write_cache/now_stamp, build_parser — o análogo de rede/cache)
    - cairn/scripts/cairn-trend.sh (a forma inteira do wrapper)
    - tests/cairn-trend.bats linhas 1-109 (cabeçalho de regras, fixtures locais, atalhos run+jq)
    - .planning/research/v1.6-transplante-gsd.md §2.3 (as duas regexes e os números) e §6 riscos 1 e 12
    - .gitignore (bloco .cairn/pr-cache.json como molde do comentário)
  </read_first>
  <action>
    Criar `cairn/scripts/cairn-inventory.py` no molde do cairn-trend.py: docstring
    com WHY/Usage/Exit codes, `die()` local com prefixo `[cairn-inventory]`,
    constantes de exit code comentadas (EXIT_OK=0, EXIT_USAGE=2, EXIT_NO_HELPER=5,
    EXIT_BAD_CORPUS=6), argparse no formato do cairn-review.py (o script cruza o
    limiar de 2 flags), stdlib-only. O docstring declara em voz alta que este é
    um script que fala com a rede na primeira execução (a convenção que o
    cairn-review.py estabeleceu) e que cache-hit roda 100% offline.

    Camada de corpus (D-01): resolver raiz via CLAUDE_PROJECT_DIR como o trend;
    cache default `.cairn/cache/gsd-core-v1.10.0` sob a raiz, overridável por
    `--cache-dir`. Fonte default `SOURCE_URL = https://github.com/open-gsd/gsd-core`,
    overridável por `--source <url|path>`. Se o cache não existe (ou `--refresh`):
    `git clone --depth 1 --branch v1.10.0 <source> <cache>` via subprocess.run
    dentro de try/except no padrão fetch_one do review — falha vira die(EXIT_NO_HELPER)
    nomeando o comando e o stderr truncado, nunca traceback. Validação sempre
    (hit ou clone fresco): `git -C <cache> rev-parse HEAD` comparado com o commit
    esperado — a constante `TAG_COMMIT`, que o executor pina na implementação com
    o sha PEELED obtido por `git ls-remote https://github.com/open-gsd/gsd-core refs/tags/v1.10.0^{}`
    (tag anotada: usar a linha `^{}`) — overridável por `--expect-commit <sha>`
    (é o seam que os fixtures usam). Divergência → die(EXIT_BAD_CORPUS) nomeando
    o sha esperado e o encontrado (a doutrina do review: cache sem o commit
    verificado é pior que cache nenhum).

    Camada de varredura (REM-01/REM-02): `BROAD_RE`, a métrica adotada, como
    constante compilada com comentário do porquê: `gsd_run (query )?[a-z][a-z-]*(\.[a-z-]+)?`.
    `WORKFLOWS_8` = discuss-phase, plan-phase, execute-phase, verify-work, quick,
    fast, autonomous, debug; o escopo workflows8 = para cada nome, o arquivo
    `gsd-core/workflows/<nome>.md` mais os arquivos sob `gsd-core/workflows/<nome>/`
    (steps/modes). Nesta fatia, varrer só o escopo workflows8: cada match vira um
    site {file (relativo à raiz do clone), line, scope: "workflows8", command
    (query ou o token top-level), verb (token capturado, sem o prefixo query),
    raw (o trecho casado)}. Leitura tolerante de arquivo no padrão read_text do
    trend.

    Modelo único: build_model() produz source/metric/scopes/sites/summary
    mínimos; main() imprime `--json` com json.dumps(indent=2, sort_keys=True,
    ensure_ascii=False) ou uma saída humana mínima prefixada `[cairn-inventory]`
    (o render completo é a Task 3; aqui basta uma linha com o total de sítios,
    vinda do modelo).

    Wrapper `cairn/scripts/cairn-inventory.sh`: copiar a forma inteira do
    cairn-trend.sh (comentário reapresentando usage + tabela de exit codes SEM
    contagens vivas, set -euo pipefail, HERE, exec python3), chmod +x.

    `.gitignore`: bloco novo no molde do bloco pr-cache — comentário nomeando o
    dono (cairn-inventory.py), a chave de invalidação (commit da tag v1.10.0) e
    o motivo de nunca commitar (corpus externo de ~880k linhas), seguido da
    entrada `.cairn/cache/`.

    `tests/cairn-inventory.bats`: cabeçalho com as regras de asserção da casa
    (status exato, grep -qF, zero contagens da árvore real digitadas à mão),
    `load 'helpers'`, fixtures LOCAIS ao arquivo: um builder que cria um repo
    git local com tag v1.10.0 contendo um mini-corpus sintético
    (gsd-core/workflows/fast.md com sítios gsd_run conhecidos) e devolve o sha
    do commit taggeado. Testes desta fatia: (1) clone a partir do repo local via
    `--source <path> --expect-commit <sha do fixture>` produz --json com os
    sítios esperados (file/line/verb assertados por jq); (2) cache-hit é
    offline: com o cache já populado, rodar com `--source /nonexistent` sai 0 e
    devolve o mesmo modelo; (3) `--expect-commit` divergente sai exatamente 6 e
    o stderr nomeia os dois shas; (4) flag desconhecida sai exatamente 2;
    (5) git clone que falha (source inexistente e sem cache) sai exatamente 5.
    Nenhum teste toca o GitHub.

    Depois do verde offline, UMA execução real de ponta a ponta:
    `cairn/scripts/cairn-inventory.sh --json` sem overrides (clona a tag real
    para .cairn/cache/, valida TAG_COMMIT, varre os 8) — é a prova do tracer de
    que a arquitetura fecha contra o corpus verdadeiro.
  </action>
  <verify>
    <automated>bats tests/cairn-inventory.bats</automated>
  </verify>
  <acceptance_criteria>
    - `bats tests/cairn-inventory.bats` sai 0 (fixtures 100% locais, sem rede)
    - `cairn/scripts/cairn-inventory.sh --json | jq -r '.source.commit'` devolve o sha pinado em TAG_COMMIT (execução real, cache criado)
    - `cairn/scripts/cairn-inventory.sh --json | jq '.sites | length'` devolve > 0 e `jq -r '.sites[0] | [.file, (.line|tostring), .verb] | join(":")'` devolve caminho relativo ao clone, linha e verbo
    - `git -C .cairn/cache/gsd-core-v1.10.0 rev-parse HEAD` == TAG_COMMIT
    - `git check-ignore .cairn/cache/gsd-core-v1.10.0` sai 0 (cache ignorado)
    - `test -x cairn/scripts/cairn-inventory.sh` sai 0
    - `git log --oneline <base>..HEAD -- cairn/scripts/cairn-doctor.py` devolve vazio, com `<base>` = o sha de HEAD registrado no início da execução do plano — nenhum commit deste plano toca o doctor (D-03; o arquivo já está modificado no working tree, então `git status --porcelain` não distingue modificação nova de pré-existente)
  </acceptance_criteria>
  <done>O caminho único clone→validação→varredura→JSON funciona ponta a ponta contra o corpus real e está coberto offline por fixtures; wrapper e .gitignore no padrão da casa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classificação completa — escopos, normalização, contabilidade de completude</name>
  <files>cairn/scripts/cairn-inventory.py, tests/cairn-inventory.bats</files>
  <read_first>
    - .planning/research/v1.6-transplante-gsd.md §2.3 inteiro (dupla grafia, loop render-hooks, contabilidade 651 = 189 + 460 + 2, delta 12-vs-13 agentes)
    - tests/cairn-trend.bats linhas 365-396 (teste de recomputação: a expectativa vem dos arquivos, nunca digitada)
    - cairn/scripts/cairn-inventory.py (o estado que a Task 1 deixou)
  </read_first>
  <behavior>
    - Escopo agents: sites dos arquivos de AGENTS_SCOPE ganham scope "agents"; summary.agents traz sites e verbos distintos
    - Calibração: CALIBRATION_RE (`gsd_run query [a-z][a-z0-9._-]+`) sobre os roots gsd-core/workflows e commands do clone produz summary.corpus_calibration {calls, verbs}; a calibração alimenta APENAS summary.corpus_calibration e NUNCA gera entradas em sites[] ou verbs{} — `.verbs | keys[]` é o universo de cobertura que os planos 02-04 consomem (interface costly)
    - Normalização: um site cujo verb capturado é "verification" seguido do token "status" vira verbo `verification.status` com spelling "query verification status"; um site `verification.status` literal vira o mesmo verbo com spelling própria — as duas grafias contam UM verbo
    - Subcomando: o token [a-z-]+ imediatamente após o match (ex.: render-hooks depois de loop) entra no site como subcommand; summary.loop_render_hooks_sites conta os sites com verb=loop e subcommand=render-hooks no escopo workflows8
    - Contabilidade POR ESCOPO: toda ocorrência bruta da substring gsd_run é classificada dentro da fatia do seu escopo — accounting.workflows8 e accounting.agents são objetos separados (a identidade do research foi medida só sobre os 8; um accounting misturado divergiria por construção) — linha que define o shim (contém `gsd_run() {`) classifica todas as suas ocorrências como shim_preambles; ocorrência que casa BROAD_RE é calls; o resto é other, enumerado sítio a sítio em accounting.<escopo>.other_sites com file/line/excerpt
    - Identidade: total_raw == calls + shim_preambles + other assertada em CADA fatia (accounting.workflows8 e accounting.agents), sempre
    - Controle negativo: um fixture com uma ocorrência estranha (ex.: `gsd_run @@@` em prosa) produz other == 1 na fatia do escopo onde vive, com o sítio enumerado — prova que o balde está vivo
  </behavior>
  <action>
    RED primeiro: escrever os testes bats dos comportamentos acima sobre
    fixtures sintéticos estendidos (o builder da Task 1 ganha um workflow com
    preâmbulo shim de verdade — uma linha `gsd_run() { node "$GSD" "$@"; }` com
    múltiplas ocorrências da substring —, um agente sob agents/, um sítio
    `query verification status`, um `verification.status`, um `loop render-hooks`
    e uma ocorrência de prosa). A identidade da contabilidade é RECOMPUTADA no
    teste, por escopo, a partir do próprio fixture (grep -c sobre os arquivos
    de cada escopo do fixture), nunca digitada — o molde é o teste de
    recomputação do trend.bats.

    GREEN: implementar em build_model() a classificação em três baldes
    (calls/shim_preambles/other) por escopo — accounting.workflows8 e
    accounting.agents como objetos separados, cada um com sua própria
    identidade —, a normalização da grafia dupla, o campo
    subcommand, o escopo agents (AGENTS_SCOPE como constante declarada — a
    lista o executor deriva UMA vez grepando os 8 workflows do clone por
    referências de dispatch a nomes de agente que existam sob agents/ no clone,
    e o comando usado fica registrado no bloco datado da Task 3; o research
    mediu 12 despachados contra 13 no fecho — a lista declarada decide e a
    divergência é anotada), o escopo corpus_calibration com CALIBRATION_RE
    sobre gsd-core/workflows + commands, e o objeto verbs {verbo → count,
    scopes, spellings}. Nenhum número de summary é computado fora do modelo.
  </action>
  <verify>
    <automated>bats tests/cairn-inventory.bats</automated>
  </verify>
  <acceptance_criteria>
    - `bats tests/cairn-inventory.bats` sai 0, incluindo o controle negativo de other e a identidade recomputada do fixture
    - No fixture com grafia dupla, `jq -r '.verbs["verification.status"].spellings | length'` == 2 e o verbo conta uma vez
    - No fixture com preâmbulo, `jq '.accounting.workflows8.shim_preambles'` > 1 (todas as ocorrências da linha do shim classificadas juntas) e `jq '[.accounting.workflows8, .accounting.agents] | all(.total_raw == .calls + .shim_preambles + .other)'` == true
    - `jq '[.accounting.workflows8, .accounting.agents] | all((.other_sites | length) == .other)'` == true em todo fixture
    - `jq -r '.metric.regex'` devolve a regex larga literal e `jq -r '.metric.calibration_regex'` a de calibração (a métrica única declarada no artefato, REM-02)
  </acceptance_criteria>
  <done>O inventário classifica tudo (nada escapa da regex sem balde), normaliza a grafia dupla, cobre os três escopos e expõe a contabilidade completa, por escopo, no modelo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Saída humana, GUARDs de proveniência e os números de aceitação contra o corpus real</name>
  <files>cairn/scripts/cairn-inventory.py, cairn/scripts/cairn-inventory.sh, tests/cairn-inventory.bats</files>
  <read_first>
    - tests/cairn-trend.bats linhas 727-860 (GUARD numbers_not_in_json com controle negativo; GUARD do bloco datado; erros de uso)
    - cairn/scripts/cairn-trend.py linhas 83-121 (o bloco MEASURED VERSUS ASSUMED) e 681-736 (renderer que não computa)
    - .planning/phases/31-a-baseline-remedida-contra-a-tag/31-CONTEXT.md §specifics (os números de aceitação e a asserção de completude)
  </read_first>
  <behavior>
    - Saída humana: render(model) só posiciona strings do modelo (nenhuma divisão, nenhuma contagem no renderer); linhas prefixadas [cairn-inventory]; resume summary por escopo, os verbos mais chamados e a contabilidade
    - GUARD 1: todo token numérico da saída humana existe como escalar no --json; controle negativo prova que a checagem rejeita um número forjado
    - GUARD 2: nenhuma contagem viva fora do bloco datado MEASURED VERSUS ASSUMED no .py, e nenhuma no .sh
    - Aceitação real (skip-gated): com o cache real presente, os números do research reproduzem — ou o valor corrigido está assertado com o comando de reprodução escrito no próprio teste
  </behavior>
  <action>
    RED: portar para o cairn-inventory.bats os dois GUARDs do trend.bats — o
    helper numbers_not_in_json é copiado LOCALMENTE (não mexer em helpers.bash),
    com o controle negativo de número forjado; o GUARD do bloco datado varre .py
    e .sh adaptando os substantivos (verbos, sítios, chamadas, workflows,
    agentes). Acrescentar o bloco de testes reais skip-gated: cada um começa com
    `[ -d "$CAIRN_REPO_ROOT/.cairn/cache/gsd-core-v1.10.0" ] || skip "cache do clone ausente — rode cairn-inventory.sh uma vez com rede"`
    e roda o script contra o cache real assertando por jq: summary.workflows8.sites,
    summary.agents.{sites,verbs}, summary.corpus_calibration.{calls,verbs},
    summary.loop_render_hooks_sites e a contabilidade POR ESCOPO — a identidade
    total_raw == calls + shim_preambles + other assertada em accounting.workflows8
    E em accounting.agents. Os valores esperados são os do research — 189; 64/42;
    534/116; 17; e 651 = 189 + 460 + 2 assertado SOBRE A FATIA
    accounting.workflows8, onde o research mediu a identidade (o other esperado
    2 vale só nessa fatia; na fatia agents asserta-se a identidade sem valor
    pré-fixado do research) — e o número de
    verbos distintos dos 8 fica na faixa que a normalização decide (o research
    balizou 60-61). Se a execução real divergir de algum valor, o teste é
    corrigido para o valor MEDIDO com comentário citando o comando — nunca
    ajustar o script para forçar o número do research (é a cláusula
    "reproduzi-los ou corrigi-los com o comando junto" do CONTEXT).

    GREEN: implementar render() completo no molde do trend (strings vêm todas
    do modelo); escrever o docstring definitivo com o bloco datado MEASURED
    VERSUS ASSUMED carregando os números MEDIDOS da execução real desta task
    (datados de hoje, com o comando ao lado de cada um, incluindo o comando de
    derivação de AGENTS_SCOPE e o veredito humano sobre os sítios de
    accounting.other_sites — a parte ASSUMED registra o que não foi medido);
    conferir que o .sh reapresenta usage e exit codes sem nenhuma contagem viva.
  </action>
  <verify>
    <automated>bats tests/cairn-inventory.bats</automated>
  </verify>
  <acceptance_criteria>
    - `bats tests/cairn-inventory.bats` sai 0 com o cache real presente (os skip-gated rodam de verdade nesta máquina) e os GUARDs passam com seus controles negativos
    - `cairn/scripts/cairn-inventory.sh --json | jq '.summary.workflows8.sites'` devolve o número medido assertado no teste (esperado 189, corrigido-com-comando se divergir)
    - `cairn/scripts/cairn-inventory.sh --json | jq '[.accounting.workflows8, .accounting.agents] | all(.total_raw == .calls + .shim_preambles + .other)'` == true contra o corpus real; a fatia workflows8 reproduz 651 = 189 + 460 + 2 (ou os valores medidos, corrigidos com comando junto), com `jq '.accounting.workflows8.other'` pequeno e enumerado (esperado 2 — só nesta fatia)
    - A saída humana sem --json imprime só números presentes no JSON (GUARD verde) e o bloco MEASURED VERSUS ASSUMED do .py contém data de 2026-08 e um comando ao lado de cada contagem
  </acceptance_criteria>
  <done>O inventário reproduz (ou corrige com comando junto) todos os números de aceitação contra a tag real, a saída humana é uma view mecânica do JSON, e os números vivem só no bloco datado.</done>
</task>

</tasks>

<verification>
- `bats tests/cairn-inventory.bats` — suíte inteira verde, offline por fixtures; skip-gated reais rodando quando o cache existe.
- `cairn/scripts/cairn-inventory.sh --json | jq '.summary, .accounting'` contra o cache real — números de aceitação reproduzidos ou corrigidos com comando registrado.
- `bats tests/` (regressão) — nenhuma suíte existente quebra; `git log --oneline <base>..HEAD -- cairn/scripts/cairn-doctor.py` devolve vazio (nenhum commit deste plano toca o doctor; `<base>` = HEAD registrado no início da execução).
</verification>

<success_criteria>
- REM-01: o inventário é um comando reproduzível sobre a tag v1.10.0 com arquivo, verbo, escopo e linha por sítio.
- REM-02: uma única métrica declarada no artefato (regex larga literal no JSON e no docstring); 147-vs-189 explicado pelo método registrado; a contabilidade de completude prova que nada escapa sem classificação.
- D-01 e D-03 implementados como especificados; as três proibições do frontmatter respeitadas.
</success_criteria>

<output>
Criar `.planning/phases/31-a-baseline-remedida-contra-a-tag/31-01-SUMMARY.md` ao concluir, registrando os números medidos e qualquer correção feita sobre os valores do research (com o comando junto).
</output>
