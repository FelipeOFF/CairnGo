---
phase: 35-o-binario-python-checagem-e-verbos-orfaos
plan: 04
type: execute
wave: 4
depends_on: ["35-03"]
files_modified:
  - cairn/scripts/cairn-gsd-check.py
  - tests/cairn-gsd.bats
  - tests/fixtures/gsd-goldens/scenarios.json
  - tests/fixtures/gsd-goldens/*.golden.json
  - tests/fixtures/gsd-goldens/divergences.json
autonomous: true
requirements: [CHECK-01]
beads:
  CHECK-01: CairnGo-ma8r
must_haves:
  truths:
    - "`query user-story.validate --story` valida a string pura contra o formato canônico 'As a [role], I want to [capability], so that [outcome].' com guards por cláusula produzindo erros acionáveis um a um; história inválida NÃO é erro — exit 0 com valid false e errors; zero acesso a .planning/ (CHECK-01)"
    - "`query uat.render-checkpoint --file` renderiza o bloco do teste corrente da seção ## Current Test no response_language do config; sessão já completa ([testing complete]) é ERRO exit 1 (CHECK-01)"
    - "`query uat.classify-coverage --summary` classifica o bloco coverage em auto_passed (human_judgment false + toda verification pass) vs present (tudo o mais vai a humano — fail-safe); --file é alias aceito; SUMMARY ausente/path inseguro/flag faltando = exit 1 (CHECK-01)"
    - "os cenários separam veredito-no-payload (valid false com exit 0) de erro-de-uso (exit 1) em cada um dos 3 verbos"
  artifacts:
    - cairn/scripts/cairn-gsd-check.py
    - tests/fixtures/gsd-goldens/scenarios.json
  key_links:
    - "uat.render-checkpoint/classify-coverage → parser de seção na forma leniente da casa (molde verification_status, cairn-status.py:1066-1085: regex + strip, sem lib) — nenhum parser de markdown genérico novo"
    - "user-story.validate → validação de string pura: guards por cláusula ANTES da regex cheia — a ordem é a semântica (erros um a um, acionáveis)"
    - "response_language → .planning/config.json (documento) — pinado por planning_config no fixture para golden determinístico"
  prohibitions:
    - statement: "Nenhuma task escreve sob cairn/gsd/**; state/init/doctor intocados"
    - statement: "História inválida nunca sai por exit != 0 — o veredito vive em valid/errors (doutrina da família)"
---

<objective>
Os três verbos de conteúdo da família checagem: user-story.validate (validação de string
pura, o único sem filesystem), uat.render-checkpoint (parser da seção ## Current Test —
sem análogo na casa) e uat.classify-coverage (classificador fail-safe do bloco coverage).
Com eles, os 11 da família estão completos — falta só o plano dos órfãos para fechar 87.

Purpose: completar CHECK-01 — a superfície de checagem inteira que a fase 36 vai apontar
dos preâmbulos precisa existir antes do fecho.
Output: 11/11 verbos de checagem em --list-implemented do irmão, cenários cobrindo
veredito vs erro-de-uso, suíte verde.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-o-binario-python-checagem-e-verbos-orfaos/35-CONTEXT.md
@.planning/phases/35-o-binario-python-checagem-e-verbos-orfaos/35-PATTERNS.md
@cairn/gsd/contracts/checagem.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: user-story.validate — validação de string pura com erros acionáveis</name>
  <files>cairn/scripts/cairn-gsd-check.py, tests/cairn-gsd.bats, tests/fixtures/gsd-goldens/scenarios.json, tests/fixtures/gsd-goldens/user-story-*.golden.json</files>
  <read_first>
    - cairn/gsd/contracts/checagem.json, verbo user-story.validate (notes completas: USER_STORY_RE com grupos role/capability/outcome, guards por cláusula, 'Story is empty', exit 1 só para subcommand ≠ validate, zero .planning/)
    - .cairn/cache/gsd-core-v1.10.0/gsd-core/bin/gsd-tools.cjs L3179-L3230 (a fonte bakeada declarada no source_ref — regex e guards exatos, se o cache estiver presente; as notes do contrato já carregam a semântica extraída)
  </read_first>
  <behavior>
    - Cenário `user-story-valida`: --story canônica completa → exit 0, valid true, slots com role/capability/outcome
    - Cenário `user-story-sem-outcome`: falta a cláusula 'so that' → exit 0, valid false, errors nomeando A cláusula (guard, não regex cheia)
    - Cenário `user-story-vazia`: --story ausente/vazia → exit 0, valid false, errors com 'Story is empty'
    - Cenário `user-story-subcommand-errado`: subcommand ≠ validate → exit 1 (Unknown user-story subcommand)
  </behavior>
  <action>
    Implementar user-story.validate como validação de string pura (zero filesystem, zero
    .planning/): guards por cláusula na ordem da fonte (role, capability, outcome, ponto
    final), cada um produzindo seu erro acionável; a regex cheia com grupos
    role/capability/outcome SÓ roda com os guards limpos; slots preenchidos no sucesso.
    História inválida sai 0 com valid false — o veredito no envelope, nunca no exit;
    exit 1 é exclusivo do subcommand desconhecido. A grafia 'query user-story.validate' é
    splitada no primeiro ponto pelo mecanismo de spellings do dispatcher — nenhum código
    novo de rota. Shape: errors, slots, valid. Cenários e goldens derived-from-contract
    por forma; recorded viável (entrada é argv puro) se o cache estiver disponível.

    NO MESMO commit: RETIRAR os testes de representante-de-família da era 33/34 em
    tests/cairn-gsd.bats — o representante único (L502-511, repontado pelo 35-02 para
    uat.render-checkpoint) e o teste inteiro "familia nao-trivial: cada representante
    morre exit 4" (L666-692, cuja linha de user-story.validate deixa de valer com esta
    task; com 11/11 da checagem implementados não sobra verbo pra repontar — a mecânica
    representante-de-família cumpriu o papel). Ficam como guardas: o controle negativo do
    verbo forjado e o representante de órfão (L694+, válido até o 35-05, que o retira com
    a rota).
  </action>
  <verify>
    <automated>bats tests/cairn-gsd.bats && R="$PWD" && P="$R/cairn/scripts/cairn-gsd.sh" && ("$P" query user-story.validate --story "As a dev, I want to ship, so that users win." | jq -e '.valid == true') && ("$P" query user-story.validate --story "banana" | jq -e '.valid == false and (.errors | length > 0)') && test "$(wc -l < "$R/cairn/scripts/cairn-gsd-check.py")" -le 1500</automated>
  </verify>
  <done>Validação canônica com erros um a um, acionáveis; inválido nunca é erro de processo; 9/11 da família no ar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: uat.render-checkpoint e uat.classify-coverage — os leitores de UAT/SUMMARY</name>
  <files>cairn/scripts/cairn-gsd-check.py, tests/cairn-gsd.bats, tests/fixtures/gsd-goldens/scenarios.json, tests/fixtures/gsd-goldens/uat-*.golden.json, tests/fixtures/gsd-goldens/divergences.json</files>
  <read_first>
    - cairn/gsd/contracts/checagem.json, verbos uat.render-checkpoint e uat.classify-coverage (shapes, os dois exits de cada um, --file alias de --summary, [testing complete] = erro)
    - .cairn/cache/gsd-core-v1.10.0/gsd-core/src/uat.cts L235-L261 (parseCurrentTest — a forma do bloco renderizado) e src/coverage.cts L426+ e L470-L491 (classifyContent/ClassifyResult — as regras de classificação)
    - cairn/scripts/cairn-status.py L1066-1085 (o molde de parse leniente de seção — regex + strip, sem lib de markdown)
    - 35-PATTERNS.md, "No Analog Found" linha 3 (nenhum script da casa lê UAT.md — o molde substituto)
  </read_first>
  <behavior>
    - Cenário `uat-render-checkpoint`: fixture UAT.md com ## Current Test preenchida + planning_config com response_language pinado → exit 0, as 4 chaves (checkpoint, file_path, test_name, test_number)
    - Cenário `uat-render-checkpoint-sessao-completa`: seção com [testing complete] → exit 1 (não há checkpoint pendente — erro do contrato)
    - Cenário `uat-render-checkpoint-sem-file`: flag ausente → exit 1
    - Cenário `uat-classify-coverage-auto-passed`: SUMMARY com bloco coverage todo human_judgment false + verification pass → exit 0, all_auto_covered refletindo, auto_passed populado
    - Cenário `uat-classify-coverage-fail-safe`: bloco com item human_judgment true → exit 0, item em present (vai a humano)
    - Cenário `uat-classify-coverage-legacy`: SUMMARY sem bloco coverage → exit 0, mode distinguindo ausente/legacy
  </behavior>
  <action>
    uat.render-checkpoint: --file obrigatório; parse da seção ## Current Test na forma
    leniente da casa (molde verification_status: regex + strip, sem lib); renderizar o
    bloco de checkpoint do teste corrente no response_language do config (.planning/
    config.json — documento; fixture pina a língua para golden determinístico). Sessão já
    completa ([testing complete]) → erro exit 1 nomeado; --file ausente, arquivo
    inexistente, seção ausente/vazia → exit 1. Shape: checkpoint, file_path, test_name,
    test_number. A forma exata do bloco renderizado vem de parseCurrentTest da tag
    (cache); distância deliberada → divergences.json.

    uat.classify-coverage: --summary obrigatório (--file alias aceito); classificar o
    bloco coverage do SUMMARY em auto_passed (human_judgment false E toda verification
    pass) vs present (tudo o mais — fail-safe: na dúvida, vai a humano); mode distingue
    coverage presente/ausente/legacy; errors carrega violações de schema do bloco.
    SUMMARY ausente, path inseguro ou flag faltando → exit 1. Shape: all_auto_covered,
    auto_passed, errors, mode, present, summary_file, total.

    Cenários e goldens no seam de sempre; recorded viável para os dois (entrada é
    arquivo — exatamente o que o recorder reproduz), derived-from-contract onde gravar
    não compensar.
  </action>
  <verify>
    <automated>bats tests/cairn-gsd.bats && R="$PWD" && P="$R/cairn/scripts/cairn-gsd.sh" && ("$P" query uat.render-checkpoint >/dev/null 2>&1; test $? -eq 1) && ("$P" query uat.classify-coverage >/dev/null 2>&1; test $? -eq 1) && test "$("$P" --list-implemented | sort -u | wc -l | tr -d ' ')" -eq 82 && test -z "$(git status --porcelain cairn/gsd/)" && test "$(wc -l < "$R/cairn/scripts/cairn-gsd-check.py")" -le 1500</automated>
  </verify>
  <done>Os 11 verbos de checagem respondem — --list-implemented agregado soma 82 (71 da 34 + 11); só os 5 órfãos separam a superfície de 87; suíte verde.</done>
</task>

</tasks>

<verification>
- `bats tests/cairn-gsd.bats` verde offline — cenários novos + regressão zero
- `--list-implemented` agregado = 82 verbos (11/11 da checagem no irmão check)
- Doutrina exit-code coberta por cenário nos 3 verbos (veredito ≠ erro de uso)
- `git status --porcelain cairn/gsd/` vazio; teto 1500; pin CHECK-04 verde
</verification>

<success_criteria>
- CHECK-01 completo: a família checagem inteira responde em python, na forma dos contratos, com as duas grafias de verification.status provadas desde o plano 01
- Nenhum parser genérico de markdown — a leniência da casa, seção a seção
</success_criteria>

<output>
Criar `.planning/phases/35-o-binario-python-checagem-e-verbos-orfaos/35-04-SUMMARY.md`
ao concluir. Registrar: a forma do bloco renderizado do checkpoint (proveniência
parseCurrentTest), o tratamento de mode legacy do classify-coverage e a contagem
--list-implemented (82).
</output>
