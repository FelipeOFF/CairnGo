---
phase: 35-o-binario-python-checagem-e-verbos-orfaos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/cairn-gsd.bats
  - cairn/scripts/cairn-gsd.py
  - cairn/scripts/cairn-gsd-check.py
  - tests/cairn-command-surfaces.bats
  - tests/fixtures/gsd-goldens/scenarios.json
  - tests/fixtures/gsd-goldens/*.golden.json
  - tests/fixtures/gsd-goldens/divergences.json
autonomous: true
requirements: [CHECK-04, CHECK-01]
beads:
  CHECK-04: CairnGo-lpg6
  CHECK-01: CairnGo-ma8r
must_haves:
  truths:
    - "a baseline do cairn-doctor está fixada por teste que REPROVA se o arquivo mudar — blob e2040aea2068967eaec204e049fff0dbceb2ef50, 3907 linhas, último commit a2527ee — registrada como PRIMEIRA task da fase, antes de qualquer outra; esta fase NÃO evolui o doctor (CHECK-04)"
    - "o dispatcher roteia a família checagem por os.execv pro terceiro irmão cairn-gsd-check.py via FAMILY_SCRIPT — mesmo molde D-01 da 34, verbo canônico no argv, stdout/stderr/exit preservados (D-01)"
    - "`query verification.status <dir>` e `query verification status <dir>` caem no MESMO handler e devolvem envelope byte-igual — as duas grafias já contratadas nos spellings casam pelo n-grama do dispatcher SEM tabela nova de normalização (CHECK-01)"
    - "verification.status devolve as 4 chaves do contrato (next_action, next_command, status, verification_stale_check_indeterminate); phase-dir ausente → exit 1 nomeado"
    - "o irmão novo consome cairn_gsd_render.py como fonte única do envelope — nenhuma cópia local de emit/js_string/stringify (D-01, discrição do CONTEXT)"
    - "wc -l de cairn-gsd-check.py ≤ 1500 (teto D-01 do irmão)"
  artifacts:
    - cairn/scripts/cairn-gsd-check.py
    - cairn/scripts/cairn-gsd.py
    - tests/cairn-gsd.bats
    - tests/fixtures/gsd-goldens/scenarios.json
  key_links:
    - "dispatcher → FAMILY_SCRIPT['checagem'] → os.execv(sys.executable, [sys.executable, sibling, verbo_canônico] + rest) — o ponto de exec L2092-2098 NÃO muda; a entrada nova na tabela basta"
    - "cairn-gsd-check.py → cairn_gsd_render.py (import por sys.path.insert do dir do script) — envelope medido numa fonte só"
    - "spellings do contrato → build_routes → loop n-grama maior→menor: 'query verification status' casa em n=3, 'query verification.status' em n=2 — os dois resolvem pro verbo canônico verification.status"
    - "--list-implemented agregado → loop L2059 itera sorted(set(FAMILY_SCRIPT.values())) — o irmão novo entra na enumeração sem tocar o bats"
  prohibitions:
    - statement: "Nenhuma task toca cairn/scripts/cairn-doctor.py — o pin de CHECK-04 é a prova mecânica; evolução do doctor é a fase 37"
    - statement: "Nenhuma task escreve sob cairn/gsd/** — contratos somente-leitura; gate git status --porcelain cairn/gsd/ vazio"
    - statement: "cairn-gsd-state.py e cairn-gsd-init.py ficam intocados nesta fase salvo o roteamento do dispatcher (restrição dura do CONTEXT)"
    - statement: "Verbos de checagem devolvem veredito no PAYLOAD (passed/active/block/valid), nunca no exit — exit 0 = gate avaliado; exit 1 é reservado a erro de uso do contrato"
---

<objective>
Abertura da fase 35 em dois movimentos ordenados: (1) CHECK-04 primeiro — a baseline do
cairn-doctor fixada por teste ANTES de qualquer task que pudesse tocá-lo; (2) o tracer do
terceiro irmão — `cairn-gsd-check.py` nasce na forma da casa, o dispatcher ganha a rota da
família checagem, e UM verbo atravessa tudo de ponta a ponta: `verification.status`, o
verbo das DUAS grafias contratadas (CHECK-01), lendo artefato de fase e respondendo no
envelope da fonte única.

Purpose: provar a arquitetura do terceiro irmão (rota + exec + render + leitura de
documento + grafia dupla) num commit, antes de gastar contexto nos 15 verbos restantes; e
tornar mecânica a garantia de que esta fase não evolui o doctor.
Output: pin de baseline em bats, cairn-gsd-check.py com verification.status, rota checagem
no dispatcher, cenários das duas grafias com goldens, guard de cobertura em modo de
entrega incremental.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-o-binario-python-checagem-e-verbos-orfaos/35-CONTEXT.md
@.planning/phases/35-o-binario-python-checagem-e-verbos-orfaos/35-PATTERNS.md
@cairn/gsd/contracts/checagem.json
@cairn/gsd/contracts/contracts.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: CHECK-04 — a baseline do doctor pinada por teste, antes de tudo</name>
  <files>tests/cairn-gsd.bats</files>
  <read_first>
    - 35-PATTERNS.md, seção "Baseline do doctor (CHECK-04)" — fatos medidos e os três moldes de registro da casa
    - tests/cairn-vendoring.bats L179-189 (assert_tree_bytes_match — a forma de um teste que REPROVA se o arquivo mudou)
  </read_first>
  <action>
    Adicionar em tests/cairn-gsd.bats (perto da seção de cobertura) um @test de pin:
    "CHECK-04: baseline do cairn-doctor fixada — a fase 35 nao evolui o doctor". O teste
    compara `git hash-object cairn/scripts/cairn-doctor.py` com o blob registrado
    `e2040aea2068967eaec204e049fff0dbceb2ef50` e `wc -l` com `3907`, com mensagem de falha
    nomeada dizendo que o doctor mudou dentro da fase 35 e que o pin só é atualizado
    deliberadamente pela fase que evoluir o doctor (a 37). Registrar em comentário do
    teste o commit da baseline (a2527ee, 2026-08-07) — o teste É o registro: valores
    pinados como constantes locais do próprio @test, um dono só, nenhum arquivo extra
    (discrição de CHECK-04 exercida: forma mínima verificável). Caminho do doctor
    resolvido pelo root do repo do harness ($CAIRN_SCRIPTS_DIR/afins), nunca path
    absoluto de máquina.
  </action>
  <verify>
    <automated>test "$(git hash-object cairn/scripts/cairn-doctor.py)" = "e2040aea2068967eaec204e049fff0dbceb2ef50" && test "$(wc -l < cairn/scripts/cairn-doctor.py)" -eq 3907 && bats tests/cairn-gsd.bats -f 'CHECK-04'</automated>
  </verify>
  <done>O pin existe e passa; qualquer mudança futura em cairn-doctor.py dentro desta fase reprova a suíte com mensagem nomeada. CHECK-04 registrado antes de qualquer task de implementação.</done>
</task>

<task type="tracer">
  <name>Task 2: dispatcher → cairn-gsd-check.py → verification.status, de ponta a ponta nas duas grafias</name>
  <files>cairn/scripts/cairn-gsd.py, cairn/scripts/cairn-gsd-check.py, tests/cairn-command-surfaces.bats</files>
  <precondition>Cache do clone da tag presente em .cairn/cache/gsd-core-v1.10.0/ (crie com `./cairn/scripts/cairn-inventory.sh` — precisa de rede uma vez); necessário para ler a VERIFICATION_ROUTING_TABLE real em src/verification.cts L395-L516 da tag</precondition>
  <read_first>
    - 35-PATTERNS.md inteiro (todos os moldes abaixo têm arquivo:linha lá)
    - cairn/scripts/cairn-gsd-state.py L1-18 (docstring-contrato), L27-37 (imports + render como fonte única + constantes de exit), L97-99 (die), L1439-1496 (HANDLERS, main, family_of, --list-implemented)
    - cairn/scripts/cairn-gsd.py L106-125 (PHASE_BY_FAMILY + FAMILY_SCRIPT — o ponto da rota nova), L184-214 (build_routes — por que as duas grafias já casam), L2075-2098 (loop de despacho + exec — NÃO muda)
    - cairn/scripts/cairn_gsd_render.py (81 linhas — o módulo a consumir)
    - cairn/scripts/cairn-status.py L1066-1085 (verification_status — parse leniente do frontmatter de NN-VERIFICATION.md), L903-912 (escada de estado do disco), L385-387 (regexes PHASE_DIR_PREFIX/PLAN_FILE/SUMMARY_FILE — copiar do irmão de estado L93-95)
    - cairn/gsd/contracts/checagem.json, verbo verification.status (shape, notes, grafia dupla contratada, exit 1 = phase-dir ausente)
    - .cairn/cache/gsd-core-v1.10.0/gsd-core/src/verification.cts L395-L516 (readVerificationStatus + VERIFICATION_ROUTING_TABLE — a tabela real next_action/next_command da tag)
  </read_first>
  <action>
    No dispatcher (cairn-gsd.py): acrescentar `"checagem": "cairn-gsd-check.py"` a
    FAMILY_SCRIPT (L120-125). Só isso — script_for (L231) passa a resolver o irmão,
    --list-implemented agrega (loop L2059 itera os values da tabela), e o ponto de exec
    L2092-2098 não muda. A entrada "checagem" de PHASE_BY_FAMILY fica: vira a mensagem do
    estado transitório irmão-ausente-do-disco, mesmo papel das entradas da 34 entre ondas.
    ORPHANS_PHASE_35 e o roteamento misc ficam EXATAMENTE como estão (os órfãos são o
    plano 05).

    Criar cairn-gsd-check.py no molde inteiro do irmão de estado (D-01): docstring-contrato
    com usage, exits um a um (EXIT_OK/CONTRACT/USAGE/UNIMPLEMENTED idênticos aos irmãos), a
    doutrina própria da família — "veredito no payload (passed/active/block/valid), NUNCA
    no exit; exit 0 = gate avaliado; exit 1 = erro de uso do contrato" —, teto 1.5k
    declarado, "cairn/gsd/ é SOMENTE-LEITURA", divergências em divergences.json. Imports:
    sys.path.insert do dir do script + `from cairn_gsd_render import ...` (consumir, nunca
    duplicar). die com TAG_PREFIX "[cairn-gsd-check]". HANDLERS dict + main no molde do
    irmão: verbo canônico em argv[1]; verbo da família sem handler → die exit 4 nomeando o
    verbo e a fase 35; --list-implemented imprime sorted(HANDLERS). Regexes de
    fase/plano copiadas de cairn-gsd-state.py L93-95 (terceira cópia idêntica é aceitável
    na casa; import cruzado entre irmãos não tem precedente).

    handler verification.status (o único deste plano): argv[0] = phase-dir; ausente → die
    exit 1 nomeado (contrato). Resolve o `status:` literal do frontmatter do
    NN-VERIFICATION.md mais recente pelo molde verification_status de cairn-status.py
    (regex leniente, sem lib YAML) e a escada de estado do disco (has VERIFICATION →
    verified; planos done/total; has SUMMARY → executed). next_action/next_command vêm da
    VERIFICATION_ROUTING_TABLE transcrita da tag (src/verification.cts — ler no cache do
    clone; a tabela é dado, entra como constante com comentário de proveniência).
    verification_stale_check_indeterminate SÓ aparece quando o staleness check interno não
    completou (fail-open, #3057 B3) — na implementação da casa, se o check de staleness
    não se aplica, a chave sai false/ausente conforme a letra da tag. Envelope pela fonte
    única: default JSON.stringify(v, null, 2), --raw = String(valor), sem newline final.

    As duas grafias NÃO pedem código novo: checagem.json já contrata os spellings
    "query verification status" e "query verification.status"; build_routes registra os
    dois e o loop n-grama (maior→menor) casa a espaçada em n=3 — provar por teste, não
    por tabela nova.

    Em tests/cairn-command-surfaces.bats: registrar a razão escrita do script novo no
    guard "every cairn script is reachable by command", igual aos irmãos da 34 — "detalhe
    de implementação do dispatcher, nunca comando" (desvio 1 do 34-05, obrigatório para a
    suíte fechar).
  </action>
  <verify>
    <automated>R="$PWD" && T=$(mktemp -d) && cd "$T" && git init -q && mkdir -p .planning/phases/01-x && printf -- '---\nstatus: passed\n---\n' > .planning/phases/01-x/01-VERIFICATION.md && echo '{}' > .planning/config.json && P="$R/cairn/scripts/cairn-gsd.sh" && "$P" query verification.status .planning/phases/01-x > a.json && "$P" query verification status .planning/phases/01-x > b.json && diff a.json b.json && jq -e 'has("status") and has("next_action") and has("next_command")' a.json && ("$P" query verification.status >/dev/null 2>&1; test $? -eq 1) && test "$(wc -l < "$R/cairn/scripts/cairn-gsd-check.py")" -le 1500</automated>
  </verify>
  <done>O caminho fino inteiro — spelling (nas duas grafias) → build_routes → verbo canônico → exec pro terceiro irmão → leitura do artefato de fase → envelope da fonte única — funciona num repo real; envelopes byte-iguais entre as grafias; exit 1 nomeado sem phase-dir; command-surfaces com razão escrita.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: cenários das duas grafias, goldens e o guard em modo entrega incremental</name>
  <files>tests/cairn-gsd.bats, tests/fixtures/gsd-goldens/scenarios.json, tests/fixtures/gsd-goldens/verification-status-*.golden.json, tests/fixtures/gsd-goldens/divergences.json</files>
  <read_first>
    - tests/cairn-gsd.bats L43-53 (o seam: cenário = manifesto + golden irmão, zero mudança no runner), L75-121 (build_scenario_fixture), L167-179 (apply_mask_file — mask por VALOR), L239 e L331 (comparação por FORMA + schema do golden), L583-612 (trivial_verbs_of + teste de cobertura — o que este plano afrouxa)
    - tests/fixtures/gsd-goldens/scenarios.json (shape de cenário: id/verb/argv/fixture/compare/mask)
    - 34-01-SUMMARY.md, desvio 4 (o precedente exato do guard em entrega incremental)
  </read_first>
  <behavior>
    - Cenário `verification-status-grafia-pontuada`: fixture.files com fase + VERIFICATION.md, argv ["query","verification.status","<dir>"] → exit 0, golden derived-from-contract por FORMA (4 chaves)
    - Cenário `verification-status-grafia-espacada`: MESMO fixture, argv ["query","verification","status","<dir>"] → exit 0, MESMO shape — par que prova o handler único
    - Cenário `verification-status-sem-verification-md`: fase só com SUMMARY → exit 0, status refletindo a escada do disco
    - Cenário `verification-status-phase-dir-ausente`: sem argv de dir → exit 1 (caminho de erro do contrato, separado dos gates avaliados)
    - Guard: com a família checagem em entrega incremental, a direção "extras" compara contra o universo COMPLETO de contracts.json (nenhuma superfície fantasma), a direção "missing" segue no universo 10-famílias−órfãos — fecha bidirecional no plano 05
  </behavior>
  <action>
    Escrever os 4 cenários do behavior no manifesto com goldens irmãos provenance
    derived-from-contract (comparação por FORMA; masks por VALOR se algum campo variar
    entre execuções — nunca por chave). Fixtures via fixture.files ($PWD do harness, nunca
    path absoluto).

    Afrouxar o guard de cobertura pelo precedente registrado no 34-01 (desvio 4): durante
    a entrega incremental da fase, `extra` (implementado − universo coberto) é validado
    contra TODOS os verbos de contracts.json — verbo implementado que não existe no
    contrato reprova; `missing` segue derivado das 10 famílias − órfãos (a checagem entra
    no filtro só no plano 05, quando os 16 estiverem de pé). Deixar comentário no bats
    apontando que o plano 35-05 restaura o fecho bidirecional.

    NO MESMO commit do afrouxamento: na tabela de representantes do teste "familia
    nao-trivial: cada representante morre exit 4" (tests/cairn-gsd.bats L666-692), remover
    a linha `query verification.status|checagem|fase 35` — o verbo agora responde pelo
    irmão e a asserção de exit 4 deixaria a suíte vermelha. As linhas de
    verify.plan-structure e user-story.validate FICAM (seguem exit 4 até as ondas 2 e 4;
    os planos 35-02 e 35-04 cuidam delas).

    Registrar em divergences.json qualquer distância deliberada entre a
    VERIFICATION_ROUTING_TABLE transcrita e a semântica upstream que não se aplique à casa
    (se nenhuma, nada a registrar — não inventar entrada).
  </action>
  <verify>
    <automated>bats tests/cairn-gsd.bats && test -z "$(git status --porcelain cairn/gsd/)" && test "$(wc -l < cairn/scripts/cairn-gsd-check.py)" -le 1500</automated>
  </verify>
  <done>Suíte inteira verde offline com os cenários novos; o par de grafias está coberto por golden; o guard aceita a entrega incremental sem abrir porta pra superfície fantasma; a tabela de representantes não asserta mais exit 4 pra verification.status; contratos intocados.</done>
</task>

</tasks>

<verification>
- `bats tests/cairn-gsd.bats` verde offline — pin CHECK-04, tracer e cenários das duas grafias
- Sequência E2E manual (verify da Task 2) limpa num repo temporário
- `git status --porcelain cairn/gsd/` vazio; cairn-doctor.py intocado (o pin prova)
- `wc -l cairn/scripts/cairn-gsd-check.py` ≤ 1500; state (1499) e init (1325) byte-iguais ao HEAD anterior salvo nada (só o dispatcher muda)
</verification>

<success_criteria>
- CHECK-04 completo: baseline registrada e mecanicamente vigiada antes de qualquer implementação
- CHECK-01 iniciado: a família checagem tem rota, irmão e o verbo de grafia dupla provado nas duas formas
- D-01 da fase materializado: terceiro irmão no molde exec, render como fonte única, teto vigiado por gate
- Arquitetura provada com 1 verbo antes dos 15 restantes — o custo de um erro é um commit
</success_criteria>

<output>
Criar `.planning/phases/35-o-binario-python-checagem-e-verbos-orfaos/35-01-SUMMARY.md`
ao concluir. Registrar: a forma escolhida do pin CHECK-04 (constantes no próprio @test,
commit em comentário), a transcrição da VERIFICATION_ROUTING_TABLE (proveniência
src/verification.cts da tag) e o estado do guard (incremental, fecho no 35-05).
</output>
