---
phase: 35-o-binario-python-checagem-e-verbos-orfaos
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - cairn/scripts/cairn-gsd-check.py
  - tests/cairn-gsd.bats
  - tests/fixtures/gsd-goldens/scenarios.json
  - tests/fixtures/gsd-goldens/*.golden.json
  - tests/fixtures/gsd-goldens/divergences.json
autonomous: true
requirements: [CHECK-01]
beads:
  CHECK-01: CairnGo-ma8r
must_haves:
  truths:
    - "`query verify.plan-structure <plan>` valida frontmatter obrigatório, estrutura de tasks e conflitos de gate negativo file-wide, com fail-loud em corrupção NUL/binária ANTES das checagens (#2701) — veredito em valid/errors com exit 0; arquivo inexistente = {error} no payload com exit 0; path ausente = exit 1 (CHECK-01)"
    - "`query verify.artifacts <plan>` confere must_haves.artifacts contra o disco (existência + contains); `query verify.key-links <plan>` confere cada aresta from→to via pattern no conteúdo real, com link pendente de wave igual/posterior NÃO contando contra all_verified (#1202) (CHECK-01)"
    - "`query verify.commits <hash>...` particiona em valid/invalid via git cat-file -t; --raw emite 'valid'/'invalid'; nenhum hash = exit 1 (CHECK-01)"
    - "`verify <subcommand>` roteia por VERIFY_SUBCOMMANDS; codebase-drift compara .planning/codebase/STRUCTURE.md com o HEAD e sem STRUCTURE.md dá skip verde; subcommand desconhecido = error exit 1 (CHECK-01)"
    - "os cenários de golden separam 'gate reprovou' (exit 0, payload false) de 'gate não avaliou' (exit 1) — a doutrina exit-code da família, provada verbo a verbo"
  artifacts:
    - cairn/scripts/cairn-gsd-check.py
    - tests/fixtures/gsd-goldens/scenarios.json
  key_links:
    - "verify.plan-structure/artifacts/key-links → parser de frontmatter na forma de parse_frontmatter_lines (cairn-gsd-init.py:876-890, subset YAML plano com span) — cópia de forma, nunca import cruzado entre irmãos"
    - "verify.commits → run_git no molde subprocess defensivo da casa (cairn-gsd.py:818-832: TimeoutExpired e exit != 0 viram valor, nunca traceback)"
    - "verify (família) → dict subcommand→função interno ao handler, molde HANDLERS — nenhum roteador genérico novo; subcommands que já existem como verbo dedicado compartilham a MESMA função"
  prohibitions:
    - statement: "Nenhuma task escreve sob cairn/gsd/**; state/init/doctor intocados"
    - statement: "Nenhum subcommand não observado ganha envelope inventado — indisponibilidade declarada pelo molde 34-05 e registrada em divergences.json, nunca silenciada"
    - statement: "Veredito no payload, nunca no exit: gate reprovado sai 0 com valid/all_passed/all_verified false"
---

<objective>
A família verify inteira responde em python no terceiro irmão: o leitor de estrutura de
PLAN.md (verify.plan-structure), os dois conferidores de must_haves (verify.artifacts,
verify.key-links), o validador de hashes (verify.commits) e o comando de família (verify)
com o subcommand observado codebase-drift — 5 verbos, todos na letra de checagem.json,
com a doutrina exit-code separando gate-reprovou de gate-não-avaliou.

Purpose: fechar o bloco mais denso da família checagem (os leitores de PLAN.md
compartilham parser e são a maior superfície de código do irmão) enquanto o teto de 1.5k
ainda tem folga.
Output: 5 verbos em --list-implemented do irmão check, cenários com goldens separando os
dois caminhos de exit, suíte verde.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-o-binario-python-checagem-e-verbos-orfaos/35-CONTEXT.md
@.planning/phases/35-o-binario-python-checagem-e-verbos-orfaos/35-PATTERNS.md
@cairn/gsd/contracts/checagem.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: os leitores de PLAN.md — verify.plan-structure, verify.artifacts, verify.key-links</name>
  <files>cairn/scripts/cairn-gsd-check.py, tests/cairn-gsd.bats, tests/fixtures/gsd-goldens/scenarios.json, tests/fixtures/gsd-goldens/verify-*.golden.json</files>
  <read_first>
    - cairn/gsd/contracts/checagem.json, verbos verify.plan-structure, verify.artifacts, verify.key-links (shapes, notes, os dois exits de cada um)
    - cairn/scripts/cairn-gsd-init.py L876-890 (parse_frontmatter_lines — o parser da casa, subset YAML plano; L940/L961/L1004 mostram como os handlers frontmatter.* o exercitam)
    - cairn/scripts/cairn-doctor.py L1999-2013 (parse_plan_files_modified — files_modified inline e em lista) e L2033-2045 (filtro de superseded)
    - 35-PATTERNS.md, seção do cairn-gsd-check.py (fontes por verbo) e Shared Patterns (veredito no payload; falha nomeada)
  </read_first>
  <behavior>
    - Cenário `verify-plan-structure-valido`: PLAN.md de fixture com frontmatter completo (phase, plan, type, wave, depends_on, files_modified, autonomous, must_haves) e tasks bem formadas → exit 0, valid true
    - Cenário `verify-plan-structure-invalido`: frontmatter sem must_haves + task sem name → exit 0, valid false com errors nomeando cada falta (gate REPROVOU, não deixou de avaliar)
    - Cenário `verify-plan-structure-arquivo-inexistente`: path que não existe → exit 0 com {error} no payload
    - Cenário `verify-plan-structure-sem-path`: argv vazio → exit 1 (erro de uso)
    - Cenário `verify-artifacts-passa` e `verify-artifacts-reprova`: must_haves.artifacts com arquivo presente/ausente no fixture → exit 0, all_passed true/false
    - Cenário `verify-key-links-pendente`: aresta cujo from é prometido por plano de wave posterior → exit 0, pending contado, all_verified não penalizado (#1202)
  </behavior>
  <action>
    Implementar os 3 handlers no irmão check, compartilhando UM parser de frontmatter
    copiado da forma de parse_frontmatter_lines (subset YAML plano com span do bloco —
    terceira cópia de forma é aceitável na casa; import cruzado entre irmãos não tem
    precedente) e UM extrator de must_haves (artifacts com contains opcional; key_links
    com from/to/pattern).

    verify.plan-structure: campos obrigatórios do frontmatter conforme a nota do contrato;
    validação de estrutura das tasks (name/action presentes; checkpoint vs autonomous);
    conflitos de gate negativo file-wide; fail-loud NUL/binário ANTES de qualquer checagem
    (#2701 — a letra do contrato dá {valid: false, errors} no payload e reserva exit 1 a
    "path ausente ou com null bytes": seguir a letra dos DOIS campos do contrato e cobrir
    o caso NUL por cenário; se a conciliação payload×exit exigir escolha, registrá-la em
    divergences.json com motivo, nunca silenciar). Arquivo inexistente → {error} com exit
    0. Shape completo: error, errors, frontmatter_fields, path, task_count, tasks, valid,
    warnings.

    verify.artifacts: must_haves.artifacts do frontmatter contra o disco — existência +
    contains quando declarado; sem must_haves.artifacts ou arquivo inexistente → {error}
    payload exit 0; path ausente → exit 1. Shape: all_passed, artifacts, error, passed,
    path, total.

    verify.key-links: cada aresta from→to conferida por pattern no conteúdo real do from;
    link pendente (from prometido por plano de wave igual/posterior, lido dos frontmatter
    da fase) entra em pending sem derrubar all_verified (#1202). Shape: all_verified,
    error, links, path, pending, total, verified.

    Cenários e goldens derived-from-contract por FORMA no seam da 33/34 (manifesto +
    golden irmão, zero mudança no runner); fixtures via fixture.files com paths relativos
    ao mktemp do harness.

    NO MESMO commit: os dois testes do bats que assertam exit 4 para verify.plan-structure
    — o representante único de "dispatcher: verbo de familia nao implementada morre exit
    4" (tests/cairn-gsd.bats L502-511) e a linha `query verify.plan-structure|checagem|
    fase 35` da tabela de representantes (L666-692) — são REPONTADOS para
    `query uat.render-checkpoint`, verbo de checagem que só chega no 35-04 (que os retira
    de vez, registro na action de lá). Sem o repoint, o bats desta própria task reprova.
  </action>
  <verify>
    <automated>bats tests/cairn-gsd.bats && R="$PWD" && T=$(mktemp -d) && cd "$T" && git init -q && P="$R/cairn/scripts/cairn-gsd.sh" && ("$P" query verify.plan-structure /caminho/que/nao/existe.md | jq -e 'has("error")') && ("$P" query verify.plan-structure >/dev/null 2>&1; test $? -eq 1) && test "$(wc -l < "$R/cairn/scripts/cairn-gsd-check.py")" -le 1500</automated>
  </verify>
  <done>Os 3 leitores de PLAN.md respondem na letra do contrato com um parser só; gate reprovado e gate não-avaliado saem por caminhos distintos e ambos estão cobertos por cenário.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: verify.commits e o comando de família verify</name>
  <files>cairn/scripts/cairn-gsd-check.py, tests/cairn-gsd.bats, tests/fixtures/gsd-goldens/scenarios.json, tests/fixtures/gsd-goldens/verify-*.golden.json, tests/fixtures/gsd-goldens/divergences.json</files>
  <read_first>
    - cairn/gsd/contracts/checagem.json, verbos verify e verify.commits (VERIFY_SUBCOMMANDS na nota; shape do codebase-drift; --skip só em schema-drift)
    - cairn/scripts/cairn-gsd.py L818-832 (run_git — o molde subprocess defensivo)
    - 34-05-SUMMARY.md (doutrina da indisponibilidade declarada no envelope e dos fantasmas pelo caminho de erro do contrato)
    - tests/cairn-gsd.bats L99-121 (fixture.git_commit — commit real para os hashes de verify.commits)
  </read_first>
  <behavior>
    - Cenário `verify-commits-particiona`: fixture.git_commit + um hash real e um forjado → exit 0, valid com 1, invalid com 1, all_valid false (mask por VALOR no hash real: regex ^[0-9a-f]{7,40}$ → <masked>)
    - Cenário `verify-commits-sem-hash`: argv vazio → exit 1
    - Cenário `verify-codebase-drift-skip-verde`: fixture sem .planning/codebase/STRUCTURE.md → exit 0, skipped true (o skip é veredito, não erro)
    - Cenário `verify-subcommand-desconhecido`: `verify nao-existe` → exit 1 com mensagem "Unknown ... Available: ..." na forma do contrato
  </behavior>
  <action>
    verify.commits: cada hash validado com `git cat-file -t` == "commit" no repo corrente
    pelo molde run_git; partição valid/invalid; --raw emite 'valid'/'invalid' (a string,
    fora do envelope JSON, conforme a flag do contrato). Nenhum hash → exit 1 nomeado.

    verify (família): dict interno subcommand→função com os 8 de VERIFY_SUBCOMMANDS
    (plan-structure, phase-completeness, references, commits, artifacts, key-links,
    schema-drift, codebase-drift). plan-structure/commits/artifacts/key-links compartilham
    a MESMA função dos verbos dedicados (um dono por semântica). codebase-drift (o
    observado do universo): compara .planning/codebase/STRUCTURE.md com o HEAD e emite o
    shape do contrato (action_required, block, contract, directive, elements, reason,
    skipped); sem STRUCTURE.md → skip verde. schema-drift respeita --skip. Os subcommands
    do router sem contrato próprio nem sítio observado (phase-completeness, references, e
    o caminho não-skip de schema-drift) respondem indisponibilidade declarada no envelope
    pelo molde do 34-05 — nunca envelope inventado — com entrada em divergences.json
    (family checagem, aspect subcommand-nao-observado, reason citando que o universo da 31
    só exercita codebase-drift). Subcommand fora da lista → error exit 1.
  </action>
  <verify>
    <automated>bats tests/cairn-gsd.bats && R="$PWD" && P="$R/cairn/scripts/cairn-gsd.sh" && T=$(mktemp -d) && cd "$T" && git init -q && git commit -q --allow-empty -m x && H=$(git rev-parse HEAD) && ("$P" query verify.commits "$H" | jq -e '.all_valid == true') && ("$P" verify nao-existe >/dev/null 2>&1; test $? -eq 1) && ("$P" verify codebase-drift | jq -e 'has("block") and has("skipped")') && test -z "$(cd "$R" && git status --porcelain cairn/gsd/)" && test "$(wc -l < "$R/cairn/scripts/cairn-gsd-check.py")" -le 1500</automated>
  </verify>
  <done>A família verify inteira roteia; 6/11 verbos de checagem em --list-implemented do irmão (verification.status + os 5 deste plano); toda distância deliberada declarada em divergences.json; suíte verde.</done>
</task>

</tasks>

<verification>
- `bats tests/cairn-gsd.bats` verde offline — cenários novos + regressão zero (33, 34, 35-01)
- Doutrina exit-code provada por cenário em cada verbo: reprova ≠ não-avalia
- `git status --porcelain cairn/gsd/` vazio; `wc -l cairn-gsd-check.py` ≤ 1500
- Pin CHECK-04 segue verde (doctor intocado)
</verification>

<success_criteria>
- CHECK-01 avança: 6 dos 11 verbos de checagem respondem em python na letra do contrato
- Um parser de frontmatter e um extrator de must_haves — nenhuma duplicação interna
- Subcommands não observados declarados, nunca inventados
</success_criteria>

<output>
Criar `.planning/phases/35-o-binario-python-checagem-e-verbos-orfaos/35-02-SUMMARY.md`
ao concluir. Registrar: a conciliação payload×exit do caso NUL de plan-structure (se houve
escolha), as entradas novas de divergences.json e a contagem de linhas do irmão vs teto.
</output>
