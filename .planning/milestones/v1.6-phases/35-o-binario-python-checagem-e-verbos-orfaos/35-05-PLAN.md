---
phase: 35-o-binario-python-checagem-e-verbos-orfaos
plan: 05
type: execute
wave: 5
depends_on: ["35-04"]
files_modified:
  - cairn/scripts/cairn-gsd-check.py
  - cairn/scripts/cairn-gsd.py
  - tests/cairn-gsd.bats
  - tests/fixtures/gsd-goldens/scenarios.json
  - tests/fixtures/gsd-goldens/*.golden.json
  - tests/fixtures/gsd-goldens/divergences.json
autonomous: true
requirements: [CHECK-03, CHECK-01]
beads:
  CHECK-03: CairnGo-jmss
  CHECK-01: CairnGo-ma8r
must_haves:
  truths:
    - "os 5 órfãos respondem com a semântica extraída na 31 (REM-04): audit-open varre o .planning/ inteiro; review-lane flags emite texto linha a linha; agent.classify-failure classifica em quota-exceeded/classify-handoff-bug/unknown-failure; task.is-behavior-adding avalia o predicado do gate MVP+TDD; run-with-timeout embrulha subprocess com a tabela de exits do GNU timeout (CHECK-03)"
    - "run-with-timeout é subprocess com timeout e passthrough de stdio — NUNCA 'esperar evento'; exits: 0..255 do comando, 124 estouro, 125 spawn genérico, 126 EACCES, 127 ENOENT, 128+n morto por sinal, 2 usage do wrapper; argv do comando é OPACO (o dispatcher entrega argv[n:] intocado)"
    - "o roteamento dos órfãos é EXPLÍCITO pro irmão check: a constante de exclusão do dispatcher mudou de papel (exclusão→rota) na MESMA onda em que o bats parou de derivá-la — nenhum órfão cai no ramo misc de script_for rumo ao irmão errado (D-01, armadilhas 1 e 2 do PATTERNS)"
    - "o guard de cobertura fecha o universo: 11 famílias no filtro, exclusão zerada, comm vazio NOS DOIS SENTIDOS — 87 verbos do contrato == 87 implementados agregados (CHECK-01, CHECK-03)"
    - "o controle negativo (verbo forjado no contrato fixture) segue detectando — o fecho não cegou o guard"
  artifacts:
    - cairn/scripts/cairn-gsd-check.py
    - cairn/scripts/cairn-gsd.py
    - tests/cairn-gsd.bats
    - tests/fixtures/gsd-goldens/divergences.json
  key_links:
    - "script_for ramo misc → três destinos: MISC_STATE_VERBS → cairn-gsd-state.py; o set dos 5 ex-órfãos → cairn-gsd-check.py; resto → cairn-gsd-init.py (molde MISC_STATE_VERBS, PATTERNS armadilha 1)"
    - "trivial_verbs_of → 11 famílias (soma 'checagem') SEM exclusão por comm — o universo derivado do inventário é o próprio 87"
    - "run-with-timeout → subprocess.Popen SEM capture (stdio herdado) + process group próprio + wait(timeout) + SIGTERM→SIGKILL — a morte do filho vira exit code contratado, nunca die"
  prohibitions:
    - statement: "Nenhuma task escreve sob cairn/gsd/**; doctor intocado (pin CHECK-04 prova); state/init intocados salvo o roteamento do dispatcher"
    - statement: "Dispatcher e bats mudam na MESMA task — a suíte nunca fica num commit em que a constante sumiu e o guard ainda a procura"
    - statement: "Divergência consciente da semântica do bundle (ex.: caminho Win32) vai em divergences.json com motivo, nunca silenciada"
---

<objective>
O fecho da fase: os 5 verbos órfãos (audit-open, review-lane, agent.classify-failure,
task.is-behavior-adding, run-with-timeout) respondem no irmão check com a semântica
extraída na 31 (REM-04), o dispatcher troca a exclusão por rota explícita, e o guard de
cobertura fecha o universo 87/87 com comm vazio nos dois sentidos.

Purpose: a fase 36 aponta os preâmbulos dos workflows pro dispatcher — a superfície
precisa estar completa (87/87) antes; os órfãos são os últimos 5 verbos que negam.
Output: 16/16 da fase implementados, roteamento três-destinos no misc, guard bidirecional
restaurado, suíte da casa inteira verde.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-o-binario-python-checagem-e-verbos-orfaos/35-CONTEXT.md
@.planning/phases/35-o-binario-python-checagem-e-verbos-orfaos/35-PATTERNS.md
@cairn/gsd/contracts/misc.json
@cairn/gsd/contracts/contracts.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: os três órfãos de classificação e varredura — audit-open, agent.classify-failure, task.is-behavior-adding</name>
  <files>cairn/scripts/cairn-gsd-check.py, tests/fixtures/gsd-goldens/divergences.json</files>
  <precondition>Cache do clone da tag presente em .cairn/cache/gsd-core-v1.10.0/ (criado na onda 1) — fonte das heurísticas de AGENT_FAILURE_CLASSES (src/agent-command-router.cts L105-L113), do scanner auditOpenArtifacts (src/audit.cts L785-L867) e do predicado isBehaviorAddingTaskContent (src/task-command-router.cts L37-L111)</precondition>
  <read_first>
    - cairn/gsd/contracts/misc.json, verbos agent.classify-failure, audit-open, task.is-behavior-adding (shapes, notes com proveniência REM-04, os dois exits de cada um)
    - Os três fontes da tag no cache (paths/linhas dos source_ref acima) — as heurísticas de classificação e a lista de tipos de artefato aberto são DADO, transcritas com proveniência em comentário
    - 35-PATTERNS.md: "Órfãos de classificação pura" e a nota de masks (scanned_at não está em free_of_timestamp_keys — a chave pode existir, o VALOR leva mask por regex)
  </read_first>
  <behavior>
    - Cenário `agent-classify-quota`: texto de erro com o padrão de quota da tag → exit 0, class quota-exceeded (sentinel/retryAfterSeconds quando presentes no texto)
    - Cenário `agent-classify-unknown`: texto sem padrão → exit 0, class unknown-failure
    - Cenário `audit-open-vazio`: .planning/ sem artefato aberto → exit 0, has_open_items false, counts zerados, scanned_at mascarado por VALOR
    - Cenário `audit-open-com-itens`: fixture com debug session aberta + todo pendente → exit 0, items refletindo, has_open_items true
    - Cenário `task-is-behavior-adding-true`: --task-content com tdd="true" + <behavior> + arquivo-fonte não-teste → exit 0, is_behavior_adding true, checks com o breakdown
    - Cenário `task-is-behavior-adding-false`: task sem behavior → exit 0, false com reason
    - Cenário `task-is-behavior-adding-path-invalido`: path fora do escopo → exit 1 (USAGE)
  </behavior>
  <action>
    agent.classify-failure: filtrar tokens '--' do argv, unir o resto por espaço e
    classificar pelo AGENT_FAILURE_CLASSES transcrito da tag: quota-exceeded (com sentinel
    e retryAfterSeconds opcional extraídos do texto), classify-handoff-bug,
    unknown-failure. Subcommand ≠ classify-failure → exit 1. Shape: class,
    retryAfterSeconds, sentinel.

    audit-open: varrer os tipos de artefato aberto do .planning/ na lista da tag (debug
    sessions, quick tasks, threads, todos, seeds, uat_gaps, verification, context
    questions) e devolver o AuditResult; --json emite o JSON do shape (counts,
    has_open_items, items, scanned_at); SEM --json sai o relatório humano na forma de
    formatAuditReport da tag. scanned_at leva mask por VALOR no golden (regex de
    timestamp → <masked>) — a chave não está na lista proibida, o valor é que varia.
    audit-open não tem subcommands: token extra → exit 1.

    task.is-behavior-adding: path de plan-file OU --task-content (uma das duas formas
    obrigatória); o predicado do gate MVP+TDD transcrito de
    isBehaviorAddingTaskContent — tdd="true" + bloco <behavior> + arquivos-fonte
    não-teste em <files> — devolvendo checks (breakdown por checagem) e reason quando
    false. Path fora do escopo do projeto ou inexistente → exit 1 USAGE. Veredito no
    payload: task que reprova o predicado sai 0 com is_behavior_adding false.

    ATENÇÃO ao sequenciamento (blocker do checker): a rota do dispatcher pros órfãos só
    nasce na Task 3 — até lá o dispatcher morre exit 4 pros 5 (comportamento correto,
    coberto pelos testes existentes que NÃO mudam nesta task). Por isso os cenários e
    goldens dos behaviors acima entram na Task 3 (o runner de goldens invoca via $GSD), e
    a verificação desta task invoca o irmão DIRETO. Heurística divergente da tag por
    decisão consciente → divergences.json com motivo.
  </action>
  <verify>
    <automated>bats tests/cairn-gsd.bats && R="$PWD" && C="$R/cairn/scripts/cairn-gsd-check.py" && T=$(mktemp -d) && cd "$T" && git init -q && mkdir -p .planning && echo '{}' > .planning/config.json && (python3 "$C" agent.classify-failure -- "erro qualquer sem padrao" | jq -e '.class == "unknown-failure"') && (python3 "$C" audit-open --json | jq -e 'has("counts") and has("has_open_items")') && (python3 "$C" task.is-behavior-adding >/dev/null 2>&1; test $? -eq 1) && test "$(wc -l < "$C")" -le 1500</automated>
  </verify>
  <done>Três órfãos respondem no irmão (invocação direta — a rota do dispatcher é a Task 3) na semântica do bundle com proveniência anotada; veredito no payload, erro de uso no exit 1; suíte existente segue verde (dispatcher intocado).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: os dois órfãos de saída atípica — review-lane (texto) e run-with-timeout (exit-only)</name>
  <files>cairn/scripts/cairn-gsd-check.py, tests/cairn-gsd.bats, tests/fixtures/gsd-goldens/divergences.json</files>
  <precondition>Cache do clone da tag presente (REVIEWER_LANES vive em src/review-lane-descriptor.cts L229-L604; a mecânica do wrapper em gsd-tools.cjs L3452-L3590 — source_ref do contrato)</precondition>
  <read_first>
    - cairn/gsd/contracts/misc.json, verbos review-lane e run-with-timeout (notes COMPLETAS — a semântica inteira do wrapper está lá: tabela de exits, sufixo 's', 0 = sem timer, vazio/negativo = usage 2, process group, SIGTERM→SIGKILL 3s, encaminhamento de sinais, argv opaco, Win32 .cmd/.bat)
    - .cairn/cache/gsd-core-v1.10.0/gsd-core/src/review-lane-descriptor.cts L229-L604 (REVIEWER_LANES — o dado das lanes) e src/review-lane-invocation.cts (resolveLanePlan)
    - 35-PATTERNS.md: "run-with-timeout — análogo PARCIAL" (por que TODO subprocess da casa faz o contrário) e "No Analog Found" linha 2 (o substituto: Popen sem capture + wait(timeout) + kill de process group)
    - cairn/scripts/cairn-gsd.py L2075-2077 (nenhum parse global de flags antes do verbo — por que o argv embrulhado chega intocado ao irmão)
  </read_first>
  <behavior>
    - Cenário `review-lane-flags`: `review-lane flags` → exit 0, texto linha a linha (kind text — fora do envelope JSON), lanes default do REVIEWER_LANES
    - Cenário `review-lane-sections`: slug<TAB>reviewsSection por linha
    - Cenário `review-lane-subcommand-invalido`: fora de plan|invoke|sections|flags → exit 1 usage
    - bats direto (exit-only, sem golden): comando sai 7 → wrapper sai 7; sleep estourando o timer → 124; comando inexistente → 127; sem permissão de exec → 126; seconds 0 → roda sem timer; seconds vazio → 2; stdout do filho atravessa o wrapper intocado
  </behavior>
  <action>
    review-lane: subcommands plan|invoke|sections|flags; o dado das lanes é REVIEWER_LANES
    transcrito da tag (com proveniência; merge com capabilities instaladas não se aplica —
    a casa não instala capabilities de reviewer: registrar em divergences.json se a
    transcrição simplificar o merge). flags emite as flags válidas das lanes escolhidas
    como TEXTO linha a linha, fora do envelope JSON (kind text do contrato); sections
    emite slug<TAB>reviewsSection; plan e invoke emitem JSON; --selected CSV (vazio =
    todas as lanes mergeadas), --run-dir, --repo-root com os defaults do contrato.
    Subcommand inválido → exit 1 usage.

    run-with-timeout: a EXCEÇÃO da doutrina de subprocess da casa — Popen SEM capture
    (stdio herdado: pipes e redirects atravessam), spawn em process group próprio para o
    timeout cear a árvore inteira, wait(timeout); no estouro SIGTERM no grupo com
    escalação SIGKILL após 3s → exit 124; SIGINT/SIGTERM recebidos pelo wrapper são
    encaminhados à árvore. <seconds> aceita sufixo 's'; 0 roda SEM timer; vazio, negativo
    ou não numérico → usage exit 2 (fail-safe, nunca unbounded). Tradução da morte do
    filho: ENOENT → 127, EACCES → 126, spawn genérico → 125, morto por sinal n → 128+n,
    senão o exit do próprio comando (0..255). Argv do comando é OPACO — o handler recebe
    argv[1:] do irmão como veio do exec e não parseia flag nenhuma depois do seconds/`--`.
    kind exit-only: a verificação é por bats direto, não por golden — e como a rota do
    dispatcher só nasce na Task 3, os @test novos de run-with-timeout desta task invocam
    o irmão DIRETO (python3 sobre cairn-gsd-check.py); o atravessamento pelo dispatcher
    (argv opaco pelo exec) é provado no verify da Task 3. O cenário/golden de review-lane
    (kind text) também entra na Task 3, junto com a rota. O caminho Win32 (.cmd/.bat via
    cmd.exe) não se aplica à casa (bash/bats, macOS/Linux): divergência declarada em
    divergences.json com motivo, nunca silenciada.
  </action>
  <verify>
    <automated>R="$PWD" && C="$R/cairn/scripts/cairn-gsd-check.py" && (python3 "$C" run-with-timeout 5 -- sh -c 'exit 7' >/dev/null 2>&1; test $? -eq 7) && (python3 "$C" run-with-timeout 1 -- sleep 3 >/dev/null 2>&1; test $? -eq 124) && (python3 "$C" run-with-timeout 5 -- /comando/que/nao/existe >/dev/null 2>&1; test $? -eq 127) && (python3 "$C" run-with-timeout '' -- true >/dev/null 2>&1; test $? -eq 2) && test "$(python3 "$C" run-with-timeout 5 -- echo atravessou)" = "atravessou" && (python3 "$C" review-lane flags | head -1 | grep -q .) && bats tests/cairn-gsd.bats && test "$(wc -l < "$C")" -le 1500</automated>
  </verify>
  <done>run-with-timeout devolve a tabela GNU-timeout inteira com stdio atravessando e review-lane fala texto na letra do contrato — ambos provados no irmão direto; a rota e a prova via dispatcher são a Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: exclusão vira rota + cenários dos órfãos + guard 87/87 — dispatcher e bats na MESMA task</name>
  <files>cairn/scripts/cairn-gsd.py, tests/cairn-gsd.bats, tests/fixtures/gsd-goldens/scenarios.json, tests/fixtures/gsd-goldens/audit-open-*.golden.json, tests/fixtures/gsd-goldens/agent-classify-*.golden.json, tests/fixtures/gsd-goldens/task-is-behavior-*.golden.json, tests/fixtures/gsd-goldens/review-lane-*.golden.json</files>
  <read_first>
    - 35-PATTERNS.md, armadilhas 1 e 2 (o ramo misc de script_for manda órfão pro irmão ERRADO se a constante só esvaziar; dispatcher_orphans do bats mata a suíte se a constante sumir sem o bats junto)
    - cairn/scripts/cairn-gsd.py L128-145 (MISC_STATE_VERBS — o molde — e a constante de exclusão dos 5), L217-231 (phase_for + script_for — os dois pontos que mudam)
    - tests/cairn-gsd.bats L564-612 (dispatcher_orphans + trivial_verbs_of + o teste de cobertura bidirecional) e o afrouxamento incremental introduzido no 35-01 (comentário aponta este plano)
  </read_first>
  <action>
    No dispatcher, numa edição só: a constante de exclusão dos 5 órfãos muda de papel —
    exclusão→rota (D-01: "perde a constante da lista de exclusão; os órfãos agora têm
    dono"). Renomear para MISC_CHECK_VERBS (mesmos 5 verbos, molde MISC_STATE_VERBS, com
    comentário registrando a mudança de papel e a fase); estender o ramo misc de
    script_for para TRÊS destinos: MISC_STATE_VERBS → cairn-gsd-state.py,
    MISC_CHECK_VERBS → cairn-gsd-check.py, resto → cairn-gsd-init.py. Remover o caso
    especial de órfão de phase_for (L218) — verbo de misc roteado responde do irmão, não
    de mensagem de fase. A entrada "checagem" de PHASE_BY_FAMILY fica (papel
    irmão-ausente-do-disco).

    No bats, no MESMO commit: apagar dispatcher_orphans (a função que deriva a lista da
    constante por regex — sem exclusão não há o que derivar) E o tail do teste de
    cobertura que a consome (L621-629): o laço de "exclusão nominal" que confere cada
    órfão fora do universo e o assert `dispatcher_orphans | wc -l == 5` vivem DENTRO do
    teste reescrito — somem junto com a função. Em trivial_verbs_of, somar
    `or .value.family == "checagem"` ao filtro (10→11 famílias) e remover o
    `comm -23` de exclusão; reverter o afrouxamento incremental do 35-01 — a direção
    extras volta a comparar contra o universo COBERTO (11 famílias, que agora É o
    inventário inteiro) — o comm fecha vazio NOS DOIS SENTIDOS em 87. Conferir que o
    controle negativo do verbo forjado (L632) e a cobertura cruzada skip-gated (L745)
    seguem passando sem mudança. Remover os dois testes que assertam exit 4 pros
    ex-órfãos: "dispatcher: orfao de CHECK-03 aponta a fase 35" (L516+) e "orfaos de
    CHECK-03: os 5 verbos morrem exit 4" (L694+) — o fantasma de verbo forjado fica como
    controle da via exit-4.

    Ainda no MESMO commit, somar os cenários e goldens dos órfãos definidos nos behaviors
    das Tasks 1-2 (movidos pra cá porque o runner de goldens invoca via $GSD — só passam
    com a rota de pé): agent-classify-quota/unknown, audit-open-vazio/com-itens
    (scanned_at mascarado por VALOR), task-is-behavior-adding-true/false/path-invalido,
    review-lane-flags/sections/subcommand-invalido. Recorded viável em todos (entrada é
    argv/arquivo); derived-from-contract por forma onde gravar não compensar.
  </action>
  <verify>
    <automated>bats tests/cairn-gsd.bats && bats tests/cairn-command-surfaces.bats && R="$PWD" && P="$R/cairn/scripts/cairn-gsd.sh" && test "$("$P" --list-implemented | sort -u | wc -l | tr -d ' ')" -eq 87 && T=$(mktemp -d) && cd "$T" && git init -q && mkdir -p .planning && echo '{}' > .planning/config.json && ("$P" query audit-open --json | jq -e 'has("counts")') && ("$P" run-with-timeout 5 -- sh -c 'exit 7' >/dev/null 2>&1; test $? -eq 7) && test -z "$(cd "$R" && git status --porcelain cairn/gsd/)" && test "$(wc -l < "$R/cairn/scripts/cairn-gsd-check.py")" -le 1500 && test "$(git -C "$R" hash-object cairn/scripts/cairn-doctor.py)" = "e2040aea2068967eaec204e049fff0dbceb2ef50"</automated>
  </verify>
  <done>Universo fechado: 87 no contrato, 87 agregados, comm vazio nos dois sentidos, exclusão extinta com o bats atualizado no mesmo commit; os 5 ex-órfãos respondem pelo dispatcher de ponta a ponta; doctor byte-igual à baseline.</done>
</task>

</tasks>

<verification>
- `bats tests/cairn-gsd.bats` e `bats tests/cairn-command-surfaces.bats` verdes offline
- Suíte da casa INTEIRA verde ao fim da fase: `./cairn/scripts/cairn-test.sh` (molde 34-05, 1125+ testes) — zero regressão
- `--list-implemented` agregado = 87 = universo de contracts.json; guard bidirecional
- Pin CHECK-04 verde: cairn-doctor.py byte-igual à baseline registrada no plano 01
- `git status --porcelain cairn/gsd/` vazio; teto 1500 do irmão check respeitado
</verification>

<success_criteria>
- CHECK-03 completo: os 5 órfãos com a semântica REM-04, divergências conscientes declaradas
- CHECK-01 selado: universo 87/87, superfície completa para a fase 36 apontar os preâmbulos
- As duas armadilhas do PATTERNS desarmadas: rota explícita (não ramo misc default) e dispatcher+bats na mesma task
</success_criteria>

<output>
Criar `.planning/phases/35-o-binario-python-checagem-e-verbos-orfaos/35-05-SUMMARY.md`
ao concluir. Registrar: a partição misc final (7 estado / 5 check / 17 init), a contagem
87/87 com o comm dos dois sentidos, as divergências novas da fase consolidadas (Win32,
merge de lanes, subcommands não observados, e o que mais surgir), e a contagem final de
linhas do irmão check vs teto 1500.
</output>
