---
phase: 35-o-binario-python-checagem-e-verbos-orfaos
plan: 03
type: execute
wave: 3
depends_on: ["35-02"]
files_modified:
  - cairn/scripts/cairn-gsd-check.py
  - tests/cairn-gsd.bats
  - tests/fixtures/gsd-goldens/scenarios.json
  - tests/fixtures/gsd-goldens/*.golden.json
  - tests/fixtures/gsd-goldens/divergences.json
autonomous: true
requirements: [CHECK-01, CHECK-02]
beads:
  CHECK-01: CairnGo-ma8r
  CHECK-02: CairnGo-tnu6
must_haves:
  truths:
    - "`check <subcommand>` normaliza pontos→hifens no subcommand e roteia os 12 nomes do contrato; subcommand desconhecido = error exit 1; o veredito de todo gate avaliado vai no payload (passed/active/block), nunca no exit (CHECK-01)"
    - "`check auto-mode` devolve {active, source, auto_chain_active, auto_advance}; `check auto.mode` (grafia pontuada) devolve envelope byte-igual — a normalização provada por par (CHECK-01)"
    - "`query check.decision-coverage-plan <phase-dir> <context-path>` é fail-closed em erro de caller (#2770): contextPath vazio → passed false com mensagem explicando que argumento vazio é erro do caller; CONTEXT.md realmente inexistente → skip verde; desligável por workflow.context_coverage_gate; exit SEMPRE 0 (CHECK-01)"
    - "o avaliador de predicado do ADR-2008 (check predicate) responde na semântica da tag, e o diferencial contra o gsd-tools REAL existe como goldens recorded no corpus — reproduzível pelo --record skip-gated (CHECK-02)"
  artifacts:
    - cairn/scripts/cairn-gsd-check.py
    - tests/fixtures/gsd-goldens/scenarios.json
  key_links:
    - "check (família) → dict interno subcommand→função, molde HANDLERS — decision-coverage-plan compartilha a MESMA função do verbo dedicado check.decision-coverage-plan"
    - "predicado ADR-2008 → semântica lida do fonte da tag no cache do clone (src/check-command-router.cts L1447-L1524 e a implementação do predicate que ele roteia) — transcrita com proveniência em comentário, nunca inventada"
    - "diferencial CHECK-02 → cairn-gsd-record.py grava do binário real da tag → goldens provenance recorded comparados byte a byte offline pelo runner"
  prohibitions:
    - statement: "Nenhuma task escreve sob cairn/gsd/**; state/init/doctor intocados"
    - statement: "Subcommand do router check sem sítio observado e sem semântica extraída não ganha envelope inventado — indisponibilidade declarada (molde 34-05) + divergences.json"
    - statement: "Exit 0 = gate avaliado (mesmo reprovando); exit 1 só para o caminho de erro do contrato (subcommand desconhecido)"
---

<objective>
O dispatcher de gates da família: `check` roteia os 12 subcommands com normalização
pontos→hifens, os observados do universo respondem (auto-mode, ui-plan-gate,
decision-coverage-plan, predicate), e o avaliador de predicado do ADR-2008 nasce com
diferencial contra o gsd-tools real da tag — goldens recorded, a prova de CHECK-02.

Purpose: check é o hub que a fase 36 vai apontar dos preâmbulos de workflow
(${hook.check.query} do gate-dispatch genérico) — a superfície precisa estar completa e o
predicado, que não tem análogo em código na casa, precisa da âncora do real.
Output: check + check.decision-coverage-plan implementados (8/11 da família), goldens
recorded do predicado no corpus, suíte verde.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-o-binario-python-checagem-e-verbos-orfaos/35-CONTEXT.md
@.planning/phases/35-o-binario-python-checagem-e-verbos-orfaos/35-PATTERNS.md
@cairn/gsd/contracts/checagem.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: check — o dispatcher de gates com normalização, auto-mode e decision-coverage-plan</name>
  <files>cairn/scripts/cairn-gsd-check.py, tests/fixtures/gsd-goldens/scenarios.json, tests/fixtures/gsd-goldens/check-*.golden.json, tests/fixtures/gsd-goldens/divergences.json</files>
  <precondition>Cache do clone da tag presente em .cairn/cache/gsd-core-v1.10.0/ (criado na onda 1; se ausente, `./cairn/scripts/cairn-inventory.sh` com rede) — fonte da semântica de cmdAutoMode, ui-plan-gate e decision-coverage-plan</precondition>
  <read_first>
    - cairn/gsd/contracts/checagem.json, verbos check e check.decision-coverage-plan (os 12 subcommands na nota; shape por-subcommand; fail-closed #2770)
    - .cairn/cache/gsd-core-v1.10.0/gsd-core/src/check-command-router.cts L1447-L1524 (routeCheckCommand — a normalização e a tabela), L107-L123 (cmdAutoMode), L281-L353 (decision-coverage-plan) e o trecho do ui-plan-gate que o router aponta
    - 35-PATTERNS.md, "Dispatchers internos" (o molde: dict subcommand→função dentro do handler, nenhum roteador genérico novo)
    - 34-05-SUMMARY.md (indisponibilidade declarada — o molde para subcommands não observados)
  </read_first>
  <behavior>
    - Cenário `check-auto-mode`: fixture com planning_config workflow → exit 0, as 4 chaves do contrato
    - Cenário `check-normalizacao-pontuada`: `check auto.mode` → envelope byte-igual ao de `check auto-mode` (par no manifesto)
    - Cenário `check-subcommand-desconhecido`: `check nao-existe` → exit 1
    - Cenário `check-decision-coverage-plan-fail-closed`: context-path vazio → exit 0, passed false, message explicando erro de caller (#2770)
    - Cenário `check-decision-coverage-plan-skip-verde`: phase-dir sem CONTEXT.md → exit 0, skipped true
    - Cenário `check-decision-coverage-plan-cobre`: CONTEXT.md com decisões D-NN citadas nos PLAN.md do fixture → exit 0, passed true, covered == total
  </behavior>
  <action>
    handler check: normalizar pontos→hifens no subcommand recebido e rotear pelos 12 nomes
    do contrato (api-coverage-verify-pre, auto-mode, decision-coverage-plan,
    decision-coverage-verify, gap-analysis-plan-post, predicate, prohibition-enforcement,
    tdd-review-checkpoint, ui-plan-gate, ui-safety-gate, verify-schema-drift,
    verify-codebase-drift) via dict interno. Implementar da semântica da tag os que o
    universo observa: auto-mode ({active, source, auto_chain_active, auto_advance} lidos
    do workflow config, cmdAutoMode L107-L123), ui-plan-gate (a leitura que o fonte da tag
    define), decision-coverage-plan (a MESMA função do verbo dedicado abaixo),
    verify-schema-drift/verify-codebase-drift delegando às funções da família verify do
    plano 35-02. predicate é a Task 2. Os demais (api-coverage-verify-pre,
    decision-coverage-verify, gap-analysis-plan-post, prohibition-enforcement,
    tdd-review-checkpoint, ui-safety-gate) sem sítio no universo: indisponibilidade
    declarada no envelope pelo molde 34-05, entrada única em divergences.json (family
    checagem, listando os subcommands, reason citando o universo da 31). Subcommand fora
    dos 12 → error exit 1 "Unknown ... Available: ...".

    handler check.decision-coverage-plan (verbo dedicado): argv phase-dir + context-path.
    Fail-closed #2770: context-path vazio → passed false com mensagem de erro-de-caller
    (argumento vazio não é evidência de ausência); CONTEXT.md realmente inexistente → skip
    verde; gate desligável por workflow.context_coverage_gate no config. Cobertura: as
    decisões trackáveis (D-NN) do CONTEXT.md procuradas nos PLAN.md da fase — a mecânica
    de extração vem do fonte da tag (L281-L353), transcrita com proveniência. Shape:
    covered, message, passed, reason, skipped, total, uncovered; exit SEMPRE 0.
  </action>
  <verify>
    <automated>bats tests/cairn-gsd.bats && R="$PWD" && P="$R/cairn/scripts/cairn-gsd.sh" && T=$(mktemp -d) && cd "$T" && git init -q && mkdir -p .planning && echo '{}' > .planning/config.json && "$P" check auto-mode > a.json && "$P" check auto.mode > b.json && diff a.json b.json && jq -e 'has("active") and has("auto_advance")' a.json && ("$P" check nao-existe >/dev/null 2>&1; test $? -eq 1) && test "$(wc -l < "$R/cairn/scripts/cairn-gsd-check.py")" -le 1500</automated>
  </verify>
  <done>check roteia os 12 com normalização provada por par de cenários; decision-coverage-plan cobre os três caminhos (fail-closed, skip verde, cobertura real); nada inventado, tudo declarado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: o avaliador de predicado ADR-2008 com diferencial contra o real (CHECK-02)</name>
  <files>cairn/scripts/cairn-gsd-check.py, tests/cairn-gsd.bats, tests/fixtures/gsd-goldens/scenarios.json, tests/fixtures/gsd-goldens/check-predicate-*.golden.json, tests/fixtures/gsd-goldens/divergences.json</files>
  <precondition>Cache do clone da tag + node no PATH + runtime buildado (os mesmos três skip-gates de cairn-gsd.bats L1479-1481; `./cairn/scripts/cairn-gsd-record.sh` prepara o runtime) — sem eles não há gravação do diferencial, e gravar é o núcleo de CHECK-02</precondition>
  <read_first>
    - cairn/gsd/contracts/checagem.json, verbo check (flag --predicate: "objeto predicate como JSON, só no subcommand predicate, #2008"; nota "avaliador genérico que recebe --flag value pairs")
    - .cairn/cache/gsd-core-v1.10.0/gsd-core/src/check-command-router.cts (o trecho do predicate que routeCheckCommand aponta — a semântica inteira do avaliador: operadores, forma do objeto, resultado)
    - cairn/scripts/cairn-gsd-record.py (como o recorder produz goldens recorded) e tests/cairn-gsd.bats L1477-1490 (o teste de reprodução skip-gated)
    - 35-PATTERNS.md, "No Analog Found" linha 1 (por que o predicado não tem molde na casa e o que usar no lugar)
  </read_first>
  <behavior>
    - Cenário `check-predicate-passa`: predicado JSON simples avaliado contra --flag value pairs que o satisfazem → exit 0, veredito positivo no payload — golden RECORDED do binário real
    - Cenário `check-predicate-reprova`: mesmos flags, predicado insatisfeito → exit 0, veredito negativo no payload — golden RECORDED
    - Cenário `check-predicate-malformado`: --predicate com JSON inválido → o caminho de erro que o binário real exibir (gravado, não suposto)
    - Pelo menos um cenário composto (predicado com mais de uma condição/operador) — a forma exata sai do fonte da tag, não da imaginação
  </behavior>
  <action>
    Implementar o subcommand predicate do check: avaliador genérico que recebe o objeto
    predicate como JSON (--predicate) e pares --flag value, na semântica transcrita do
    fonte da tag (operadores, coerção de tipos, forma do resultado — proveniência em
    comentário com arquivo:linha do cache). Nenhum avaliador de expressão existe na casa
    (No Analog Found): a âncora de correção é o DIFERENCIAL, não um molde local.

    O diferencial (CHECK-02, discrição do CONTEXT exercida na forma recorded): gravar os
    cenários do behavior com cairn-gsd-record.py contra o gsd-tools real da tag — goldens
    provenance `recorded`, comparados byte a byte pelo runner OFFLINE depois de
    commitados; o teste de reprodução skip-gated (cache/node/runtime) re-grava e confere.
    Onde a gravação não compensar (caso que dependa de estado não reproduzível), golden
    derived-from-contract por forma, com o motivo anotado. Divergência consciente entre o
    avaliador python e o real → divergences.json com motivo, nunca silenciada; se o
    diferencial fechar limpo, nenhuma entrada.
  </action>
  <verify>
    <automated>bats tests/cairn-gsd.bats && test "$(ls tests/fixtures/gsd-goldens/check-predicate-*.golden.json | wc -l | tr -d ' ')" -ge 3 && cat tests/fixtures/gsd-goldens/check-predicate-*.golden.json | jq -s '[.[] | select(.provenance == "recorded")] | length >= 2' | grep -q true && test -z "$(git status --porcelain cairn/gsd/)" && test "$(wc -l < cairn/scripts/cairn-gsd-check.py)" -le 1500</automated>
  </verify>
  <done>O predicado ADR-2008 responde na semântica da tag e o corpus carrega goldens recorded que qualquer máquina com o cache reproduz byte a byte — CHECK-02 provado por diferencial, não por fé.</done>
</task>

</tasks>

<verification>
- `bats tests/cairn-gsd.bats` verde offline — recorded comparados byte a byte, derived por forma
- Normalização pontos→hifens provada por par de envelopes byte-iguais
- `git status --porcelain cairn/gsd/` vazio; teto 1500 respeitado; pin CHECK-04 verde
- 8/11 verbos de checagem em --list-implemented do irmão (acumulado: verification.status, os 5 verify, check, check.decision-coverage-plan)
</verification>

<success_criteria>
- CHECK-02 completo: avaliador com diferencial recorded contra o real da tag
- CHECK-01 avança: o hub check inteiro roteável pela fase 36 (${hook.check.query})
- Todo subcommand não observado declarado em divergences.json — superfície honesta
</success_criteria>

<output>
Criar `.planning/phases/35-o-binario-python-checagem-e-verbos-orfaos/35-03-SUMMARY.md`
ao concluir. Registrar: a semântica transcrita do predicado (operadores e forma, com
arquivo:linha da tag), o resultado do diferencial (fechou limpo ou divergências
declaradas) e a lista dos subcommands declarados indisponíveis.
</output>
